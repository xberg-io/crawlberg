name: Validate PR

on:
  pull_request:
    types: [opened, edited, synchronize]

jobs:
  validate:
    uses: xberg-io/actions/.github/workflows/reusable-validate-pr.yml@8dd8c168edb57e4606b4840b1143855149390261 # v1.8.141
    secrets: inherit
