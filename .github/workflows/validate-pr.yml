name: Validate PR

on:
  pull_request:
    types: [opened, edited, synchronize]

jobs:
  validate:
    uses: xberg-io/actions/.github/workflows/reusable-validate-pr.yml@c0ba6ab12fd1bff702d928e555e089d5ba4ba21c # v1.11.0
    secrets: inherit
