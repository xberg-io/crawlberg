name: CI Docs

on:
  push:
    branches: [main]
    paths:
      - "docs-site/**"
      - "alef.toml"
      - ".github/workflows/ci-docs.yaml"
  pull_request:
    branches: [main]
    paths:
      - "docs-site/**"
      - "alef.toml"
      - ".github/workflows/ci-docs.yaml"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: ci-docs-${{ github.ref }}
  cancel-in-progress: false

jobs:
  docs:
    uses: xberg-io/actions/.github/workflows/reusable-docs-deploy.yml@8dd8c168edb57e4606b4840b1143855149390261 # v1.8.141
    with:
      deploy: ${{ github.event_name == 'push' && github.ref == 'refs/heads/main' }}
