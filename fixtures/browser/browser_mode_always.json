{
	"id": "browser_mode_always",
	"description": "Browser mode 'always' uses browser even for normal server-rendered pages",
	"docs": {
		"topic": "browser",
		"stem": "browser-mode-always",
		"title": "Browser mode 'always' uses browser even for normal server-rendered pages"
	},
	"tags": ["browser", "config"],
	"skip": {
		"languages": ["wasm"],
		"reason": "wasm32 has no chromium backend"
	},
	"input": {
		"mock_responses": [
			{
				"status_code": 200,
				"headers": {
					"content-type": "text/html; charset=utf-8"
				},
				"body_file": "html/basic_page.html"
			}
		],
		"config": {
			"browser": {
				"mode": "always"
			}
		}
	},
	"assertions": [
		{
			"type": "equals",
			"field": "browser_used",
			"value": true
		}
	]
}
