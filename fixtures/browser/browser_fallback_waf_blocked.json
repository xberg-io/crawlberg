{
	"id": "browser_fallback_waf_blocked",
	"description": "Browser fallback is used when browser mode is always, simulating WAF-blocked scenario",
	"docs": {
		"topic": "browser",
		"stem": "browser-fallback-waf-blocked",
		"title": "Browser fallback is used when browser mode is always, simulating WAF-blocked scenario"
	},
	"tags": ["browser", "fallback", "waf"],
	"skip": {
		"languages": ["wasm"],
		"reason": "wasm32 has no chromium backend"
	},
	"input": {
		"mock_responses": [
			{
				"status_code": 200,
				"headers": {
					"content-type": "text/html; charset=utf-8"
				},
				"body_inline": "<html><body><h1>Browser Fallback Test</h1></body></html>"
			}
		],
		"config": {
			"browser": {
				"mode": "always"
			}
		}
	},
	"assertions": [
		{
			"type": "equals",
			"field": "browser_used",
			"value": true
		}
	]
}
