<?php

// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:cb78c37bb8ce17d69a94861fa2f12bc3ccfc6e3ea7ce509250f4aab469a9b057
// To regenerate: alef generate
// To verify freshness: alef verify
// Type stubs for the native PHP extension — declares classes
// provided at runtime by the compiled Rust extension (.so/.dll).
// Include this in phpstan.neon scanFiles for static analysis.

declare(strict_types=1);

namespace Crawlberg {
    class CrawlbergException extends \RuntimeException
    {
        public function getErrorCode(): int
        {
            throw new \RuntimeException('Not implemented.');
        }
    }

    /**
     * Result from a single page action execution.
     */
    final class ActionResult
    {
        public function __construct(
            /** @var int Zero-based index of the action in the sequence. */
            public readonly int $actionIndex,
            /** @var string The type of action that was executed. */
            public readonly string $actionType,
            /** @var bool Whether the action completed successfully. */
            public readonly bool $success,
            /** @var ?string Error message if the action failed. */
            public readonly ?string $error = null,
        ) {}

        /**
         * Construct from a JSON string — the only way to build this type's nested/complex fields from PHP, since ext-php-rs exposes them read-only.
         */
        public static function from_json(string $json): self
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getActionIndex(): int
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getActionType(): string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getSuccess(): bool
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        /**
         * @readonly Not settable via the constructor — this field's type has no ext-php-rs #[php(prop)]/constructor-param support, so it can only be read via this getter.
         */
        public function getData(): ?string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getError(): ?string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }
    }

    /**
     * Article metadata extracted from `article:*` Open Graph tags.
     */
    final class ArticleMetadata
    {
        public function __construct(
            /** @var array<string> The article tags. */
            public readonly array $tags,
            /** @var ?string The article publication time. */
            public readonly ?string $publishedTime = null,
            /** @var ?string The article modification time. */
            public readonly ?string $modifiedTime = null,
            /** @var ?string The article author. */
            public readonly ?string $author = null,
            /** @var ?string The article section. */
            public readonly ?string $section = null,
        ) {}

        /**
         * Construct from a JSON string — the only way to build this type's nested/complex fields from PHP, since ext-php-rs exposes them read-only.
         */
        public static function from_json(string $json): self
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getPublishedTime(): ?string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getModifiedTime(): ?string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getAuthor(): ?string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getSection(): ?string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        /** @return array<string> */
        public function getTags(): array
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }
    }

    /**
     * Result from a single URL in a batch crawl operation.
     */
    final class BatchCrawlResult
    {
        public function __construct(
            /** @var string The seed URL that was crawled. */
            public readonly string $url,
            /** @var ?string The error message, if the crawl failed. */
            public readonly ?string $error = null,
        ) {}

        /**
         * Construct from a JSON string — the only way to build this type's nested/complex fields from PHP, since ext-php-rs exposes them read-only.
         */
        public static function from_json(string $json): self
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getUrl(): string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        /**
         * @readonly Not settable via the constructor — this field's type has no ext-php-rs #[php(prop)]/constructor-param support, so it can only be read via this getter.
         */
        public function getResult(): ?CrawlResult
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getError(): ?string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }
    }

    /**
     * Aggregate result of a batch crawl, exposing per-URL results plus precomputed counts.
     *
     * The counts are derived once at construction so every binding language can read them
     * as plain integer fields without re-iterating the `results` vector.
     */
    final class BatchCrawlResults
    {
        public function __construct(
            /** @var int Total number of seed URLs in the batch (equal to `results.len()`). */
            public readonly int $totalCount,
            /** @var int Number of seed URLs whose crawl succeeded (`error` is `None`). */
            public readonly int $completedCount,
            /** @var int Number of seed URLs whose crawl failed (`error` is `Some`). */
            public readonly int $failedCount,
        ) {}

        /**
         * Construct from a JSON string — the only way to build this type's nested/complex fields from PHP, since ext-php-rs exposes them read-only.
         */
        public static function from_json(string $json): self
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        /** @return array<\Crawlberg\BatchCrawlResult> */
        /**
         * @readonly Not settable via the constructor — this field's type has no ext-php-rs #[php(prop)]/constructor-param support, so it can only be read via this getter.
         */
        public function getResults(): array
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getTotalCount(): int
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getCompletedCount(): int
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getFailedCount(): int
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }
    }

    /**
     * Request to begin a multi-URL streaming crawl.
     *
     * Wraps a set of seed URLs for delivery through the streaming-adapter binding
     * surface. Required as a struct because alef's streaming adapter requires a
     * named request type — primitives are not supported.
     */
    final class BatchCrawlStreamRequest
    {
        public function __construct(
            /**
             * The seed URLs to crawl. Each URL is followed independently up to the
             * engine's configured depth.
             *
             * @var array<string>
             */
            public readonly array $urls,
        ) {}

        /**
         * Construct from a JSON string — the only way to build this type's nested/complex fields from PHP, since ext-php-rs exposes them read-only.
         */
        public static function from_json(string $json): self
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        /** @return array<string> */
        public function getUrls(): array
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }
    }

    /**
     * Result from a single URL in a batch scrape operation.
     */
    final class BatchScrapeResult
    {
        public function __construct(
            /** @var string The URL that was scraped. */
            public readonly string $url,
            /** @var ?string The error message, if the scrape failed. */
            public readonly ?string $error = null,
        ) {}

        /**
         * Construct from a JSON string — the only way to build this type's nested/complex fields from PHP, since ext-php-rs exposes them read-only.
         */
        public static function from_json(string $json): self
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getUrl(): string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        /**
         * @readonly Not settable via the constructor — this field's type has no ext-php-rs #[php(prop)]/constructor-param support, so it can only be read via this getter.
         */
        public function getResult(): ?ScrapeResult
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getError(): ?string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }
    }

    /**
     * Aggregate result of a batch scrape, exposing per-URL results plus precomputed counts.
     *
     * The counts are derived once at construction so every binding language can read them
     * as plain integer fields without re-iterating the `results` vector.
     */
    final class BatchScrapeResults
    {
        public function __construct(
            /** @var int Total number of URLs in the batch (equal to `results.len()`). */
            public readonly int $totalCount,
            /** @var int Number of URLs whose scrape succeeded (`error` is `None`). */
            public readonly int $completedCount,
            /** @var int Number of URLs whose scrape failed (`error` is `Some`). */
            public readonly int $failedCount,
        ) {}

        /**
         * Construct from a JSON string — the only way to build this type's nested/complex fields from PHP, since ext-php-rs exposes them read-only.
         */
        public static function from_json(string $json): self
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        /** @return array<\Crawlberg\BatchScrapeResult> */
        /**
         * @readonly Not settable via the constructor — this field's type has no ext-php-rs #[php(prop)]/constructor-param support, so it can only be read via this getter.
         */
        public function getResults(): array
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getTotalCount(): int
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getCompletedCount(): int
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getFailedCount(): int
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }
    }

    /**
     * Browser fallback configuration.
     */
    final class BrowserConfig
    {
        public function __construct(
            /** @var string When to use the headless browser fallback. */
            public readonly string $mode,
            /** @var string Browser backend used to render JavaScript-heavy pages. */
            public readonly string $backend,
            /** @var string Wait strategy after browser navigation. */
            public readonly string $wait,
            /**
             * URL patterns to block before the network request fires. Supports `*`
             * wildcards. Useful for skipping ads/analytics/large images. Honored by
             * `BrowserBackend::Native`; chromiumoxide ignores this field today.
             *
             * @var array<string>
             */
            public readonly array $blockUrlPatterns,
            /**
             * Capture the full network event stream into the result. Default false
             * (only the document event is captured). Native only.
             *
             * @var bool
             */
            public readonly bool $captureNetworkEvents,
            /**
             * Enable session affinity: reuse chromiumoxide Pages for same-domain
             * requests so cookies + fingerprint + solved challenges persist.
             * Default: true. When false, each request gets a fresh Page.
             *
             * @var bool
             */
            public readonly bool $sessionAffinity,
            /** @var ?string CDP WebSocket endpoint for connecting to an external browser instance. */
            public readonly ?string $endpoint = null,
            /** @var ?int Timeout for browser page load and rendering (in milliseconds when serialized). */
            public readonly ?int $timeout = null,
            /** @var ?string CSS selector to wait for when `wait` is `Selector`. */
            public readonly ?string $waitSelector = null,
            /** @var ?int Extra time to wait after the wait condition is met. */
            public readonly ?int $extraWait = null,
            /**
             * JavaScript snippet evaluated after navigation completes.
             *
             * Scraping captures the native backend result in `ScrapeResult.browser.eval_result`.
             * Interactions run this script before page actions on both browser backends but do
             * not include the script result in `InteractionResult`.
             *
             * @var ?string
             */
            public readonly ?string $evalScript = null,
            /**
             * User-agent used when fetching robots.txt. Defaults to `BrowserConfig.user_agent`
             * (or crawlberg's default) if unset. Native only.
             *
             * @var ?string
             */
            public readonly ?string $robotsUserAgent = null,
        ) {}

        /**
         * Construct from a JSON string — the only way to build this type's nested/complex fields from PHP, since ext-php-rs exposes them read-only.
         */
        public static function from_json(string $json): self
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getMode(): string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getBackend(): string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getEndpoint(): ?string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getTimeout(): ?int
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getWait(): string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getWaitSelector(): ?string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getExtraWait(): ?int
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        /**
         * @readonly Not settable via the constructor — this field's type has no ext-php-rs #[php(prop)]/constructor-param support, so it can only be read via this getter.
         */
        public function getProxy(): ?ProxyConfig
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        /** @return array<string> */
        public function getBlockUrlPatterns(): array
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getEvalScript(): ?string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getRobotsUserAgent(): ?string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getCaptureNetworkEvents(): bool
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getSessionAffinity(): bool
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public static function default(): BrowserConfig
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }
    }

    /**
     * Browser-specific extras populated when the native browser backend was used.
     *
     * Available on `ScrapeResult.browser` when `BrowserBackend.Native` handled the request.
     */
    final class BrowserExtras
    {
        /**
         * Construct from a JSON string — the only way to build this type's nested/complex fields from PHP, since ext-php-rs exposes them read-only.
         */
        public static function from_json(string $json): self
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        /**
         * @readonly Not settable via the constructor — this field's type has no ext-php-rs #[php(prop)]/constructor-param support, so it can only be read via this getter.
         */
        public function getEvalResult(): ?string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        /** @return array<\Crawlberg\ResponseMeta> */
        /**
         * @readonly Not settable via the constructor — this field's type has no ext-php-rs #[php(prop)]/constructor-param support, so it can only be read via this getter.
         */
        public function getNetworkEvents(): array
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        /** @return array<\Crawlberg\CookieInfo> */
        /**
         * @readonly Not settable via the constructor — this field's type has no ext-php-rs #[php(prop)]/constructor-param support, so it can only be read via this getter.
         */
        public function getCookies(): array
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }
    }

    /**
     * A single numbered reference in a citation list — produced by the citation
     * extractor when content uses inline `[N]`-style markers.
     */
    final class CitationReference
    {
        public function __construct(
            /** @var int 1-based reference number as it appears in the source text. */
            public readonly int $index,
            /** @var string Resolved absolute URL for this reference. */
            public readonly string $url,
            /** @var string Human-readable anchor text or title for the reference. */
            public readonly string $text,
        ) {}

        /**
         * Construct from a JSON string — the only way to build this type's nested/complex fields from PHP, since ext-php-rs exposes them read-only.
         */
        public static function from_json(string $json): self
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getIndex(): int
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getUrl(): string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getText(): string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }
    }

    /**
     * Result of citation conversion.
     */
    final class CitationResult
    {
        public function __construct(
            /** @var string Markdown with links replaced by numbered citations. */
            public readonly string $content,
        ) {}

        /**
         * Construct from a JSON string — the only way to build this type's nested/complex fields from PHP, since ext-php-rs exposes them read-only.
         */
        public static function from_json(string $json): self
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getContent(): string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        /** @return array<\Crawlberg\CitationReference> */
        /**
         * @readonly Not settable via the constructor — this field's type has no ext-php-rs #[php(prop)]/constructor-param support, so it can only be read via this getter.
         */
        public function getReferences(): array
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }
    }

    /**
     * Content extraction and conversion configuration.
     *
     * Controls how HTML is converted to the output format. Uses
     * html-to-markdown-rs as the conversion engine for all formats
     * (markdown, plain text, djot).
     */
    final class ContentConfig
    {
        public function __construct(
            /** @var string Output format: `"markdown"` (default), `"plain"`, `"djot"`. */
            public readonly string $outputFormat,
            /**
             * Preprocessing aggressiveness: `"minimal"`, `"standard"` (default), `"aggressive"`.
             *
             * - Minimal: only scripts/styles removed.
             * - Standard: also removes nav, nav-hinted headers/footers/asides, forms.
             * - Aggressive: removes all footers/asides unconditionally.
             *
             * @var string
             */
            public readonly string $preprocessingPreset,
            /** @var bool Remove navigation elements (nav, breadcrumbs, menus). Default: `true`. */
            public readonly bool $removeNavigation,
            /** @var bool Remove form elements. Default: `true`. */
            public readonly bool $removeForms,
            /**
             * HTML tag names to strip (render children only, remove the tag wrapper).
             * Default: `[]`.
             *
             * @var array<string>
             */
            public readonly array $stripTags,
            /** @var array<string> HTML tag names to preserve as raw HTML in output. */
            public readonly array $preserveTags,
            /**
             * CSS selectors for elements to exclude entirely (element + all content).
             *
             * Unlike `strip_tags` (which removes the wrapper but keeps children),
             * excluded elements and all descendants are dropped. Supports CSS selectors:
             * `.class`, `#id`, `[attribute]`, compound selectors.
             *
             * Default: `["noscript"]`. `<noscript>` fallback content (no-JS notices,
             * tracking pixels, GTM iframes) is meant for browsers with JavaScript
             * disabled, not for a markdown reader, and `strip_tags` cannot drop it —
             * on `preprocessing_preset: "standard"` (crawlberg's only path) it only
             * removes the wrapper and still renders the children.
             *
             * Example: `[".cookie-banner", "#ad-container", "[role='complementary']"]`
             *
             * @var array<string>
             */
            public readonly array $excludeSelectors,
            /** @var bool Skip image elements in output. Default: `false`. */
            public readonly bool $skipImages,
            /** @var bool Enable line wrapping. Default: `false`. */
            public readonly bool $wrap,
            /** @var int Wrap width when `wrap` is enabled. Default: `80`. */
            public readonly int $wrapWidth,
            /** @var bool Include document structure tree in output. Default: `true`. */
            public readonly bool $includeDocumentStructure,
            /** @var ?int Max DOM traversal depth. Prevents stack overflow on deeply nested HTML. */
            public readonly ?int $maxDepth = null,
        ) {}

        /**
         * Construct from a JSON string — the only way to build this type's nested/complex fields from PHP, since ext-php-rs exposes them read-only.
         */
        public static function from_json(string $json): self
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getOutputFormat(): string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getPreprocessingPreset(): string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getRemoveNavigation(): bool
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getRemoveForms(): bool
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        /** @return array<string> */
        public function getStripTags(): array
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        /** @return array<string> */
        public function getPreserveTags(): array
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        /** @return array<string> */
        public function getExcludeSelectors(): array
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getSkipImages(): bool
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getMaxDepth(): ?int
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getWrap(): bool
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getWrapWidth(): int
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getIncludeDocumentStructure(): bool
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public static function default(): ContentConfig
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }
    }

    /**
     * Information about an HTTP cookie received from a response.
     */
    final class CookieInfo
    {
        public function __construct(
            /** @var string The cookie name. */
            public readonly string $name,
            /** @var string The cookie value. */
            public readonly string $value,
            /** @var ?string The cookie domain, if specified. */
            public readonly ?string $domain = null,
            /** @var ?string The cookie path, if specified. */
            public readonly ?string $path = null,
        ) {}

        /**
         * Construct from a JSON string — the only way to build this type's nested/complex fields from PHP, since ext-php-rs exposes them read-only.
         */
        public static function from_json(string $json): self
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getName(): string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getValue(): string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getDomain(): ?string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getPath(): ?string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }
    }

    /**
     * Configuration for crawl, scrape, and map operations.
     */
    final class CrawlConfig
    {
        public function __construct(
            /**
             * Traversal order. Defaults to breadth-first.
             *
             * A frontier or strategy set explicitly on `CrawlEngineBuilder` takes precedence over
             * this field.
             *
             * @var string
             */
            public readonly string $crawlStrategy,
            /** @var bool Whether to respect robots.txt directives. */
            public readonly bool $respectRobotsTxt,
            /**
             * When true, HTTP-level error responses (404 NotFound, 403 Forbidden, WAF blocks)
             * are surfaced as `ScrapeResult` records with the matching `status_code` rather
             * than raised as `CrawlError`. Default `false` preserves the historical
             * throw-on-error contract for direct fetches. Independently of this flag,
             * 404s reached at the end of a redirect chain are *always* surfaced softly —
             * the user opted into redirect-following, so receiving a 404 there is part of
             * the normal flow rather than an unexpected error.
             *
             * @var bool
             */
            public readonly bool $softHttpErrors,
            /** @var bool Whether to restrict crawling to the same domain. */
            public readonly bool $stayOnDomain,
            /** @var bool Whether to allow subdomains when `stay_on_domain` is true. */
            public readonly bool $allowSubdomains,
            /** @var array<string> Regex patterns for paths to include during crawling. */
            public readonly array $includePaths,
            /** @var array<string> Regex patterns for paths to exclude during crawling. */
            public readonly array $excludePaths,
            /** @var array<string, string> Custom HTTP headers to send with each request. */
            public readonly array $customHeaders,
            /** @var int Maximum number of redirects to follow. */
            public readonly int $maxRedirects,
            /** @var int Number of retry attempts for failed requests. */
            public readonly int $retryCount,
            /** @var array<int> HTTP status codes that should trigger a retry. */
            public readonly array $retryCodes,
            /** @var bool Whether to enable cookie handling. */
            public readonly bool $cookiesEnabled,
            /** @var array<string> CSS selectors for tags to remove from HTML before processing. */
            public readonly array $removeTags,
            /** @var bool Whether to download assets (CSS, JS, images, etc.) from the page. */
            public readonly bool $downloadAssets,
            /** @var array<string> Filter for asset categories to download. */
            public readonly array $assetTypes,
            /** @var array<string> List of user-agent strings for rotation. If non-empty, overrides `user_agent`. */
            public readonly array $userAgents,
            /**
             * Whether to capture a screenshot when using the browser.
             *
             * Only supported by `scrape()` with `BrowserBackend::Chromiumoxide` and
             * `BrowserMode::Always` or `Stealth`. A screenshot is 100–500 KB of PNG per page,
             * so `crawl()` does not carry screenshots in `CrawlPageResult`/`CrawlResult` at
             * all — a multi-thousand-page crawl holding one per page in memory is not a safe
             * default. Setting this with any other configuration (a different backend,
             * `BrowserMode::Auto`/`Never`, or during `crawl()`) has no effect and logs a
             * warning rather than silently doing nothing.
             *
             * @var bool
             */
            public readonly bool $captureScreenshot,
            /**
             * Re-enqueue discovered `LinkType::Document` URLs into the crawl frontier so
             * the crawl follows links *from* document pages (PDFs, etc.) as it would
             * from HTML pages. Default: `false` (documents terminate at materialisation).
             *
             * @var bool
             */
            public readonly bool $followDocumentUrls,
            /**
             * Whether to download non-HTML documents (PDF, DOCX, images, code, etc.) instead of skipping them.
             * Defaults to `true` — unlike `download_assets` and `capture_screenshot`, which default to `false`.
             *
             * @var bool
             */
            public readonly bool $downloadDocuments,
            /** @var array<string> Allowlist of MIME types to download. If empty, uses built-in defaults. */
            public readonly array $documentMimeTypes,
            /** @var bool Whether to save changes back to the browser profile on exit. */
            public readonly bool $saveBrowserProfile,
            /** @var ?int Maximum crawl depth (number of link hops from the start URL). */
            public readonly ?int $maxDepth = null,
            /** @var ?int Maximum number of pages to crawl. */
            public readonly ?int $maxPages = null,
            /**
             * Maximum links enqueued from a single page. Defaults to 10000.
             *
             * Bounds the work one hostile or pathological page can create; links past the
             * cap are dropped and a warning is logged.
             *
             * @var ?int
             */
            public readonly ?int $maxLinksPerPage = null,
            /** @var ?int Maximum number of concurrent requests. */
            public readonly ?int $maxConcurrent = null,
            /**
             * Content filter applied to each page. `None` keeps every page.
             *
             * A content filter set explicitly on `CrawlEngineBuilder` takes precedence.
             *
             * @var ?string
             */
            public readonly ?string $contentFilter = null,
            /** @var ?string Query the BM25 content filter scores pages against. Required by `ContentFilterKind::Bm25`. */
            public readonly ?string $bm25Query = null,
            /** @var ?float Minimum BM25 score a page must reach to be kept. Defaults to `0.0`. */
            public readonly ?float $bm25Threshold = null,
            /** @var ?string Custom user-agent string. */
            public readonly ?string $userAgent = null,
            /** @var ?int Timeout for individual HTTP requests (in milliseconds when serialized). */
            public readonly ?int $requestTimeout = null,
            /**
             * Per-domain rate limit in milliseconds. When set, enforces a minimum delay
             * between requests to the same domain. Defaults to 200ms when `None`.
             *
             * @var ?int
             */
            public readonly ?int $rateLimitMs = null,
            /**
             * Maximum response body size in bytes.
             *
             * `None` does not mean unbounded: an unset cap falls back to a 100 MiB safety
             * ceiling, because HTTP responses are decompressed while being read and a few
             * hundred compressed bytes can otherwise expand to gigabytes in memory. To read
             * bodies larger than that, set this explicitly.
             *
             * @var ?int
             */
            public readonly ?int $maxBodySize = null,
            /** @var ?int Maximum number of URLs to return from a map operation. */
            public readonly ?int $mapLimit = null,
            /** @var ?string Search filter for map results (case-insensitive substring match on URLs). */
            public readonly ?string $mapSearch = null,
            /** @var ?int Maximum size in bytes for individual asset downloads. */
            public readonly ?int $maxAssetSize = null,
            /**
             * Maximum document-depth (from the seed URL through document links only)
             * when `follow_document_urls` is true. `None` means inherit `max_depth`.
             * Independent of `max_depth`: a document URL is enqueued only if BOTH the
             * outer `max_depth` and (if set) `document_url_depth` permit it.
             *
             * @var ?int
             */
            public readonly ?int $documentUrlDepth = null,
            /** @var ?int Maximum size in bytes for document downloads. Defaults to 50 MB. */
            public readonly ?int $documentMaxSize = null,
            /**
             * Directory to stream downloaded document bytes into instead of holding them in
             * memory on `DownloadedDocument.content`. When set, `content` is left empty and
             * `DownloadedDocument.content_path` is populated with `<dir>/<content_hash>.<ext>`.
             * `None` (default) preserves today's in-memory-only behavior. Has no effect on
             * wasm32, which has no filesystem — use `document_content_encoding` there instead.
             *
             * @var ?string
             */
            public readonly ?string $documentOutputDir = null,
            /**
             * Opt-in encoding that duplicates `DownloadedDocument.content` into a serializable
             * field for language bindings that need the bytes in-memory (`content` itself is
             * `alef(skip)`ed). `None` (default) means no encoding is produced. Independent of
             * `document_output_dir` — set both to get a file on disk and an in-memory copy.
             *
             * @var ?string
             */
            public readonly ?string $documentContentEncoding = null,
            /** @var ?string Path to write WARC output. If `None`, WARC output is disabled. */
            public readonly ?string $warcOutput = null,
            /**
             * Named browser profile for persistent sessions (cookies, localStorage).
             *
             * Chromiumoxide backend only. The native backend runs an in-process JavaScript
             * engine with no Chrome process and therefore no profile directory, so this is
             * ignored there and logs a warning. It is also ignored — with a warning — when a
             * shared browser pool is in use (the pool launches before any per-crawl config
             * exists) or when connecting to an external CDP endpoint whose process crawlberg
             * does not own.
             *
             * @var ?string
             */
            public readonly ?string $browserProfile = null,
            /**
             * Pins [`SsrfPolicy::deny_private`] to a caller-chosen value, bypassing the
             * `CRAWLBERG_ALLOW_PRIVATE_NETWORK` operator override entirely for this config.
             *
             * `ssrf.deny_private` is a plain, always-serialized `bool`: several alef-generated
             * bindings construct `SsrfPolicy::default()` (hardcoding `deny_private: true`)
             * whenever their caller never touches SSRF settings at all, so `true` on that field
             * alone cannot distinguish "the caller wants private networks denied" from "the
             * binding's own structural default landed on `true`". The environment variable
             * exists precisely to resolve that ambiguity in the common case by treating any
             * `true` as inconclusive and deferring to the operator.
             *
             * Set this field when that default-deferral is wrong for your call — e.g. a test
             * that must prove `deny_private: true` still denies even while the operator has set
             * `CRAWLBERG_ALLOW_PRIVATE_NETWORK` suite-wide for every other call. `None` (default)
             * preserves today's behavior: the environment variable may still flip
             * `ssrf.deny_private` to `false`. `Some(value)` pins `ssrf.deny_private` to `value`
             * and the environment variable is not consulted for this config.
             *
             * @var ?bool
             */
            public readonly ?bool $ssrfDenyPrivateExplicit = null,
        ) {}

        /**
         * Construct from a JSON string — the only way to build this type's nested/complex fields from PHP, since ext-php-rs exposes them read-only.
         */
        public static function from_json(string $json): self
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getMaxDepth(): ?int
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getMaxPages(): ?int
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getMaxLinksPerPage(): ?int
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getMaxConcurrent(): ?int
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getCrawlStrategy(): string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getContentFilter(): ?string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getBm25Query(): ?string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getBm25Threshold(): ?float
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getRespectRobotsTxt(): bool
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getSoftHttpErrors(): bool
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getUserAgent(): ?string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getStayOnDomain(): bool
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getAllowSubdomains(): bool
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        /** @return array<string> */
        public function getIncludePaths(): array
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        /** @return array<string> */
        public function getExcludePaths(): array
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        /** @return array<string, string> */
        public function getCustomHeaders(): array
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getRequestTimeout(): ?int
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getRateLimitMs(): ?int
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getMaxRedirects(): int
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getRetryCount(): int
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        /** @return array<int> */
        public function getRetryCodes(): array
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getCookiesEnabled(): bool
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        /**
         * @readonly Not settable via the constructor — this field's type has no ext-php-rs #[php(prop)]/constructor-param support, so it can only be read via this getter.
         */
        public function getAuth(): ?AuthConfig
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getMaxBodySize(): ?int
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        /** @return array<string> */
        public function getRemoveTags(): array
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        /**
         * @readonly Not settable via the constructor — this field's type has no ext-php-rs #[php(prop)]/constructor-param support, so it can only be read via this getter.
         */
        public function getContent(): ContentConfig
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getMapLimit(): ?int
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getMapSearch(): ?string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getDownloadAssets(): bool
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        /** @return array<\Crawlberg\AssetCategory> */
        public function getAssetTypes(): array
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getMaxAssetSize(): ?int
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        /**
         * @readonly Not settable via the constructor — this field's type has no ext-php-rs #[php(prop)]/constructor-param support, so it can only be read via this getter.
         */
        public function getBrowser(): BrowserConfig
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        /**
         * @readonly Not settable via the constructor — this field's type has no ext-php-rs #[php(prop)]/constructor-param support, so it can only be read via this getter.
         */
        public function getProxy(): ?ProxyConfig
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        /** @return array<string> */
        public function getUserAgents(): array
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getCaptureScreenshot(): bool
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getFollowDocumentUrls(): bool
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getDocumentUrlDepth(): ?int
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getDownloadDocuments(): bool
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getDocumentMaxSize(): ?int
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        /** @return array<string> */
        public function getDocumentMimeTypes(): array
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getDocumentOutputDir(): ?string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getDocumentContentEncoding(): ?string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getWarcOutput(): ?string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getBrowserProfile(): ?string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getSaveBrowserProfile(): bool
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        /**
         * @readonly Not settable via the constructor — this field's type has no ext-php-rs #[php(prop)]/constructor-param support, so it can only be read via this getter.
         */
        public function getSsrf(): SsrfPolicy
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getSsrfDenyPrivateExplicit(): ?bool
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public static function default(): CrawlConfig
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function validate(): void {}
    }

    /**
     * The result of crawling a single page during a crawl operation.
     */
    final class CrawlPageResult
    {
        public function __construct(
            /** @var string The original URL of the page. */
            public readonly string $url,
            /** @var string The normalized URL of the page. */
            public readonly string $normalizedUrl,
            /** @var int The HTTP status code of the response. */
            public readonly int $statusCode,
            /** @var string The Content-Type header value. */
            public readonly string $contentType,
            /** @var string The HTML body of the response. */
            public readonly string $html,
            /** @var int The size of the response body in bytes. */
            public readonly int $bodySize,
            /** @var int The depth of this page from the start URL. */
            public readonly int $depth,
            /** @var bool Whether this page is on the same domain as the start URL. */
            public readonly bool $stayedOnDomain,
            /** @var bool Whether this page was skipped (binary or PDF content). */
            public readonly bool $wasSkipped,
            /** @var bool Whether the content is a PDF. */
            public readonly bool $isPdf,
            /** @var bool Whether the browser fallback was used to fetch this page. */
            public readonly bool $browserUsed,
            /** @var ?string The detected character set encoding. */
            public readonly ?string $detectedCharset = null,
        ) {}

        /**
         * Construct from a JSON string — the only way to build this type's nested/complex fields from PHP, since ext-php-rs exposes them read-only.
         */
        public static function from_json(string $json): self
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getUrl(): string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getNormalizedUrl(): string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getStatusCode(): int
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getContentType(): string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getHtml(): string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getBodySize(): int
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        /**
         * @readonly Not settable via the constructor — this field's type has no ext-php-rs #[php(prop)]/constructor-param support, so it can only be read via this getter.
         */
        public function getMetadata(): PageMetadata
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        /** @return array<\Crawlberg\LinkInfo> */
        /**
         * @readonly Not settable via the constructor — this field's type has no ext-php-rs #[php(prop)]/constructor-param support, so it can only be read via this getter.
         */
        public function getLinks(): array
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        /** @return array<\Crawlberg\ImageInfo> */
        /**
         * @readonly Not settable via the constructor — this field's type has no ext-php-rs #[php(prop)]/constructor-param support, so it can only be read via this getter.
         */
        public function getImages(): array
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        /** @return array<\Crawlberg\FeedInfo> */
        /**
         * @readonly Not settable via the constructor — this field's type has no ext-php-rs #[php(prop)]/constructor-param support, so it can only be read via this getter.
         */
        public function getFeeds(): array
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        /** @return array<\Crawlberg\JsonLdEntry> */
        /**
         * @readonly Not settable via the constructor — this field's type has no ext-php-rs #[php(prop)]/constructor-param support, so it can only be read via this getter.
         */
        public function getJsonLd(): array
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getDepth(): int
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getStayedOnDomain(): bool
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getWasSkipped(): bool
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getIsPdf(): bool
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getDetectedCharset(): ?string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        /**
         * @readonly Not settable via the constructor — this field's type has no ext-php-rs #[php(prop)]/constructor-param support, so it can only be read via this getter.
         */
        public function getMarkdown(): ?MarkdownResult
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        /**
         * @readonly Not settable via the constructor — this field's type has no ext-php-rs #[php(prop)]/constructor-param support, so it can only be read via this getter.
         */
        public function getExtractedData(): ?string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        /**
         * @readonly Not settable via the constructor — this field's type has no ext-php-rs #[php(prop)]/constructor-param support, so it can only be read via this getter.
         */
        public function getExtractionMeta(): ?ExtractionMeta
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        /**
         * @readonly Not settable via the constructor — this field's type has no ext-php-rs #[php(prop)]/constructor-param support, so it can only be read via this getter.
         */
        public function getDownloadedDocument(): ?DownloadedDocument
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getBrowserUsed(): bool
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }
    }

    /**
     * The result of a multi-page crawl operation.
     */
    final class CrawlResult
    {
        public function __construct(
            /** @var string The final URL after following redirects. */
            public readonly string $finalUrl,
            /** @var int The number of redirects followed. */
            public readonly int $redirectCount,
            /** @var bool Whether any page was skipped during crawling. */
            public readonly bool $wasSkipped,
            /** @var bool Whether all crawled pages stayed on the same domain as the start URL. */
            public readonly bool $stayedOnDomain,
            /** @var bool Whether the browser fallback was used for any page in this crawl. */
            public readonly bool $browserUsed,
            /** @var ?string An error message, if the crawl encountered an issue. */
            public readonly ?string $error = null,
        ) {}

        /**
         * Construct from a JSON string — the only way to build this type's nested/complex fields from PHP, since ext-php-rs exposes them read-only.
         */
        public static function from_json(string $json): self
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        /** @return array<\Crawlberg\CrawlPageResult> */
        /**
         * @readonly Not settable via the constructor — this field's type has no ext-php-rs #[php(prop)]/constructor-param support, so it can only be read via this getter.
         */
        public function getPages(): array
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getFinalUrl(): string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getRedirectCount(): int
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getWasSkipped(): bool
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getError(): ?string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        /** @return array<\Crawlberg\CookieInfo> */
        /**
         * @readonly Not settable via the constructor — this field's type has no ext-php-rs #[php(prop)]/constructor-param support, so it can only be read via this getter.
         */
        public function getCookies(): array
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getStayedOnDomain(): bool
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getBrowserUsed(): bool
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function uniqueNormalizedUrls(): int
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }
    }

    /**
     * Request to begin a single-URL streaming crawl.
     *
     * Wraps a single seed URL for delivery through the streaming-adapter binding
     * surface. Required as a struct because alef's streaming adapter requires a
     * named request type — primitives are not supported.
     */
    final class CrawlStreamRequest
    {
        public function __construct(
            /** @var string The seed URL to crawl. */
            public readonly string $url,
        ) {}

        /**
         * Construct from a JSON string — the only way to build this type's nested/complex fields from PHP, since ext-php-rs exposes them read-only.
         */
        public static function from_json(string $json): self
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getUrl(): string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }
    }

    /**
     * A downloaded asset from a page.
     */
    final class DownloadedAsset
    {
        public function __construct(
            /** @var string The original URL of the asset. */
            public readonly string $url,
            /** @var string The SHA-256 content hash of the asset. */
            public readonly string $contentHash,
            /** @var int The size of the asset in bytes. */
            public readonly int $size,
            /** @var string The category of the asset. */
            public readonly string $assetCategory,
            /** @var ?string The MIME type from the Content-Type header. */
            public readonly ?string $mimeType = null,
            /** @var ?string The HTML tag that referenced this asset (e.g., "link", "script", "img"). */
            public readonly ?string $htmlTag = null,
        ) {}

        /**
         * Construct from a JSON string — the only way to build this type's nested/complex fields from PHP, since ext-php-rs exposes them read-only.
         */
        public static function from_json(string $json): self
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getUrl(): string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getContentHash(): string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getMimeType(): ?string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getSize(): int
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getAssetCategory(): string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getHtmlTag(): ?string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }
    }

    /**
     * A downloaded non-HTML document (PDF, DOCX, image, code file, etc.).
     *
     * When the crawler encounters non-HTML content and `download_documents` is
     * enabled, it downloads the raw bytes and populates this struct instead of
     * skipping the resource.
     */
    final class DownloadedDocument
    {
        public function __construct(
            /** @var string The URL the document was fetched from. */
            public readonly string $url,
            /** @var string The MIME type from the Content-Type header. */
            public readonly string $mimeType,
            /** @var int Size of the document in bytes. */
            public readonly int $size,
            /** @var string SHA-256 hex digest of the content. */
            public readonly string $contentHash,
            /** @var array<string, string> Selected response headers. */
            public readonly array $headers,
            /**
             * True when `content` (or the file at `content_path`) was truncated to
             * `document_max_size`; `size` still reports the original, untruncated length.
             *
             * @var bool
             */
            public readonly bool $truncated,
            /** @var ?string Filename extracted from Content-Disposition or URL path. */
            public readonly ?string $filename = null,
            /**
             * Filesystem path the document was streamed to when `document_output_dir` was
             * set. `content` is empty in memory when this is populated.
             *
             * @var ?string
             */
            public readonly ?string $contentPath = null,
            /**
             * Base64-encoded copy of `content`, populated only when
             * `document_content_encoding` was set to `Base64`.
             *
             * @var ?string
             */
            public readonly ?string $contentBase64 = null,
        ) {}

        /**
         * Construct from a JSON string — the only way to build this type's nested/complex fields from PHP, since ext-php-rs exposes them read-only.
         */
        public static function from_json(string $json): self
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getUrl(): string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getMimeType(): string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getSize(): int
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getFilename(): ?string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getContentHash(): string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        /** @return array<string, string> */
        public function getHeaders(): array
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getTruncated(): bool
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getContentPath(): ?string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getContentBase64(): ?string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }
    }

    /**
     * Metadata about an LLM extraction pass.
     */
    final class ExtractionMeta
    {
        public function __construct(
            /** @var int Number of content chunks sent to the LLM. */
            public readonly int $chunksProcessed,
            /** @var ?float Estimated cost of the LLM call in USD. */
            public readonly ?float $cost = null,
            /** @var ?int Number of prompt (input) tokens consumed. */
            public readonly ?int $promptTokens = null,
            /** @var ?int Number of completion (output) tokens generated. */
            public readonly ?int $completionTokens = null,
            /** @var ?string The model identifier used for extraction. */
            public readonly ?string $model = null,
        ) {}

        /**
         * Construct from a JSON string — the only way to build this type's nested/complex fields from PHP, since ext-php-rs exposes them read-only.
         */
        public static function from_json(string $json): self
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getCost(): ?float
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getPromptTokens(): ?int
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getCompletionTokens(): ?int
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getModel(): ?string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getChunksProcessed(): int
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }
    }

    /**
     * Information about a favicon or icon link.
     */
    final class FaviconInfo
    {
        public function __construct(
            /** @var string The icon URL. */
            public readonly string $url,
            /** @var string The `rel` attribute (e.g., "icon", "apple-touch-icon"). */
            public readonly string $rel,
            /** @var ?string The `sizes` attribute, if present. */
            public readonly ?string $sizes = null,
            /** @var ?string The MIME type, if present. */
            public readonly ?string $mimeType = null,
        ) {}

        /**
         * Construct from a JSON string — the only way to build this type's nested/complex fields from PHP, since ext-php-rs exposes them read-only.
         */
        public static function from_json(string $json): self
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getUrl(): string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getRel(): string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getSizes(): ?string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getMimeType(): ?string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }
    }

    /**
     * Information about a feed link found on a page.
     */
    final class FeedInfo
    {
        public function __construct(
            /** @var string The feed URL. */
            public readonly string $url,
            /** @var string The type of feed. */
            public readonly string $feedType,
            /** @var ?string The feed title, if present. */
            public readonly ?string $title = null,
        ) {}

        /**
         * Construct from a JSON string — the only way to build this type's nested/complex fields from PHP, since ext-php-rs exposes them read-only.
         */
        public static function from_json(string $json): self
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getUrl(): string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getTitle(): ?string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getFeedType(): string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }
    }

    /**
     * A heading element extracted from the page.
     */
    final class HeadingInfo
    {
        public function __construct(
            /** @var int The heading level (1-6). */
            public readonly int $level,
            /** @var string The heading text content. */
            public readonly string $text,
        ) {}

        /**
         * Construct from a JSON string — the only way to build this type's nested/complex fields from PHP, since ext-php-rs exposes them read-only.
         */
        public static function from_json(string $json): self
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getLevel(): int
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getText(): string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }
    }

    /**
     * An hreflang alternate link entry.
     */
    final class HreflangEntry
    {
        public function __construct(
            /** @var string The language code (e.g., "en", "fr", "x-default"). */
            public readonly string $lang,
            /** @var string The URL for this language variant. */
            public readonly string $url,
        ) {}

        /**
         * Construct from a JSON string — the only way to build this type's nested/complex fields from PHP, since ext-php-rs exposes them read-only.
         */
        public static function from_json(string $json): self
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getLang(): string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getUrl(): string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }
    }

    /**
     * Information about an image found on a page.
     */
    final class ImageInfo
    {
        public function __construct(
            /** @var string The image URL. */
            public readonly string $url,
            /** @var string The source of the image reference. */
            public readonly string $source,
            /** @var ?string The alt text, if present. */
            public readonly ?string $alt = null,
            /** @var ?int The width attribute, if present and parseable. */
            public readonly ?int $width = null,
            /** @var ?int The height attribute, if present and parseable. */
            public readonly ?int $height = null,
        ) {}

        /**
         * Construct from a JSON string — the only way to build this type's nested/complex fields from PHP, since ext-php-rs exposes them read-only.
         */
        public static function from_json(string $json): self
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getUrl(): string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getAlt(): ?string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getWidth(): ?int
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getHeight(): ?int
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getSource(): string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }
    }

    /**
     * Result of executing a sequence of page interaction actions.
     */
    final class InteractionResult
    {
        public function __construct(
            /** @var string Final page HTML after all actions completed. */
            public readonly string $finalHtml,
            /** @var string Final page URL (may have changed due to navigation). */
            public readonly string $finalUrl,
            /**
             * Base64-encoded PNG screenshot taken after all actions.
             *
             * Populated only when a `PageAction::Screenshot` action actually ran, so
             * callers that never request a screenshot do not pay the encoding cost.
             *
             * @var ?string
             */
            public readonly ?string $screenshotBase64 = null,
        ) {}

        /**
         * Construct from a JSON string — the only way to build this type's nested/complex fields from PHP, since ext-php-rs exposes them read-only.
         */
        public static function from_json(string $json): self
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        /** @return array<\Crawlberg\ActionResult> */
        /**
         * @readonly Not settable via the constructor — this field's type has no ext-php-rs #[php(prop)]/constructor-param support, so it can only be read via this getter.
         */
        public function getActionResults(): array
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getFinalHtml(): string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getFinalUrl(): string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getScreenshotBase64(): ?string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }
    }

    /**
     * A JSON-LD structured data entry found on a page.
     */
    final class JsonLdEntry
    {
        public function __construct(
            /** @var string The `@type` value from the JSON-LD object. */
            public readonly string $schemaType,
            /** @var string The raw JSON-LD string. */
            public readonly string $raw,
            /** @var ?string The `name` value, if present. */
            public readonly ?string $name = null,
        ) {}

        /**
         * Construct from a JSON string — the only way to build this type's nested/complex fields from PHP, since ext-php-rs exposes them read-only.
         */
        public static function from_json(string $json): self
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getSchemaType(): string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getName(): ?string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getRaw(): string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }
    }

    /**
     * Information about a link found on a page.
     */
    final class LinkInfo
    {
        public function __construct(
            /** @var string The resolved URL of the link. */
            public readonly string $url,
            /** @var string The visible text of the link. */
            public readonly string $text,
            /** @var string The classification of the link. */
            public readonly string $linkType,
            /** @var bool Whether the link has `rel="nofollow"`. */
            public readonly bool $nofollow,
            /** @var ?string The `rel` attribute value, if present. */
            public readonly ?string $rel = null,
        ) {}

        /**
         * Construct from a JSON string — the only way to build this type's nested/complex fields from PHP, since ext-php-rs exposes them read-only.
         */
        public static function from_json(string $json): self
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getUrl(): string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getText(): string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getLinkType(): string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getRel(): ?string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getNofollow(): bool
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }
    }

    /**
     * The result of a map operation, containing discovered URLs.
     */
    final class MapResult
    {
        /**
         * Construct from a JSON string — the only way to build this type's nested/complex fields from PHP, since ext-php-rs exposes them read-only.
         */
        public static function from_json(string $json): self
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        /** @return array<\Crawlberg\SitemapUrl> */
        /**
         * @readonly Not settable via the constructor — this field's type has no ext-php-rs #[php(prop)]/constructor-param support, so it can only be read via this getter.
         */
        public function getUrls(): array
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }
    }

    /**
     * Rich markdown conversion result from HTML processing.
     */
    final class MarkdownResult
    {
        public function __construct(
            /** @var string Converted markdown text. */
            public readonly string $content,
            /** @var array<string> Non-fatal processing warnings. */
            public readonly array $warnings,
            /**
             * Whether citation conversion was applied and produced at least one reference.
             *
             * `true` when the markdown contained inline links that were converted to
             * numbered citation references. The converted content (with `[N]` markers)
             * is available in `content`; the full reference list is accessible via
             * `generate_citations` if needed separately.
             *
             * @var bool
             */
            public readonly bool $citations,
            /** @var ?string Content-filtered markdown optimized for LLM consumption. */
            public readonly ?string $fitContent = null,
        ) {}

        /**
         * Construct from a JSON string — the only way to build this type's nested/complex fields from PHP, since ext-php-rs exposes them read-only.
         */
        public static function from_json(string $json): self
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getContent(): string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        /**
         * @readonly Not settable via the constructor — this field's type has no ext-php-rs #[php(prop)]/constructor-param support, so it can only be read via this getter.
         */
        public function getDocumentStructure(): ?string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        /**
         * @readonly Not settable via the constructor — this field's type has no ext-php-rs #[php(prop)]/constructor-param support, so it can only be read via this getter.
         */
        public function getTables(): ?string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        /** @return array<string> */
        public function getWarnings(): array
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getCitations(): bool
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getFitContent(): ?string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }
    }

    /**
     * Metadata extracted from an HTML page's `<meta>` tags and `<title>` element.
     */
    final class PageMetadata
    {
        /**
         * Construct from a JSON string — the only way to build this type's nested/complex fields from PHP, since ext-php-rs exposes them read-only.
         */
        public static function from_json(string $json): self
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getTitle(): ?string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getDescription(): ?string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getCanonicalUrl(): ?string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getKeywords(): ?string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getAuthor(): ?string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getViewport(): ?string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getThemeColor(): ?string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getGenerator(): ?string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getRobots(): ?string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getHtmlLang(): ?string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getHtmlDir(): ?string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getOgTitle(): ?string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getOgType(): ?string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getOgImage(): ?string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getOgDescription(): ?string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getOgUrl(): ?string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getOgSiteName(): ?string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getOgLocale(): ?string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getOgVideo(): ?string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getOgAudio(): ?string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        /** @return array<string> */
        public function getOgLocaleAlternates(): ?array
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getTwitterCard(): ?string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getTwitterTitle(): ?string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getTwitterDescription(): ?string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getTwitterImage(): ?string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getTwitterSite(): ?string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getTwitterCreator(): ?string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getDcTitle(): ?string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getDcCreator(): ?string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getDcSubject(): ?string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getDcDescription(): ?string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getDcPublisher(): ?string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getDcDate(): ?string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getDcType(): ?string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getDcFormat(): ?string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getDcIdentifier(): ?string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getDcLanguage(): ?string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getDcRights(): ?string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        /**
         * @readonly Not settable via the constructor — this field's type has no ext-php-rs #[php(prop)]/constructor-param support, so it can only be read via this getter.
         */
        public function getArticle(): ?ArticleMetadata
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        /** @return array<\Crawlberg\HreflangEntry> */
        /**
         * @readonly Not settable via the constructor — this field's type has no ext-php-rs #[php(prop)]/constructor-param support, so it can only be read via this getter.
         */
        public function getHreflangs(): ?array
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        /** @return array<\Crawlberg\FaviconInfo> */
        /**
         * @readonly Not settable via the constructor — this field's type has no ext-php-rs #[php(prop)]/constructor-param support, so it can only be read via this getter.
         */
        public function getFavicons(): ?array
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        /** @return array<\Crawlberg\HeadingInfo> */
        /**
         * @readonly Not settable via the constructor — this field's type has no ext-php-rs #[php(prop)]/constructor-param support, so it can only be read via this getter.
         */
        public function getHeadings(): ?array
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getWordCount(): ?int
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }
    }

    /**
     * Proxy configuration for HTTP requests.
     */
    final class ProxyConfig
    {
        public function __construct(
            /** @var string Proxy URL (e.g. "http://proxy:8080", "socks5://proxy:1080"). */
            public readonly string $url,
            /** @var ?string Optional username for proxy authentication. */
            public readonly ?string $username = null,
            /** @var ?string Optional password for proxy authentication. */
            public readonly ?string $password = null,
        ) {}

        /**
         * Construct from a JSON string — the only way to build this type's nested/complex fields from PHP, since ext-php-rs exposes them read-only.
         */
        public static function from_json(string $json): self
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getUrl(): string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getUsername(): ?string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getPassword(): ?string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }
    }

    /**
     * Response metadata extracted from HTTP headers.
     */
    final class ResponseMeta
    {
        /**
         * Construct from a JSON string — the only way to build this type's nested/complex fields from PHP, since ext-php-rs exposes them read-only.
         */
        public static function from_json(string $json): self
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getEtag(): ?string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getLastModified(): ?string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getCacheControl(): ?string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getServer(): ?string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getXPoweredBy(): ?string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getContentLanguage(): ?string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getContentEncoding(): ?string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }
    }

    /**
     * The result of a single-page scrape operation.
     */
    final class ScrapeResult
    {
        public function __construct(
            /** @var int The HTTP status code of the response. */
            public readonly int $statusCode,
            /** @var string The final URL after following all redirects. */
            public readonly string $finalUrl,
            /** @var string The Content-Type header value. */
            public readonly string $contentType,
            /** @var string The HTML body of the response. */
            public readonly string $html,
            /** @var int The size of the response body in bytes. */
            public readonly int $bodySize,
            /** @var bool Whether the URL is allowed by robots.txt. */
            public readonly bool $isAllowed,
            /** @var bool Whether a noindex directive was detected. */
            public readonly bool $noindexDetected,
            /** @var bool Whether a nofollow directive was detected. */
            public readonly bool $nofollowDetected,
            /** @var bool Whether the content is a PDF. */
            public readonly bool $isPdf,
            /** @var bool Whether the page was skipped (binary or PDF content). */
            public readonly bool $wasSkipped,
            /** @var bool Whether an authentication header was sent with the request. */
            public readonly bool $authHeaderSent,
            /** @var bool Whether the page content suggests JavaScript rendering is needed. */
            public readonly bool $jsRenderHint,
            /** @var bool Whether the browser fallback was used to fetch this page. */
            public readonly bool $browserUsed,
            /** @var ?int The crawl delay from robots.txt, in seconds. */
            public readonly ?int $crawlDelay = null,
            /** @var ?string The X-Robots-Tag header value, if present. */
            public readonly ?string $xRobotsTag = null,
            /** @var ?string The detected character set encoding. */
            public readonly ?string $detectedCharset = null,
            /**
             * Base64-encoded PNG screenshot of the page.
             *
             * Populated only when `CrawlConfig.capture_screenshot` was enabled for this
             * request, so callers that never requested a screenshot do not pay the
             * encoding cost.
             *
             * @var ?string
             */
            public readonly ?string $screenshotBase64 = null,
        ) {}

        /**
         * Construct from a JSON string — the only way to build this type's nested/complex fields from PHP, since ext-php-rs exposes them read-only.
         */
        public static function from_json(string $json): self
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getStatusCode(): int
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getFinalUrl(): string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getContentType(): string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getHtml(): string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getBodySize(): int
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        /**
         * @readonly Not settable via the constructor — this field's type has no ext-php-rs #[php(prop)]/constructor-param support, so it can only be read via this getter.
         */
        public function getMetadata(): PageMetadata
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        /** @return array<\Crawlberg\LinkInfo> */
        /**
         * @readonly Not settable via the constructor — this field's type has no ext-php-rs #[php(prop)]/constructor-param support, so it can only be read via this getter.
         */
        public function getLinks(): array
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        /** @return array<\Crawlberg\ImageInfo> */
        /**
         * @readonly Not settable via the constructor — this field's type has no ext-php-rs #[php(prop)]/constructor-param support, so it can only be read via this getter.
         */
        public function getImages(): array
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        /** @return array<\Crawlberg\FeedInfo> */
        /**
         * @readonly Not settable via the constructor — this field's type has no ext-php-rs #[php(prop)]/constructor-param support, so it can only be read via this getter.
         */
        public function getFeeds(): array
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        /** @return array<\Crawlberg\JsonLdEntry> */
        /**
         * @readonly Not settable via the constructor — this field's type has no ext-php-rs #[php(prop)]/constructor-param support, so it can only be read via this getter.
         */
        public function getJsonLd(): array
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getIsAllowed(): bool
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getCrawlDelay(): ?int
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getNoindexDetected(): bool
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getNofollowDetected(): bool
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getXRobotsTag(): ?string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getIsPdf(): bool
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getWasSkipped(): bool
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getDetectedCharset(): ?string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getAuthHeaderSent(): bool
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        /**
         * @readonly Not settable via the constructor — this field's type has no ext-php-rs #[php(prop)]/constructor-param support, so it can only be read via this getter.
         */
        public function getResponseMeta(): ?ResponseMeta
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        /** @return array<\Crawlberg\DownloadedAsset> */
        /**
         * @readonly Not settable via the constructor — this field's type has no ext-php-rs #[php(prop)]/constructor-param support, so it can only be read via this getter.
         */
        public function getAssets(): array
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getJsRenderHint(): bool
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getBrowserUsed(): bool
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        /**
         * @readonly Not settable via the constructor — this field's type has no ext-php-rs #[php(prop)]/constructor-param support, so it can only be read via this getter.
         */
        public function getMarkdown(): ?MarkdownResult
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        /**
         * @readonly Not settable via the constructor — this field's type has no ext-php-rs #[php(prop)]/constructor-param support, so it can only be read via this getter.
         */
        public function getExtractedData(): ?string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        /**
         * @readonly Not settable via the constructor — this field's type has no ext-php-rs #[php(prop)]/constructor-param support, so it can only be read via this getter.
         */
        public function getExtractionMeta(): ?ExtractionMeta
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getScreenshotBase64(): ?string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        /**
         * @readonly Not settable via the constructor — this field's type has no ext-php-rs #[php(prop)]/constructor-param support, so it can only be read via this getter.
         */
        public function getDownloadedDocument(): ?DownloadedDocument
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        /**
         * @readonly Not settable via the constructor — this field's type has no ext-php-rs #[php(prop)]/constructor-param support, so it can only be read via this getter.
         */
        public function getBrowser(): ?BrowserExtras
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }
    }

    /**
     * A URL entry from a sitemap.
     */
    final class SitemapUrl
    {
        public function __construct(
            /** @var string The URL. */
            public readonly string $url,
            /** @var ?string The last modification date, if present. */
            public readonly ?string $lastmod = null,
            /** @var ?string The change frequency, if present. */
            public readonly ?string $changefreq = null,
            /** @var ?string The priority, if present. */
            public readonly ?string $priority = null,
        ) {}

        /**
         * Construct from a JSON string — the only way to build this type's nested/complex fields from PHP, since ext-php-rs exposes them read-only.
         */
        public static function from_json(string $json): self
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getUrl(): string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getLastmod(): ?string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getChangefreq(): ?string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getPriority(): ?string
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }
    }

    /**
     * SSRF policy configuration.
     */
    final class SsrfPolicy
    {
        public function __construct(
            /** @var bool If true, reject URLs that resolve to private/metadata IP ranges. */
            public readonly bool $denyPrivate,
            /** @var int Maximum number of HTTP redirects to follow during validation. */
            public readonly int $maxRedirects,
            /**
             * Allowed URI schemes. Default: `["http", "https"]`.
             *
             * Only `http` and `https` are supported. An empty list denies every URL.
             *
             * @var array<string>
             */
            public readonly array $schemeAllowlist,
        ) {}

        /**
         * Construct from a JSON string — the only way to build this type's nested/complex fields from PHP, since ext-php-rs exposes them read-only.
         */
        public static function from_json(string $json): self
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getDenyPrivate(): bool
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        /** @return array<\Crawlberg\HostMatcher> */
        /**
         * @readonly Not settable via the constructor — this field's type has no ext-php-rs #[php(prop)]/constructor-param support, so it can only be read via this getter.
         */
        public function getAllowlist(): array
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public function getMaxRedirects(): int
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        /** @return array<string> */
        public function getSchemeAllowlist(): array
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public static function default(): SsrfPolicy
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public static function fromEnv(): SsrfPolicy
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }
    }

    final class AssetCategory
    {
        public const DOCUMENT = 'document';
        public const IMAGE = 'image';
        public const AUDIO = 'audio';
        public const VIDEO = 'video';
        public const FONT = 'font';
        public const STYLESHEET = 'stylesheet';
        public const SCRIPT = 'script';
        public const ARCHIVE = 'archive';
        public const DATA = 'data';
        public const OTHER = 'other';
    }

    /**
     * Authentication configuration.
     */
    final class AuthConfig
    {
        /** @var string The variant discriminator carried by the `type` JSON field. */
        public readonly string $type_tag;
        /** @var ?string Username sent in the `Authorization: Basic` header. */
        public readonly ?string $username;
        /** @var ?string Password sent in the `Authorization: Basic` header. */
        public readonly ?string $password;
        /** @var ?string Token sent in the `Authorization: Bearer` header. */
        public readonly ?string $token;
        /** @var ?string HTTP header name to set on each request. */
        public readonly ?string $name;
        /** @var ?string HTTP header value to send. */
        public readonly ?string $value;

        /**
         * Construct from a JSON string — the flat class's only whole-value constructor. The payload's tag field selects the variant; an unrecognised tag throws.
         */
        public static function from_json(string $json): self
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public static function basic(string $username, string $password): AuthConfig
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public static function bearer(string $token): AuthConfig
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public static function header(string $name, string $value): AuthConfig
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }
    }

    final class BrowserBackend
    {
        public const CHROMIUMOXIDE = 'chromiumoxide';
        public const NATIVE = 'native';
    }

    final class BrowserMode
    {
        public const AUTO = 'auto';
        public const ALWAYS = 'always';
        public const NEVER = 'never';
        public const STEALTH = 'stealth';
    }

    final class BrowserWait
    {
        public const NETWORKIDLE = 'network_idle';
        public const SELECTOR = 'selector';
        public const FIXED = 'fixed';
    }

    final class ContentFilterKind
    {
        public const BM25 = 'bm25';
    }

    /**
     * An event emitted during a streaming crawl operation.
     *
     * Not available on `wasm32` targets — streaming requires native concurrency
     * primitives (tokio channels, `JoinSet`) that are not supported on wasm32.
     *
     * Delivered to bindings through each target's native streaming idiom.
     */
    final class CrawlEvent
    {
        /** @var string The variant discriminator carried by the `type` JSON field. */
        public readonly string $type_tag;
        /** @var ?CrawlPageResult The crawled page result. */
        public readonly ?CrawlPageResult $result;
        /** @var ?string The URL that failed. */
        public readonly ?string $url;
        /** @var ?string The error message. */
        public readonly ?string $error;
        /** @var ?int Total number of pages crawled. */
        public readonly ?int $pages_crawled;

        /**
         * Construct from a JSON string — the flat class's only whole-value constructor. The payload's tag field selects the variant; an unrecognised tag throws.
         */
        public static function from_json(string $json): self
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public static function page(CrawlPageResult $result): CrawlEvent
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public static function error(string $url, string $error): CrawlEvent
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public static function complete(int $pagesCrawled): CrawlEvent
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }
    }

    final class CrawlStrategyKind
    {
        public const BFS = 'bfs';
        public const DFS = 'dfs';
        public const BESTFIRST = 'best_first';
        public const ADAPTIVE = 'adaptive';
    }

    final class DocumentContentEncoding
    {
        public const BASE64 = 'base64';
    }

    final class FeedType
    {
        public const RSS = 'rss';
        public const ATOM = 'atom';
        public const JSONFEED = 'json_feed';
    }

    /**
     * Hostname/IP allowlist matcher for SSRF policy.
     *
     * Serializes as an internally-tagged object so each variant is distinguishable on the
     * wire and round-trips losslessly:
     *
     * ```json
     * {"type": "exact",  "value": "api.example.com"}
     * {"type": "suffix", "value": ".example.com"}
     * {"type": "cidr",   "value": "10.0.0.0/8"}
     * ```
     *
     * A bare JSON string is still accepted on deserialization and resolves to `Exact`,
     * preserving configs written against the previous untagged representation.
     *
     * `Exact`: HostMatcher.Exact
     */
    final class HostMatcher
    {
        /** @var string The variant discriminator carried by the `type` JSON field. */
        public readonly string $type_tag;
        /** @var ?string The hostname to match. */
        public readonly ?string $value;

        /**
         * Construct from a JSON string — the flat class's only whole-value constructor. The payload's tag field selects the variant; an unrecognised tag throws.
         */
        public static function from_json(string $json): self
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public static function exact(string $value): HostMatcher
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public static function suffix(string $value): HostMatcher
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public static function cidr(string $value): HostMatcher
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }
    }

    final class ImageSource
    {
        public const IMG = 'img';
        public const PICTURESOURCE = 'picture_source';
        public const OGIMAGE = 'og:image';
        public const TWITTERIMAGE = 'twitter:image';
    }

    final class LinkType
    {
        public const INTERNAL = 'internal';
        public const EXTERNAL = 'external';
        public const ANCHOR = 'anchor';
        public const DOCUMENT = 'document';
    }

    /**
     * A single page interaction action.
     *
     * Actions are serialized with a `type` tag using camelCase naming,
     * except `ExecuteJs` which is explicitly renamed to `"executeJs"`.
     */
    final class PageAction
    {
        /** @var string The variant discriminator carried by the `type` JSON field. */
        public readonly string $type_tag;
        /** @var ?string CSS selector for the element to click. */
        public readonly ?string $selector;
        /** @var ?string Text to type into the element. */
        public readonly ?string $text;
        /** @var ?string Key name to press. */
        public readonly ?string $key;
        /** @var ?string Direction to scroll. */
        public readonly ?string $direction;
        /** @var ?int Optional pixel amount to scroll. Uses a default if absent. */
        public readonly ?int $amount;
        /** @var ?int Milliseconds to wait. Ignored if `selector` is provided. */
        public readonly ?int $milliseconds;
        /**
         * Whether to capture the full scrollable page. Defaults to viewport only.
         *
         * Accepts both the canonical `fullPage` (camelCase) form and the
         * `full_page` (snake_case) alias so language bindings and fixtures can
         * use either convention without error.
         *
         * @var ?bool
         */
        public readonly ?bool $full_page;
        /** @var ?string JavaScript source code to execute. Max 1 MB. */
        public readonly ?string $script;

        /**
         * Construct from a JSON string — the flat class's only whole-value constructor. The payload's tag field selects the variant; an unrecognised tag throws.
         */
        public static function from_json(string $json): self
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public static function click(string $selector): PageAction
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public static function typeText(string $selector, string $text): PageAction
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public static function press(string $key): PageAction
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public static function scroll(string $direction, ?string $selector = null, ?int $amount = null): PageAction
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public static function wait(?int $milliseconds = null, ?string $selector = null): PageAction
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public static function screenshot(?bool $fullPage = null): PageAction
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }

        public static function executeJs(string $script): PageAction
        {
            throw new \RuntimeException('Not implemented — provided by the native extension.');
        }
    }

    final class ScrollDirection
    {
        public const UP = 'up';
        public const DOWN = 'down';
    }

    class CrawlbergApi
    {
        /**
         * @param \Crawlberg\CrawlEngineHandle $engine
         * @param array<string> $urls
         * @return \Crawlberg\BatchCrawlResults
         */
        public static function batchCrawl(
            \Crawlberg\CrawlEngineHandle $engine,
            array $urls,
        ): \Crawlberg\BatchCrawlResults {
            throw new \RuntimeException('Not implemented.');
        }

        /**
         * @param \Crawlberg\CrawlEngineHandle $engine
         * @param array<string> $urls
         * @return \Crawlberg\BatchScrapeResults
         */
        public static function batchScrape(
            \Crawlberg\CrawlEngineHandle $engine,
            array $urls,
        ): \Crawlberg\BatchScrapeResults {
            throw new \RuntimeException('Not implemented.');
        }

        public static function crawl(\Crawlberg\CrawlEngineHandle $engine, string $url): \Crawlberg\CrawlResult
        {
            throw new \RuntimeException('Not implemented.');
        }

        public static function createEngine(?\Crawlberg\CrawlConfig $config = null): \Crawlberg\CrawlEngineHandle
        {
            throw new \RuntimeException('Not implemented.');
        }

        public static function generateCitations(string $markdown): \Crawlberg\CitationResult
        {
            throw new \RuntimeException('Not implemented.');
        }

        /**
         * @param \Crawlberg\CrawlEngineHandle $engine
         * @param string $url
         * @param array<\Crawlberg\PageAction> $actions
         * @return \Crawlberg\InteractionResult
         */
        public static function interact(
            \Crawlberg\CrawlEngineHandle $engine,
            string $url,
            array $actions,
        ): \Crawlberg\InteractionResult {
            throw new \RuntimeException('Not implemented.');
        }

        public static function mapUrls(\Crawlberg\CrawlEngineHandle $engine, string $url): \Crawlberg\MapResult
        {
            throw new \RuntimeException('Not implemented.');
        }

        public static function scrape(\Crawlberg\CrawlEngineHandle $engine, string $url): \Crawlberg\ScrapeResult
        {
            throw new \RuntimeException('Not implemented.');
        }
    }
} // end namespace
