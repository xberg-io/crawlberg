// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:1b83825e283826b963ff9b0d1a61f3f14dcb78acf456379160691117b7425bf6
// To regenerate: alef generate
// To verify freshness: alef verify
const std = @import("std");
const testing = std.testing;
const crawlberg = @import("crawlberg");

extern "c" fn setenv(name: [*:0]const u8, value: [*:0]const u8, overwrite: c_int) c_int;
fn allow_private_network() void {
    _ = setenv("CRAWLBERG_ALLOW_PRIVATE_NETWORK", "true", 1);
}

// E2e tests for category: metadata

test "metadata_article_times" {
    // Extracts article:published_time, modified_time, author, section, and tags
    allow_private_network();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const url = if (std.c.getenv("MOCK_SERVER_METADATA_ARTICLE_TIMES")) |_pf| try std.fmt.allocPrint(allocator, "{s}", .{std.mem.span(_pf)}) else try std.fmt.allocPrint(allocator, "{s}/fixtures/metadata_article_times", .{if (std.c.getenv("MOCK_SERVER_URL")) |v| std.mem.span(v) else "http://localhost:8080"});
    defer allocator.free(url);
    const _result_json = try crawlberg.scrape("{}", url);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expectEqual(@as(i64, 200), result.object.get("status_code").?.integer);
    try testing.expectEqualStrings("2024-01-15T10:00:00Z", result.object.get("metadata").?.object.get("article").?.object.get("published_time").?.string);
    try testing.expectEqualStrings("2024-06-20T14:30:00Z", result.object.get("metadata").?.object.get("article").?.object.get("modified_time").?.string);
    try testing.expectEqualStrings("Jane Developer", result.object.get("metadata").?.object.get("article").?.object.get("author").?.string);
    try testing.expectEqualStrings("Technology", result.object.get("metadata").?.object.get("article").?.object.get("section").?.string);
    try testing.expectEqual(@as(usize, 3), result.object.get("metadata").?.object.get("article").?.object.get("tags").?.array.items.len);
}

test "metadata_favicons" {
    // Extracts favicon link tags including apple-touch-icon
    allow_private_network();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const url = if (std.c.getenv("MOCK_SERVER_METADATA_FAVICONS")) |_pf| try std.fmt.allocPrint(allocator, "{s}", .{std.mem.span(_pf)}) else try std.fmt.allocPrint(allocator, "{s}/fixtures/metadata_favicons", .{if (std.c.getenv("MOCK_SERVER_URL")) |v| std.mem.span(v) else "http://localhost:8080"});
    defer allocator.free(url);
    const _result_json = try crawlberg.scrape("{}", url);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expectEqual(@as(i64, 200), result.object.get("status_code").?.integer);
    try testing.expectEqual(@as(usize, 5), result.object.get("metadata").?.object.get("favicons").?.array.items.len);
    {
        const _WildcardCheck = struct {
            fn check(_wce: std.json.Value) anyerror!void {
                {
                    const _jv = (_wce.object.get("rel") orelse return error.WildcardElementFieldMissing);
                    const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
                    defer if (_jv != .string) std.heap.c_allocator.free(_js);
                    try testing.expect(std.mem.indexOf(u8, _js, "apple-touch-icon") != null);
                }
            }
        };
        var _wc_found = false;
        for (result.object.get("metadata").?.object.get("favicons").?.array.items) |_wc_item| {
            _WildcardCheck.check(_wc_item) catch continue;
            _wc_found = true;
            break;
        }
        try testing.expect(_wc_found); // no element of 'metadata.favicons' matched
    }
}

test "metadata_headings" {
    // Extracts heading hierarchy (h1-h6) from HTML page
    allow_private_network();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const url = if (std.c.getenv("MOCK_SERVER_METADATA_HEADINGS")) |_pf| try std.fmt.allocPrint(allocator, "{s}", .{std.mem.span(_pf)}) else try std.fmt.allocPrint(allocator, "{s}/fixtures/metadata_headings", .{if (std.c.getenv("MOCK_SERVER_URL")) |v| std.mem.span(v) else "http://localhost:8080"});
    defer allocator.free(url);
    const _result_json = try crawlberg.scrape("{}", url);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expectEqual(@as(i64, 200), result.object.get("status_code").?.integer);
    try testing.expectEqual(@as(i64, 1), result.object.get("metadata").?.object.get("headings").?.array.items[0].object.get("level").?.integer);
    try testing.expectEqualStrings("Primary Heading", result.object.get("metadata").?.object.get("headings").?.array.items[0].object.get("text").?.string);
    try testing.expectEqual(@as(usize, 8), result.object.get("metadata").?.object.get("headings").?.array.items.len);
}

test "metadata_hreflang" {
    // Extracts hreflang alternate link tags
    allow_private_network();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const url = if (std.c.getenv("MOCK_SERVER_METADATA_HREFLANG")) |_pf| try std.fmt.allocPrint(allocator, "{s}", .{std.mem.span(_pf)}) else try std.fmt.allocPrint(allocator, "{s}/fixtures/metadata_hreflang", .{if (std.c.getenv("MOCK_SERVER_URL")) |v| std.mem.span(v) else "http://localhost:8080"});
    defer allocator.free(url);
    const _result_json = try crawlberg.scrape("{}", url);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expectEqual(@as(i64, 200), result.object.get("status_code").?.integer);
    try testing.expectEqual(@as(usize, 4), result.object.get("metadata").?.object.get("hreflangs").?.array.items.len);
    {
        const _WildcardCheck = struct {
            fn check(_wce: std.json.Value) anyerror!void {
                {
                    const _jv = (_wce.object.get("lang") orelse return error.WildcardElementFieldMissing);
                    const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
                    defer if (_jv != .string) std.heap.c_allocator.free(_js);
                    try testing.expect(std.mem.indexOf(u8, _js, "en") != null);
                }
            }
        };
        var _wc_found = false;
        for (result.object.get("metadata").?.object.get("hreflangs").?.array.items) |_wc_item| {
            _WildcardCheck.check(_wc_item) catch continue;
            _wc_found = true;
            break;
        }
        try testing.expect(_wc_found); // no element of 'metadata.hreflangs' matched
    }
}

test "metadata_keywords_author" {
    // Extracts keywords, author, viewport, generator, theme-color, robots, lang, dir metadata
    allow_private_network();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const url = if (std.c.getenv("MOCK_SERVER_METADATA_KEYWORDS_AUTHOR")) |_pf| try std.fmt.allocPrint(allocator, "{s}", .{std.mem.span(_pf)}) else try std.fmt.allocPrint(allocator, "{s}/fixtures/metadata_keywords_author", .{if (std.c.getenv("MOCK_SERVER_URL")) |v| std.mem.span(v) else "http://localhost:8080"});
    defer allocator.free(url);
    const _result_json = try crawlberg.scrape("{}", url);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expectEqual(@as(i64, 200), result.object.get("status_code").?.integer);
    try testing.expectEqualStrings("Comprehensive Metadata Test Page", result.object.get("metadata").?.object.get("title").?.string);
    {
        const _ne = result.object.get("metadata").?.object.get("canonical_url").?;
        try testing.expect(switch (_ne) {
            .null => false,
            .string => |_s| _s.len > 0,
            .array => |_a| _a.items.len > 0,
            .object => |_o| _o.count() > 0,
            else => true,
        });
    }
    {
        const _ne = result.object.get("metadata").?.object.get("keywords").?;
        try testing.expect(switch (_ne) {
            .null => false,
            .string => |_s| _s.len > 0,
            .array => |_a| _a.items.len > 0,
            .object => |_o| _o.count() > 0,
            else => true,
        });
    }
    {
        const _jv = result.object.get("metadata").?.object.get("keywords").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "rust") != null);
    }
    try testing.expectEqualStrings("Jane Developer", result.object.get("metadata").?.object.get("author").?.string);
    {
        const _ne = result.object.get("metadata").?.object.get("viewport").?;
        try testing.expect(switch (_ne) {
            .null => false,
            .string => |_s| _s.len > 0,
            .array => |_a| _a.items.len > 0,
            .object => |_o| _o.count() > 0,
            else => true,
        });
    }
    try testing.expectEqualStrings("crawlberg/1.0", result.object.get("metadata").?.object.get("generator").?.string);
    try testing.expectEqualStrings("#ff6600", result.object.get("metadata").?.object.get("theme_color").?.string);
    try testing.expectEqualStrings("index, follow", result.object.get("metadata").?.object.get("robots").?.string);
    try testing.expectEqualStrings("en", result.object.get("metadata").?.object.get("html_lang").?.string);
    try testing.expectEqualStrings("ltr", result.object.get("metadata").?.object.get("html_dir").?.string);
}

test "metadata_og_video_audio" {
    // Extracts og:video, og:audio, and og:locale:alternate metadata
    allow_private_network();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const url = if (std.c.getenv("MOCK_SERVER_METADATA_OG_VIDEO_AUDIO")) |_pf| try std.fmt.allocPrint(allocator, "{s}", .{std.mem.span(_pf)}) else try std.fmt.allocPrint(allocator, "{s}/fixtures/metadata_og_video_audio", .{if (std.c.getenv("MOCK_SERVER_URL")) |v| std.mem.span(v) else "http://localhost:8080"});
    defer allocator.free(url);
    const _result_json = try crawlberg.scrape("{}", url);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expectEqual(@as(i64, 200), result.object.get("status_code").?.integer);
    try testing.expectEqualStrings("https://example.com/video.mp4", result.object.get("metadata").?.object.get("og_video").?.string);
    try testing.expectEqualStrings("https://example.com/audio.mp3", result.object.get("metadata").?.object.get("og_audio").?.string);
    try testing.expectEqual(@as(usize, 2), result.object.get("metadata").?.object.get("og_locale_alternates").?.array.items.len);
}

test "metadata_response_headers" {
    // Extracts response metadata from HTTP headers (etag, server, content-language)
    allow_private_network();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const url = if (std.c.getenv("MOCK_SERVER_METADATA_RESPONSE_HEADERS")) |_pf| try std.fmt.allocPrint(allocator, "{s}", .{std.mem.span(_pf)}) else try std.fmt.allocPrint(allocator, "{s}/fixtures/metadata_response_headers", .{if (std.c.getenv("MOCK_SERVER_URL")) |v| std.mem.span(v) else "http://localhost:8080"});
    defer allocator.free(url);
    const _result_json = try crawlberg.scrape("{}", url);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expectEqual(@as(i64, 200), result.object.get("status_code").?.integer);
    {
        const _jv = result.object.get("response_meta").?.object.get("etag").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "abc123") != null);
    }
    {
        const _jv = result.object.get("response_meta").?.object.get("last_modified").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "01 Jan 2025") != null);
    }
    {
        const _jv = result.object.get("response_meta").?.object.get("server").?;
        const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
        defer if (_jv != .string) std.heap.c_allocator.free(_js);
        try testing.expect(std.mem.indexOf(u8, _js, "nginx") != null);
    }
    try testing.expectEqualStrings("en-US", result.object.get("response_meta").?.object.get("content_language").?.string);
}

test "metadata_word_count" {
    // Computes word count from visible page text
    allow_private_network();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const url = if (std.c.getenv("MOCK_SERVER_METADATA_WORD_COUNT")) |_pf| try std.fmt.allocPrint(allocator, "{s}", .{std.mem.span(_pf)}) else try std.fmt.allocPrint(allocator, "{s}/fixtures/metadata_word_count", .{if (std.c.getenv("MOCK_SERVER_URL")) |v| std.mem.span(v) else "http://localhost:8080"});
    defer allocator.free(url);
    const _result_json = try crawlberg.scrape("{}", url);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expectEqual(@as(i64, 200), result.object.get("status_code").?.integer);
    try testing.expect(result.object.get("metadata").?.object.get("word_count").?.integer > @as(i64, 99));
    try testing.expect(result.object.get("metadata").?.object.get("word_count").?.integer < @as(i64, 301));
}
