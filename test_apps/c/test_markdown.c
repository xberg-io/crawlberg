/*
 * This file is auto-generated by alef — DO NOT EDIT.
 * alef:hash:913e9cd05efb7e772fb8974f87da9846582cd7eb46102009f83b3fbdca205479
 * To regenerate: alef generate
 * To verify freshness: alef verify
 */
/* E2e tests for category: markdown */

#include "crawlberg.h"
#include "test_runner.h"
#include <assert.h>
#include <stdint.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

void test_citations_balanced_parens(void) {
  /* Citations correctly handle links inside parentheses with balanced rendering
   */
  CBERGAlefHandle config_handle = cberg_crawl_config_from_json("{}");
  assert(config_handle != 0 && "failed to parse config");
  CBERGAlefHandle engine = cberg_create_engine(config_handle);
  cberg_crawl_config_free(config_handle);
  assert(engine != 0 && "failed to create engine");
  const char *mock_per_fixture =
      getenv("MOCK_SERVER_CITATIONS_BALANCED_PARENS");
  const char *mock_base = getenv("MOCK_SERVER_URL");
  char url[2048];
  if (mock_per_fixture && mock_per_fixture[0] != '\0') {
    snprintf(url, sizeof(url), "%s", mock_per_fixture);
  } else {
    assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
    snprintf(url, sizeof(url), "%s/fixtures/citations_balanced_parens",
             mock_base);
  }
  CBERGAlefHandle result = cberg_scrape(engine, url);
  assert(result != 0 && "expected call to succeed");
  uint16_t status_code = cberg_scrape_result_status_code(result);
  CBERGAlefHandle markdown_handle = cberg_scrape_result_markdown(result);
  assert(markdown_handle != 0);
  char *markdown_content = cberg_markdown_result_content(markdown_handle);
  assert(status_code == 200 && "equals assertion failed");
  assert(markdown_content != NULL && strlen(markdown_content) > 0 &&
         "expected non-empty value");
  assert(markdown_content != NULL && strstr(markdown_content, "(") != NULL &&
         "expected to contain substring");
  cberg_free_string(markdown_content);
  cberg_markdown_result_free(markdown_handle);
  cberg_scrape_result_free(result);
  cberg_crawl_engine_handle_free(engine);
}

void test_citations_duplicate_urls(void) {
  /* Citations deduplicates multiple links to the same URL */
  CBERGAlefHandle config_handle = cberg_crawl_config_from_json("{}");
  assert(config_handle != 0 && "failed to parse config");
  CBERGAlefHandle engine = cberg_create_engine(config_handle);
  cberg_crawl_config_free(config_handle);
  assert(engine != 0 && "failed to create engine");
  const char *mock_per_fixture = getenv("MOCK_SERVER_CITATIONS_DUPLICATE_URLS");
  const char *mock_base = getenv("MOCK_SERVER_URL");
  char url[2048];
  if (mock_per_fixture && mock_per_fixture[0] != '\0') {
    snprintf(url, sizeof(url), "%s", mock_per_fixture);
  } else {
    assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
    snprintf(url, sizeof(url), "%s/fixtures/citations_duplicate_urls",
             mock_base);
  }
  CBERGAlefHandle result = cberg_scrape(engine, url);
  assert(result != 0 && "expected call to succeed");
  uint16_t status_code = cberg_scrape_result_status_code(result);
  CBERGAlefHandle markdown_handle = cberg_scrape_result_markdown(result);
  assert(markdown_handle != 0);
  char *markdown_content = cberg_markdown_result_content(markdown_handle);
  int32_t markdown_citations = cberg_markdown_result_citations(markdown_handle);
  assert(status_code == 200 && "equals assertion failed");
  assert(markdown_content != NULL && strlen(markdown_content) > 0 &&
         "expected non-empty value");
  assert(markdown_citations);
  cberg_free_string(markdown_content);
  cberg_markdown_result_free(markdown_handle);
  cberg_scrape_result_free(result);
  cberg_crawl_engine_handle_free(engine);
}

void test_markdown_basic_conversion(void) {
  /* HTML is always converted to markdown alongside raw HTML */
  CBERGAlefHandle config_handle = cberg_crawl_config_from_json("{}");
  assert(config_handle != 0 && "failed to parse config");
  CBERGAlefHandle engine = cberg_create_engine(config_handle);
  cberg_crawl_config_free(config_handle);
  assert(engine != 0 && "failed to create engine");
  const char *mock_per_fixture =
      getenv("MOCK_SERVER_MARKDOWN_BASIC_CONVERSION");
  const char *mock_base = getenv("MOCK_SERVER_URL");
  char url[2048];
  if (mock_per_fixture && mock_per_fixture[0] != '\0') {
    snprintf(url, sizeof(url), "%s", mock_per_fixture);
  } else {
    assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
    snprintf(url, sizeof(url), "%s/fixtures/markdown_basic_conversion",
             mock_base);
  }
  CBERGAlefHandle result = cberg_scrape(engine, url);
  assert(result != 0 && "expected call to succeed");
  uint16_t status_code = cberg_scrape_result_status_code(result);
  CBERGAlefHandle metadata_handle = cberg_scrape_result_metadata(result);
  assert(metadata_handle != 0);
  char *metadata_title = cberg_page_metadata_title(metadata_handle);
  char *html = cberg_scrape_result_html(result);
  CBERGAlefHandle markdown_handle = cberg_scrape_result_markdown(result);
  assert(markdown_handle != 0);
  char *markdown_content = cberg_markdown_result_content(markdown_handle);
  assert(status_code == 200 && "equals assertion failed");
  assert(metadata_title != NULL && strcmp(metadata_title, "Test") == 0 &&
         "equals assertion failed");
  assert(html != NULL && strlen(html) > 0 && "expected non-empty value");
  assert(markdown_content != NULL && strlen(markdown_content) > 0 &&
         "expected non-empty value");
  assert(markdown_content != NULL &&
         strstr(markdown_content, "Hello World") != NULL &&
         "expected to contain substring");
  cberg_free_string(metadata_title);
  cberg_free_string(html);
  cberg_free_string(markdown_content);
  cberg_markdown_result_free(markdown_handle);
  cberg_page_metadata_free(metadata_handle);
  cberg_scrape_result_free(result);
  cberg_crawl_engine_handle_free(engine);
}

void test_markdown_crawl_all_pages(void) {
  /* All crawled pages have markdown field populated */
  CBERGAlefHandle config_handle =
      cberg_crawl_config_from_json("{\"max_depth\":1}");
  assert(config_handle != 0 && "failed to parse config");
  CBERGAlefHandle engine = cberg_create_engine(config_handle);
  cberg_crawl_config_free(config_handle);
  assert(engine != 0 && "failed to create engine");
  const char *mock_per_fixture = getenv("MOCK_SERVER_MARKDOWN_CRAWL_ALL_PAGES");
  const char *mock_base = getenv("MOCK_SERVER_URL");
  char url[2048];
  if (mock_per_fixture && mock_per_fixture[0] != '\0') {
    snprintf(url, sizeof(url), "%s", mock_per_fixture);
  } else {
    assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
    snprintf(url, sizeof(url), "%s/fixtures/markdown_crawl_all_pages",
             mock_base);
  }
  CBERGAlefHandle result = cberg_crawl(engine, url);
  assert(result != 0 && "expected call to succeed");
  char *pages_json = cberg_crawl_result_pages(result);
  int crawl_pages_crawled = alef_json_array_count(pages_json);
  assert(crawl_pages_crawled == 2 && "equals assertion failed");
  cberg_free_string(pages_json);
  cberg_crawl_result_free(result);
  cberg_crawl_engine_handle_free(engine);
}

void test_markdown_fit_content(void) {
  /* Fit markdown removes navigation and boilerplate content */
  CBERGAlefHandle config_handle = cberg_crawl_config_from_json("{}");
  assert(config_handle != 0 && "failed to parse config");
  CBERGAlefHandle engine = cberg_create_engine(config_handle);
  cberg_crawl_config_free(config_handle);
  assert(engine != 0 && "failed to create engine");
  const char *mock_per_fixture = getenv("MOCK_SERVER_MARKDOWN_FIT_CONTENT");
  const char *mock_base = getenv("MOCK_SERVER_URL");
  char url[2048];
  if (mock_per_fixture && mock_per_fixture[0] != '\0') {
    snprintf(url, sizeof(url), "%s", mock_per_fixture);
  } else {
    assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
    snprintf(url, sizeof(url), "%s/fixtures/markdown_fit_content", mock_base);
  }
  CBERGAlefHandle result = cberg_scrape(engine, url);
  assert(result != 0 && "expected call to succeed");
  uint16_t status_code = cberg_scrape_result_status_code(result);
  CBERGAlefHandle markdown_handle = cberg_scrape_result_markdown(result);
  assert(markdown_handle != 0);
  char *markdown_content = cberg_markdown_result_content(markdown_handle);
  assert(status_code == 200 && "equals assertion failed");
  assert(markdown_content != NULL && strlen(markdown_content) > 0 &&
         "expected non-empty value");
  cberg_free_string(markdown_content);
  cberg_markdown_result_free(markdown_handle);
  cberg_scrape_result_free(result);
  cberg_crawl_engine_handle_free(engine);
}

void test_markdown_headings_and_paragraphs(void) {
  /* Markdown conversion preserves heading hierarchy and paragraph text */
  CBERGAlefHandle config_handle = cberg_crawl_config_from_json("{}");
  assert(config_handle != 0 && "failed to parse config");
  CBERGAlefHandle engine = cberg_create_engine(config_handle);
  cberg_crawl_config_free(config_handle);
  assert(engine != 0 && "failed to create engine");
  const char *mock_per_fixture =
      getenv("MOCK_SERVER_MARKDOWN_HEADINGS_AND_PARAGRAPHS");
  const char *mock_base = getenv("MOCK_SERVER_URL");
  char url[2048];
  if (mock_per_fixture && mock_per_fixture[0] != '\0') {
    snprintf(url, sizeof(url), "%s", mock_per_fixture);
  } else {
    assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
    snprintf(url, sizeof(url), "%s/fixtures/markdown_headings_and_paragraphs",
             mock_base);
  }
  CBERGAlefHandle result = cberg_scrape(engine, url);
  assert(result != 0 && "expected call to succeed");
  CBERGAlefHandle markdown_handle = cberg_scrape_result_markdown(result);
  assert(markdown_handle != 0);
  char *markdown_content = cberg_markdown_result_content(markdown_handle);
  assert(markdown_content != NULL && strlen(markdown_content) > 0 &&
         "expected non-empty value");
  assert(markdown_content != NULL &&
         strstr(markdown_content, "Main Title") != NULL &&
         "expected to contain substring");
  cberg_free_string(markdown_content);
  cberg_markdown_result_free(markdown_handle);
  cberg_scrape_result_free(result);
  cberg_crawl_engine_handle_free(engine);
}

void test_markdown_links_converted(void) {
  /* HTML links are converted to markdown link syntax */
  CBERGAlefHandle config_handle = cberg_crawl_config_from_json("{}");
  assert(config_handle != 0 && "failed to parse config");
  CBERGAlefHandle engine = cberg_create_engine(config_handle);
  cberg_crawl_config_free(config_handle);
  assert(engine != 0 && "failed to create engine");
  const char *mock_per_fixture = getenv("MOCK_SERVER_MARKDOWN_LINKS_CONVERTED");
  const char *mock_base = getenv("MOCK_SERVER_URL");
  char url[2048];
  if (mock_per_fixture && mock_per_fixture[0] != '\0') {
    snprintf(url, sizeof(url), "%s", mock_per_fixture);
  } else {
    assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
    snprintf(url, sizeof(url), "%s/fixtures/markdown_links_converted",
             mock_base);
  }
  CBERGAlefHandle result = cberg_scrape(engine, url);
  assert(result != 0 && "expected call to succeed");
  uint16_t status_code = cberg_scrape_result_status_code(result);
  char *html = cberg_scrape_result_html(result);
  CBERGAlefHandle markdown_handle = cberg_scrape_result_markdown(result);
  assert(markdown_handle != 0);
  char *markdown_content = cberg_markdown_result_content(markdown_handle);
  assert(status_code == 200 && "equals assertion failed");
  assert(html != NULL && strlen(html) > 0 && "expected non-empty value");
  assert(markdown_content != NULL && strlen(markdown_content) > 0 &&
         "expected non-empty value");
  assert(markdown_content != NULL &&
         strstr(markdown_content, "Example") != NULL &&
         "expected to contain substring");
  cberg_free_string(html);
  cberg_free_string(markdown_content);
  cberg_markdown_result_free(markdown_handle);
  cberg_scrape_result_free(result);
  cberg_crawl_engine_handle_free(engine);
}

void test_markdown_with_citations(void) {
  /* Markdown includes citation conversion with numbered references */
  CBERGAlefHandle config_handle = cberg_crawl_config_from_json("{}");
  assert(config_handle != 0 && "failed to parse config");
  CBERGAlefHandle engine = cberg_create_engine(config_handle);
  cberg_crawl_config_free(config_handle);
  assert(engine != 0 && "failed to create engine");
  const char *mock_per_fixture = getenv("MOCK_SERVER_MARKDOWN_WITH_CITATIONS");
  const char *mock_base = getenv("MOCK_SERVER_URL");
  char url[2048];
  if (mock_per_fixture && mock_per_fixture[0] != '\0') {
    snprintf(url, sizeof(url), "%s", mock_per_fixture);
  } else {
    assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
    snprintf(url, sizeof(url), "%s/fixtures/markdown_with_citations",
             mock_base);
  }
  CBERGAlefHandle result = cberg_scrape(engine, url);
  assert(result != 0 && "expected call to succeed");
  uint16_t status_code = cberg_scrape_result_status_code(result);
  CBERGAlefHandle markdown_handle = cberg_scrape_result_markdown(result);
  assert(markdown_handle != 0);
  char *markdown_content = cberg_markdown_result_content(markdown_handle);
  assert(status_code == 200 && "equals assertion failed");
  assert(markdown_content != NULL && strlen(markdown_content) > 0 &&
         "expected non-empty value");
  cberg_free_string(markdown_content);
  cberg_markdown_result_free(markdown_handle);
  cberg_scrape_result_free(result);
  cberg_crawl_engine_handle_free(engine);
}
