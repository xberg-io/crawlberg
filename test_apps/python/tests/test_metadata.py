# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:ba3d1bb774f29d7b5008ef9ab4117ac59922b16cee70d8386eb61aba43a5bef7
# To regenerate: alef generate
# To verify freshness: alef verify

"""E2e tests for category: metadata."""

import os

import pytest
from crawlberg import scrape, create_engine


def _alef_e2e_text(value: object) -> str:
    return "" if value is None else str(value)


@pytest.mark.asyncio
async def test_metadata_article_times() -> None:
    """Extracts article:published_time, modified_time, author, section, and tags."""
    engine = create_engine(None)
    url = os.environ["MOCK_SERVER_URL"] + "/fixtures/metadata_article_times"

    result = await scrape(engine, url)
    assert result.status_code == 200
    assert (result.metadata.article.published_time if result.metadata.article else None) == "2024-01-15T10:00:00Z"
    assert (result.metadata.article.modified_time if result.metadata.article else None) == "2024-06-20T14:30:00Z"
    assert (result.metadata.article.author if result.metadata.article else None) == "Jane Developer"
    assert (result.metadata.article.section if result.metadata.article else None) == "Technology"
    assert (len(result.metadata.article.tags) if result.metadata.article else None) == 3


@pytest.mark.asyncio
async def test_metadata_favicons() -> None:
    """Extracts favicon link tags including apple-touch-icon."""
    engine = create_engine(None)
    url = os.environ["MOCK_SERVER_URL"] + "/fixtures/metadata_favicons"

    result = await scrape(engine, url)
    assert result.status_code == 200
    assert (len(result.metadata.favicons) if result.metadata.favicons else None) == 5
    assert any("apple-touch-icon" in str(_e.rel) for _e in (result.metadata.favicons or []))


@pytest.mark.asyncio
async def test_metadata_headings() -> None:
    """Extracts heading hierarchy (h1-h6) from HTML page."""
    engine = create_engine(None)
    url = os.environ["MOCK_SERVER_URL"] + "/fixtures/metadata_headings"

    result = await scrape(engine, url)
    assert result.status_code == 200
    assert (result.metadata.headings[0].level if result.metadata.headings else None) == 1
    assert (
        _alef_e2e_text((result.metadata.headings[0].text if result.metadata.headings else None)).lower()
        == "Primary Heading".lower()
    )
    assert (len(result.metadata.headings) if result.metadata.headings else None) == 8


@pytest.mark.asyncio
async def test_metadata_hreflang() -> None:
    """Extracts hreflang alternate link tags."""
    engine = create_engine(None)
    url = os.environ["MOCK_SERVER_URL"] + "/fixtures/metadata_hreflang"

    result = await scrape(engine, url)
    assert result.status_code == 200
    assert (len(result.metadata.hreflangs) if result.metadata.hreflangs else None) == 4
    assert any("en" in str(_e.lang) for _e in (result.metadata.hreflangs or []))


@pytest.mark.asyncio
async def test_metadata_keywords_author() -> None:
    """Extracts keywords, author, viewport, generator, theme-color, robots, lang, dir metadata."""
    engine = create_engine(None)
    url = os.environ["MOCK_SERVER_URL"] + "/fixtures/metadata_keywords_author"

    result = await scrape(engine, url)
    assert result.status_code == 200
    assert result.metadata.title == "Comprehensive Metadata Test Page"
    assert result.metadata.canonical_url is not None and (
        not hasattr(result.metadata.canonical_url, "__len__") or len(result.metadata.canonical_url) > 0
    )
    assert result.metadata.keywords is not None and (
        not hasattr(result.metadata.keywords, "__len__") or len(result.metadata.keywords) > 0
    )
    assert result.metadata.keywords is not None
    assert "rust" in result.metadata.keywords
    assert result.metadata.author == "Jane Developer"
    assert result.metadata.viewport is not None and (
        not hasattr(result.metadata.viewport, "__len__") or len(result.metadata.viewport) > 0
    )
    assert result.metadata.generator == "crawlberg/1.0"
    assert result.metadata.theme_color == "#ff6600"
    assert result.metadata.robots == "index, follow"
    assert result.metadata.html_lang == "en"
    assert result.metadata.html_dir == "ltr"


@pytest.mark.asyncio
async def test_metadata_og_video_audio() -> None:
    """Extracts og:video, og:audio, and og:locale:alternate metadata."""
    engine = create_engine(None)
    url = os.environ["MOCK_SERVER_URL"] + "/fixtures/metadata_og_video_audio"

    result = await scrape(engine, url)
    assert result.status_code == 200
    assert result.metadata.og_video == "https://example.com/video.mp4"
    assert result.metadata.og_audio == "https://example.com/audio.mp3"
    assert (len(result.metadata.og_locale_alternates) if result.metadata.og_locale_alternates else None) == 2


@pytest.mark.asyncio
async def test_metadata_response_headers() -> None:
    """Extracts response metadata from HTTP headers (etag, server, content-language)."""
    engine = create_engine(None)
    url = os.environ["MOCK_SERVER_URL"] + "/fixtures/metadata_response_headers"

    result = await scrape(engine, url)
    assert result.status_code == 200
    assert (result.response_meta.etag if result.response_meta else None) is not None
    assert "abc123" in (result.response_meta.etag if result.response_meta else None)
    assert (result.response_meta.last_modified if result.response_meta else None) is not None
    assert "01 Jan 2025" in (result.response_meta.last_modified if result.response_meta else None)
    assert (result.response_meta.server if result.response_meta else None) is not None
    assert "nginx" in (result.response_meta.server if result.response_meta else None)
    assert (result.response_meta.content_language if result.response_meta else None) == "en-US"


@pytest.mark.asyncio
async def test_metadata_word_count() -> None:
    """Computes word count from visible page text."""
    engine = create_engine(None)
    url = os.environ["MOCK_SERVER_URL"] + "/fixtures/metadata_word_count"

    result = await scrape(engine, url)
    assert result.status_code == 200
    assert result.metadata.word_count > 99
    assert result.metadata.word_count < 301
