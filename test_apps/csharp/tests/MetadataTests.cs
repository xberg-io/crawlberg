// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:8298fd194154146815defd48db3e2180eb4bd94528d6ed21044b739b87f79d80
// To regenerate: alef generate
// To verify freshness: alef verify

using System;
using System.Collections.Generic;
using System.Linq;
using System.Net.Http;
using System.Text;
using System.Text.Json;
using System.Text.Json.Serialization;
using System.Threading.Tasks;
using Xunit;
using Crawlberg;
using static Crawlberg.CrawlbergConverter;

namespace Crawlberg {
/// <summary>E2e tests for category: metadata.</summary>
public class MetadataTests {
  private static readonly JsonSerializerOptions ConfigOptions =
      new() { Converters = { new JsonStringEnumConverter(
                  JsonNamingPolicy.SnakeCaseLower) },
              DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingDefault };

  [Fact]
  public async Task Test_MetadataArticleTimes() {
    // Extracts article:published_time, modified_time, author, section, and tags
    var engine = CrawlbergConverter.CreateEngine(null);
    var url = Environment.GetEnvironmentVariable("MOCK_SERVER_URL") +
              "/fixtures/metadata_article_times";
    var result = await CrawlbergConverter.ScrapeAsync(engine, url);
    Assert.True(result.StatusCode == 200);
    Assert.Equal("2024-01-15T10:00:00Z",
                 result.Metadata.Article!.PublishedTime);
    Assert.Equal("2024-06-20T14:30:00Z", result.Metadata.Article!.ModifiedTime);
    Assert.Equal("Jane Developer", result.Metadata.Article!.Author);
    Assert.Equal("Technology", result.Metadata.Article!.Section);
    Assert.True(result.Metadata.Article!.Tags.Count == 3);
  }

  [Fact]
  public async Task Test_MetadataFavicons() {
    // Extracts favicon link tags including apple-touch-icon
    var engine = CrawlbergConverter.CreateEngine(null);
    var url = Environment.GetEnvironmentVariable("MOCK_SERVER_URL") +
              "/fixtures/metadata_favicons";
    var result = await CrawlbergConverter.ScrapeAsync(engine, url);
    Assert.True(result.StatusCode == 200);
    Assert.True(result.Metadata.Favicons!.Count == 5);
    Assert.True(result.Metadata.Favicons?.Any(
                    e53736157 => Convert.ToString(e53736157.Rel)!.Contains(
                        "apple-touch-icon")) == true,
                "element of 'favicons[].rel': apple-touch-icon");
  }

  [Fact]
  public async Task Test_MetadataHeadings() {
    // Extracts heading hierarchy (h1-h6) from HTML page
    var engine = CrawlbergConverter.CreateEngine(null);
    var url = Environment.GetEnvironmentVariable("MOCK_SERVER_URL") +
              "/fixtures/metadata_headings";
    var result = await CrawlbergConverter.ScrapeAsync(engine, url);
    Assert.True(result.StatusCode == 200);
    Assert.True(result.Metadata.Headings[0].Level == 1);
    Assert.Equal("Primary Heading", result.Metadata.Headings[0].Text);
    Assert.True(result.Metadata.Headings!.Count == 8);
  }

  [Fact]
  public async Task Test_MetadataHreflang() {
    // Extracts hreflang alternate link tags
    var engine = CrawlbergConverter.CreateEngine(null);
    var url = Environment.GetEnvironmentVariable("MOCK_SERVER_URL") +
              "/fixtures/metadata_hreflang";
    var result = await CrawlbergConverter.ScrapeAsync(engine, url);
    Assert.True(result.StatusCode == 200);
    Assert.True(result.Metadata.Hreflangs!.Count == 4);
    Assert.True(result.Metadata.Hreflangs?.Any(
                    e7d2e929c => Convert.ToString(e7d2e929c.Lang)!.Contains(
                        "en")) == true,
                "element of 'hreflang[].lang': en");
  }

  [Fact]
  public async Task Test_MetadataKeywordsAuthor() {
    // Extracts keywords, author, viewport, generator, theme-color, robots,
    // lang, dir metadata
    var engine = CrawlbergConverter.CreateEngine(null);
    var url = Environment.GetEnvironmentVariable("MOCK_SERVER_URL") +
              "/fixtures/metadata_keywords_author";
    var result = await CrawlbergConverter.ScrapeAsync(engine, url);
    Assert.True(result.StatusCode == 200);
    Assert.Equal("Comprehensive Metadata Test Page", result.Metadata.Title);
    Assert.True(
        ((object?)result.Metadata.CanonicalUrl) switch {
          null => false,
          string text => text.Length > 0,
          System.Collections.ICollection items => items.Count > 0,
          _ => true,
        },
        "expected non-empty value");
    Assert.True(
        ((object?)result.Metadata.Keywords) switch {
          null => false,
          string text => text.Length > 0,
          System.Collections.ICollection items => items.Count > 0,
          _ => true,
        },
        "expected non-empty value");
    Assert.Contains("rust", result.Metadata.Keywords.ToString());
    Assert.Equal("Jane Developer", result.Metadata.Author);
    Assert.True(
        ((object?)result.Metadata.Viewport) switch {
          null => false,
          string text => text.Length > 0,
          System.Collections.ICollection items => items.Count > 0,
          _ => true,
        },
        "expected non-empty value");
    Assert.Equal("crawlberg/1.0", result.Metadata.Generator);
    Assert.Equal("#ff6600", result.Metadata.ThemeColor);
    Assert.Equal("index, follow", result.Metadata.Robots);
    Assert.Equal("en", result.Metadata.HtmlLang);
    Assert.Equal("ltr", result.Metadata.HtmlDir);
  }

  [Fact]
  public async Task Test_MetadataOgVideoAudio() {
    // Extracts og:video, og:audio, and og:locale:alternate metadata
    var engine = CrawlbergConverter.CreateEngine(null);
    var url = Environment.GetEnvironmentVariable("MOCK_SERVER_URL") +
              "/fixtures/metadata_og_video_audio";
    var result = await CrawlbergConverter.ScrapeAsync(engine, url);
    Assert.True(result.StatusCode == 200);
    Assert.Equal("https://example.com/video.mp4", result.Metadata.OgVideo);
    Assert.Equal("https://example.com/audio.mp3", result.Metadata.OgAudio);
    Assert.True(result.Metadata.OgLocaleAlternates!.Count == 2);
  }

  [Fact]
  public async Task Test_MetadataResponseHeaders() {
    // Extracts response metadata from HTTP headers (etag, server,
    // content-language)
    var engine = CrawlbergConverter.CreateEngine(null);
    var url = Environment.GetEnvironmentVariable("MOCK_SERVER_URL") +
              "/fixtures/metadata_response_headers";
    var result = await CrawlbergConverter.ScrapeAsync(engine, url);
    Assert.True(result.StatusCode == 200);
    Assert.Contains("abc123", result.ResponseMeta!.Etag.ToString());
    Assert.Contains("01 Jan 2025",
                    result.ResponseMeta!.LastModified.ToString());
    Assert.Contains("nginx", result.ResponseMeta!.Server.ToString());
    Assert.Equal("en-US", result.ResponseMeta!.ContentLanguage);
  }

  [Fact]
  public async Task Test_MetadataWordCount() {
    // Computes word count from visible page text
    var engine = CrawlbergConverter.CreateEngine(null);
    var url = Environment.GetEnvironmentVariable("MOCK_SERVER_URL") +
              "/fixtures/metadata_word_count";
    var result = await CrawlbergConverter.ScrapeAsync(engine, url);
    Assert.True(result.StatusCode == 200);
    Assert.True(result.Metadata.WordCount > 99, "expected > 99");
    Assert.True(result.Metadata.WordCount < 301, "expected < 301");
  }
}
}
