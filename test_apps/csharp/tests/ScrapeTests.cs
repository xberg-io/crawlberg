// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:c632a7b137687f6e3e8a4084746f555c8f273772862985087f5b3efd39d5d89e
// To regenerate: alef generate
// To verify freshness: alef verify

using System;
using System.Collections.Generic;
using System.Linq;
using System.Net.Http;
using System.Text;
using System.Text.Json;
using System.Text.Json.Serialization;
using System.Threading.Tasks;
using Xunit;
using Crawlberg;
using static Crawlberg.CrawlbergConverter;

namespace Crawlberg {
/// <summary>E2e tests for category: scrape.</summary>
public class ScrapeTests {
  private static readonly JsonSerializerOptions ConfigOptions =
      new() { Converters = { new JsonStringEnumConverter(
                  JsonNamingPolicy.SnakeCaseLower) },
              DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingDefault };

  [Fact]
  public async Task Test_ScrapeAssetDedup() {
    // Same asset linked twice results in one download with one unique hash
    var engineConfig = JsonSerializer.Deserialize<CrawlConfig>(
        "{\"download_assets\":true}", ConfigOptions)!;
    var engine = CrawlbergConverter.CreateEngine(engineConfig);
    var _pfUrl_url =
        Environment.GetEnvironmentVariable("MOCK_SERVER_SCRAPE_ASSET_DEDUP");
    var url = !string.IsNullOrEmpty(_pfUrl_url)
                  ? _pfUrl_url
                  : Environment.GetEnvironmentVariable("MOCK_SERVER_URL") +
                        "/fixtures/scrape_asset_dedup";
    var result = await CrawlbergConverter.ScrapeAsync(engine, url);
    Assert.True(result.StatusCode == 200);
    Assert.True(result.Assets.Count == 2);
    Assert.True(
        result.Assets?.Any(e527b7187 => !string.IsNullOrEmpty(Convert.ToString(
                               e527b7187.ContentHash))) == true,
        "expected some element of 'assets[].content_hash' to be non-empty");
  }

  [Fact]
  public async Task Test_ScrapeAssetMaxSize() {
    // Skips assets exceeding max_asset_size limit
    var engineConfig = JsonSerializer.Deserialize<CrawlConfig>(
        "{\"download_assets\":true,\"max_asset_size\":150}", ConfigOptions)!;
    var engine = CrawlbergConverter.CreateEngine(engineConfig);
    var _pfUrl_url =
        Environment.GetEnvironmentVariable("MOCK_SERVER_SCRAPE_ASSET_MAX_SIZE");
    var url = !string.IsNullOrEmpty(_pfUrl_url)
                  ? _pfUrl_url
                  : Environment.GetEnvironmentVariable("MOCK_SERVER_URL") +
                        "/fixtures/scrape_asset_max_size";
    var result = await CrawlbergConverter.ScrapeAsync(engine, url);
    Assert.True(result.StatusCode == 200);
    Assert.True(result.Assets.Count == 2);
  }

  [Fact]
  public async Task Test_ScrapeAssetTypeFilter() {
    // Only downloads image assets when asset_types filter is set
    var engineConfig = JsonSerializer.Deserialize<CrawlConfig>(
        "{\"asset_types\":[\"image\"],\"download_assets\":true}",
        ConfigOptions)!;
    var engine = CrawlbergConverter.CreateEngine(engineConfig);
    var _pfUrl_url = Environment.GetEnvironmentVariable(
        "MOCK_SERVER_SCRAPE_ASSET_TYPE_FILTER");
    var url = !string.IsNullOrEmpty(_pfUrl_url)
                  ? _pfUrl_url
                  : Environment.GetEnvironmentVariable("MOCK_SERVER_URL") +
                        "/fixtures/scrape_asset_type_filter";
    var result = await CrawlbergConverter.ScrapeAsync(engine, url);
    Assert.True(result.StatusCode == 200);
    Assert.True(result.Assets.Count == 1);
    Assert.True(
        result.Assets?.Any(
            ee95b160 => Convert.ToString(ee95b160.AssetCategory)!.Contains(
                "image")) == true,
        "element of 'assets[].category': image");
  }

  [Fact]
  public async Task Test_ScrapeBasicHtmlPage() {
    // Scrapes a simple HTML page and extracts title, description, and links
    var engineConfig = JsonSerializer.Deserialize<CrawlConfig>(
        "{\"max_depth\":0,\"respect_robots_txt\":false}", ConfigOptions)!;
    var engine = CrawlbergConverter.CreateEngine(engineConfig);
    var url = Environment.GetEnvironmentVariable("MOCK_SERVER_URL") +
              "/fixtures/scrape_basic_html_page";
    var result = await CrawlbergConverter.ScrapeAsync(engine, url);
    Assert.True(result.StatusCode == 200);
    Assert.Equal("text/html", result.ContentType);
    Assert.True(
        ((object?)result.Html) switch {
          null => false,
          string text => text.Length > 0,
          System.Collections.ICollection items => items.Count > 0,
          _ => true,
        },
        "expected non-empty value");
    Assert.Equal("Example Domain", result.Metadata.Title);
    Assert.Contains("illustrative examples",
                    result.Metadata.Description.ToString());
    Assert.True(
        ((object?)result.Metadata.CanonicalUrl) switch {
          null => false,
          string text => text.Length > 0,
          System.Collections.ICollection items => items.Count > 0,
          _ => true,
        },
        "expected non-empty value");
    Assert.True(result.Links.Count > 0, "expected > 0");
    Assert.True(result.Links?.Any(
                    ebf0942ab => Convert.ToString(ebf0942ab.LinkType)!.Contains(
                        "external")) == true,
                "element of 'links[].link_type': external");
    Assert.True(result.Images.Count == 0);
    Assert.True(string.IsNullOrEmpty(result.Metadata.OgTitle?.ToString()));
  }

  [Fact]
  public async Task Test_ScrapeComplexLinks() {
    // Classifies links by type: internal, external, anchor, document, image
    var engine = CrawlbergConverter.CreateEngine(null);
    var url = Environment.GetEnvironmentVariable("MOCK_SERVER_URL") +
              "/fixtures/scrape_complex_links";
    var result = await CrawlbergConverter.ScrapeAsync(engine, url);
    Assert.True(result.StatusCode == 200);
    Assert.True(result.Links.Count > 9, "expected > 9");
    Assert.True(result.Links?.Any(e28c3370a => !string.IsNullOrEmpty(
                                      Convert.ToString(e28c3370a.Url))) == true,
                "expected some element of 'links[].url' to be non-empty");
  }

  [Fact]
  public async Task Test_ScrapeDownloadAssets() {
    // Downloads CSS, JS, and image assets from page
    var engineConfig = JsonSerializer.Deserialize<CrawlConfig>(
        "{\"download_assets\":true}", ConfigOptions)!;
    var engine = CrawlbergConverter.CreateEngine(engineConfig);
    var _pfUrl_url = Environment.GetEnvironmentVariable(
        "MOCK_SERVER_SCRAPE_DOWNLOAD_ASSETS");
    var url = !string.IsNullOrEmpty(_pfUrl_url)
                  ? _pfUrl_url
                  : Environment.GetEnvironmentVariable("MOCK_SERVER_URL") +
                        "/fixtures/scrape_download_assets";
    var result = await CrawlbergConverter.ScrapeAsync(engine, url);
    Assert.True(result.StatusCode == 200);
    Assert.True(result.Assets.Count > 2, "expected > 2");
  }

  [Fact]
  public async Task Test_ScrapeDublinCore() {
    // Extracts Dublin Core metadata from a page
    var engine = CrawlbergConverter.CreateEngine(null);
    var url = Environment.GetEnvironmentVariable("MOCK_SERVER_URL") +
              "/fixtures/scrape_dublin_core";
    var result = await CrawlbergConverter.ScrapeAsync(engine, url);
    Assert.True(result.StatusCode == 200);
    Assert.True(
        ((object?)result.Metadata.DcTitle) switch {
          null => false,
          string text => text.Length > 0,
          System.Collections.ICollection items => items.Count > 0,
          _ => true,
        },
        "expected non-empty value");
    Assert.Equal("Effects of Climate Change on Marine Biodiversity",
                 result.Metadata.DcTitle);
    Assert.Equal("Dr. Jane Smith", result.Metadata.DcCreator);
  }

  [Fact]
  public async Task Test_ScrapeEmptyPage() {
    // Handles an empty HTML document without errors
    var engine = CrawlbergConverter.CreateEngine(null);
    var url = Environment.GetEnvironmentVariable("MOCK_SERVER_URL") +
              "/fixtures/scrape_empty_page";
    var result = await CrawlbergConverter.ScrapeAsync(engine, url);
    Assert.True(result.StatusCode == 200);
    Assert.True(result.Links.Count > -1, "expected > -1");
    Assert.True(result.Images.Count == 0);
  }

  [Fact]
  public async Task Test_ScrapeFeedDiscovery() {
    // Discovers RSS, Atom, and JSON feed links
    var engine = CrawlbergConverter.CreateEngine(null);
    var url = Environment.GetEnvironmentVariable("MOCK_SERVER_URL") +
              "/fixtures/scrape_feed_discovery";
    var result = await CrawlbergConverter.ScrapeAsync(engine, url);
    Assert.True(result.StatusCode == 200);
    Assert.True(result.Feeds.Count >= 3, "expected >= 3");
  }

  [Fact]
  public async Task Test_ScrapeImageSources() {
    // Extracts images from img, picture, og:image, twitter:image
    var engine = CrawlbergConverter.CreateEngine(null);
    var url = Environment.GetEnvironmentVariable("MOCK_SERVER_URL") +
              "/fixtures/scrape_image_sources";
    var result = await CrawlbergConverter.ScrapeAsync(engine, url);
    Assert.True(result.StatusCode == 200);
    Assert.True(result.Images.Count > 4, "expected > 4");
    Assert.Equal("https://example.com/images/og-hero.jpg",
                 result.Metadata.OgImage);
  }

  [Fact]
  public async Task Test_ScrapeJsHeavySpa() {
    // Handles SPA page with JavaScript-only content (no server-rendered HTML)
    var engine = CrawlbergConverter.CreateEngine(null);
    var url = Environment.GetEnvironmentVariable("MOCK_SERVER_URL") +
              "/fixtures/scrape_js_heavy_spa";
    var result = await CrawlbergConverter.ScrapeAsync(engine, url);
    Assert.True(
        ((object?)result.Html) switch {
          null => false,
          string text => text.Length > 0,
          System.Collections.ICollection items => items.Count > 0,
          _ => true,
        },
        "expected non-empty value");
  }

  [Fact]
  public async Task Test_ScrapeJsonLd() {
    // Extracts JSON-LD structured data from a page
    var engine = CrawlbergConverter.CreateEngine(null);
    var url = Environment.GetEnvironmentVariable("MOCK_SERVER_URL") +
              "/fixtures/scrape_json_ld";
    var result = await CrawlbergConverter.ScrapeAsync(engine, url);
    Assert.True(result.StatusCode == 200);
    Assert.NotEmpty(result.JsonLd);
    Assert.Equal("Recipe", result.JsonLd[0].SchemaType);
    Assert.Equal("Best Chocolate Cake", result.JsonLd[0].Name);
  }

  [Fact]
  public async Task Test_ScrapeMalformedHtml() {
    // Gracefully handles broken HTML without crashing
    var engine = CrawlbergConverter.CreateEngine(null);
    var url = Environment.GetEnvironmentVariable("MOCK_SERVER_URL") +
              "/fixtures/scrape_malformed_html";
    var result = await CrawlbergConverter.ScrapeAsync(engine, url);
    Assert.True(result.StatusCode == 200);
    Assert.True(
        ((object?)result.Html) switch {
          null => false,
          string text => text.Length > 0,
          System.Collections.ICollection items => items.Count > 0,
          _ => true,
        },
        "expected non-empty value");
    Assert.Contains("broken HTML", result.Metadata.Description.ToString());
  }

  [Fact]
  public async Task Test_ScrapeOgMetadata() {
    // Extracts full Open Graph metadata from a page
    var engine = CrawlbergConverter.CreateEngine(null);
    var url = Environment.GetEnvironmentVariable("MOCK_SERVER_URL") +
              "/fixtures/scrape_og_metadata";
    var result = await CrawlbergConverter.ScrapeAsync(engine, url);
    Assert.True(result.StatusCode == 200);
    Assert.True(
        ((object?)result.Metadata.OgTitle) switch {
          null => false,
          string text => text.Length > 0,
          System.Collections.ICollection items => items.Count > 0,
          _ => true,
        },
        "expected non-empty value");
    Assert.Equal("Article Title", result.Metadata.OgTitle);
    Assert.Equal("article", result.Metadata.OgType);
    Assert.Equal("https://example.com/images/article-hero.jpg",
                 result.Metadata.OgImage);
    Assert.True(
        ((object?)result.Metadata.OgDescription) switch {
          null => false,
          string text => text.Length > 0,
          System.Collections.ICollection items => items.Count > 0,
          _ => true,
        },
        "expected non-empty value");
    Assert.Equal("Article Title - Example Blog", result.Metadata.Title);
  }

  [Fact]
  public async Task Test_ScrapeTwitterCard() {
    // Extracts Twitter Card metadata from a page
    var engine = CrawlbergConverter.CreateEngine(null);
    var url = Environment.GetEnvironmentVariable("MOCK_SERVER_URL") +
              "/fixtures/scrape_twitter_card";
    var result = await CrawlbergConverter.ScrapeAsync(engine, url);
    Assert.True(result.StatusCode == 200);
    Assert.True(
        ((object?)result.Metadata.TwitterCard) switch {
          null => false,
          string text => text.Length > 0,
          System.Collections.ICollection items => items.Count > 0,
          _ => true,
        },
        "expected non-empty value");
    Assert.Equal("summary_large_image", result.Metadata.TwitterCard);
    Assert.Equal("New Product Launch", result.Metadata.TwitterTitle);
  }
}
}
