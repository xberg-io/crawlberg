// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:1e2540c60ed6e4c93241bf84b0ecdafcf31f0afda1af2dc8d2ee341f7144aa64
// To regenerate: alef generate
// To verify freshness: alef verify

using System;
using System.Collections.Generic;
using System.Linq;
using System.Net.Http;
using System.Text;
using System.Text.Json;
using System.Text.Json.Serialization;
using System.Threading.Tasks;
using Xunit;
using Crawlberg;
using static Crawlberg.CrawlbergConverter;

namespace Crawlberg {
/// <summary>E2e tests for category: markdown.</summary>
public class MarkdownTests {
  private static readonly JsonSerializerOptions ConfigOptions =
      new() { Converters = { new JsonStringEnumConverter(
                  JsonNamingPolicy.SnakeCaseLower) },
              DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingDefault };

  [Fact]
  public async Task Test_CitationsBalancedParens() {
    // Citations correctly handle links inside parentheses with balanced
    // rendering
    var engine = CrawlbergConverter.CreateEngine(null);
    var url = Environment.GetEnvironmentVariable("MOCK_SERVER_URL") +
              "/fixtures/citations_balanced_parens";
    var result = await CrawlbergConverter.ScrapeAsync(engine, url);
    Assert.True(result.StatusCode == 200);
    Assert.True(
        ((object?)result.Markdown!.Content) switch {
          null => false,
          string text => text.Length > 0,
          System.Collections.ICollection items => items.Count > 0,
          _ => true,
        },
        "expected non-empty value");
    Assert.Contains("(", result.Markdown!.Content.ToString());
  }

  [Fact]
  public async Task Test_CitationsDuplicateUrls() {
    // Citations deduplicates multiple links to the same URL
    var engine = CrawlbergConverter.CreateEngine(null);
    var url = Environment.GetEnvironmentVariable("MOCK_SERVER_URL") +
              "/fixtures/citations_duplicate_urls";
    var result = await CrawlbergConverter.ScrapeAsync(engine, url);
    Assert.True(result.StatusCode == 200);
    Assert.True(
        ((object?)result.Markdown!.Content) switch {
          null => false,
          string text => text.Length > 0,
          System.Collections.ICollection items => items.Count > 0,
          _ => true,
        },
        "expected non-empty value");
    Assert.True(
        ((object?)result.Markdown!.Citations) switch {
          null => false,
          string text => text.Length > 0,
          System.Collections.ICollection items => items.Count > 0,
          _ => true,
        },
        "expected non-empty value");
  }

  [Fact]
  public async Task Test_MarkdownBasicConversion() {
    // HTML is always converted to markdown alongside raw HTML
    var engine = CrawlbergConverter.CreateEngine(null);
    var url = Environment.GetEnvironmentVariable("MOCK_SERVER_URL") +
              "/fixtures/markdown_basic_conversion";
    var result = await CrawlbergConverter.ScrapeAsync(engine, url);
    Assert.True(result.StatusCode == 200);
    Assert.Equal("Test", result.Metadata.Title);
    Assert.True(
        ((object?)result.Html) switch {
          null => false,
          string text => text.Length > 0,
          System.Collections.ICollection items => items.Count > 0,
          _ => true,
        },
        "expected non-empty value");
    Assert.True(
        ((object?)result.Markdown!.Content) switch {
          null => false,
          string text => text.Length > 0,
          System.Collections.ICollection items => items.Count > 0,
          _ => true,
        },
        "expected non-empty value");
    Assert.Contains("Hello World", result.Markdown!.Content.ToString());
  }

  [Fact]
  public async Task Test_MarkdownCrawlAllPages() {
    // All crawled pages have markdown field populated
    var engineConfig = JsonSerializer.Deserialize<CrawlConfig>(
        "{\"max_depth\":1}", ConfigOptions)!;
    var engine = CrawlbergConverter.CreateEngine(engineConfig);
    var _pfUrl_url = Environment.GetEnvironmentVariable(
        "MOCK_SERVER_MARKDOWN_CRAWL_ALL_PAGES");
    var url = !string.IsNullOrEmpty(_pfUrl_url)
                  ? _pfUrl_url
                  : Environment.GetEnvironmentVariable("MOCK_SERVER_URL") +
                        "/fixtures/markdown_crawl_all_pages";
    var result = await CrawlbergConverter.CrawlAsync(engine, url);
    Assert.True(result.Pages.Count == 2);
  }

  [Fact]
  public async Task Test_MarkdownFitContent() {
    // Fit markdown removes navigation and boilerplate content
    var engine = CrawlbergConverter.CreateEngine(null);
    var _pfUrl_url =
        Environment.GetEnvironmentVariable("MOCK_SERVER_MARKDOWN_FIT_CONTENT");
    var url = !string.IsNullOrEmpty(_pfUrl_url)
                  ? _pfUrl_url
                  : Environment.GetEnvironmentVariable("MOCK_SERVER_URL") +
                        "/fixtures/markdown_fit_content";
    var result = await CrawlbergConverter.ScrapeAsync(engine, url);
    Assert.True(result.StatusCode == 200);
    Assert.True(
        ((object?)result.Markdown!.Content) switch {
          null => false,
          string text => text.Length > 0,
          System.Collections.ICollection items => items.Count > 0,
          _ => true,
        },
        "expected non-empty value");
  }

  [Fact]
  public async Task Test_MarkdownHeadingsAndParagraphs() {
    // Markdown conversion preserves heading hierarchy and paragraph text
    var engine = CrawlbergConverter.CreateEngine(null);
    var url = Environment.GetEnvironmentVariable("MOCK_SERVER_URL") +
              "/fixtures/markdown_headings_and_paragraphs";
    var result = await CrawlbergConverter.ScrapeAsync(engine, url);
    Assert.True(
        ((object?)result.Markdown!.Content) switch {
          null => false,
          string text => text.Length > 0,
          System.Collections.ICollection items => items.Count > 0,
          _ => true,
        },
        "expected non-empty value");
    Assert.Contains("Main Title", result.Markdown!.Content.ToString());
  }

  [Fact]
  public async Task Test_MarkdownLinksConverted() {
    // HTML links are converted to markdown link syntax
    var engine = CrawlbergConverter.CreateEngine(null);
    var url = Environment.GetEnvironmentVariable("MOCK_SERVER_URL") +
              "/fixtures/markdown_links_converted";
    var result = await CrawlbergConverter.ScrapeAsync(engine, url);
    Assert.True(result.StatusCode == 200);
    Assert.True(
        ((object?)result.Html) switch {
          null => false,
          string text => text.Length > 0,
          System.Collections.ICollection items => items.Count > 0,
          _ => true,
        },
        "expected non-empty value");
    Assert.True(
        ((object?)result.Markdown!.Content) switch {
          null => false,
          string text => text.Length > 0,
          System.Collections.ICollection items => items.Count > 0,
          _ => true,
        },
        "expected non-empty value");
    Assert.Contains("Example", result.Markdown!.Content.ToString());
  }

  [Fact]
  public async Task Test_MarkdownWithCitations() {
    // Markdown includes citation conversion with numbered references
    var engine = CrawlbergConverter.CreateEngine(null);
    var url = Environment.GetEnvironmentVariable("MOCK_SERVER_URL") +
              "/fixtures/markdown_with_citations";
    var result = await CrawlbergConverter.ScrapeAsync(engine, url);
    Assert.True(result.StatusCode == 200);
    Assert.True(
        ((object?)result.Markdown!.Content) switch {
          null => false,
          string text => text.Length > 0,
          System.Collections.ICollection items => items.Count > 0,
          _ => true,
        },
        "expected non-empty value");
  }
}
}
