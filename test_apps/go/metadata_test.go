// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:d92def5a456441cb11600cbaed391f40a67f3a3824eb0992d3ee501840bd00fb
// To regenerate: alef generate
// To verify freshness: alef verify

// E2e tests for category: metadata
package e2e_test

import (
	"fmt"
	"os"
	"strings"
	"testing"

	pkg "github.com/xberg-io/crawlberg/packages/go"
)

func Test_MetadataArticleTimes(t *testing.T) {
	// Extracts article:published_time, modified_time, author, section, and tags
	engine, createErr := pkg.CreateEngine(nil)
	if createErr != nil {
		t.Fatalf("create handle failed: %v", createErr)
	}
	url := os.Getenv("MOCK_SERVER_URL") + "/fixtures/metadata_article_times"
	result, err := pkg.Scrape(engine, url)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	var metadataArticlePublishedTime string
	if result.Metadata.Article.PublishedTime != nil {
		metadataArticlePublishedTime = string(*result.Metadata.Article.PublishedTime)
	}
	var metadataArticleModifiedTime string
	if result.Metadata.Article.ModifiedTime != nil {
		metadataArticleModifiedTime = string(*result.Metadata.Article.ModifiedTime)
	}
	var metadataArticleAuthor string
	if result.Metadata.Article.Author != nil {
		metadataArticleAuthor = string(*result.Metadata.Article.Author)
	}
	var metadataArticleSection string
	if result.Metadata.Article.Section != nil {
		metadataArticleSection = string(*result.Metadata.Article.Section)
	}
	if result.StatusCode != 200 {
		t.Errorf("equals mismatch: got %v", result.StatusCode)
	}
	if string(metadataArticlePublishedTime) != `2024-01-15T10:00:00Z` {
		t.Errorf("equals mismatch: got %v", metadataArticlePublishedTime)
	}
	if string(metadataArticleModifiedTime) != `2024-06-20T14:30:00Z` {
		t.Errorf("equals mismatch: got %v", metadataArticleModifiedTime)
	}
	if string(metadataArticleAuthor) != `Jane Developer` {
		t.Errorf("equals mismatch: got %v", metadataArticleAuthor)
	}
	if string(metadataArticleSection) != `Technology` {
		t.Errorf("equals mismatch: got %v", metadataArticleSection)
	}
	if len(result.Metadata.Article.Tags) != 3 {
		t.Errorf("equals mismatch: got %v", len(result.Metadata.Article.Tags))
	}
}

func Test_MetadataFavicons(t *testing.T) {
	// Extracts favicon link tags including apple-touch-icon
	engine, createErr := pkg.CreateEngine(nil)
	if createErr != nil {
		t.Fatalf("create handle failed: %v", createErr)
	}
	url := os.Getenv("MOCK_SERVER_URL") + "/fixtures/metadata_favicons"
	result, err := pkg.Scrape(engine, url)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if result.StatusCode != 200 {
		t.Errorf("equals mismatch: got %v", result.StatusCode)
	}
	if len(result.Metadata.Favicons) != 5 {
		t.Errorf("equals mismatch: got %v", len(result.Metadata.Favicons))
	}
	founde261f0f3 := false
	for _, e := range result.Metadata.Favicons {
		if strings.Contains(fmt.Sprintf("%v", e.Rel), `apple-touch-icon`) {
			founde261f0f3 = true
			break
		}
	}
	if !founde261f0f3 {
		t.Errorf("expected some element of 'favicons[].rel' to contain %v", `apple-touch-icon`)
	}
}

func Test_MetadataHeadings(t *testing.T) {
	// Extracts heading hierarchy (h1-h6) from HTML page
	engine, createErr := pkg.CreateEngine(nil)
	if createErr != nil {
		t.Fatalf("create handle failed: %v", createErr)
	}
	url := os.Getenv("MOCK_SERVER_URL") + "/fixtures/metadata_headings"
	result, err := pkg.Scrape(engine, url)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if result.StatusCode != 200 {
		t.Errorf("equals mismatch: got %v", result.StatusCode)
	}
	if len(result.Metadata.Headings) == 0 {
		t.Fatalf("expected non-empty %s", `result.Metadata.Headings`)
	}
	if result.Metadata.Headings[0].Level != 1 {
		t.Errorf("equals mismatch: got %v", result.Metadata.Headings[0].Level)
	}
	if string(result.Metadata.Headings[0].Text) != `Primary Heading` {
		t.Errorf("equals mismatch: got %v", result.Metadata.Headings[0].Text)
	}
	if len(result.Metadata.Headings) != 8 {
		t.Errorf("equals mismatch: got %v", len(result.Metadata.Headings))
	}
}

func Test_MetadataHreflang(t *testing.T) {
	// Extracts hreflang alternate link tags
	engine, createErr := pkg.CreateEngine(nil)
	if createErr != nil {
		t.Fatalf("create handle failed: %v", createErr)
	}
	url := os.Getenv("MOCK_SERVER_URL") + "/fixtures/metadata_hreflang"
	result, err := pkg.Scrape(engine, url)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if result.StatusCode != 200 {
		t.Errorf("equals mismatch: got %v", result.StatusCode)
	}
	if len(result.Metadata.Hreflangs) != 4 {
		t.Errorf("equals mismatch: got %v", len(result.Metadata.Hreflangs))
	}
	foundb41d5e5c := false
	for _, e := range result.Metadata.Hreflangs {
		if strings.Contains(fmt.Sprintf("%v", e.Lang), `en`) {
			foundb41d5e5c = true
			break
		}
	}
	if !foundb41d5e5c {
		t.Errorf("expected some element of 'hreflang[].lang' to contain %v", `en`)
	}
}

func Test_MetadataKeywordsAuthor(t *testing.T) {
	// Extracts keywords, author, viewport, generator, theme-color, robots, lang, dir metadata
	engine, createErr := pkg.CreateEngine(nil)
	if createErr != nil {
		t.Fatalf("create handle failed: %v", createErr)
	}
	url := os.Getenv("MOCK_SERVER_URL") + "/fixtures/metadata_keywords_author"
	result, err := pkg.Scrape(engine, url)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	var metadataTitle string
	if result.Metadata.Title != nil {
		metadataTitle = string(*result.Metadata.Title)
	}
	var metadataKeywords string
	if result.Metadata.Keywords != nil {
		metadataKeywords = string(*result.Metadata.Keywords)
	}
	var metadataAuthor string
	if result.Metadata.Author != nil {
		metadataAuthor = string(*result.Metadata.Author)
	}
	var metadataGenerator string
	if result.Metadata.Generator != nil {
		metadataGenerator = string(*result.Metadata.Generator)
	}
	var metadataThemeColor string
	if result.Metadata.ThemeColor != nil {
		metadataThemeColor = string(*result.Metadata.ThemeColor)
	}
	var metadataRobots string
	if result.Metadata.Robots != nil {
		metadataRobots = string(*result.Metadata.Robots)
	}
	var metadataHTMLLang string
	if result.Metadata.HTMLLang != nil {
		metadataHTMLLang = string(*result.Metadata.HTMLLang)
	}
	var metadataHTMLDir string
	if result.Metadata.HTMLDir != nil {
		metadataHTMLDir = string(*result.Metadata.HTMLDir)
	}
	if result.StatusCode != 200 {
		t.Errorf("equals mismatch: got %v", result.StatusCode)
	}
	if string(metadataTitle) != `Comprehensive Metadata Test Page` {
		t.Errorf("equals mismatch: got %v", metadataTitle)
	}
	if result.Metadata.CanonicalURL == nil {
		t.Errorf("expected non-empty value")
	}
	if len(metadataKeywords) == 0 {
		t.Errorf("expected non-empty value")
	}
	if !strings.Contains(string(metadataKeywords), `rust`) {
		t.Errorf("expected to contain %s, got %v", `rust`, metadataKeywords)
	}
	if string(metadataAuthor) != `Jane Developer` {
		t.Errorf("equals mismatch: got %v", metadataAuthor)
	}
	if result.Metadata.Viewport == nil {
		t.Errorf("expected non-empty value")
	}
	if string(metadataGenerator) != `crawlberg/1.0` {
		t.Errorf("equals mismatch: got %v", metadataGenerator)
	}
	if string(metadataThemeColor) != `#ff6600` {
		t.Errorf("equals mismatch: got %v", metadataThemeColor)
	}
	if string(metadataRobots) != `index, follow` {
		t.Errorf("equals mismatch: got %v", metadataRobots)
	}
	if string(metadataHTMLLang) != `en` {
		t.Errorf("equals mismatch: got %v", metadataHTMLLang)
	}
	if string(metadataHTMLDir) != `ltr` {
		t.Errorf("equals mismatch: got %v", metadataHTMLDir)
	}
}

func Test_MetadataOgVideoAudio(t *testing.T) {
	// Extracts og:video, og:audio, and og:locale:alternate metadata
	engine, createErr := pkg.CreateEngine(nil)
	if createErr != nil {
		t.Fatalf("create handle failed: %v", createErr)
	}
	url := os.Getenv("MOCK_SERVER_URL") + "/fixtures/metadata_og_video_audio"
	result, err := pkg.Scrape(engine, url)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	var metadataOgVideo string
	if result.Metadata.OgVideo != nil {
		metadataOgVideo = string(*result.Metadata.OgVideo)
	}
	var metadataOgAudio string
	if result.Metadata.OgAudio != nil {
		metadataOgAudio = string(*result.Metadata.OgAudio)
	}
	if result.StatusCode != 200 {
		t.Errorf("equals mismatch: got %v", result.StatusCode)
	}
	if string(metadataOgVideo) != `https://example.com/video.mp4` {
		t.Errorf("equals mismatch: got %v", metadataOgVideo)
	}
	if string(metadataOgAudio) != `https://example.com/audio.mp3` {
		t.Errorf("equals mismatch: got %v", metadataOgAudio)
	}
	if len(result.Metadata.OgLocaleAlternates) != 2 {
		t.Errorf("equals mismatch: got %v", len(result.Metadata.OgLocaleAlternates))
	}
}

func Test_MetadataResponseHeaders(t *testing.T) {
	// Extracts response metadata from HTTP headers (etag, server, content-language)
	engine, createErr := pkg.CreateEngine(nil)
	if createErr != nil {
		t.Fatalf("create handle failed: %v", createErr)
	}
	url := os.Getenv("MOCK_SERVER_URL") + "/fixtures/metadata_response_headers"
	result, err := pkg.Scrape(engine, url)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	var responseMetaEtag string
	if result.ResponseMeta.Etag != nil {
		responseMetaEtag = string(*result.ResponseMeta.Etag)
	}
	var responseMetaLastModified string
	if result.ResponseMeta.LastModified != nil {
		responseMetaLastModified = string(*result.ResponseMeta.LastModified)
	}
	var responseMetaServer string
	if result.ResponseMeta.Server != nil {
		responseMetaServer = string(*result.ResponseMeta.Server)
	}
	var responseMetaContentLanguage string
	if result.ResponseMeta.ContentLanguage != nil {
		responseMetaContentLanguage = string(*result.ResponseMeta.ContentLanguage)
	}
	if result.StatusCode != 200 {
		t.Errorf("equals mismatch: got %v", result.StatusCode)
	}
	if !strings.Contains(string(responseMetaEtag), `abc123`) {
		t.Errorf("expected to contain %s, got %v", `abc123`, responseMetaEtag)
	}
	if !strings.Contains(string(responseMetaLastModified), `01 Jan 2025`) {
		t.Errorf("expected to contain %s, got %v", `01 Jan 2025`, responseMetaLastModified)
	}
	if !strings.Contains(string(responseMetaServer), `nginx`) {
		t.Errorf("expected to contain %s, got %v", `nginx`, responseMetaServer)
	}
	if string(responseMetaContentLanguage) != `en-US` {
		t.Errorf("equals mismatch: got %v", responseMetaContentLanguage)
	}
}

func Test_MetadataWordCount(t *testing.T) {
	// Computes word count from visible page text
	engine, createErr := pkg.CreateEngine(nil)
	if createErr != nil {
		t.Fatalf("create handle failed: %v", createErr)
	}
	url := os.Getenv("MOCK_SERVER_URL") + "/fixtures/metadata_word_count"
	result, err := pkg.Scrape(engine, url)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if result.StatusCode != 200 {
		t.Errorf("equals mismatch: got %v", result.StatusCode)
	}
	if result.Metadata.WordCount != nil {
		if *result.Metadata.WordCount < 100 {
			t.Errorf("expected > 99, got %v", *result.Metadata.WordCount)
		}
	}
	if result.Metadata.WordCount != nil {
		if *result.Metadata.WordCount >= 301 {
			t.Errorf("expected < 301, got %v", *result.Metadata.WordCount)
		}
	}
}
