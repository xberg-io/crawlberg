# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:793f98d189721c613646e0e55ff5a94ec320744ccd0127d0e9932c9b7db18d04
# To regenerate: alef generate
# To verify freshness: alef verify
# E2e tests for category: metadata
defmodule E2e.MetadataTest do
  use ExUnit.Case, async: false

  describe "metadata_article_times" do
    test "metadata_article_times" do
      {:ok, engine} = Crawlberg.create_engine(nil)
      url = (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/metadata_article_times"
      {:ok, result} = Crawlberg.scrape(engine, url)
      assert result.status_code == 200
      assert result.metadata.article.published_time == "2024-01-15T10:00:00Z"
      assert result.metadata.article.modified_time == "2024-06-20T14:30:00Z"
      assert result.metadata.article.author == "Jane Developer"
      assert result.metadata.article.section == "Technology"
      assert length(result.metadata.article.tags) == 3
    end
  end

  describe "metadata_favicons" do
    test "metadata_favicons" do
      {:ok, engine} = Crawlberg.create_engine(nil)
      url = (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/metadata_favicons"
      {:ok, result} = Crawlberg.scrape(engine, url)
      assert result.status_code == 200
      assert length(result.metadata.favicons) == 5
      assert Enum.any?(result.metadata.favicons || [], fn e -> String.contains?(to_string(e.rel), "apple-touch-icon") end)
    end
  end

  describe "metadata_headings" do
    test "metadata_headings" do
      {:ok, engine} = Crawlberg.create_engine(nil)
      url = (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/metadata_headings"
      {:ok, result} = Crawlberg.scrape(engine, url)
      assert result.status_code == 200
      assert Enum.at(result.metadata.headings, 0).level == 1
      assert Enum.at(result.metadata.headings, 0).text == "Primary Heading"
      assert length(result.metadata.headings) == 8
    end
  end

  describe "metadata_hreflang" do
    test "metadata_hreflang" do
      {:ok, engine} = Crawlberg.create_engine(nil)
      url = (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/metadata_hreflang"
      {:ok, result} = Crawlberg.scrape(engine, url)
      assert result.status_code == 200
      assert length(result.metadata.hreflangs) == 4
      assert Enum.any?(result.metadata.hreflangs || [], fn e -> String.contains?(to_string(e.lang), "en") end)
    end
  end

  describe "metadata_keywords_author" do
    test "metadata_keywords_author" do
      {:ok, engine} = Crawlberg.create_engine(nil)
      url = (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/metadata_keywords_author"
      {:ok, result} = Crawlberg.scrape(engine, url)
      assert result.status_code == 200
      assert result.metadata.title == "Comprehensive Metadata Test Page"
      assert result.metadata.canonical_url not in [nil, "", [], %{}]
      assert result.metadata.keywords not in [nil, "", [], %{}]
      assert String.contains?(to_string(result.metadata.keywords), "rust")
      assert result.metadata.author == "Jane Developer"
      assert result.metadata.viewport not in [nil, "", [], %{}]
      assert result.metadata.generator == "crawlberg/1.0"
      assert result.metadata.theme_color == "\#ff6600"
      assert result.metadata.robots == "index, follow"
      assert result.metadata.html_lang == "en"
      assert result.metadata.html_dir == "ltr"
    end
  end

  describe "metadata_og_video_audio" do
    test "metadata_og_video_audio" do
      {:ok, engine} = Crawlberg.create_engine(nil)
      url = (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/metadata_og_video_audio"
      {:ok, result} = Crawlberg.scrape(engine, url)
      assert result.status_code == 200
      assert result.metadata.og_video == "https://example.com/video.mp4"
      assert result.metadata.og_audio == "https://example.com/audio.mp3"
      assert length(result.metadata.og_locale_alternates) == 2
    end
  end

  describe "metadata_response_headers" do
    test "metadata_response_headers" do
      {:ok, engine} = Crawlberg.create_engine(nil)
      url = (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/metadata_response_headers"
      {:ok, result} = Crawlberg.scrape(engine, url)
      assert result.status_code == 200
      assert String.contains?(to_string(result.response_meta.etag), "abc123")
      assert String.contains?(to_string(result.response_meta.last_modified), "01 Jan 2025")
      assert String.contains?(to_string(result.response_meta.server), "nginx")
      assert result.response_meta.content_language == "en-US"
    end
  end

  describe "metadata_word_count" do
    test "metadata_word_count" do
      {:ok, engine} = Crawlberg.create_engine(nil)
      url = (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/metadata_word_count"
      {:ok, result} = Crawlberg.scrape(engine, url)
      assert result.status_code == 200
      assert result.metadata.word_count > 99
      assert result.metadata.word_count < 301
    end
  end
end
