#!/usr/bin/env bash
# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:181a3fb29fe30d85151906611a399dd8a2ecbdee1f8a766366ac95c88ce97d85
# To regenerate: alef generate
# To verify freshness: alef verify
# E2e tests for category: metadata
set -euo pipefail

test_metadata_article_times() {
  # Extracts article:published_time, modified_time, author, section, and tags
  local output
  output=$(crawlberg scrape "${MOCK_SERVER_METADATA_ARTICLE_TIMES:-${MOCK_SERVER_URL}/fixtures/metadata_article_times}" --config '{}' --format json --browser-mode never)

  local val_status_code
  val_status_code=$(echo "$output" | jq -r '.status_code')
  assert_equals "$val_status_code" '200' 'status_code'
  local val_article_published_time
  val_article_published_time=$(echo "$output" | jq -r '.metadata.article.published_time')
  assert_equals "$val_article_published_time" '2024-01-15T10:00:00Z' 'article.published_time'
  local val_article_modified_time
  val_article_modified_time=$(echo "$output" | jq -r '.metadata.article.modified_time')
  assert_equals "$val_article_modified_time" '2024-06-20T14:30:00Z' 'article.modified_time'
  local val_article_author
  val_article_author=$(echo "$output" | jq -r '.metadata.article.author')
  assert_equals "$val_article_author" 'Jane Developer' 'article.author'
  local val_article_section
  val_article_section=$(echo "$output" | jq -r '.metadata.article.section')
  assert_equals "$val_article_section" 'Technology' 'article.section'
  local val_article_tags_length
  val_article_tags_length=$(echo "$output" | jq -r '.metadata.article.tags | length')
  assert_equals "$val_article_tags_length" '3' 'article.tags.length'
}

test_metadata_favicons() {
  # Extracts favicon link tags including apple-touch-icon
  local output
  output=$(crawlberg scrape "${MOCK_SERVER_METADATA_FAVICONS:-${MOCK_SERVER_URL}/fixtures/metadata_favicons}" --config '{}' --format json --browser-mode never)

  local val_status_code
  val_status_code=$(echo "$output" | jq -r '.status_code')
  assert_equals "$val_status_code" '200' 'status_code'
  local val_favicons_length
  val_favicons_length=$(echo "$output" | jq -r '.metadata.favicons | length')
  assert_equals "$val_favicons_length" '5' 'favicons.length'
  local val_favicons___rel
  val_favicons___rel=$(echo "$output" | jq -r '.metadata.favicons[].rel')
  assert_contains "$val_favicons___rel" 'apple-touch-icon' 'favicons[].rel'
}

test_metadata_headings() {
  # Extracts heading hierarchy (h1-h6) from HTML page
  local output
  output=$(crawlberg scrape "${MOCK_SERVER_METADATA_HEADINGS:-${MOCK_SERVER_URL}/fixtures/metadata_headings}" --config '{}' --format json --browser-mode never)

  local val_status_code
  val_status_code=$(echo "$output" | jq -r '.status_code')
  assert_equals "$val_status_code" '200' 'status_code'
  local val_headings_0__level
  val_headings_0__level=$(echo "$output" | jq -r '.metadata.headings[0].level')
  assert_equals "$val_headings_0__level" '1' 'headings[0].level'
  local val_headings_0__text
  val_headings_0__text=$(echo "$output" | jq -r '.metadata.headings[0].text')
  assert_equals "$val_headings_0__text" 'Primary Heading' 'headings[0].text'
  local val_headings_length
  val_headings_length=$(echo "$output" | jq -r '.metadata.headings | length')
  assert_equals "$val_headings_length" '8' 'headings.length'
}

test_metadata_hreflang() {
  # Extracts hreflang alternate link tags
  local output
  output=$(crawlberg scrape "${MOCK_SERVER_METADATA_HREFLANG:-${MOCK_SERVER_URL}/fixtures/metadata_hreflang}" --config '{}' --format json --browser-mode never)

  local val_status_code
  val_status_code=$(echo "$output" | jq -r '.status_code')
  assert_equals "$val_status_code" '200' 'status_code'
  local val_hreflang_length
  val_hreflang_length=$(echo "$output" | jq -r '.metadata.hreflangs | length')
  assert_equals "$val_hreflang_length" '4' 'hreflang.length'
  local val_hreflang___lang
  val_hreflang___lang=$(echo "$output" | jq -r '.metadata.hreflangs[].lang')
  assert_contains "$val_hreflang___lang" 'en' 'hreflang[].lang'
}

test_metadata_keywords_author() {
  # Extracts keywords, author, viewport, generator, theme-color, robots, lang, dir metadata
  local output
  output=$(crawlberg scrape "${MOCK_SERVER_METADATA_KEYWORDS_AUTHOR:-${MOCK_SERVER_URL}/fixtures/metadata_keywords_author}" --config '{}' --format json --browser-mode never)

  local val_status_code
  val_status_code=$(echo "$output" | jq -r '.status_code')
  assert_equals "$val_status_code" '200' 'status_code'
  local val_metadata_title
  val_metadata_title=$(echo "$output" | jq -r '.metadata.title')
  assert_equals "$val_metadata_title" 'Comprehensive Metadata Test Page' 'metadata.title'
  local val_metadata_canonical_url
  val_metadata_canonical_url=$(echo "$output" | jq -r '.metadata.canonical_url')
  assert_not_empty "$val_metadata_canonical_url" 'metadata.canonical_url'
  local val_metadata_keywords
  val_metadata_keywords=$(echo "$output" | jq -r '.metadata.keywords')
  assert_not_empty "$val_metadata_keywords" 'metadata.keywords'
  local val_metadata_keywords
  val_metadata_keywords=$(echo "$output" | jq -r '.metadata.keywords')
  assert_contains "$val_metadata_keywords" 'rust' 'metadata.keywords'
  local val_metadata_author
  val_metadata_author=$(echo "$output" | jq -r '.metadata.author')
  assert_equals "$val_metadata_author" 'Jane Developer' 'metadata.author'
  local val_metadata_viewport
  val_metadata_viewport=$(echo "$output" | jq -r '.metadata.viewport')
  assert_not_empty "$val_metadata_viewport" 'metadata.viewport'
  local val_metadata_generator
  val_metadata_generator=$(echo "$output" | jq -r '.metadata.generator')
  assert_equals "$val_metadata_generator" 'crawlberg/1.0' 'metadata.generator'
  local val_metadata_theme_color
  val_metadata_theme_color=$(echo "$output" | jq -r '.metadata.theme_color')
  assert_equals "$val_metadata_theme_color" '#ff6600' 'metadata.theme_color'
  local val_metadata_robots
  val_metadata_robots=$(echo "$output" | jq -r '.metadata.robots')
  assert_equals "$val_metadata_robots" 'index, follow' 'metadata.robots'
  local val_metadata_lang
  val_metadata_lang=$(echo "$output" | jq -r '.metadata.html_lang')
  assert_equals "$val_metadata_lang" 'en' 'metadata.lang'
  local val_metadata_dir
  val_metadata_dir=$(echo "$output" | jq -r '.metadata.html_dir')
  assert_equals "$val_metadata_dir" 'ltr' 'metadata.dir'
}

test_metadata_og_video_audio() {
  # Extracts og:video, og:audio, and og:locale:alternate metadata
  local output
  output=$(crawlberg scrape "${MOCK_SERVER_METADATA_OG_VIDEO_AUDIO:-${MOCK_SERVER_URL}/fixtures/metadata_og_video_audio}" --config '{}' --format json --browser-mode never)

  local val_status_code
  val_status_code=$(echo "$output" | jq -r '.status_code')
  assert_equals "$val_status_code" '200' 'status_code'
  local val_og_video
  val_og_video=$(echo "$output" | jq -r '.metadata.og_video')
  assert_equals "$val_og_video" 'https://example.com/video.mp4' 'og.video'
  local val_og_audio
  val_og_audio=$(echo "$output" | jq -r '.metadata.og_audio')
  assert_equals "$val_og_audio" 'https://example.com/audio.mp3' 'og.audio'
  local val_og_locale_alternate_length
  val_og_locale_alternate_length=$(echo "$output" | jq -r '.metadata.og_locale_alternates | length')
  assert_equals "$val_og_locale_alternate_length" '2' 'og.locale_alternate.length'
}

test_metadata_response_headers() {
  # Extracts response metadata from HTTP headers (etag, server, content-language)
  local output
  output=$(crawlberg scrape "${MOCK_SERVER_METADATA_RESPONSE_HEADERS:-${MOCK_SERVER_URL}/fixtures/metadata_response_headers}" --config '{}' --format json --browser-mode never)

  local val_status_code
  val_status_code=$(echo "$output" | jq -r '.status_code')
  assert_equals "$val_status_code" '200' 'status_code'
  local val_response_headers_etag
  val_response_headers_etag=$(echo "$output" | jq -r '.response_meta.etag')
  assert_contains "$val_response_headers_etag" 'abc123' 'response_headers.etag'
  local val_response_headers_last_modified
  val_response_headers_last_modified=$(echo "$output" | jq -r '.response_meta.last_modified')
  assert_contains "$val_response_headers_last_modified" '01 Jan 2025' 'response_headers.last_modified'
  local val_response_headers_server
  val_response_headers_server=$(echo "$output" | jq -r '.response_meta.server')
  assert_contains "$val_response_headers_server" 'nginx' 'response_headers.server'
  local val_response_headers_content_language
  val_response_headers_content_language=$(echo "$output" | jq -r '.response_meta.content_language')
  assert_equals "$val_response_headers_content_language" 'en-US' 'response_headers.content_language'
}

test_metadata_word_count() {
  # Computes word count from visible page text
  local output
  output=$(crawlberg scrape "${MOCK_SERVER_METADATA_WORD_COUNT:-${MOCK_SERVER_URL}/fixtures/metadata_word_count}" --config '{}' --format json --browser-mode never)

  local val_status_code
  val_status_code=$(echo "$output" | jq -r '.status_code')
  assert_equals "$val_status_code" '200' 'status_code'
  local val_metadata_word_count
  val_metadata_word_count=$(echo "$output" | jq -r '.metadata.word_count')
  assert_greater_than "$val_metadata_word_count" '99' 'metadata.word_count'
  local val_metadata_word_count
  val_metadata_word_count=$(echo "$output" | jq -r '.metadata.word_count')
  assert_less_than "$val_metadata_word_count" '301' 'metadata.word_count'
}

run_tests_metadata() {
  run_test test_metadata_article_times
  run_test test_metadata_favicons
  run_test test_metadata_headings
  run_test test_metadata_hreflang
  run_test test_metadata_keywords_author
  run_test test_metadata_og_video_audio
  run_test test_metadata_response_headers
  run_test test_metadata_word_count
}
