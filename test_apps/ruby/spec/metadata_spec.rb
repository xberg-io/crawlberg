# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:dc255b09d24a8452168f20c19d2cb9a8b540b8636b5032d009a7fc9c9ad30871
# To regenerate: alef generate
# To verify freshness: alef verify
# frozen_string_literal: true

require 'spec_helper'
require 'crawlberg'
require 'json'

RSpec.describe 'metadata' do
  it 'metadata_article_times: Extracts article:published_time, modified_time, author, section, and tags' do
    engine = Crawlberg.create_engine(nil)
    url = "#{ENV.fetch('MOCK_SERVER_URL')}/fixtures/metadata_article_times"
    result = Crawlberg.scrape(engine, url)
    expect(result.status_code).to eq(200)
    expect(result.metadata.article.published_time.to_s).to eq('2024-01-15T10:00:00Z')
    expect(result.metadata.article.modified_time.to_s).to eq('2024-06-20T14:30:00Z')
    expect(result.metadata.article.author.to_s).to eq('Jane Developer')
    expect(result.metadata.article.section.to_s).to eq('Technology')
    expect(result.metadata.article.tags.length).to eq(3)
  end

  it 'metadata_favicons: Extracts favicon link tags including apple-touch-icon' do
    engine = Crawlberg.create_engine(nil)
    url = "#{ENV.fetch('MOCK_SERVER_URL')}/fixtures/metadata_favicons"
    result = Crawlberg.scrape(engine, url)
    expect(result.status_code).to eq(200)
    expect(result.metadata.favicons.length).to eq(5)
    expect((result.metadata.favicons || []).any? { |e| e.rel.to_s.include?('apple-touch-icon') }).to be true
  end

  it 'metadata_headings: Extracts heading hierarchy (h1-h6) from HTML page' do
    engine = Crawlberg.create_engine(nil)
    url = "#{ENV.fetch('MOCK_SERVER_URL')}/fixtures/metadata_headings"
    result = Crawlberg.scrape(engine, url)
    expect(result.status_code).to eq(200)
    expect(result.metadata.headings[0].level).to eq(1)
    expect(result.metadata.headings[0].text.to_s).to eq('Primary Heading')
    expect(result.metadata.headings.length).to eq(8)
  end

  it 'metadata_hreflang: Extracts hreflang alternate link tags' do
    engine = Crawlberg.create_engine(nil)
    url = "#{ENV.fetch('MOCK_SERVER_URL')}/fixtures/metadata_hreflang"
    result = Crawlberg.scrape(engine, url)
    expect(result.status_code).to eq(200)
    expect(result.metadata.hreflangs.length).to eq(4)
    expect((result.metadata.hreflangs || []).any? { |e| e.lang.to_s.include?('en') }).to be true
  end

  it 'metadata_keywords_author: Extracts keywords, author, viewport, generator, theme-color, robots, lang, dir metadata' do
    engine = Crawlberg.create_engine(nil)
    url = "#{ENV.fetch('MOCK_SERVER_URL')}/fixtures/metadata_keywords_author"
    result = Crawlberg.scrape(engine, url)
    expect(result.status_code).to eq(200)
    expect(result.metadata.title.to_s).to eq('Comprehensive Metadata Test Page')
    expect(result.metadata.canonical_url.respond_to?(:empty?) ? !result.metadata.canonical_url.empty? : !result.metadata.canonical_url.nil?).to be(true)
    expect(result.metadata.keywords.respond_to?(:empty?) ? !result.metadata.keywords.empty? : !result.metadata.keywords.nil?).to be(true)
    expect(result.metadata.keywords.to_s).to include('rust')
    expect(result.metadata.author.to_s).to eq('Jane Developer')
    expect(result.metadata.viewport.respond_to?(:empty?) ? !result.metadata.viewport.empty? : !result.metadata.viewport.nil?).to be(true)
    expect(result.metadata.generator.to_s).to eq('crawlberg/1.0')
    expect(result.metadata.theme_color.to_s).to eq('#ff6600')
    expect(result.metadata.robots.to_s).to eq('index, follow')
    expect(result.metadata.html_lang.to_s).to eq('en')
    expect(result.metadata.html_dir.to_s).to eq('ltr')
  end

  it 'metadata_og_video_audio: Extracts og:video, og:audio, and og:locale:alternate metadata' do
    engine = Crawlberg.create_engine(nil)
    url = "#{ENV.fetch('MOCK_SERVER_URL')}/fixtures/metadata_og_video_audio"
    result = Crawlberg.scrape(engine, url)
    expect(result.status_code).to eq(200)
    expect(result.metadata.og_video.to_s).to eq('https://example.com/video.mp4')
    expect(result.metadata.og_audio.to_s).to eq('https://example.com/audio.mp3')
    expect(result.metadata.og_locale_alternates.length).to eq(2)
  end

  it 'metadata_response_headers: Extracts response metadata from HTTP headers (etag, server, content-language)' do
    engine = Crawlberg.create_engine(nil)
    url = "#{ENV.fetch('MOCK_SERVER_URL')}/fixtures/metadata_response_headers"
    result = Crawlberg.scrape(engine, url)
    expect(result.status_code).to eq(200)
    expect(result.response_meta.etag.to_s).to include('abc123')
    expect(result.response_meta.last_modified.to_s).to include('01 Jan 2025')
    expect(result.response_meta.server.to_s).to include('nginx')
    expect(result.response_meta.content_language.to_s).to eq('en-US')
  end

  it 'metadata_word_count: Computes word count from visible page text' do
    engine = Crawlberg.create_engine(nil)
    url = "#{ENV.fetch('MOCK_SERVER_URL')}/fixtures/metadata_word_count"
    result = Crawlberg.scrape(engine, url)
    expect(result.status_code).to eq(200)
    expect(result.metadata.word_count).to be > 99
    expect(result.metadata.word_count).to be < 301
  end
end
