import XCTest

@testable import Crawlberg

final class CrawlbergTests: XCTestCase {
  func testPlaceholder() throws {
    // Placeholder test so `swift test` has a target to run.
    // Replace or extend with real tests against the Crawlberg module.
    XCTAssertTrue(true)
  }
}
