// Generated by alef. Do not edit by hand.
// alef:hash:f977e2339a86bab57c1af75ce053d704d91b5d6a0aae36ceec03b75569952ea3
@file:Suppress(
    "ktlint:standard:trailing-comma-on-call-site",
    "ktlint:standard:trailing-comma-on-declaration-site",
    "ktlint:standard:spacing-between-declarations-with-comments",
    "ktlint:standard:spacing-between-declarations-with-annotations",
    "ktlint:standard:when-entry-bracing",
    "ktlint:standard:blank-line-between-when-conditions",
    "ktlint:standard:blank-line-before-declaration",
    "ktlint:standard:chain-method-continuation",
    "ktlint:standard:annotation",
    "ktlint:standard:max-line-length",
    "ktlint:standard:no-semi",
    "ktlint:standard:statement-wrapping",
    "MaxLineLength",
    "TooManyFunctions",
    "FunctionParameterNaming",
    "LongParameterList",
    "CyclomaticComplexMethod",
    "LongMethod",
    "MagicNumber",
    "NestedBlockDepth",
    "ReturnCount",
    "UnusedParameter",
)

package io.xberg.crawlberg.android

/** Errors that can occur during crawling, scraping, or mapping operations. */
sealed class CrawlError(message: String) : Exception(message) {
    data class NotFound(
        override val message: String,
        val source: String?,
    ) : CrawlError("not_found: {message}")
    data class Unauthorized(
        override val message: String,
        val source: String?,
    ) : CrawlError("unauthorized: {message}")
    data class Forbidden(
        override val message: String,
        val source: String?,
    ) : CrawlError("forbidden: {message}")
    data class WafBlocked(
        val vendor: String,
        override val message: String,
    ) : CrawlError("forbidden: waf/blocked: {message}")
    data class Timeout(
        override val message: String,
        val source: String?,
    ) : CrawlError("timeout: {message}")
    data class RateLimited(
        override val message: String,
        val source: String?,
    ) : CrawlError("rate_limited: {message}")
    data class ServerError(
        override val message: String,
        val source: String?,
    ) : CrawlError("server_error: {message}")
    data class BadGateway(
        override val message: String,
        val source: String?,
    ) : CrawlError("bad_gateway: {message}")
    data class Gone(
        override val message: String,
        val source: String?,
    ) : CrawlError("gone: {message}")
    data class Connection(
        override val message: String,
        val source: String?,
    ) : CrawlError("connection: {message}")
    data class Dns(override val message: String, val source: String?) : CrawlError("dns: {message}")
    data class Ssl(override val message: String, val source: String?) : CrawlError("ssl: {message}")
    data class DataLoss(
        override val message: String,
        val source: String?,
    ) : CrawlError("data_loss: {message}")
    data class BrowserError(
        override val message: String,
        val source: String?,
    ) : CrawlError("browser: {message}")
    data class BrowserTimeout(
        override val message: String,
        val source: String?,
    ) : CrawlError("browser_timeout: {message}")
    data class InvalidConfig(
        override val message: String,
        val source: String?,
    ) : CrawlError("invalid_config: {message}")
    data class Unsupported(
        override val message: String,
        val source: String?,
    ) : CrawlError("unsupported: {message}")
    data class SsrfPolicyViolation(
        val url: String,
        val reason: String,
        val source: String?,
    ) : CrawlError("ssrf_policy_violation: {url} - {reason}")
    data class Other(
        override val message: String,
        val source: String?,
    ) : CrawlError("other: {message}")
}
