# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:8e1e699254db7dc5a8b6480ca979c54e292ef877f2bf2f45bda8c10a975447ca
# To regenerate: alef generate
# To verify freshness: alef verify
defmodule Crawlberg.CrawlEngineHandle do
  @moduledoc """
  Opaque handle to a configured crawl engine.

  Constructed via [`create_engine`] with an optional [`CrawlConfig`].
  Default implementations for all pluggable components are used internally.
  """

  alias Crawlberg.Native
  defstruct [:ref]

  @typedoc "Opaque handle to a configured crawl engine."
  @type t :: %__MODULE__{ref: reference()}

  @doc "Stream a single-URL crawl, yielding [`CrawlEvent`]s as pages are processed."
  def crawl_stream(obj, req) do
    case Native.crawlenginehandle_crawl_stream_start(obj.ref, (cond do is_nil(req) -> nil; is_binary(req) -> req; true -> Jason.encode!(req) end)) do
      {:ok, handle} ->
        stream =
          Stream.unfold(handle, fn h ->
            case Native.crawlenginehandle_crawl_stream_next(h) do
              {:ok, nil} ->
                nil

              {:ok, chunk_json} when is_binary(chunk_json) ->
                {Jason.decode!(chunk_json, keys: :atoms), h}

              {:ok, chunk} ->
                {chunk, h}

              {:error, _} ->
                nil
            end
          end)

        {:ok, stream}

      {:error, reason} ->
        {:error, reason}
    end
  end

  @doc "Stream a multi-URL crawl, yielding [`CrawlEvent`]s across all seeds."
  def batch_crawl_stream(obj, req) do
    case Native.crawlenginehandle_batch_crawl_stream_start(obj.ref, (cond do is_nil(req) -> nil; is_binary(req) -> req; true -> Jason.encode!(req) end)) do
      {:ok, handle} ->
        stream =
          Stream.unfold(handle, fn h ->
            case Native.crawlenginehandle_batch_crawl_stream_next(h) do
              {:ok, nil} ->
                nil

              {:ok, chunk_json} when is_binary(chunk_json) ->
                {Jason.decode!(chunk_json, keys: :atoms), h}

              {:ok, chunk} ->
                {chunk, h}

              {:error, _} ->
                nil
            end
          end)

        {:ok, stream}

      {:error, reason} ->
        {:error, reason}
    end
  end
end
