# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:b918915d7c5014d3bd5e433aad2b62d1b2489584dbd937a02e60a9d94f3a2170
# To regenerate: alef generate
# To verify freshness: alef verify
defmodule Crawlberg.HostMatcher do
  @moduledoc """
  Hostname/IP allowlist matcher for SSRF policy.

  Serializes as an internally-tagged object so each variant is distinguishable on the
  wire and round-trips losslessly:

  ```json
  {"type": "exact",  "value": "api.example.com"}
  {"type": "suffix", "value": ".example.com"}
  {"type": "cidr",   "value": "10.0.0.0/8"}
  ```

  A bare JSON string is still accepted on deserialization and resolves to [`Exact`],
  preserving configs written against the previous untagged representation.

  [`Exact`]: HostMatcher::Exact
  """

  @typedoc "Hostname/IP allowlist matcher for SSRF policy."
  @type t :: term()

  @typedoc "Exact hostname match (case-insensitive)."
  @type exact :: %{type: :exact, value: String.t()}
  @typedoc "Suffix match: \".xberg.io\" matches \"api.xberg.io\" and \"xberg.io\"."
  @type suffix :: %{type: :suffix, value: String.t()}
  @typedoc "CIDR match: \"10.0.0.0/8\" matches IP addresses in that range."
  @type cidr :: %{type: :cidr, value: String.t()}

  def exact(value), do: {:exact, %{value: value}}
  def suffix(value), do: {:suffix, %{value: value}}
  def cidr(value), do: {:cidr, %{value: value}}
end
