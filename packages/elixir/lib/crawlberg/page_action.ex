# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:889091e4b38aeb23e2736ef6d10a28217552c1396750d5fdb76abf006ea91697
# To regenerate: alef generate
# To verify freshness: alef verify
defmodule Crawlberg.PageAction do
  @moduledoc """
  A single page interaction action.

  Actions are serialized with a `type` tag using camelCase naming,
  except `ExecuteJs` which is explicitly renamed to `"executeJs"`.
  """

  @typedoc "A single page interaction action."
  @type t :: term()

  @typedoc "Click on an element matching the given CSS selector."
  @type click :: %{type: :click, selector: String.t()}
  @typedoc "Type text into an element matching the given CSS selector."
  @type type_text :: %{type: :type_text, selector: String.t(), text: String.t()}
  @typedoc "Press a keyboard key (e.g. \"Enter\", \"Tab\", \"Escape\")."
  @type press :: %{type: :press, key: String.t()}
  @typedoc "Scroll the page or a specific element."
  @type scroll :: %{type: :scroll, direction: map(), selector: String.t(), amount: integer()}
  @typedoc "Wait for a duration or for an element to appear."
  @type wait :: %{type: :wait, milliseconds: integer(), selector: String.t()}
  @typedoc "Take a screenshot of the current page."
  @type screenshot :: %{type: :screenshot, full_page: boolean()}
  @typedoc "Execute arbitrary JavaScript in the page context."
  @type execute_js :: %{type: :execute_js, script: String.t()}
  @typedoc "Scrape the current page HTML."
  @type scrape :: :scrape

  def click(selector), do: {:click, %{selector: selector}}
  def type_text(selector, text), do: {:type_text, %{selector: selector, text: text}}
  def press(key), do: {:press, %{key: key}}
  def scroll(direction, selector, amount), do: {:scroll, %{direction: direction, selector: selector, amount: amount}}
  def wait(milliseconds, selector), do: {:wait, %{milliseconds: milliseconds, selector: selector}}
  def screenshot(full_page), do: {:screenshot, %{full_page: full_page}}
  def execute_js(script), do: {:execute_js, %{script: script}}

  @doc "Returns the serde wire value this variant carries on the JSON boundary (see `wire_variant_value` in alef's naming module) -- distinct from the atom's Elixir spelling."
  @spec wire_value(t()) :: String.t()
  def wire_value(:click), do: "click"
  def wire_value(:type_text), do: "type"
  def wire_value(:press), do: "press"
  def wire_value(:scroll), do: "scroll"
  def wire_value(:wait), do: "wait"
  def wire_value(:screenshot), do: "screenshot"
  def wire_value(:execute_js), do: "executeJs"
  def wire_value(:scrape), do: "scrape"
  def wire_value(value) when is_tuple(value), do: wire_value(elem(value, 0))
end
