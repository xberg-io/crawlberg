// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:e00a5de71fc4b89b46a815c4e5c187a22f86a126f2822a5df81b3cb09273d7b6
// To regenerate: alef generate
// To verify freshness: alef verify
package io.xberg.crawlberg;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;

/**
 * Traversal order for a crawl.
 *
 * Selects both the queue discipline and the selection strategy, because global
 * order is a property of the frontier: the engine hands its bounded selection
 * window to the strategy, so a strategy alone can only reorder URLs that have
 * already been dequeued.
 */
@SuppressWarnings("PMD")
public enum CrawlStrategyKind {
  /**
   * Breadth-first: a FIFO frontier visits every URL at one depth before the
   * next.
   */
  BFS("bfs"),
  /**
   * Depth-first: a LIFO frontier descends into a page's children before its
   * siblings.
   */
  DFS("dfs"),
  /**
   * Highest-priority-first within the selection window, scored by {@code
   * CrawlStrategy.score_url}.
   */
  BEST_FIRST("best_first"),
  /**
   * Like {@code BestFirst}, but stops once newly crawled pages stop
   * contributing new terms.
   */
  ADAPTIVE("adaptive");

  /** The string value. */
  private final String value;

  CrawlStrategyKind(final String value) { this.value = value; }

  /** Returns the string value. */
  @JsonValue
  public String getValue() {
    return value;
  }

  /** Creates an instance from a string value. */
  @JsonCreator
  public static CrawlStrategyKind fromValue(final String value) {
    for (CrawlStrategyKind e : values()) {
      if (e.value.equalsIgnoreCase(value)) {
        return e;
      }
    }
    throw new IllegalArgumentException("Unknown CrawlStrategyKind value: " +
                                       value);
  }

  /** Returns the wire-format string value (matches JSON serialization). */
  @Override
  public String toString() {
    return value;
  }
}
