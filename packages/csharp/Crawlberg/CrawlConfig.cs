// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:928639b574d5fca9c5b51f86fe815c5bc0779c92416dbaa12f74160457caaf3a
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
#nullable enable

using System;
using System.Collections.Generic;
using System.Text.Json;
using System.Text.Json.Serialization;

namespace Crawlberg;

/// <summary>
/// Configuration for crawl, scrape, and map operations.
/// </summary>
public sealed record CrawlConfig {
  /// <summary>
  /// Maximum crawl depth (number of link hops from the start URL).
  /// </summary>
  [JsonPropertyName("max_depth")]
  public ulong ? MaxDepth { get; init; } = null;

  /// <summary>
  /// Maximum number of pages to crawl.
  /// </summary>
  [JsonPropertyName("max_pages")]
  public ulong
      ? MaxPages {
          get; init;
        } = null;

  /// <summary>
  /// Maximum links enqueued from a single page. Defaults to 10000.
  ///
  /// Bounds the work one hostile or pathological page can create; links past
  /// the cap are dropped and a warning is logged.
  /// </summary>
  [JsonPropertyName("max_links_per_page")]
  public ulong
      ? MaxLinksPerPage {
          get; init;
        } = null;

  /// <summary>
  /// Maximum number of concurrent requests.
  /// </summary>
  [JsonPropertyName("max_concurrent")]
  public ulong
      ? MaxConcurrent {
          get; init;
        } = null;

  /// <summary>
  /// Whether to respect robots.txt directives.
  /// </summary>
  [JsonPropertyName("respect_robots_txt")]
  public bool RespectRobotsTxt {
    get; init;
  } = false;

  /// <summary>
  /// When true, HTTP-level error responses (404 NotFound, 403 Forbidden, WAF
  /// blocks) are surfaced as `ScrapeResult` records with the matching
  /// `status_code` rather than raised as `CrawlError`. Default `false`
  /// preserves the historical throw-on-error contract for direct fetches.
  /// Independently of this flag, 404s reached at the end of a redirect chain
  /// are *always* surfaced softly — the user opted into redirect-following, so
  /// receiving a 404 there is part of the normal flow rather than an unexpected
  /// error.
  /// </summary>
  [JsonPropertyName("soft_http_errors")]
  public bool SoftHttpErrors {
    get; init;
  } = false;

  /// <summary>
  /// Custom user-agent string.
  /// </summary>
  [JsonPropertyName("user_agent")]
  public string
      ? UserAgent {
          get; init;
        } = null;

  /// <summary>
  /// Whether to restrict crawling to the same domain.
  /// </summary>
  [JsonPropertyName("stay_on_domain")]
  public bool StayOnDomain {
    get; init;
  } = false;

  /// <summary>
  /// Whether to allow subdomains when `stay_on_domain` is true.
  /// </summary>
  [JsonPropertyName("allow_subdomains")]
  public bool AllowSubdomains {
    get; init;
  } = false;

  /// <summary>
  /// Regex patterns for paths to include during crawling.
  /// </summary>
  [JsonPropertyName("include_paths")]
  public List<string> IncludePaths {
    get; init;
  } = [];

  /// <summary>
  /// Regex patterns for paths to exclude during crawling.
  /// </summary>
  [JsonPropertyName("exclude_paths")]
  public List<string> ExcludePaths {
    get; init;
  } = [];

  /// <summary>
  /// Custom HTTP headers to send with each request.
  /// </summary>
  [JsonPropertyName("custom_headers")]
  public Dictionary<string, string> CustomHeaders {
    get; init;
  } = new Dictionary<string, string>();

  /// <summary>
  /// Timeout for individual HTTP requests (in milliseconds when serialized).
  /// </summary>
  [JsonPropertyName("request_timeout")]
  public ulong
      ? RequestTimeout {
          get; init;
        } = null;

  /// <summary>
  /// Per-domain rate limit in milliseconds. When set, enforces a minimum delay
  /// between requests to the same domain. Defaults to 200ms when `None`.
  /// </summary>
  [JsonPropertyName("rate_limit_ms")]
  public ulong
      ? RateLimitMs {
          get; init;
        } = null;

  /// <summary>
  /// Maximum number of redirects to follow.
  /// </summary>
  [JsonPropertyName("max_redirects")]
  public ulong MaxRedirects {
    get; init;
  } = 10;

  /// <summary>
  /// Number of retry attempts for failed requests.
  /// </summary>
  [JsonPropertyName("retry_count")]
  public ulong RetryCount {
    get; init;
  } = 0;

  /// <summary>
  /// HTTP status codes that should trigger a retry.
  /// </summary>
  [JsonPropertyName("retry_codes")]
  public List<ushort> RetryCodes {
    get; init;
  } = [];

  /// <summary>
  /// Whether to enable cookie handling.
  /// </summary>
  [JsonPropertyName("cookies_enabled")]
  public bool CookiesEnabled {
    get; init;
  } = false;

  /// <summary>
  /// Authentication configuration.
  /// </summary>
  [JsonPropertyName("auth")]
  public AuthConfig
      ? Auth {
          get; init;
        } = null;

  /// <summary>
  /// Maximum response body size in bytes.
  /// </summary>
  [JsonPropertyName("max_body_size")]
  public ulong
      ? MaxBodySize {
          get; init;
        } = null;

  /// <summary>
  /// CSS selectors for tags to remove from HTML before processing.
  /// </summary>
  [JsonPropertyName("remove_tags")]
  public List<string> RemoveTags {
    get; init;
  } = [];

  /// <summary>
  /// Content extraction and conversion configuration.
  /// </summary>
  [JsonPropertyName("content")]
  public ContentConfig Content {
    get; init;
  } = default!;

  /// <summary>
  /// Maximum number of URLs to return from a map operation.
  /// </summary>
  [JsonPropertyName("map_limit")]
  public ulong
      ? MapLimit {
          get; init;
        } = null;

  /// <summary>
  /// Search filter for map results (case-insensitive substring match on URLs).
  /// </summary>
  [JsonPropertyName("map_search")]
  public string
      ? MapSearch {
          get; init;
        } = null;

  /// <summary>
  /// Whether to download assets (CSS, JS, images, etc.) from the page.
  /// </summary>
  [JsonPropertyName("download_assets")]
  public bool DownloadAssets {
    get; init;
  } = false;

  /// <summary>
  /// Filter for asset categories to download.
  /// </summary>
  [JsonPropertyName("asset_types")]
  public List<AssetCategory> AssetTypes {
    get; init;
  } = [];

  /// <summary>
  /// Maximum size in bytes for individual asset downloads.
  /// </summary>
  [JsonPropertyName("max_asset_size")]
  public ulong
      ? MaxAssetSize {
          get; init;
        } = null;

  /// <summary>
  /// Browser configuration.
  /// </summary>
  [JsonPropertyName("browser")]
  public BrowserConfig Browser {
    get; init;
  } = default!;

  /// <summary>
  /// Proxy configuration for HTTP requests.
  /// </summary>
  [JsonPropertyName("proxy")]
  public ProxyConfig
      ? Proxy {
          get; init;
        } = null;

  /// <summary>
  /// List of user-agent strings for rotation. If non-empty, overrides
  /// `user_agent`.
  /// </summary>
  [JsonPropertyName("user_agents")]
  public List<string> UserAgents {
    get; init;
  } = [];

  /// <summary>
  /// Whether to capture a screenshot when using the browser.
  ///
  /// Only supported by `scrape()` with `BrowserBackend.Chromiumoxide` and
  /// `BrowserMode.Always` or `Stealth`. A screenshot is 100–500 KB of PNG per
  /// page, so `crawl()` does not carry screenshots in
  /// `CrawlPageResult`/`CrawlResult` at all — a multi-thousand-page crawl
  /// holding one per page in memory is not a safe default. Setting this with
  /// any other configuration (a different backend, `BrowserMode.Auto`/`Never`,
  /// or during `crawl()`) has no effect and logs a warning rather than silently
  /// doing nothing.
  /// </summary>
  [JsonPropertyName("capture_screenshot")]
  public bool CaptureScreenshot {
    get; init;
  } = false;

  /// <summary>
  /// Re-enqueue discovered `LinkType.Document` URLs into the crawl frontier so
  /// the crawl follows links *from* document pages (PDFs, etc.) as it would
  /// from HTML pages. Default: `false` (documents terminate at
  /// materialisation).
  /// </summary>
  [JsonPropertyName("follow_document_urls")]
  public bool FollowDocumentUrls {
    get; init;
  } = false;

  /// <summary>
  /// Maximum document-depth (from the seed URL through document links only)
  /// when `follow_document_urls` is true. `None` means inherit `max_depth`.
  /// Independent of `max_depth`: a document URL is enqueued only if BOTH the
  /// outer `max_depth` and (if set) `document_url_depth` permit it.
  /// </summary>
  [JsonPropertyName("document_url_depth")]
  public uint
      ? DocumentUrlDepth {
          get; init;
        } = null;

  /// <summary>
  /// Whether to download non-HTML documents (PDF, DOCX, images, code, etc.)
  /// instead of skipping them. Defaults to `true` — unlike `download_assets`
  /// and `capture_screenshot`, which default to `false`.
  /// </summary>
  [JsonPropertyName("download_documents")]
  public bool DownloadDocuments {
    get; init;
  } = true;

  /// <summary>
  /// Maximum size in bytes for document downloads. Defaults to 50 MB.
  /// </summary>
  [JsonPropertyName("document_max_size")]
  public ulong
      ? DocumentMaxSize {
          get; init;
        } = null;

  /// <summary>
  /// Allowlist of MIME types to download. If empty, uses built-in defaults.
  /// </summary>
  [JsonPropertyName("document_mime_types")]
  public List<string> DocumentMimeTypes {
    get; init;
  } = [];

  /// <summary>
  /// Directory to stream downloaded document bytes into instead of holding them
  /// in memory on `DownloadedDocument.content`. When set, `content` is left
  /// empty and `DownloadedDocument.content_path` is populated with
  /// `&lt;dir&gt;/&lt;content_hash&gt;.&lt;ext&gt;`. `None` (default) preserves
  /// today's in-memory-only behavior. Has no effect on wasm32, which has no
  /// filesystem — use `document_content_encoding` there instead.
  /// </summary>
  [JsonPropertyName("document_output_dir")]
  public string
      ? DocumentOutputDir {
          get; init;
        } = null;

  /// <summary>
  /// Opt-in encoding that duplicates `DownloadedDocument.content` into a
  /// serializable field for language bindings that need the bytes in-memory
  /// (`content` itself is `alef(skip)`ed). `None` (default) means no encoding
  /// is produced. Independent of `document_output_dir` — set both to get a file
  /// on disk and an in-memory copy.
  /// </summary>
  [JsonPropertyName("document_content_encoding")]
  public DocumentContentEncoding
      ? DocumentContentEncoding {
          get; init;
        } = null;

  /// <summary>
  /// Path to write WARC output. If `None`, WARC output is disabled.
  /// </summary>
  [JsonPropertyName("warc_output")]
  public string
      ? WarcOutput {
          get; init;
        } = null;

  /// <summary>
  /// Named browser profile for persistent sessions (cookies, localStorage).
  ///
  /// Chromiumoxide backend only. The native backend runs an in-process
  /// JavaScript engine with no Chrome process and therefore no profile
  /// directory, so this is ignored there and logs a warning. It is also ignored
  /// — with a warning — when a shared browser pool is in use (the pool launches
  /// before any per-crawl config exists) or when connecting to an external CDP
  /// endpoint whose process crawlberg does not own.
  /// </summary>
  [JsonPropertyName("browser_profile")]
  public string
      ? BrowserProfile {
          get; init;
        } = null;

  /// <summary>
  /// Whether to save changes back to the browser profile on exit.
  /// </summary>
  [JsonPropertyName("save_browser_profile")]
  public bool SaveBrowserProfile {
    get; init;
  } = false;

  /// <summary>
  /// SSRF policy for outbound network requests. Default: deny private networks,
  /// allow http/https only, max 5 redirects.
  ///
  /// `deny_private`, `allowlist` and `max_redirects` are exposed to all
  /// language bindings. `scheme_allowlist` stays Rust-only — see `SsrfPolicy`.
  ///
  /// **wasm32 (including Node.js): `deny_private` does not stop hostname-based
  /// requests.** There is no DNS resolution on this target, so only a literal
  /// IP host is checked against the policy — a domain name is always permitted,
  /// regardless of `deny_private`. Under Node, where `fetch` enforces no CORS,
  /// this means a service embedding the wasm binding can be driven to internal
  /// hosts by domain name even with `deny_private = true`. Enforce egress
  /// restrictions at the network layer for that deployment target; do not rely
  /// on this field. See `crawlberg.net.validate_url`.
  /// </summary>
  [JsonPropertyName("ssrf")]
  public SsrfPolicy Ssrf {
    get; init;
  } = default!;

  /// <summary>
  /// Pins `SsrfPolicy.deny_private` to a caller-chosen value, bypassing the
  /// `CRAWLBERG_ALLOW_PRIVATE_NETWORK` operator override entirely for this
  /// config.
  ///
  /// `ssrf.deny_private` is a plain, always-serialized `bool`: several
  /// alef-generated bindings construct `SsrfPolicy.default()` (hardcoding
  /// `deny_private: true`) whenever their caller never touches SSRF settings at
  /// all, so `true` on that field alone cannot distinguish "the caller wants
  /// private networks denied" from "the binding's own structural default landed
  /// on `true`". The environment variable exists precisely to resolve that
  /// ambiguity in the common case by treating any `true` as inconclusive and
  /// deferring to the operator.
  ///
  /// Set this field when that default-deferral is wrong for your call — e.g. a
  /// test that must prove `deny_private: true` still denies even while the
  /// operator has set `CRAWLBERG_ALLOW_PRIVATE_NETWORK` suite-wide for every
  /// other call. `None` (default) preserves today's behavior: the environment
  /// variable may still flip `ssrf.deny_private` to `false`. `Some(value)` pins
  /// `ssrf.deny_private` to `value` and the environment variable is not
  /// consulted for this config.
  /// </summary>
  [JsonPropertyName("ssrf_deny_private_explicit")]
  public bool
      ? SsrfDenyPrivateExplicit {
          get; init;
        } = null;

  /// <summary>
  /// Parse a <see cref="CrawlConfig"/> from a JSON string.
  /// </summary>
  /// <exception cref="CrawlbergException">When the JSON cannot be
  /// deserialised.</exception>
  public static CrawlConfig FromJson(string json) {
    try {
      return JsonSerializer.Deserialize<CrawlConfig>(json, JsonOptions) ??
             throw new CrawlbergException(
                 $"Failed to parse CrawlConfig from JSON: deserializer returned null");
    } catch (CrawlbergException) {
      throw;
    } catch (Exception e) {
      throw new CrawlbergException(
          $"Failed to parse CrawlConfig from JSON: {e.Message}", e);
    }
  }

  private static readonly JsonSerializerOptions JsonOptions = new() {
    DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingDefault,
    Converters = { new JsonStringEnumConverter(JsonNamingPolicy.SnakeCaseLower),
                   new ByteArrayJsonConverter() },
  };

  /// <summary>Options for serializing config/input objects to FFI. Strips nulls
  /// (nullable C# fields default to null and would override required Rust
  /// fields with non-deserialisable nulls) but preserves explicit false/0 so
  /// caller intent is kept.</summary>
  private static readonly JsonSerializerOptions JsonSerializationOptions =
      new() {
        DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingNull,
        Converters = { new JsonStringEnumConverter(
                           JsonNamingPolicy.SnakeCaseLower),
                       new ByteArrayJsonConverter() },
      };

  public static CrawlConfig Default() {
    var nativeResult = NativeMethods.CrawlConfigDefault();
    var jsonPtr = NativeMethods.CrawlConfigToJson(nativeResult);
    var json =
        global::System.Runtime.InteropServices.Marshal.PtrToStringUTF8(jsonPtr);
    NativeMethods.FreeString(jsonPtr);
    NativeMethods.CrawlConfigFree(nativeResult);
    return JsonSerializer.Deserialize<CrawlConfig>(json ?? "null",
                                                   JsonOptions)!;
  }
}
