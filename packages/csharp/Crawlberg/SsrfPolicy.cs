// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:2c5f3aa6df2804a61d79bcebb143e934293e35be3e6feb4df276b0219413f504
// To regenerate: alef generate
// To verify freshness: alef verify
#nullable enable

using System;
using System.Collections.Generic;
using System.Text.Json;
using System.Text.Json.Serialization;

namespace Crawlberg;

/// <summary>
/// SSRF policy configuration.
/// </summary>
public sealed record SsrfPolicy {
  /// <summary>
  /// If true, reject URLs that resolve to private/metadata IP ranges.
  /// </summary>
  [JsonPropertyName("deny_private")]
  public bool DenyPrivate { get; init; } = true;

  /// <summary>
  /// Hostnames and IP ranges permitted regardless of `deny_private`.
  ///
  /// The allowlist is an *override* of `deny_private`, not an intersection with
  /// it. Precedence, in order:
  ///
  /// 1. `deny_private == false` permits everything; the allowlist is not
  /// consulted.
  /// 2. A hostname matching an `Exact` or `Suffix` entry is permitted
  /// immediately,
  ///    *before* DNS resolution — so the deny-list is never applied to it. This
  ///    trusts the host string: a name that resolves into private space is
  ///    still permitted.
  /// 3. A literal or resolved IP inside a `Cidr` entry is permitted even though
  /// it is
  ///    in the default deny-list.
  /// 4. Otherwise the default deny-list decides.
  ///
  /// An empty allowlist therefore denies nothing by itself — it simply leaves
  /// `deny_private` and the deny-list in sole control.
  /// </summary>
  [JsonPropertyName("allowlist")]
  public List<HostMatcher> Allowlist {
    get; init;
  } = [];

  /// <summary>
  /// Maximum number of HTTP redirects to follow during validation.
  /// </summary>
  [JsonPropertyName("max_redirects")]
  public byte MaxRedirects {
    get; init;
  } = 5;

  /// <summary>
  /// Allowed URI schemes. Default: `["http", "https"]`.
  ///
  /// Only `http` and `https` are supported. An empty list denies every URL.
  /// </summary>
  [JsonPropertyName("scheme_allowlist")]
  public List<string>
      ? SchemeAllowlist {
          get; init;
        } = null;

  /// <summary>
  /// Parse a <see cref="SsrfPolicy"/> from a JSON string.
  /// </summary>
  /// <exception cref="CrawlbergException">When the JSON cannot be
  /// deserialised.</exception>
  public static SsrfPolicy FromJson(string json) {
    try {
      return JsonSerializer.Deserialize<SsrfPolicy>(json, JsonOptions) ??
             throw new CrawlbergException(
                 $"Failed to parse SsrfPolicy from JSON: deserializer returned null");
    } catch (CrawlbergException) {
      throw;
    } catch (Exception e) {
      throw new CrawlbergException(
          $"Failed to parse SsrfPolicy from JSON: {e.Message}", e);
    }
  }

  private static readonly JsonSerializerOptions JsonOptions = new() {
    DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingDefault,
    Converters = { new JsonStringEnumConverter(JsonNamingPolicy.SnakeCaseLower),
                   new ByteArrayJsonConverter() },
  };

  /// <summary>Options for serializing config/input objects to FFI. Strips nulls
  /// (nullable C# fields default to null and would override required Rust
  /// fields with non-deserialisable nulls) but preserves explicit false/0 so
  /// caller intent is kept.</summary>
  private static readonly JsonSerializerOptions JsonSerializationOptions =
      new() {
        DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingNull,
        Converters = { new JsonStringEnumConverter(
                           JsonNamingPolicy.SnakeCaseLower),
                       new ByteArrayJsonConverter() },
      };

  public static SsrfPolicy Default() {
    var nativeResult = NativeMethods.SsrfPolicyDefault();
    var jsonPtr = NativeMethods.SsrfPolicyToJson(nativeResult);
    var json =
        global::System.Runtime.InteropServices.Marshal.PtrToStringUTF8(jsonPtr);
    NativeMethods.FreeString(jsonPtr);
    NativeMethods.SsrfPolicyFree(nativeResult);
    return JsonSerializer.Deserialize<SsrfPolicy>(json ?? "null", JsonOptions)!;
  }

  /// <summary>
  /// Create a policy from environment variables.
  /// </summary>
  public static SsrfPolicy FromEnv() {
    var nativeResult = NativeMethods.SsrfPolicyFromEnv();
    var jsonPtr = NativeMethods.SsrfPolicyToJson(nativeResult);
    var json =
        global::System.Runtime.InteropServices.Marshal.PtrToStringUTF8(jsonPtr);
    NativeMethods.FreeString(jsonPtr);
    NativeMethods.SsrfPolicyFree(nativeResult);
    return JsonSerializer.Deserialize<SsrfPolicy>(json ?? "null", JsonOptions)!;
  }
}
