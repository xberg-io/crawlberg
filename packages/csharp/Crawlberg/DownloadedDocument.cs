// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:fe445c974733c703099671c7022b34ef9df4b85f8fee3260e7ec4150907af3b0
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
#nullable enable

using System;
using System.Collections.Generic;
using System.Text.Json;
using System.Text.Json.Serialization;

namespace Crawlberg;

/// <summary>
/// A downloaded non-HTML document (PDF, DOCX, image, code file, etc.).
///
/// When the crawler encounters non-HTML content and `download_documents` is
/// enabled, it downloads the raw bytes and populates this struct instead of
/// skipping the resource.
/// </summary>
public sealed record DownloadedDocument {
  /// <summary>
  /// The URL the document was fetched from.
  /// </summary>
  [JsonPropertyName("url")]
  public string Url { get; init; } = "";

  /// <summary>
  /// The MIME type from the Content-Type header.
  /// </summary>
  [JsonPropertyName("mime_type")]
  public string MimeType {
    get; init;
  } = "";

  /// <summary>
  /// Size of the document in bytes.
  /// </summary>
  [JsonPropertyName("size")]
  public ulong Size {
    get; init;
  } = 0;

  /// <summary>
  /// Filename extracted from Content-Disposition or URL path.
  /// </summary>
  [JsonPropertyName("filename")]
  public string
      ? Filename {
          get; init;
        } = null;

  /// <summary>
  /// SHA-256 hex digest of the content.
  /// </summary>
  [JsonPropertyName("content_hash")]
  public string ContentHash {
    get; init;
  } = "";

  /// <summary>
  /// Selected response headers.
  /// </summary>
  [JsonPropertyName("headers")]
  public Dictionary<string, string> Headers {
    get; init;
  } = new Dictionary<string, string>();

  /// <summary>
  /// True when `content` (or the file at `content_path`) was truncated to
  /// `document_max_size`; `size` still reports the original, untruncated
  /// length.
  /// </summary>
  [JsonPropertyName("truncated")]
  public bool Truncated {
    get; init;
  } = false;

  /// <summary>
  /// Filesystem path the document was streamed to when `document_output_dir`
  /// was set. `content` is empty in memory when this is populated.
  /// </summary>
  [JsonPropertyName("content_path")]
  public string
      ? ContentPath {
          get; init;
        } = null;

  /// <summary>
  /// Base64-encoded copy of `content`, populated only when
  /// `document_content_encoding` was set to `Base64`.
  /// </summary>
  [JsonPropertyName("content_base64")]
  public string
      ? ContentBase64 {
          get; init;
        } = null;

  /// <summary>
  /// Parse a <see cref="DownloadedDocument"/> from a JSON string.
  /// </summary>
  /// <exception cref="CrawlbergException">When the JSON cannot be
  /// deserialised.</exception>
  public static DownloadedDocument FromJson(string json) {
    try {
      return JsonSerializer.Deserialize<DownloadedDocument>(json,
                                                            JsonOptions) ??
             throw new CrawlbergException(
                 $"Failed to parse DownloadedDocument from JSON: deserializer returned null");
    } catch (CrawlbergException) {
      throw;
    } catch (Exception e) {
      throw new CrawlbergException(
          $"Failed to parse DownloadedDocument from JSON: {e.Message}", e);
    }
  }

  private static readonly JsonSerializerOptions JsonOptions = new() {
    DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingDefault,
    Converters = { new JsonStringEnumConverter(JsonNamingPolicy.SnakeCaseLower),
                   new ByteArrayJsonConverter() },
  };

  /// <summary>Options for serializing config/input objects to FFI. Strips nulls
  /// (nullable C# fields default to null and would override required Rust
  /// fields with non-deserialisable nulls) but preserves explicit false/0 so
  /// caller intent is kept.</summary>
  private static readonly JsonSerializerOptions JsonSerializationOptions =
      new() {
        DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingNull,
        Converters = { new JsonStringEnumConverter(
                           JsonNamingPolicy.SnakeCaseLower),
                       new ByteArrayJsonConverter() },
      };
}
