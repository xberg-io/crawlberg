// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:2d8f3380df26543ec37ceb900750758be13d8fb62b31e43586f2eabff82e5e5d
// To regenerate: alef generate
// To verify freshness: alef verify
#nullable enable

using System;
using System.Collections.Generic;
using System.Text.Json;
using System.Text.Json.Serialization;

namespace Crawlberg;

/// <summary>
/// Aggregate result of a batch scrape, exposing per-URL results plus precomputed counts.
///
/// The counts are derived once at construction so every binding language can read them
/// as plain integer fields without re-iterating the `results` vector.
/// </summary>
public sealed record BatchScrapeResults
{
    /// <summary>
    /// Per-URL scrape results, in the order URLs were submitted.
    /// </summary>
    [JsonPropertyName("results")]
    public List<BatchScrapeResult> Results { get; init; } = [];

    /// <summary>
    /// Total number of URLs in the batch (equal to `results.len()`).
    /// </summary>
    [JsonPropertyName("total_count")]
    public ulong TotalCount { get; init; } = 0;

    /// <summary>
    /// Number of URLs whose scrape succeeded (`error` is `None`).
    /// </summary>
    [JsonPropertyName("completed_count")]
    public ulong CompletedCount { get; init; } = 0;

    /// <summary>
    /// Number of URLs whose scrape failed (`error` is `Some`).
    /// </summary>
    [JsonPropertyName("failed_count")]
    public ulong FailedCount { get; init; } = 0;


    /// <summary>
    /// Parse a <see cref="BatchScrapeResults"/> from a JSON string.
    /// </summary>
    /// <exception cref="CrawlbergException">When the JSON cannot be deserialised.</exception>
    public static BatchScrapeResults FromJson(string json)
    {
        try
        {
            return JsonSerializer.Deserialize<BatchScrapeResults>(json, JsonOptions)
                ?? throw new CrawlbergException($"Failed to parse BatchScrapeResults from JSON: deserializer returned null");
        }
        catch (CrawlbergException)
        {
            throw;
        }
        catch (Exception e)
        {
            throw new CrawlbergException($"Failed to parse BatchScrapeResults from JSON: {e.Message}", e);
        }
    }

    private static readonly JsonSerializerOptions JsonOptions = new()
    {
        DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingDefault,
        Converters = { new JsonStringEnumConverter(JsonNamingPolicy.SnakeCaseLower), new ByteArrayJsonConverter() },
    };

    /// <summary>Options for serializing config/input objects to FFI. Strips nulls
    /// (nullable C# fields default to null and would override required Rust fields with
    /// non-deserialisable nulls) but preserves explicit false/0 so caller intent is kept.</summary>
    private static readonly JsonSerializerOptions JsonSerializationOptions = new()
    {
        DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingNull,
        Converters = { new JsonStringEnumConverter(JsonNamingPolicy.SnakeCaseLower), new ByteArrayJsonConverter() },
    };
}
