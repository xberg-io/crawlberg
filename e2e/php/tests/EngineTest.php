<?php

// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:2ff9403466d910c44ffa6969e96384165e3af3962f1522ac41ca9218ce7f9eed
// To regenerate: alef generate
// To verify freshness: alef verify


declare(strict_types=1);

namespace Crawlberg\E2e;

use PHPUnit\Framework\TestCase;
use Crawlberg\Crawlberg;
use Crawlberg\CrawlConfig;
use Crawlberg\CrawlStreamRequest;

/** E2e tests for category: engine. */
final class EngineTest extends TestCase
{
    /** CrawlEngine with defaults batch scrapes like the free function */
    public function test_engine_batch_basic(): void
    {
        $engine = Crawlberg::createEngine(null);
        $urls_base = getenv('MOCK_SERVER_ENGINE_BATCH_BASIC') ?: getenv('MOCK_SERVER_URL') . '/fixtures/engine_batch_basic';
        $urls = array_map(fn ($p) => str_starts_with($p, 'http') ? $p : $urls_base . $p, ["/page1", "/page2"]);
        $result = Crawlberg::batchScrape($engine, $urls);

        $this->assertEquals(2, $result->completedCount);
        $this->assertEquals(2, $result->totalCount);


    }


    /** CrawlEngine with defaults crawls multiple pages like the free function */
    public function test_engine_crawl_basic(): void
    {
        $engine_config = CrawlConfig::from_json(json_encode(["maxDepth" => 1]));
        $engine = Crawlberg::createEngine($engine_config);
        $url = getenv('MOCK_SERVER_ENGINE_CRAWL_BASIC') ?: getenv('MOCK_SERVER_URL') . '/fixtures/engine_crawl_basic';
        $result = Crawlberg::crawl($engine, $url);

        $this->assertEquals(3, count($result->getPages()));
        $this->assertGreaterThanOrEqual(3, count($result->getPages()));


    }


    /** CrawlEngine with defaults discovers URLs like the free function */
    public function test_engine_map_basic(): void
    {
        $engine = Crawlberg::createEngine(null);
        $url = getenv('MOCK_SERVER_ENGINE_MAP_BASIC') ?: getenv('MOCK_SERVER_URL') . '/fixtures/engine_map_basic';
        $result = Crawlberg::mapUrls($engine, $url);

        $this->assertGreaterThanOrEqual(2, count($result->getUrls()));


    }


    /** CrawlEngine with defaults scrapes a page identically to the free function */
    public function test_engine_scrape_basic(): void
    {
        $engine = Crawlberg::createEngine(null);
        $url = getenv('MOCK_SERVER_ENGINE_SCRAPE_BASIC') ?: getenv('MOCK_SERVER_URL') . '/fixtures/engine_scrape_basic';
        $result = Crawlberg::scrape($engine, $url);

        $this->assertEquals(200, $result->statusCode);
        $this->assertEquals("text/html", $result->contentType);
        $this->assertEquals("Engine Test", ($result->getMetadata()->title ?? null));
        $this->assertStringContainsString("Testing the engine", ($result->getMetadata()->description ?? null));
        $this->assertGreaterThanOrEqual(1, count($result->getLinks()));
        $this->assertGreaterThanOrEqual(1, count($result->getMetadata()->getHeadings()));


    }


    /** CrawlEngine with defaults streams events like the free function */
    public function test_engine_stream_basic(): void
    {
        $engine_config = CrawlConfig::from_json(json_encode(["maxDepth" => 1]));
        $engine = Crawlberg::createEngine($engine_config);
        $url = getenv('MOCK_SERVER_ENGINE_STREAM_BASIC') ?: getenv('MOCK_SERVER_URL') . '/fixtures/engine_stream_basic';
        $url_req = new CrawlStreamRequest($url);
        $result = $engine->crawlStream($url_req);
        $__camel = function ($v) use (&$__camel) {
            if (is_array($v)) {
                $out = [];
                foreach ($v as $k => $vv) {
                    $key = is_string($k) ? lcfirst(str_replace(' ', '', ucwords(str_replace('_', ' ', $k)))) : $k;
                    $out[$key] = $__camel($vv);
                } return (array_keys($out) === range(0, count($out) - 1)) ? $out : (object) $out;
            } if (is_object($v)) {
                $out = new \stdClass();
                foreach (get_object_vars($v) as $k => $vv) {
                    $key = lcfirst(str_replace(' ', '', ucwords(str_replace('_', ' ', $k))));
                    $out->{$key} = $__camel($vv);
                } return $out;
            } return $v;
        };
        $__decode_chunk = fn ($c) => $__camel(is_string($c) ? json_decode($c, true) : (is_array($c) || is_object($c) ? json_decode(json_encode($c), true) : $c));
        $chunks = is_string($result) ? array_map($__decode_chunk, (array)(json_decode($result, true) ?: [])) : (is_array($result) ? array_map($__decode_chunk, $result) : array_map($__decode_chunk, iterator_to_array($result)));

        // skipped: streaming assertion on unsupported field 'stream.has_page_event'
        // skipped: streaming assertion on unsupported field 'stream.has_complete_event'
        $this->assertGreaterThanOrEqual(3, count($chunks));


    }

}
