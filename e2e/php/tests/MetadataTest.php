<?php

// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:6e30edaa83ca12b948a71f61ffe86fff3f577e387ad3d63af48151d98c4c163d
// To regenerate: alef generate
// To verify freshness: alef verify


declare(strict_types=1);

namespace Crawlberg\E2e;

use PHPUnit\Framework\TestCase;
use Crawlberg\Crawlberg;
use Crawlberg\CrawlConfig;

/** E2e tests for category: metadata. */
final class MetadataTest extends TestCase
{
    /** Extracts article:published_time, modified_time, author, section, and tags */
    public function test_metadata_article_times(): void
    {
        $engine = Crawlberg::createEngine(null);
        $url = getenv('MOCK_SERVER_URL') . '/fixtures/metadata_article_times';
        $result = Crawlberg::scrape($engine, $url);

        $this->assertEquals(200, $result->statusCode);
        $this->assertEquals("2024-01-15T10:00:00Z", $result->getMetadata()->getArticle()->publishedTime);
        $this->assertEquals("2024-06-20T14:30:00Z", $result->getMetadata()->getArticle()->modifiedTime);
        $this->assertEquals("Jane Developer", $result->getMetadata()->getArticle()->author);
        $this->assertEquals("Technology", $result->getMetadata()->getArticle()->section);
        $this->assertEquals(3, count($result->getMetadata()->getArticle()->tags));


    }


    /** Extracts favicon link tags including apple-touch-icon */
    public function test_metadata_favicons(): void
    {
        $engine = Crawlberg::createEngine(null);
        $url = getenv('MOCK_SERVER_URL') . '/fixtures/metadata_favicons';
        $result = Crawlberg::scrape($engine, $url);

        $this->assertEquals(200, $result->statusCode);
        $this->assertEquals(5, count($result->getMetadata()->getFavicons()));
        $this->assertTrue((bool)array_filter(($result->getMetadata()->getFavicons() ?? []), fn ($e) => str_contains((string)$e->rel, "apple-touch-icon")));


    }


    /** Extracts heading hierarchy (h1-h6) from HTML page */
    public function test_metadata_headings(): void
    {
        $engine = Crawlberg::createEngine(null);
        $url = getenv('MOCK_SERVER_URL') . '/fixtures/metadata_headings';
        $result = Crawlberg::scrape($engine, $url);

        $this->assertEquals(200, $result->statusCode);
        $this->assertEquals(1, $result->getMetadata()->getHeadings()[0]->level);
        $this->assertEquals("Primary Heading", $result->getMetadata()->getHeadings()[0]->text);
        $this->assertEquals(8, count($result->getMetadata()->getHeadings()));


    }


    /** Extracts hreflang alternate link tags */
    public function test_metadata_hreflang(): void
    {
        $engine = Crawlberg::createEngine(null);
        $url = getenv('MOCK_SERVER_URL') . '/fixtures/metadata_hreflang';
        $result = Crawlberg::scrape($engine, $url);

        $this->assertEquals(200, $result->statusCode);
        $this->assertEquals(4, count($result->getMetadata()->getHreflangs()));
        $this->assertTrue((bool)array_filter(($result->getMetadata()->getHreflangs() ?? []), fn ($e) => str_contains((string)$e->lang, "en")));


    }


    /** Extracts keywords, author, viewport, generator, theme-color, robots, lang, dir metadata */
    public function test_metadata_keywords_author(): void
    {
        $engine = Crawlberg::createEngine(null);
        $url = getenv('MOCK_SERVER_URL') . '/fixtures/metadata_keywords_author';
        $result = Crawlberg::scrape($engine, $url);

        $this->assertEquals(200, $result->statusCode);
        $this->assertEquals("Comprehensive Metadata Test Page", ($result->getMetadata()->title ?? null));
        $this->assertNotSame('', ($result->getMetadata()->canonicalUrl ?? null) ?? '', 'expected non-empty value');
        $this->assertNotSame('', ($result->getMetadata()->keywords ?? null) ?? '', 'expected non-empty value');
        $this->assertStringContainsString("rust", ($result->getMetadata()->keywords ?? null));
        $this->assertEquals("Jane Developer", ($result->getMetadata()->author ?? null));
        $this->assertNotSame('', ($result->getMetadata()->viewport ?? null) ?? '', 'expected non-empty value');
        $this->assertEquals("crawlberg/1.0", ($result->getMetadata()->generator ?? null));
        $this->assertEquals("#ff6600", ($result->getMetadata()->themeColor ?? null));
        $this->assertEquals("index, follow", ($result->getMetadata()->robots ?? null));
        $this->assertEquals("en", $result->getMetadata()->htmlLang);
        $this->assertEquals("ltr", $result->getMetadata()->htmlDir);


    }


    /** Extracts og:video, og:audio, and og:locale:alternate metadata */
    public function test_metadata_og_video_audio(): void
    {
        $engine = Crawlberg::createEngine(null);
        $url = getenv('MOCK_SERVER_URL') . '/fixtures/metadata_og_video_audio';
        $result = Crawlberg::scrape($engine, $url);

        $this->assertEquals(200, $result->statusCode);
        $this->assertEquals("https://example.com/video.mp4", $result->getMetadata()->ogVideo);
        $this->assertEquals("https://example.com/audio.mp3", $result->getMetadata()->ogAudio);
        $this->assertEquals(2, count($result->getMetadata()->ogLocaleAlternates));


    }


    /** Extracts response metadata from HTTP headers (etag, server, content-language) */
    public function test_metadata_response_headers(): void
    {
        $engine = Crawlberg::createEngine(null);
        $url = getenv('MOCK_SERVER_URL') . '/fixtures/metadata_response_headers';
        $result = Crawlberg::scrape($engine, $url);

        $this->assertEquals(200, $result->statusCode);
        $this->assertStringContainsString("abc123", $result->getResponseMeta()->etag);
        $this->assertStringContainsString("01 Jan 2025", $result->getResponseMeta()->lastModified);
        $this->assertStringContainsString("nginx", $result->getResponseMeta()->server);
        $this->assertEquals("en-US", $result->getResponseMeta()->contentLanguage);


    }


    /** Computes word count from visible page text */
    public function test_metadata_word_count(): void
    {
        $engine = Crawlberg::createEngine(null);
        $url = getenv('MOCK_SERVER_URL') . '/fixtures/metadata_word_count';
        $result = Crawlberg::scrape($engine, $url);

        $this->assertEquals(200, $result->statusCode);
        $this->assertGreaterThan(99, ($result->getMetadata()->wordCount ?? null));
        $this->assertLessThan(301, ($result->getMetadata()->wordCount ?? null));


    }

}
