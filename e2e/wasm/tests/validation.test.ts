// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:e9450e39de54decec361241ed7197f2f3eebded502892fba8c57b5c76f819abe
// To regenerate: alef generate
// To verify freshness: alef verify

import { describe, expect, it } from "vitest";
import { scrape, createEngine, WasmCrawlConfig, WasmBrowserMode, WasmAuthConfig, WasmBrowserConfig, WasmContentConfig, WasmProxyConfig, WasmSsrfPolicy } from "@xberg-io/crawlberg-wasm";

process.env.CRAWLBERG_ALLOW_PRIVATE_NETWORK ??= "true";


async function _alefE2eDecompressAndParseJson(response: Response): Promise<unknown> {
	const contentEncoding = response.headers.get("content-encoding");
	let buffer = await response.arrayBuffer();

	if (contentEncoding === "gzip") {
		const { gunzipSync } = await import("zlib");
		buffer = gunzipSync(new Uint8Array(buffer)).buffer;
	} else if (contentEncoding === "br") {
		const { brotliDecompressSync } = await import("zlib");
		buffer = brotliDecompressSync(new Uint8Array(buffer)).buffer;
	}

	const text = new TextDecoder().decode(buffer);
	return JSON.parse(text);
}

function _alefE2eText(value: unknown): string {
	return value == null ? "" : String(value);
}

function _alefE2eItemTexts(item: unknown): string[] {
	if (item == null || typeof item !== "object") {
		return [_alefE2eText(item)];
	}
	const record = item as Record<string, unknown>;
	const itemsText = Array.isArray(record.items) ? record.items.map(_alefE2eText).join(" ") : "";
	return [_alefE2eText(item), _alefE2eText(record.kind), _alefE2eText(record.name), _alefE2eText(record.source), _alefE2eText(record.alias), _alefE2eText(record.text), _alefE2eText(record.signature), itemsText];
}

function _alefE2eFormatMetadataDisplay(fm: unknown): string {
	if (fm == null) return "";
	if (typeof fm !== "object") return String(fm);
	const record = fm as Record<string, unknown>;
	const formatType = record.format_type;

	// FormatMetadata is a tagged union: { format_type: 'image', image: { format: 'PNG', ... }, ... }
	// Extract the display string based on the variant type
	if (formatType === "image" && typeof record.image === "object") {
		const imageData = record.image as Record<string, unknown>;
		if (typeof imageData.format === "string") return imageData.format;
	}

	// Fallback: return format_type variant name
	if (typeof record.format_type === "string") return record.format_type;
	return "";
}


describe("validation", () => {

	it("validation_browser_endpoint_invalid: Browser endpoint must be a valid ws:// or wss:// URL, not http://", async () => {
		await expect(async () => {
			const engineConfig = WasmCrawlConfig.default();
			engineConfig.browser = (() => { const _u0 = WasmBrowserConfig.default(); _u0.endpoint = "http://not-websocket:3000"; _u0.mode = WasmBrowserMode.Always; return _u0; })();
			engineConfig.ssrf.denyPrivate = false;
			const engine = createEngine(engineConfig);
			const url = `${process.env.MOCK_SERVER_URL}/fixtures/validation_browser_endpoint_invalid`;
			await scrape(engine, url);
		}).rejects.toSatisfy((error) => {
				const _message = error instanceof Error ? error.message : String(error);
				const _name = error instanceof Error ? error.name : "";
				return /endpoint/.test(_message) || /endpoint/.test(_name);
			});
	}, 30000);

	it("validation_invalid_auth_config: auth object with empty username in basic auth is rejected", async () => {
		await expect(async () => {
			const engineConfig = WasmCrawlConfig.default();
			engineConfig.auth = { password: "secret", type: "basic", username: "" } as WasmAuthConfig;
			engineConfig.ssrf.denyPrivate = false;
			const engine = createEngine(engineConfig);
			const url = `${process.env.MOCK_SERVER_URL}/fixtures/validation_invalid_auth_config`;
			await scrape(engine, url);
		}).rejects.toSatisfy((error) => {
				const _message = error instanceof Error ? error.message : String(error);
				const _name = error instanceof Error ? error.name : "";
				return /auth/.test(_message) || /auth/.test(_name);
			});
	}, 30000);

	it("validation_invalid_exclude_regex: Invalid regex in exclude_paths is rejected", async () => {
		await expect(async () => {
			const engineConfig = WasmCrawlConfig.default();
			engineConfig.excludePaths = ["(unclosed"];
			engineConfig.ssrf.denyPrivate = false;
			const engine = createEngine(engineConfig);
			const url = `${process.env.MOCK_SERVER_URL}/fixtures/validation_invalid_exclude_regex`;
			await scrape(engine, url);
		}).rejects.toSatisfy((error) => {
				const _message = error instanceof Error ? error.message : String(error);
				const _name = error instanceof Error ? error.name : "";
				return /exclude_path/.test(_message) || /exclude_path/.test(_name);
			});
	}, 30000);

	it("validation_invalid_include_regex: Invalid regex in include_paths is rejected", async () => {
		await expect(async () => {
			const engineConfig = WasmCrawlConfig.default();
			engineConfig.includePaths = ["[invalid"];
			engineConfig.ssrf.denyPrivate = false;
			const engine = createEngine(engineConfig);
			const url = `${process.env.MOCK_SERVER_URL}/fixtures/validation_invalid_include_regex`;
			await scrape(engine, url);
		}).rejects.toSatisfy((error) => {
				const _message = error instanceof Error ? error.message : String(error);
				const _name = error instanceof Error ? error.name : "";
				return /include_path/.test(_message) || /include_path/.test(_name);
			});
	}, 30000);

	it("validation_invalid_proxy_url: proxy with invalid URL like 'not-a-url' is rejected", async () => {
		await expect(async () => {
			const engineConfig = WasmCrawlConfig.default();
			engineConfig.proxy = (() => { const _u0 = WasmProxyConfig.default(); _u0.url = "not-a-url"; return _u0; })();
			engineConfig.ssrf.denyPrivate = false;
			const engine = createEngine(engineConfig);
			const url = `${process.env.MOCK_SERVER_URL}/fixtures/validation_invalid_proxy_url`;
			await scrape(engine, url);
		}).rejects.toSatisfy((error) => {
				const _message = error instanceof Error ? error.message : String(error);
				const _name = error instanceof Error ? error.name : "";
				return /proxy/.test(_message) || /proxy/.test(_name);
			});
	}, 30000);

	it("validation_invalid_retry_code: Retry code outside 100-599 is rejected", async () => {
		await expect(async () => {
			const engineConfig = WasmCrawlConfig.default();
			engineConfig.retryCodes = [999];
			engineConfig.ssrf.denyPrivate = false;
			const engine = createEngine(engineConfig);
			const url = `${process.env.MOCK_SERVER_URL}/fixtures/validation_invalid_retry_code`;
			await scrape(engine, url);
		}).rejects.toSatisfy((error) => {
				const _message = error instanceof Error ? error.message : String(error);
				const _name = error instanceof Error ? error.name : "";
				return /retry code/.test(_message) || /retry code/.test(_name);
			});
	}, 30000);

	it("validation_max_concurrent_zero: max_concurrent=0 is rejected as invalid config (minimum is 1)", async () => {
		await expect(async () => {
			const engineConfig = WasmCrawlConfig.default();
			engineConfig.maxConcurrent = 0;
			engineConfig.ssrf.denyPrivate = false;
			const engine = createEngine(engineConfig);
			const url = `${process.env.MOCK_SERVER_URL}/fixtures/validation_max_concurrent_zero`;
			await scrape(engine, url);
		}).rejects.toSatisfy((error) => {
				const _message = error instanceof Error ? error.message : String(error);
				const _name = error instanceof Error ? error.name : "";
				return /max_concurrent/.test(_message) || /max_concurrent/.test(_name);
			});
	}, 30000);

	it("validation_max_depth_too_high: max_depth=200 exceeds limit of 100", async () => {
		await expect(async () => {
			const engineConfig = WasmCrawlConfig.default();
			engineConfig.maxDepth = 200;
			engineConfig.ssrf.denyPrivate = false;
			const engine = createEngine(engineConfig);
			const url = `${process.env.MOCK_SERVER_URL}/fixtures/validation_max_depth_too_high`;
			await scrape(engine, url);
		}).rejects.toSatisfy((error) => {
				const _message = error instanceof Error ? error.message : String(error);
				const _name = error instanceof Error ? error.name : "";
				return /max_depth/.test(_message) || /max_depth/.test(_name);
			});
	}, 30000);

	it("validation_max_pages_zero: max_pages=0 is rejected as invalid config", async () => {
		await expect(async () => {
			const engineConfig = WasmCrawlConfig.default();
			engineConfig.maxPages = 0;
			engineConfig.ssrf.denyPrivate = false;
			const engine = createEngine(engineConfig);
			const url = `${process.env.MOCK_SERVER_URL}/fixtures/validation_max_pages_zero`;
			await scrape(engine, url);
		}).rejects.toSatisfy((error) => {
				const _message = error instanceof Error ? error.message : String(error);
				const _name = error instanceof Error ? error.name : "";
				return /max_pages/.test(_message) || /max_pages/.test(_name);
			});
	}, 30000);

	it("validation_max_redirects_too_high: max_redirects > 100 is rejected as invalid config", async () => {
		await expect(async () => {
			const engineConfig = WasmCrawlConfig.default();
			engineConfig.maxRedirects = 200;
			engineConfig.ssrf.denyPrivate = false;
			const engine = createEngine(engineConfig);
			const url = `${process.env.MOCK_SERVER_URL}/fixtures/validation_max_redirects_too_high`;
			await scrape(engine, url);
		}).rejects.toSatisfy((error) => {
				const _message = error instanceof Error ? error.message : String(error);
				const _name = error instanceof Error ? error.name : "";
				return /max_redirects/.test(_message) || /max_redirects/.test(_name);
			});
	}, 30000);

	it("validation_negative_body_size: max_body_size set to -1 is rejected as invalid config", async () => {
		await expect(async () => {
			const engineConfig = WasmCrawlConfig.default();
			engineConfig.maxBodySize = 0;
			engineConfig.ssrf.denyPrivate = false;
			const engine = createEngine(engineConfig);
			const url = `${process.env.MOCK_SERVER_URL}/fixtures/validation_negative_body_size`;
			await scrape(engine, url);
		}).rejects.toSatisfy((error) => {
				const _message = error instanceof Error ? error.message : String(error);
				const _name = error instanceof Error ? error.name : "";
				return /body_size/.test(_message) || /body_size/.test(_name);
			});
	}, 30000);

	it("validation_ssrf_allowlisted_loopback: SSRF allowlist permits a loopback mock server while private-network protection remains enabled", async () => {
		const engineConfig = WasmCrawlConfig.default();
		engineConfig.respectRobotsTxt = false;
		engineConfig.ssrf = (() => { const _u0 = WasmSsrfPolicy.default(); _u0.allowlist = [{ type: "cidr", value: "127.0.0.0/8" }]; _u0.denyPrivate = true; _u0.maxRedirects = 5; return _u0; })();
		engineConfig.ssrfDenyPrivateExplicit = true;
		engineConfig.ssrf.denyPrivate = false;
		const engine = createEngine(engineConfig);
		const url = `${process.env.MOCK_SERVER_URL}/fixtures/validation_ssrf_allowlisted_loopback`;
		const result = await scrape(engine, url);
    expect(Number(result.statusCode)).toBe(200);
    expect((result.metadata.title ?? "")).toBe("Allowlisted");

	}, 30000);

	it("validation_ssrf_ipv4_mapped_ipv6_denied: SSRF policy rejects an IPv4 loopback address embedded in an IPv6 literal", async () => {
		await expect(async () => {
			const engineConfig = WasmCrawlConfig.default();
			engineConfig.ssrf = (() => { const _u0 = WasmSsrfPolicy.default(); _u0.denyPrivate = true; return _u0; })();
			engineConfig.ssrfDenyPrivateExplicit = true;
			engineConfig.ssrf.denyPrivate = false;
			const engine = createEngine(engineConfig);
			const url = "http://[::ffff:127.0.0.1]/";
			await scrape(engine, url);
		}).rejects.toSatisfy((error) => {
				const _message = error instanceof Error ? error.message : String(error);
				const _name = error instanceof Error ? error.name : "";
				return /ssrf_policy_violation/.test(_message) || /ssrf_policy_violation/.test(_name);
			});
	}, 30000);

	it("validation_ssrf_loopback_denied: scrape() rejects loopback addresses by default SSRF policy", async () => {
		await expect(async () => {
			const engineConfig = WasmCrawlConfig.default();
			engineConfig.ssrfDenyPrivateExplicit = true;
			engineConfig.ssrf.denyPrivate = false;
			const engine = createEngine(engineConfig);
			const url = "http://127.0.0.1:9/";
			await scrape(engine, url);
		}).rejects.toSatisfy((error) => {
				const _message = error instanceof Error ? error.message : String(error);
				const _name = error instanceof Error ? error.name : "";
				return /ssrf_policy_violation/.test(_message) || /ssrf_policy_violation/.test(_name);
			});
	}, 30000);

	it("validation_ssrf_nat64_loopback_denied: SSRF policy rejects an IPv4 loopback address embedded in the well-known NAT64 prefix", async () => {
		await expect(async () => {
			const engineConfig = WasmCrawlConfig.default();
			engineConfig.ssrf = (() => { const _u0 = WasmSsrfPolicy.default(); _u0.denyPrivate = true; return _u0; })();
			engineConfig.ssrfDenyPrivateExplicit = true;
			engineConfig.ssrf.denyPrivate = false;
			const engine = createEngine(engineConfig);
			const url = "http://[64:ff9b::7f00:1]/";
			await scrape(engine, url);
		}).rejects.toSatisfy((error) => {
				const _message = error instanceof Error ? error.message : String(error);
				const _name = error instanceof Error ? error.name : "";
				return /ssrf_policy_violation/.test(_message) || /ssrf_policy_violation/.test(_name);
			});
	}, 30000);

	it("validation_timeout_zero: Zero request timeout is rejected as invalid config", async () => {
		await expect(async () => {
			const engineConfig = WasmCrawlConfig.default();
			engineConfig.requestTimeout = 0;
			engineConfig.ssrf.denyPrivate = false;
			const engine = createEngine(engineConfig);
			const url = `${process.env.MOCK_SERVER_URL}/fixtures/validation_timeout_zero`;
			await scrape(engine, url);
		}).rejects.toSatisfy((error) => {
				const _message = error instanceof Error ? error.message : String(error);
				const _name = error instanceof Error ? error.name : "";
				return /request_timeout/.test(_message) || /request_timeout/.test(_name);
			});
	}, 30000);

});
