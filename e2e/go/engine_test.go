// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:2220aa3de4cfe9969e1232cb6a5c4bb651374a6e1831c78339905296e6eeab99
// To regenerate: alef generate
// To verify freshness: alef verify

// E2e tests for category: engine
package e2e_test

import (
	"encoding/json"
	"os"
	"strings"
	"testing"

	"github.com/stretchr/testify/assert"

	pkg "github.com/xberg-io/crawlberg/packages/go"
)

func Test_EngineBatchBasic(t *testing.T) {
	// CrawlEngine with defaults batch scrapes like the free function
	engine, createErr := pkg.CreateEngine(nil)
	if createErr != nil {
		t.Fatalf("create handle failed: %v", createErr)
	}
	urlsBase := os.Getenv("MOCK_SERVER_ENGINE_BATCH_BASIC")
	if urlsBase == "" {
		urlsBase = os.Getenv("MOCK_SERVER_URL") + "/fixtures/engine_batch_basic"
	}
	var urls []string
	for _, p := range []string{`/page1`, `/page2`} {
		if strings.HasPrefix(p, "http") {
			urls = append(urls, p)
		} else {
			urls = append(urls, urlsBase+p)
		}
	}
	result, err := pkg.BatchScrape(engine, urls)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if result.CompletedCount != 2 {
		t.Errorf("equals mismatch: got %v", result.CompletedCount)
	}
	if result.TotalCount != 2 {
		t.Errorf("equals mismatch: got %v", result.TotalCount)
	}
}

func Test_EngineCrawlBasic(t *testing.T) {
	// CrawlEngine with defaults crawls multiple pages like the free function
	var engineConfig pkg.CrawlConfig
	if err := json.Unmarshal([]byte(`{"max_depth":1}`), &engineConfig); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	engine, createErr := pkg.CreateEngine(&engineConfig)
	if createErr != nil {
		t.Fatalf("create handle failed: %v", createErr)
	}
	url := os.Getenv("MOCK_SERVER_ENGINE_CRAWL_BASIC")
	if url == "" {
		url = os.Getenv("MOCK_SERVER_URL") + "/fixtures/engine_crawl_basic"
	}
	result, err := pkg.Crawl(engine, url)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if len(result.Pages) != 3 {
		t.Errorf("equals mismatch: got %v", len(result.Pages))
	}
	if len(result.Pages) < 3 {
		t.Errorf("expected >= 3, got %v", len(result.Pages))
	}
}

func Test_EngineMapBasic(t *testing.T) {
	// CrawlEngine with defaults discovers URLs like the free function
	engine, createErr := pkg.CreateEngine(nil)
	if createErr != nil {
		t.Fatalf("create handle failed: %v", createErr)
	}
	url := os.Getenv("MOCK_SERVER_ENGINE_MAP_BASIC")
	if url == "" {
		url = os.Getenv("MOCK_SERVER_URL") + "/fixtures/engine_map_basic"
	}
	result, err := pkg.MapUrls(engine, url)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if len(result.Urls) < 2 {
		t.Errorf("expected >= 2, got %v", len(result.Urls))
	}
}

func Test_EngineScrapeBasic(t *testing.T) {
	// CrawlEngine with defaults scrapes a page identically to the free function
	engine, createErr := pkg.CreateEngine(nil)
	if createErr != nil {
		t.Fatalf("create handle failed: %v", createErr)
	}
	url := os.Getenv("MOCK_SERVER_ENGINE_SCRAPE_BASIC")
	if url == "" {
		url = os.Getenv("MOCK_SERVER_URL") + "/fixtures/engine_scrape_basic"
	}
	result, err := pkg.Scrape(engine, url)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	var metadataTitle string
	if result.Metadata.Title != nil {
		metadataTitle = string(*result.Metadata.Title)
	}
	var metadataDescription string
	if result.Metadata.Description != nil {
		metadataDescription = string(*result.Metadata.Description)
	}
	if result.StatusCode != 200 {
		t.Errorf("equals mismatch: got %v", result.StatusCode)
	}
	if string(result.ContentType) != `text/html` {
		t.Errorf("equals mismatch: got %v", result.ContentType)
	}
	if string(metadataTitle) != `Engine Test` {
		t.Errorf("equals mismatch: got %v", metadataTitle)
	}
	if !strings.Contains(string(metadataDescription), `Testing the engine`) {
		t.Errorf("expected to contain %s, got %v", `Testing the engine`, metadataDescription)
	}
	if len(result.Links) < 1 {
		t.Errorf("expected >= 1, got %v", len(result.Links))
	}
	if len(result.Metadata.Headings) < 1 {
		t.Errorf("expected >= 1, got %v", len(result.Metadata.Headings))
	}
}

func Test_EngineStreamBasic(t *testing.T) {
	// CrawlEngine with defaults streams events like the free function
	var engineConfig pkg.CrawlConfig
	if err := json.Unmarshal([]byte(`{"max_depth":1}`), &engineConfig); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	engine, createErr := pkg.CreateEngine(&engineConfig)
	if createErr != nil {
		t.Fatalf("create handle failed: %v", createErr)
	}
	url := os.Getenv("MOCK_SERVER_ENGINE_STREAM_BASIC")
	if url == "" {
		url = os.Getenv("MOCK_SERVER_URL") + "/fixtures/engine_stream_basic"
	}
	stream, err := pkg.CrawlStream(engine, url)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	var chunks []pkg.CrawlEvent
	for chunk := range stream {
		chunks = append(chunks, chunk)
	}
	assert.True(t, func() bool {
		for _, e := range chunks {
			if _, _ok := e.(pkg.CrawlEventPage); _ok {
				return true
			}
		}
		return false
	}(), "expected true")
	assert.True(t, func() bool {
		for _, e := range chunks {
			if _, _ok := e.(pkg.CrawlEventComplete); _ok {
				return true
			}
		}
		return false
	}(), "expected true")
	assert.GreaterOrEqual(t, len(chunks), 3, "expected >= 3")
}
