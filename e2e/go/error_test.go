// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:fbe00e7aad07f8306e573402c7590dd3bbe4838b98d6675d11dee1baf92d78d6
// To regenerate: alef generate
// To verify freshness: alef verify

// E2e tests for category: error
package e2e_test

import (
	"encoding/json"
	"fmt"
	"os"
	"strings"
	"testing"

	pkg "github.com/xberg-io/crawlberg/packages/go"
)

func Test_Error401Unauthorized(t *testing.T) {
	// Handles 401 Unauthorized response correctly
	engine, createErr := pkg.CreateEngine(nil)
	if createErr != nil {
		t.Fatalf("create handle failed: %v", createErr)
	}
	url := os.Getenv("MOCK_SERVER_URL") + "/fixtures/error_401_unauthorized"
	_, err := pkg.Scrape(engine, url)
	if err == nil {
		t.Errorf("expected an error, but call succeeded")
	}
	if err != nil {
		if !strings.Contains(err.Error(), `unauthorized`) && !strings.Contains(fmt.Sprintf("%T", err), `unauthorized`) {
			t.Errorf("expected error to match %s, got message=%q type=%T", `unauthorized`, err.Error(), err)
		}
	}
}

func Test_Error403Forbidden(t *testing.T) {
	// Handles 403 Forbidden response correctly
	var engineConfig pkg.CrawlConfig
	if err := json.Unmarshal([]byte(`{"browser":{"mode":"never"}}`), &engineConfig); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	engine, createErr := pkg.CreateEngine(&engineConfig)
	if createErr != nil {
		t.Fatalf("create handle failed: %v", createErr)
	}
	url := os.Getenv("MOCK_SERVER_URL") + "/fixtures/error_403_forbidden"
	_, err := pkg.Scrape(engine, url)
	if err == nil {
		t.Errorf("expected an error, but call succeeded")
	}
	if err != nil {
		if !strings.Contains(err.Error(), `forbidden`) && !strings.Contains(fmt.Sprintf("%T", err), `forbidden`) {
			t.Errorf("expected error to match %s, got message=%q type=%T", `forbidden`, err.Error(), err)
		}
	}
}

func Test_Error404Page(t *testing.T) {
	// Handles 404 response correctly
	engine, createErr := pkg.CreateEngine(nil)
	if createErr != nil {
		t.Fatalf("create handle failed: %v", createErr)
	}
	url := os.Getenv("MOCK_SERVER_URL") + "/fixtures/error_404_page"
	_, err := pkg.Scrape(engine, url)
	if err == nil {
		t.Errorf("expected an error, but call succeeded")
	}
	if err != nil {
		if !strings.Contains(err.Error(), `not_found`) && !strings.Contains(fmt.Sprintf("%T", err), `not_found`) {
			t.Errorf("expected error to match %s, got message=%q type=%T", `not_found`, err.Error(), err)
		}
	}
}

func Test_Error408RequestTimeout(t *testing.T) {
	// Handles 408 Request Timeout response correctly
	engine, createErr := pkg.CreateEngine(nil)
	if createErr != nil {
		t.Fatalf("create handle failed: %v", createErr)
	}
	url := os.Getenv("MOCK_SERVER_URL") + "/fixtures/error_408_request_timeout"
	_, err := pkg.Scrape(engine, url)
	if err == nil {
		t.Errorf("expected an error, but call succeeded")
	}
	if err != nil {
		if !strings.Contains(err.Error(), `timeout`) && !strings.Contains(fmt.Sprintf("%T", err), `timeout`) {
			t.Errorf("expected error to match %s, got message=%q type=%T", `timeout`, err.Error(), err)
		}
	}
}

func Test_Error410Gone(t *testing.T) {
	// Handles 410 Gone response correctly
	engine, createErr := pkg.CreateEngine(nil)
	if createErr != nil {
		t.Fatalf("create handle failed: %v", createErr)
	}
	url := os.Getenv("MOCK_SERVER_URL") + "/fixtures/error_410_gone"
	_, err := pkg.Scrape(engine, url)
	if err == nil {
		t.Errorf("expected an error, but call succeeded")
	}
	if err != nil {
		if !strings.Contains(err.Error(), `gone`) && !strings.Contains(fmt.Sprintf("%T", err), `gone`) {
			t.Errorf("expected error to match %s, got message=%q type=%T", `gone`, err.Error(), err)
		}
	}
}

func Test_Error500Server(t *testing.T) {
	// Handles 500 server error
	engine, createErr := pkg.CreateEngine(nil)
	if createErr != nil {
		t.Fatalf("create handle failed: %v", createErr)
	}
	url := os.Getenv("MOCK_SERVER_URL") + "/fixtures/error_500_server"
	_, err := pkg.Scrape(engine, url)
	if err == nil {
		t.Errorf("expected an error, but call succeeded")
	}
	if err != nil {
		if !strings.Contains(err.Error(), `server_error`) && !strings.Contains(fmt.Sprintf("%T", err), `server_error`) {
			t.Errorf("expected error to match %s, got message=%q type=%T", `server_error`, err.Error(), err)
		}
	}
}

func Test_Error502BadGateway(t *testing.T) {
	// Handles 502 Bad Gateway response correctly
	engine, createErr := pkg.CreateEngine(nil)
	if createErr != nil {
		t.Fatalf("create handle failed: %v", createErr)
	}
	url := os.Getenv("MOCK_SERVER_URL") + "/fixtures/error_502_bad_gateway"
	_, err := pkg.Scrape(engine, url)
	if err == nil {
		t.Errorf("expected an error, but call succeeded")
	}
	if err != nil {
		if !strings.Contains(err.Error(), `bad_gateway`) && !strings.Contains(fmt.Sprintf("%T", err), `bad_gateway`) {
			t.Errorf("expected error to match %s, got message=%q type=%T", `bad_gateway`, err.Error(), err)
		}
	}
}

func Test_ErrorBrowserLaunchFailure(t *testing.T) {
	// Browser launch fails when browser mode is always but browser is unavailable
	var engineConfig pkg.CrawlConfig
	if err := json.Unmarshal([]byte(`{"browser":{"mode":"always","timeout":1}}`), &engineConfig); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	engine, createErr := pkg.CreateEngine(&engineConfig)
	if createErr != nil {
		t.Fatalf("create handle failed: %v", createErr)
	}
	url := os.Getenv("MOCK_SERVER_URL") + "/fixtures/error_browser_launch_failure"
	_, err := pkg.Scrape(engine, url)
	if err == nil {
		t.Errorf("expected an error, but call succeeded")
	}
	if err != nil {
		if !strings.Contains(err.Error(), `browser`) && !strings.Contains(fmt.Sprintf("%T", err), `browser`) {
			t.Errorf("expected error to match %s, got message=%q type=%T", `browser`, err.Error(), err)
		}
	}
}

func Test_ErrorBrowserPageTimeout(t *testing.T) {
	// Browser page load times out
	var engineConfig pkg.CrawlConfig
	if err := json.Unmarshal([]byte(`{"browser":{"mode":"always","timeout":1}}`), &engineConfig); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	engine, createErr := pkg.CreateEngine(&engineConfig)
	if createErr != nil {
		t.Fatalf("create handle failed: %v", createErr)
	}
	url := os.Getenv("MOCK_SERVER_URL") + "/fixtures/error_browser_page_timeout"
	_, err := pkg.Scrape(engine, url)
	if err == nil {
		t.Errorf("expected an error, but call succeeded")
	}
	if err != nil {
		if !strings.Contains(err.Error(), `browser_timeout`) && !strings.Contains(fmt.Sprintf("%T", err), `browser_timeout`) {
			t.Errorf("expected error to match %s, got message=%q type=%T", `browser_timeout`, err.Error(), err)
		}
	}
}

func Test_ErrorConnectionRefused(t *testing.T) {
	// Handles connection refused error gracefully
	engine, createErr := pkg.CreateEngine(nil)
	if createErr != nil {
		t.Fatalf("create handle failed: %v", createErr)
	}
	url := os.Getenv("MOCK_SERVER_URL") + "/fixtures/error_connection_refused"
	_, err := pkg.Scrape(engine, url)
	if err == nil {
		t.Errorf("expected an error, but call succeeded")
	}
	if err != nil {
		if !strings.Contains(err.Error(), `connection`) && !strings.Contains(fmt.Sprintf("%T", err), `connection`) {
			t.Errorf("expected error to match %s, got message=%q type=%T", `connection`, err.Error(), err)
		}
	}
}

func Test_ErrorDataLossTruncated(t *testing.T) {
	// Content-Length mismatch causes data loss error
	engine, createErr := pkg.CreateEngine(nil)
	if createErr != nil {
		t.Fatalf("create handle failed: %v", createErr)
	}
	url := os.Getenv("MOCK_SERVER_URL") + "/fixtures/error_data_loss_truncated"
	_, err := pkg.Scrape(engine, url)
	if err == nil {
		t.Errorf("expected an error, but call succeeded")
	}
	if err != nil {
		if !strings.Contains(err.Error(), `data_loss:`) && !strings.Contains(fmt.Sprintf("%T", err), `data_loss:`) {
			t.Errorf("expected error to match %s, got message=%q type=%T", `data_loss:`, err.Error(), err)
		}
	}
}

func Test_ErrorDnsResolution(t *testing.T) {
	// Handles DNS resolution failure gracefully
	engine, createErr := pkg.CreateEngine(nil)
	if createErr != nil {
		t.Fatalf("create handle failed: %v", createErr)
	}
	url := os.Getenv("MOCK_SERVER_URL") + "/fixtures/error_dns_resolution"
	_, err := pkg.Scrape(engine, url)
	if err == nil {
		t.Errorf("expected an error, but call succeeded")
	}
	if err != nil {
		if !strings.Contains(err.Error(), `dns`) && !strings.Contains(fmt.Sprintf("%T", err), `dns`) {
			t.Errorf("expected error to match %s, got message=%q type=%T", `dns`, err.Error(), err)
		}
	}
}

func Test_ErrorEmptyBatchUrls(t *testing.T) {
	// Scraping a URL that cannot be found returns an error containing the URL path
	engine, createErr := pkg.CreateEngine(nil)
	if createErr != nil {
		t.Fatalf("create handle failed: %v", createErr)
	}
	url := os.Getenv("MOCK_SERVER_URL") + "/fixtures/error_empty_batch_urls"
	_, err := pkg.Scrape(engine, url)
	if err == nil {
		t.Errorf("expected an error, but call succeeded")
	}
	if err != nil {
		if !strings.Contains(err.Error(), `not_found`) && !strings.Contains(fmt.Sprintf("%T", err), `not_found`) {
			t.Errorf("expected error to match %s, got message=%q type=%T", `not_found`, err.Error(), err)
		}
	}
}

func Test_ErrorEmptyResponse(t *testing.T) {
	// Handles 200 with completely empty body gracefully
	engine, createErr := pkg.CreateEngine(nil)
	if createErr != nil {
		t.Fatalf("create handle failed: %v", createErr)
	}
	url := os.Getenv("MOCK_SERVER_URL") + "/fixtures/error_empty_response"
	result, err := pkg.Scrape(engine, url)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if len(result.HTML) != 0 {
		t.Errorf("expected empty value, got %v", result.HTML)
	}
}

func Test_ErrorInvalidProxy(t *testing.T) {
	// Proxy pointing to unreachable address causes connection error during scrape
	var engineConfig pkg.CrawlConfig
	if err := json.Unmarshal([]byte(`{"proxy":{"url":"http://127.0.0.1:1"}}`), &engineConfig); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	engine, createErr := pkg.CreateEngine(&engineConfig)
	if createErr != nil {
		t.Fatalf("create handle failed: %v", createErr)
	}
	url := os.Getenv("MOCK_SERVER_URL") + "/fixtures/error_invalid_proxy"
	_, err := pkg.Scrape(engine, url)
	if err == nil {
		t.Errorf("expected an error, but call succeeded")
	}
	if err != nil {
		if !strings.Contains(err.Error(), `connection`) && !strings.Contains(fmt.Sprintf("%T", err), `connection`) {
			t.Errorf("expected error to match %s, got message=%q type=%T", `connection`, err.Error(), err)
		}
	}
}

func Test_ErrorPartialResponse(t *testing.T) {
	// Handles incomplete or truncated HTTP response
	engine, createErr := pkg.CreateEngine(nil)
	if createErr != nil {
		t.Fatalf("create handle failed: %v", createErr)
	}
	url := os.Getenv("MOCK_SERVER_URL") + "/fixtures/error_partial_response"
	_, err := pkg.Scrape(engine, url)
	if err == nil {
		t.Errorf("expected an error, but call succeeded")
	}
	if err != nil {
		if !strings.Contains(err.Error(), `data_loss`) && !strings.Contains(fmt.Sprintf("%T", err), `data_loss`) {
			t.Errorf("expected error to match %s, got message=%q type=%T", `data_loss`, err.Error(), err)
		}
	}
}

func Test_ErrorRateLimited(t *testing.T) {
	// Handles 429 rate limiting with Retry-After
	engine, createErr := pkg.CreateEngine(nil)
	if createErr != nil {
		t.Fatalf("create handle failed: %v", createErr)
	}
	url := os.Getenv("MOCK_SERVER_URL") + "/fixtures/error_rate_limited"
	_, err := pkg.Scrape(engine, url)
	if err == nil {
		t.Errorf("expected an error, but call succeeded")
	}
	if err != nil {
		if !strings.Contains(err.Error(), `rate_limited`) && !strings.Contains(fmt.Sprintf("%T", err), `rate_limited`) {
			t.Errorf("expected error to match %s, got message=%q type=%T", `rate_limited`, err.Error(), err)
		}
	}
}

func Test_ErrorRetry503(t *testing.T) {
	// Retries request on 503 Service Unavailable response
	engine, createErr := pkg.CreateEngine(nil)
	if createErr != nil {
		t.Fatalf("create handle failed: %v", createErr)
	}
	url := os.Getenv("MOCK_SERVER_URL") + "/fixtures/error_retry_503"
	_, err := pkg.Scrape(engine, url)
	if err == nil {
		t.Errorf("expected an error, but call succeeded")
	}
	if err != nil {
		if !strings.Contains(err.Error(), `server_error`) && !strings.Contains(fmt.Sprintf("%T", err), `server_error`) {
			t.Errorf("expected error to match %s, got message=%q type=%T", `server_error`, err.Error(), err)
		}
	}
}

func Test_ErrorRetryBackoff(t *testing.T) {
	// Implements exponential backoff when retrying failed requests
	engine, createErr := pkg.CreateEngine(nil)
	if createErr != nil {
		t.Fatalf("create handle failed: %v", createErr)
	}
	url := os.Getenv("MOCK_SERVER_URL") + "/fixtures/error_retry_backoff"
	_, err := pkg.Scrape(engine, url)
	if err == nil {
		t.Errorf("expected an error, but call succeeded")
	}
	if err != nil {
		if !strings.Contains(err.Error(), `rate_limited`) && !strings.Contains(fmt.Sprintf("%T", err), `rate_limited`) {
			t.Errorf("expected error to match %s, got message=%q type=%T", `rate_limited`, err.Error(), err)
		}
	}
}

func Test_ErrorSslInvalidCert(t *testing.T) {
	// Handles SSL certificate validation error
	engine, createErr := pkg.CreateEngine(nil)
	if createErr != nil {
		t.Fatalf("create handle failed: %v", createErr)
	}
	url := os.Getenv("MOCK_SERVER_URL") + "/fixtures/error_ssl_invalid_cert"
	_, err := pkg.Scrape(engine, url)
	if err == nil {
		t.Errorf("expected an error, but call succeeded")
	}
	if err != nil {
		if !strings.Contains(err.Error(), `ssl`) && !strings.Contains(fmt.Sprintf("%T", err), `ssl`) {
			t.Errorf("expected error to match %s, got message=%q type=%T", `ssl`, err.Error(), err)
		}
	}
}

func Test_ErrorTimeout(t *testing.T) {
	// Mock server delays response longer than request_timeout, surfacing a timeout error
	var engineConfig pkg.CrawlConfig
	if err := json.Unmarshal([]byte(`{"request_timeout":500}`), &engineConfig); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	engine, createErr := pkg.CreateEngine(&engineConfig)
	if createErr != nil {
		t.Fatalf("create handle failed: %v", createErr)
	}
	url := os.Getenv("MOCK_SERVER_URL") + "/fixtures/error_timeout"
	_, err := pkg.Scrape(engine, url)
	if err == nil {
		t.Errorf("expected an error, but call succeeded")
	}
	if err != nil {
		if !strings.Contains(err.Error(), `timeout`) && !strings.Contains(fmt.Sprintf("%T", err), `timeout`) {
			t.Errorf("expected error to match %s, got message=%q type=%T", `timeout`, err.Error(), err)
		}
	}
}

func Test_ErrorUnsupportedScheme(t *testing.T) {
	// Unsupported URL scheme (gopher) is rejected
	engine, createErr := pkg.CreateEngine(nil)
	if createErr != nil {
		t.Fatalf("create handle failed: %v", createErr)
	}
	url := `gopher://invalid.example.com/`
	_, err := pkg.Scrape(engine, url)
	if err == nil {
		t.Errorf("expected an error, but call succeeded")
	}
	if err != nil {
		if !strings.Contains(err.Error(), `disallowed scheme: gopher`) && !strings.Contains(fmt.Sprintf("%T", err), `disallowed scheme: gopher`) {
			t.Errorf("expected error to match %s, got message=%q type=%T", `disallowed scheme: gopher`, err.Error(), err)
		}
	}
}

func Test_ErrorWafAkamai(t *testing.T) {
	// Akamai WAF detection returns WafBlocked error
	var engineConfig pkg.CrawlConfig
	if err := json.Unmarshal([]byte(`{"browser":{"mode":"never"}}`), &engineConfig); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	engine, createErr := pkg.CreateEngine(&engineConfig)
	if createErr != nil {
		t.Fatalf("create handle failed: %v", createErr)
	}
	url := os.Getenv("MOCK_SERVER_URL") + "/fixtures/error_waf_akamai"
	_, err := pkg.Scrape(engine, url)
	if err == nil {
		t.Errorf("expected an error, but call succeeded")
	}
}

func Test_ErrorWafBlocked(t *testing.T) {
	// WAF challenge/block detection returns WafBlocked error
	var engineConfig pkg.CrawlConfig
	if err := json.Unmarshal([]byte(`{"browser":{"mode":"never"}}`), &engineConfig); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	engine, createErr := pkg.CreateEngine(&engineConfig)
	if createErr != nil {
		t.Fatalf("create handle failed: %v", createErr)
	}
	url := os.Getenv("MOCK_SERVER_URL") + "/fixtures/error_waf_blocked"
	_, err := pkg.Scrape(engine, url)
	if err == nil {
		t.Errorf("expected an error, but call succeeded")
	}
	if err != nil {
		if !strings.Contains(err.Error(), `waf`) && !strings.Contains(fmt.Sprintf("%T", err), `waf`) {
			t.Errorf("expected error to match %s, got message=%q type=%T", `waf`, err.Error(), err)
		}
	}
}

func Test_ErrorWafFalse403(t *testing.T) {
	// Detects WAF/bot protection false 403 (Cloudflare challenge page)
	var engineConfig pkg.CrawlConfig
	if err := json.Unmarshal([]byte(`{"browser":{"mode":"never"}}`), &engineConfig); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	engine, createErr := pkg.CreateEngine(&engineConfig)
	if createErr != nil {
		t.Fatalf("create handle failed: %v", createErr)
	}
	url := os.Getenv("MOCK_SERVER_URL") + "/fixtures/error_waf_false_403"
	_, err := pkg.Scrape(engine, url)
	if err == nil {
		t.Errorf("expected an error, but call succeeded")
	}
	if err != nil {
		if !strings.Contains(err.Error(), `waf`) && !strings.Contains(fmt.Sprintf("%T", err), `waf`) {
			t.Errorf("expected error to match %s, got message=%q type=%T", `waf`, err.Error(), err)
		}
	}
}

func Test_ErrorWafImperva(t *testing.T) {
	// Imperva/Incapsula WAF detection
	var engineConfig pkg.CrawlConfig
	if err := json.Unmarshal([]byte(`{"browser":{"mode":"never"}}`), &engineConfig); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	engine, createErr := pkg.CreateEngine(&engineConfig)
	if createErr != nil {
		t.Fatalf("create handle failed: %v", createErr)
	}
	url := os.Getenv("MOCK_SERVER_URL") + "/fixtures/error_waf_imperva"
	_, err := pkg.Scrape(engine, url)
	if err == nil {
		t.Errorf("expected an error, but call succeeded")
	}
}
