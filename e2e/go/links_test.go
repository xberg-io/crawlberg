// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:bbabd2e6996d3af5d0b7f4e42c30a8e25875199159e0016a7379577dfddad9ad
// To regenerate: alef generate
// To verify freshness: alef verify

// E2e tests for category: links
package e2e_test

import (
	"fmt"
	"os"
	"strings"
	"testing"

	pkg "github.com/xberg-io/crawlberg/packages/go"
)

func Test_LinksAnchorFragment(t *testing.T) {
	// Identifies fragment-only links as anchor type
	engine, createErr := pkg.CreateEngine(nil)
	if createErr != nil {
		t.Fatalf("create handle failed: %v", createErr)
	}
	url := os.Getenv("MOCK_SERVER_LINKS_ANCHOR_FRAGMENT")
	if url == "" {
		url = os.Getenv("MOCK_SERVER_URL") + "/fixtures/links_anchor_fragment"
	}
	result, err := pkg.Scrape(engine, url)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	found6e73351bv0 := false
	for _, e := range result.Links {
		if strings.Contains(fmt.Sprintf("%v", e.LinkType), `anchor`) {
			found6e73351bv0 = true
			break
		}
	}
	if !found6e73351bv0 {
		t.Errorf("expected some element of 'links[].link_type' to contain %v", `anchor`)
	}
}

func Test_LinksBaseTag(t *testing.T) {
	// Resolves relative URLs using base tag href
	engine, createErr := pkg.CreateEngine(nil)
	if createErr != nil {
		t.Fatalf("create handle failed: %v", createErr)
	}
	url := os.Getenv("MOCK_SERVER_URL") + "/fixtures/links_base_tag"
	result, err := pkg.Scrape(engine, url)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if len(result.Links) < 3 {
		t.Errorf("expected > 2, got %v", len(result.Links))
	}
	foundfba34128 := false
	for _, e := range result.Links {
		if strings.Contains(fmt.Sprintf("%v", e.URL), `example.com`) {
			foundfba34128 = true
			break
		}
	}
	if !foundfba34128 {
		t.Errorf("expected some element of 'links[].url' to contain %v", `example.com`)
	}
}

func Test_LinksDocumentTypes(t *testing.T) {
	// Detects PDF, DOCX, XLSX links as document type
	engine, createErr := pkg.CreateEngine(nil)
	if createErr != nil {
		t.Fatalf("create handle failed: %v", createErr)
	}
	url := os.Getenv("MOCK_SERVER_LINKS_DOCUMENT_TYPES")
	if url == "" {
		url = os.Getenv("MOCK_SERVER_URL") + "/fixtures/links_document_types"
	}
	result, err := pkg.Scrape(engine, url)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	found885b10f8v0 := false
	for _, e := range result.Links {
		if strings.Contains(fmt.Sprintf("%v", e.LinkType), `document`) {
			found885b10f8v0 = true
			break
		}
	}
	if !found885b10f8v0 {
		t.Errorf("expected some element of 'links[].link_type' to contain %v", `document`)
	}
}

func Test_LinksEmptyHref(t *testing.T) {
	// Handles empty href attributes without errors
	engine, createErr := pkg.CreateEngine(nil)
	if createErr != nil {
		t.Fatalf("create handle failed: %v", createErr)
	}
	url := os.Getenv("MOCK_SERVER_URL") + "/fixtures/links_empty_href"
	result, err := pkg.Scrape(engine, url)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if len(result.Links) < 1 {
		t.Errorf("expected > 0, got %v", len(result.Links))
	}
	foundf1128723 := false
	for _, e := range result.Links {
		if strings.Contains(fmt.Sprintf("%v", e.URL), `/valid`) {
			foundf1128723 = true
			break
		}
	}
	if !foundf1128723 {
		t.Errorf("expected some element of 'links[].url' to contain %v", `/valid`)
	}
}

func Test_LinksInternalExternalClassification(t *testing.T) {
	// Correctly classifies internal vs external links by domain
	engine, createErr := pkg.CreateEngine(nil)
	if createErr != nil {
		t.Fatalf("create handle failed: %v", createErr)
	}
	url := os.Getenv("MOCK_SERVER_URL") + "/fixtures/links_internal_external_classification"
	result, err := pkg.Scrape(engine, url)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if len(result.Links) < 5 {
		t.Errorf("expected > 4, got %v", len(result.Links))
	}
	foundf4499e97 := false
	for _, e := range result.Links {
		if fmt.Sprintf("%v", e.URL) != "" {
			foundf4499e97 = true
			break
		}
	}
	if !foundf4499e97 {
		t.Errorf("expected some element of 'links[].url' to be non-empty")
	}
}

func Test_LinksMailtoJavascriptSkip(t *testing.T) {
	// Skips mailto:, javascript:, and tel: scheme links
	engine, createErr := pkg.CreateEngine(nil)
	if createErr != nil {
		t.Fatalf("create handle failed: %v", createErr)
	}
	url := os.Getenv("MOCK_SERVER_URL") + "/fixtures/links_mailto_javascript_skip"
	result, err := pkg.Scrape(engine, url)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if len(result.Links) < 1 {
		t.Errorf("expected > 0, got %v", len(result.Links))
	}
	founda4019502 := false
	for _, e := range result.Links {
		if strings.Contains(fmt.Sprintf("%v", e.URL), `mailto:`) {
			founda4019502 = true
			break
		}
	}
	if founda4019502 {
		t.Errorf("expected no element of 'links[].url' to contain %v", `mailto:`)
	}
}

func Test_LinksProtocolRelative(t *testing.T) {
	// Handles protocol-relative URLs (//example.com) correctly
	engine, createErr := pkg.CreateEngine(nil)
	if createErr != nil {
		t.Fatalf("create handle failed: %v", createErr)
	}
	url := os.Getenv("MOCK_SERVER_URL") + "/fixtures/links_protocol_relative"
	result, err := pkg.Scrape(engine, url)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if len(result.Links) < 2 {
		t.Errorf("expected > 1, got %v", len(result.Links))
	}
	founde77bf8bc := false
	for _, e := range result.Links {
		if strings.Contains(fmt.Sprintf("%v", e.URL), `http://cdn.example.com/resource`) {
			founde77bf8bc = true
			break
		}
	}
	if !founde77bf8bc {
		t.Errorf("expected some element of 'links[].url' to contain %v", `http://cdn.example.com/resource`)
	}
	found971e31bc := false
	for _, e := range result.Links {
		if strings.Contains(fmt.Sprintf("%v", e.URL), `http://images.example.com/photo.jpg`) {
			found971e31bc = true
			break
		}
	}
	if !found971e31bc {
		t.Errorf("expected some element of 'links[].url' to contain %v", `http://images.example.com/photo.jpg`)
	}
}

func Test_LinksRelAttributes(t *testing.T) {
	// Preserves rel=nofollow and rel=canonical attributes
	engine, createErr := pkg.CreateEngine(nil)
	if createErr != nil {
		t.Fatalf("create handle failed: %v", createErr)
	}
	url := os.Getenv("MOCK_SERVER_URL") + "/fixtures/links_rel_attributes"
	result, err := pkg.Scrape(engine, url)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if len(result.Links) < 1 {
		t.Errorf("expected > 0, got %v", len(result.Links))
	}
}

func Test_LinksRelativeParent(t *testing.T) {
	// Resolves ../ and ./ relative parent path links correctly
	engine, createErr := pkg.CreateEngine(nil)
	if createErr != nil {
		t.Fatalf("create handle failed: %v", createErr)
	}
	url := os.Getenv("MOCK_SERVER_URL") + "/fixtures/links_relative_parent"
	result, err := pkg.Scrape(engine, url)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if len(result.Links) < 4 {
		t.Errorf("expected > 3, got %v", len(result.Links))
	}
}
