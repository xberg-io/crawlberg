// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:e83f983f3bb23e2de034ef6a561fd87ac269dad04380cde3826531236304f0b3
// To regenerate: alef generate
// To verify freshness: alef verify

// E2e tests for category: filter
package e2e_test

import (
	"encoding/json"
	"os"
	"strings"
	"testing"

	pkg "github.com/xberg-io/crawlberg/packages/go"
)

func Test_FilterBm25CrawlIntegration(t *testing.T) {
	// BM25 filter works during multi-page crawl, keeping relevant pages
	var engineConfig pkg.CrawlConfig
	if err := json.Unmarshal([]byte(`{"bm25_query":"rust programming","bm25_threshold":0.1,"content_filter":"bm25","max_concurrent":1,"max_depth":1}`), &engineConfig); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	engine, createErr := pkg.CreateEngine(&engineConfig)
	if createErr != nil {
		t.Fatalf("create handle failed: %v", createErr)
	}
	url := os.Getenv("MOCK_SERVER_FILTER_BM25_CRAWL_INTEGRATION")
	if url == "" {
		url = os.Getenv("MOCK_SERVER_URL") + "/fixtures/filter_bm25_crawl_integration"
	}
	result, err := pkg.Crawl(engine, url)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if len(result.Pages) == 0 {
		t.Fatalf("expected non-empty %s", `result.Pages`)
	}
	if !strings.Contains(string(result.Pages[0].HTML), `rust`) {
		t.Errorf("expected to contain %s, got %v", `rust`, result.Pages[0].HTML)
	}
}

func Test_FilterBm25EmptyQuery(t *testing.T) {
	// BM25 filter with empty query passes all pages through
	var engineConfig pkg.CrawlConfig
	if err := json.Unmarshal([]byte(`{"bm25_query":"","bm25_threshold":0.1,"content_filter":"bm25","max_depth":1}`), &engineConfig); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	engine, createErr := pkg.CreateEngine(&engineConfig)
	if createErr != nil {
		t.Fatalf("create handle failed: %v", createErr)
	}
	url := os.Getenv("MOCK_SERVER_FILTER_BM25_EMPTY_QUERY")
	if url == "" {
		url = os.Getenv("MOCK_SERVER_URL") + "/fixtures/filter_bm25_empty_query"
	}
	result, err := pkg.Crawl(engine, url)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if len(result.Pages) != 2 {
		t.Errorf("equals mismatch: got %v", len(result.Pages))
	}
}

func Test_FilterBm25HighThreshold(t *testing.T) {
	// BM25 filter with very high threshold filters out all pages
	var engineConfig pkg.CrawlConfig
	if err := json.Unmarshal([]byte(`{"bm25_query":"quantum physics","bm25_threshold":100.0,"content_filter":"bm25","max_depth":1}`), &engineConfig); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	engine, createErr := pkg.CreateEngine(&engineConfig)
	if createErr != nil {
		t.Fatalf("create handle failed: %v", createErr)
	}
	url := os.Getenv("MOCK_SERVER_FILTER_BM25_HIGH_THRESHOLD")
	if url == "" {
		url = os.Getenv("MOCK_SERVER_URL") + "/fixtures/filter_bm25_high_threshold"
	}
	result, err := pkg.Crawl(engine, url)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if len(result.Pages) != 0 {
		t.Errorf("equals mismatch: got %v", len(result.Pages))
	}
}

func Test_FilterBm25RelevantPages(t *testing.T) {
	// BM25 filter keeps only pages relevant to the query
	var engineConfig pkg.CrawlConfig
	if err := json.Unmarshal([]byte(`{"bm25_query":"rust programming language","bm25_threshold":0.1,"content_filter":"bm25","max_depth":1}`), &engineConfig); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	engine, createErr := pkg.CreateEngine(&engineConfig)
	if createErr != nil {
		t.Fatalf("create handle failed: %v", createErr)
	}
	url := os.Getenv("MOCK_SERVER_FILTER_BM25_RELEVANT_PAGES")
	if url == "" {
		url = os.Getenv("MOCK_SERVER_URL") + "/fixtures/filter_bm25_relevant_pages"
	}
	result, err := pkg.Crawl(engine, url)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if !strings.Contains(string(result.Pages[0].HTML), `rust`) {
		t.Errorf("expected to contain %s, got %v", `rust`, result.Pages[0].HTML)
	}
}

func Test_FilterBm25ThresholdZero(t *testing.T) {
	// BM25 filter with zero threshold passes all pages
	var engineConfig pkg.CrawlConfig
	if err := json.Unmarshal([]byte(`{"bm25_query":"unrelated query","bm25_threshold":0.0,"content_filter":"bm25","max_depth":1}`), &engineConfig); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	engine, createErr := pkg.CreateEngine(&engineConfig)
	if createErr != nil {
		t.Fatalf("create handle failed: %v", createErr)
	}
	url := os.Getenv("MOCK_SERVER_FILTER_BM25_THRESHOLD_ZERO")
	if url == "" {
		url = os.Getenv("MOCK_SERVER_URL") + "/fixtures/filter_bm25_threshold_zero"
	}
	result, err := pkg.Crawl(engine, url)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if len(result.Pages) != 2 {
		t.Errorf("equals mismatch: got %v", len(result.Pages))
	}
}

func Test_FilterNoopCrawlAllKept(t *testing.T) {
	// NoopFilter keeps all pages during a multi-page crawl
	var engineConfig pkg.CrawlConfig
	if err := json.Unmarshal([]byte(`{"max_concurrent":1,"max_depth":1}`), &engineConfig); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	engine, createErr := pkg.CreateEngine(&engineConfig)
	if createErr != nil {
		t.Fatalf("create handle failed: %v", createErr)
	}
	url := os.Getenv("MOCK_SERVER_FILTER_NOOP_CRAWL_ALL_KEPT")
	if url == "" {
		url = os.Getenv("MOCK_SERVER_URL") + "/fixtures/filter_noop_crawl_all_kept"
	}
	result, err := pkg.Crawl(engine, url)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if len(result.Pages) != 3 {
		t.Errorf("equals mismatch: got %v", len(result.Pages))
	}
}

func Test_FilterNoopPassesAll(t *testing.T) {
	// No content filter passes all crawled pages through
	var engineConfig pkg.CrawlConfig
	if err := json.Unmarshal([]byte(`{"max_depth":1}`), &engineConfig); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	engine, createErr := pkg.CreateEngine(&engineConfig)
	if createErr != nil {
		t.Fatalf("create handle failed: %v", createErr)
	}
	url := os.Getenv("MOCK_SERVER_FILTER_NOOP_PASSES_ALL")
	if url == "" {
		url = os.Getenv("MOCK_SERVER_URL") + "/fixtures/filter_noop_passes_all"
	}
	result, err := pkg.Crawl(engine, url)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if len(result.Pages) != 3 {
		t.Errorf("equals mismatch: got %v", len(result.Pages))
	}
}
