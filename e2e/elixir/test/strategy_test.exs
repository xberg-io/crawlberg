# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:d329c0de8b45584fd5528630a9cc7b4ffaeb65c9df3960c6008a286d58b0bea3
# To regenerate: alef generate
# To verify freshness: alef verify
# E2e tests for category: strategy
defmodule E2e.StrategyTest do
  use ExUnit.Case, async: false

  describe "strategy_adaptive_saturation" do
    test "strategy_adaptive_saturation" do
      engine_config = "{\"crawl_strategy\":\"best_first\",\"max_concurrent\":1,\"max_depth\":2,\"respect_robots_txt\":false}"
      {:ok, engine} = Crawlberg.create_engine(engine_config)

      url =
        System.get_env("MOCK_SERVER_STRATEGY_ADAPTIVE_SATURATION") ||
          (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/strategy_adaptive_saturation"

      {:ok, result} = Crawlberg.crawl(engine, url)
      assert length(result.pages) >= 1
    end
  end

  describe "strategy_adaptive_window" do
    test "strategy_adaptive_window" do
      engine_config = "{\"crawl_strategy\":\"best_first\",\"max_concurrent\":1,\"max_depth\":1,\"respect_robots_txt\":false}"
      {:ok, engine} = Crawlberg.create_engine(engine_config)

      url =
        System.get_env("MOCK_SERVER_STRATEGY_ADAPTIVE_WINDOW") ||
          (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/strategy_adaptive_window"

      {:ok, result} = Crawlberg.crawl(engine, url)
      assert length(result.pages) >= 3
    end
  end

  describe "strategy_best_first_seed" do
    test "strategy_best_first_seed" do
      engine_config = "{\"crawl_strategy\":\"best_first\",\"max_concurrent\":1,\"max_depth\":1}"
      {:ok, engine} = Crawlberg.create_engine(engine_config)

      url =
        System.get_env("MOCK_SERVER_STRATEGY_BEST_FIRST_SEED") ||
          (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/strategy_best_first_seed"

      {:ok, result} = Crawlberg.crawl(engine, url)
      assert length(result.pages) == 3
      assert String.contains?(to_string(Enum.at(result.pages, 0).url), "/")
    end
  end

  describe "strategy_bfs_default_order" do
    test "strategy_bfs_default_order" do
      engine_config = "{\"crawl_strategy\":\"bfs\",\"max_concurrent\":1,\"max_depth\":2}"
      {:ok, engine} = Crawlberg.create_engine(engine_config)

      url =
        System.get_env("MOCK_SERVER_STRATEGY_BFS_DEFAULT_ORDER") ||
          (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/strategy_bfs_default_order"

      {:ok, result} = Crawlberg.crawl(engine, url)
      assert length(result.pages) == 5
      assert String.contains?(to_string(Enum.at(result.pages, 1).url), "alpha")
      assert String.contains?(to_string(Enum.at(result.pages, 2).url), "beta")
      assert String.contains?(to_string(Enum.at(result.pages, 3).url), "one")
      assert String.contains?(to_string(Enum.at(result.pages, 4).url), "two")
    end
  end

  describe "strategy_dfs_depth_first" do
    test "strategy_dfs_depth_first" do
      engine_config = "{\"crawl_strategy\":\"dfs\",\"max_concurrent\":1,\"max_depth\":2}"
      {:ok, engine} = Crawlberg.create_engine(engine_config)

      url =
        System.get_env("MOCK_SERVER_STRATEGY_DFS_DEPTH_FIRST") ||
          (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/strategy_dfs_depth_first"

      {:ok, result} = Crawlberg.crawl(engine, url)
      assert length(result.pages) == 5
      assert String.contains?(to_string(Enum.at(result.pages, 1).url), "beta")
      assert String.contains?(to_string(Enum.at(result.pages, 2).url), "two")
      assert String.contains?(to_string(Enum.at(result.pages, 3).url), "alpha")
      assert String.contains?(to_string(Enum.at(result.pages, 4).url), "one")
    end
  end
end
