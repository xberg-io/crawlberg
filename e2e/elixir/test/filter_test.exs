# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:d01d3d587532aeabdcce1e4c5dec64e918b41819a3bcf5a87db3021aea6aab7f
# To regenerate: alef generate
# To verify freshness: alef verify
# E2e tests for category: filter
defmodule E2e.FilterTest do
  use ExUnit.Case, async: false

  describe "filter_bm25_crawl_integration" do
    test "filter_bm25_crawl_integration" do
      engine_config =
        "{\"bm25_query\":\"rust programming\",\"bm25_threshold\":0.1,\"content_filter\":\"bm25\",\"max_concurrent\":1,\"max_depth\":1}"

      {:ok, engine} = Crawlberg.create_engine(engine_config)

      url =
        System.get_env("MOCK_SERVER_FILTER_BM25_CRAWL_INTEGRATION") ||
          (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/filter_bm25_crawl_integration"

      {:ok, result} = Crawlberg.crawl(engine, url)
      assert String.contains?(to_string(Enum.at(result.pages, 0).html), "rust")
    end
  end

  describe "filter_bm25_empty_query" do
    test "filter_bm25_empty_query" do
      engine_config = "{\"bm25_query\":\"\",\"bm25_threshold\":0.1,\"content_filter\":\"bm25\",\"max_depth\":1}"
      {:ok, engine} = Crawlberg.create_engine(engine_config)

      url =
        System.get_env("MOCK_SERVER_FILTER_BM25_EMPTY_QUERY") ||
          (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/filter_bm25_empty_query"

      {:ok, result} = Crawlberg.crawl(engine, url)
      assert length(result.pages) == 2
    end
  end

  describe "filter_bm25_high_threshold" do
    test "filter_bm25_high_threshold" do
      engine_config = "{\"bm25_query\":\"quantum physics\",\"bm25_threshold\":100.0,\"content_filter\":\"bm25\",\"max_depth\":1}"
      {:ok, engine} = Crawlberg.create_engine(engine_config)

      url =
        System.get_env("MOCK_SERVER_FILTER_BM25_HIGH_THRESHOLD") ||
          (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/filter_bm25_high_threshold"

      {:ok, result} = Crawlberg.crawl(engine, url)
      assert length(result.pages) == 0
    end
  end

  describe "filter_bm25_relevant_pages" do
    test "filter_bm25_relevant_pages" do
      engine_config = "{\"bm25_query\":\"rust programming language\",\"bm25_threshold\":0.1,\"content_filter\":\"bm25\",\"max_depth\":1}"
      {:ok, engine} = Crawlberg.create_engine(engine_config)

      url =
        System.get_env("MOCK_SERVER_FILTER_BM25_RELEVANT_PAGES") ||
          (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/filter_bm25_relevant_pages"

      {:ok, result} = Crawlberg.crawl(engine, url)
      assert String.contains?(to_string(Enum.at(result.pages, 0).html), "rust")
    end
  end

  describe "filter_bm25_threshold_zero" do
    test "filter_bm25_threshold_zero" do
      engine_config = "{\"bm25_query\":\"unrelated query\",\"bm25_threshold\":0.0,\"content_filter\":\"bm25\",\"max_depth\":1}"
      {:ok, engine} = Crawlberg.create_engine(engine_config)

      url =
        System.get_env("MOCK_SERVER_FILTER_BM25_THRESHOLD_ZERO") ||
          (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/filter_bm25_threshold_zero"

      {:ok, result} = Crawlberg.crawl(engine, url)
      assert length(result.pages) == 2
    end
  end

  describe "filter_noop_crawl_all_kept" do
    test "filter_noop_crawl_all_kept" do
      engine_config = "{\"max_concurrent\":1,\"max_depth\":1}"
      {:ok, engine} = Crawlberg.create_engine(engine_config)

      url =
        System.get_env("MOCK_SERVER_FILTER_NOOP_CRAWL_ALL_KEPT") ||
          (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/filter_noop_crawl_all_kept"

      {:ok, result} = Crawlberg.crawl(engine, url)
      assert length(result.pages) == 3
    end
  end

  describe "filter_noop_passes_all" do
    test "filter_noop_passes_all" do
      engine_config = "{\"max_depth\":1}"
      {:ok, engine} = Crawlberg.create_engine(engine_config)

      url =
        System.get_env("MOCK_SERVER_FILTER_NOOP_PASSES_ALL") ||
          (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/filter_noop_passes_all"

      {:ok, result} = Crawlberg.crawl(engine, url)
      assert length(result.pages) == 3
    end
  end
end
