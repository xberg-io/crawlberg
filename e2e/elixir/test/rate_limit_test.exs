# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:ebaf9a79c8a59f51f645ced58e48a5318da8975e510458cd6d611588352f6ae0
# To regenerate: alef e2e generate
# To verify freshness: alef verify
# E2e tests for category: rate_limit
defmodule E2e.RateLimitTest do
  use ExUnit.Case, async: false

  describe "rate_limit_adaptive_backoff" do
    test "rate_limit_adaptive_backoff" do
      engine_config = "{\"respect_robots_txt\":false,\"retry_codes\":[429],\"retry_count\":2}"
      {:ok, engine} = Crawlberg.create_engine(engine_config)
      url = (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/rate_limit_adaptive_backoff"
      {:ok, result} = Crawlberg.scrape(engine, url)
      assert result.status_code == 200
    end
  end

  describe "rate_limit_basic_delay" do
    test "rate_limit_basic_delay" do
      engine_config = "{\"max_depth\":1}"
      {:ok, engine} = Crawlberg.create_engine(engine_config)

      url =
        System.get_env("MOCK_SERVER_RATE_LIMIT_BASIC_DELAY") ||
          (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/rate_limit_basic_delay"

      {:ok, result} = Crawlberg.crawl(engine, url)
      assert length(result.pages) == 3
      # skipped: field 'rate_limit.min_duration_ms' not available on result type
    end
  end

  describe "rate_limit_per_domain" do
    test "rate_limit_per_domain" do
      engine_config = "{\"max_concurrent\":1,\"max_depth\":1}"
      {:ok, engine} = Crawlberg.create_engine(engine_config)

      url =
        System.get_env("MOCK_SERVER_RATE_LIMIT_PER_DOMAIN") ||
          (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/rate_limit_per_domain"

      {:ok, result} = Crawlberg.crawl(engine, url)
      assert length(result.pages) >= 2
      assert Enum.at(result.pages, 0).status_code == 200
    end
  end

  describe "rate_limit_robots_crawl_delay" do
    test "rate_limit_robots_crawl_delay" do
      engine_config = "{\"max_depth\":1,\"respect_robots_txt\":true,\"user_agent\":\"TestBot\"}"
      {:ok, engine} = Crawlberg.create_engine(engine_config)

      url =
        System.get_env("MOCK_SERVER_RATE_LIMIT_ROBOTS_CRAWL_DELAY") ||
          (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/rate_limit_robots_crawl_delay"

      {:ok, result} = Crawlberg.crawl(engine, url)
      assert length(result.pages) >= 1
      assert Enum.at(result.pages, 0).status_code == 200
    end
  end

  describe "rate_limit_zero_no_delay" do
    test "rate_limit_zero_no_delay" do
      engine_config = "{\"max_depth\":1}"
      {:ok, engine} = Crawlberg.create_engine(engine_config)

      url =
        System.get_env("MOCK_SERVER_RATE_LIMIT_ZERO_NO_DELAY") ||
          (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/rate_limit_zero_no_delay"

      {:ok, result} = Crawlberg.crawl(engine, url)
      assert length(result.pages) == 2
    end
  end
end
