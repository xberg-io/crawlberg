# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:715f1c7da6c125244c88000f1731f47c535d0afd7253db78ea7d66d34c795b7f
# To regenerate: alef generate
# To verify freshness: alef verify
# E2e tests for category: error
defmodule E2e.ErrorTest do
  use ExUnit.Case, async: false

  describe "error_401_unauthorized" do
    test "error_401_unauthorized" do
      {:ok, engine} = Crawlberg.create_engine(nil)
      url = (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/error_401_unauthorized"
      assert {:error, _} = Crawlberg.scrape(engine, url)
    end
  end

  # skipped: assertion type 'error' has no accessor for error field <none> in this backend

  describe "error_403_forbidden" do
    test "error_403_forbidden" do
      engine_config = "{\"browser\":{\"mode\":\"never\"}}"
      {:ok, engine} = Crawlberg.create_engine(engine_config)
      url = (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/error_403_forbidden"
      assert {:error, _} = Crawlberg.scrape(engine, url)
    end
  end

  # skipped: assertion type 'error' has no accessor for error field <none> in this backend

  describe "error_404_page" do
    test "error_404_page" do
      {:ok, engine} = Crawlberg.create_engine(nil)
      url = (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/error_404_page"
      assert {:error, _} = Crawlberg.scrape(engine, url)
    end
  end

  # skipped: assertion type 'error' has no accessor for error field <none> in this backend

  describe "error_408_request_timeout" do
    test "error_408_request_timeout" do
      {:ok, engine} = Crawlberg.create_engine(nil)
      url = (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/error_408_request_timeout"
      assert {:error, _} = Crawlberg.scrape(engine, url)
    end
  end

  # skipped: assertion type 'error' has no accessor for error field <none> in this backend

  describe "error_410_gone" do
    test "error_410_gone" do
      {:ok, engine} = Crawlberg.create_engine(nil)
      url = (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/error_410_gone"
      assert {:error, _} = Crawlberg.scrape(engine, url)
    end
  end

  # skipped: assertion type 'error' has no accessor for error field <none> in this backend

  describe "error_500_server" do
    test "error_500_server" do
      {:ok, engine} = Crawlberg.create_engine(nil)
      url = (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/error_500_server"
      assert {:error, _} = Crawlberg.scrape(engine, url)
    end
  end

  # skipped: assertion type 'error' has no accessor for error field <none> in this backend

  describe "error_502_bad_gateway" do
    test "error_502_bad_gateway" do
      {:ok, engine} = Crawlberg.create_engine(nil)
      url = (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/error_502_bad_gateway"
      assert {:error, _} = Crawlberg.scrape(engine, url)
    end
  end

  # skipped: assertion type 'error' has no accessor for error field <none> in this backend

  describe "error_browser_launch_failure" do
    test "error_browser_launch_failure" do
      engine_config = "{\"browser\":{\"mode\":\"always\",\"timeout\":1}}"
      {:ok, engine} = Crawlberg.create_engine(engine_config)
      url = (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/error_browser_launch_failure"
      assert {:error, _} = Crawlberg.scrape(engine, url)
    end
  end

  # skipped: assertion type 'error' has no accessor for error field <none> in this backend

  describe "error_browser_page_timeout" do
    test "error_browser_page_timeout" do
      engine_config = "{\"browser\":{\"mode\":\"always\",\"timeout\":1}}"
      {:ok, engine} = Crawlberg.create_engine(engine_config)
      url = (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/error_browser_page_timeout"
      assert {:error, _} = Crawlberg.scrape(engine, url)
    end
  end

  # skipped: assertion type 'error' has no accessor for error field <none> in this backend

  describe "error_connection_refused" do
    test "error_connection_refused" do
      {:ok, engine} = Crawlberg.create_engine(nil)
      url = (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/error_connection_refused"
      assert {:error, _} = Crawlberg.scrape(engine, url)
    end
  end

  # skipped: assertion type 'error' has no accessor for error field <none> in this backend

  describe "error_data_loss_truncated" do
    test "error_data_loss_truncated" do
      {:ok, engine} = Crawlberg.create_engine(nil)
      url = (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/error_data_loss_truncated"
      assert {:error, _} = Crawlberg.scrape(engine, url)
    end
  end

  # skipped: assertion type 'error' has no accessor for error field <none> in this backend

  describe "error_dns_resolution" do
    test "error_dns_resolution" do
      {:ok, engine} = Crawlberg.create_engine(nil)
      url = (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/error_dns_resolution"
      assert {:error, _} = Crawlberg.scrape(engine, url)
    end
  end

  # skipped: assertion type 'error' has no accessor for error field <none> in this backend

  describe "error_empty_batch_urls" do
    test "error_empty_batch_urls" do
      {:ok, engine} = Crawlberg.create_engine(nil)
      url = (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/error_empty_batch_urls"
      assert {:error, __reason} = Crawlberg.scrape(engine, url)
      assert String.contains?(inspect(__reason), "urls")
    end
  end

  describe "error_empty_response" do
    test "error_empty_response" do
      {:ok, engine} = Crawlberg.create_engine(nil)
      url = (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/error_empty_response"
      {:ok, result} = Crawlberg.scrape(engine, url)
      assert is_nil(result.html) or result.html == ""
    end
  end

  describe "error_invalid_proxy" do
    test "error_invalid_proxy" do
      engine_config = "{\"proxy\":{\"url\":\"http://127.0.0.1:1\"}}"
      {:ok, engine} = Crawlberg.create_engine(engine_config)
      url = (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/error_invalid_proxy"
      assert {:error, _} = Crawlberg.scrape(engine, url)
    end
  end

  # skipped: assertion type 'error' has no accessor for error field <none> in this backend

  describe "error_partial_response" do
    test "error_partial_response" do
      {:ok, engine} = Crawlberg.create_engine(nil)
      url = (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/error_partial_response"
      assert {:error, _} = Crawlberg.scrape(engine, url)
    end
  end

  # skipped: assertion type 'error' has no accessor for error field <none> in this backend

  describe "error_rate_limited" do
    test "error_rate_limited" do
      {:ok, engine} = Crawlberg.create_engine(nil)
      url = (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/error_rate_limited"
      assert {:error, _} = Crawlberg.scrape(engine, url)
    end
  end

  # skipped: assertion type 'error' has no accessor for error field <none> in this backend

  describe "error_retry_503" do
    test "error_retry_503" do
      {:ok, engine} = Crawlberg.create_engine(nil)
      url = (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/error_retry_503"
      assert {:error, _} = Crawlberg.scrape(engine, url)
    end
  end

  # skipped: assertion type 'error' has no accessor for error field <none> in this backend

  describe "error_retry_backoff" do
    test "error_retry_backoff" do
      {:ok, engine} = Crawlberg.create_engine(nil)
      url = (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/error_retry_backoff"
      assert {:error, _} = Crawlberg.scrape(engine, url)
    end
  end

  # skipped: assertion type 'error' has no accessor for error field <none> in this backend

  describe "error_ssl_invalid_cert" do
    test "error_ssl_invalid_cert" do
      {:ok, engine} = Crawlberg.create_engine(nil)
      url = (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/error_ssl_invalid_cert"
      assert {:error, _} = Crawlberg.scrape(engine, url)
    end
  end

  # skipped: assertion type 'error' has no accessor for error field <none> in this backend

  describe "error_timeout" do
    test "error_timeout" do
      engine_config = "{\"request_timeout\":500}"
      {:ok, engine} = Crawlberg.create_engine(engine_config)
      url = (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/error_timeout"
      assert {:error, _} = Crawlberg.scrape(engine, url)
    end
  end

  # skipped: assertion type 'error' has no accessor for error field <none> in this backend

  describe "error_unsupported_scheme" do
    test "error_unsupported_scheme" do
      {:ok, engine} = Crawlberg.create_engine(nil)
      url = (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/error_unsupported_scheme"
      assert {:error, _} = Crawlberg.scrape(engine, url)
    end
  end

  # skipped: assertion type 'error' has no accessor for error field <none> in this backend

  describe "error_waf_akamai" do
    test "error_waf_akamai" do
      engine_config = "{\"browser\":{\"mode\":\"never\"}}"
      {:ok, engine} = Crawlberg.create_engine(engine_config)
      url = (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/error_waf_akamai"
      assert {:error, _} = Crawlberg.scrape(engine, url)
    end
  end

  describe "error_waf_blocked" do
    test "error_waf_blocked" do
      engine_config = "{\"browser\":{\"mode\":\"never\"}}"
      {:ok, engine} = Crawlberg.create_engine(engine_config)
      url = (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/error_waf_blocked"
      assert {:error, _} = Crawlberg.scrape(engine, url)
    end
  end

  # skipped: assertion type 'error' has no accessor for error field <none> in this backend

  describe "error_waf_false_403" do
    test "error_waf_false_403" do
      engine_config = "{\"browser\":{\"mode\":\"never\"}}"
      {:ok, engine} = Crawlberg.create_engine(engine_config)
      url = (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/error_waf_false_403"
      assert {:error, _} = Crawlberg.scrape(engine, url)
    end
  end

  # skipped: assertion type 'error' has no accessor for error field <none> in this backend

  describe "error_waf_imperva" do
    test "error_waf_imperva" do
      engine_config = "{\"browser\":{\"mode\":\"never\"}}"
      {:ok, engine} = Crawlberg.create_engine(engine_config)
      url = (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/error_waf_imperva"
      assert {:error, _} = Crawlberg.scrape(engine, url)
    end
  end
end
