# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:cfd2d77f6b1969699bfd6f3d2d4b5da9f8c604bfcd5ed42ec4db2f71ff1cf305
# To regenerate: alef generate
# To verify freshness: alef verify
# E2e tests for category: engine
defmodule E2e.EngineTest do
  use ExUnit.Case, async: false

  describe "engine_crawl_basic" do
    test "engine_crawl_basic" do
      engine_config = "{\"max_depth\":1}"
      {:ok, engine} = Crawlberg.create_engine(engine_config)
      url = System.get_env("MOCK_SERVER_ENGINE_CRAWL_BASIC") || (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/engine_crawl_basic"
      {:ok, result} = Crawlberg.crawl(engine, url)
      assert length(result.pages) == 3
      assert length(result.pages) >= 3
    end
  end

  describe "engine_map_basic" do
    test "engine_map_basic" do
      {:ok, engine} = Crawlberg.create_engine(nil)
      url = System.get_env("MOCK_SERVER_ENGINE_MAP_BASIC") || (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/engine_map_basic"
      {:ok, result} = Crawlberg.map_urls(engine, url)
      assert length(result.urls) >= 2
    end
  end

  describe "engine_scrape_basic" do
    test "engine_scrape_basic" do
      {:ok, engine} = Crawlberg.create_engine(nil)

      url =
        System.get_env("MOCK_SERVER_ENGINE_SCRAPE_BASIC") || (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/engine_scrape_basic"

      {:ok, result} = Crawlberg.scrape(engine, url)
      assert result.status_code == 200
      assert result.content_type == "text/html"
      assert result.metadata.title == "Engine Test"
      assert String.contains?(to_string(result.metadata.description), "Testing the engine")
      assert length(result.links) >= 1
      assert length(result.metadata.headings) >= 1
    end
  end

  describe "engine_stream_basic" do
    test "engine_stream_basic" do
      engine_config = "{\"max_depth\":1}"
      {:ok, engine} = Crawlberg.create_engine(engine_config)

      url =
        System.get_env("MOCK_SERVER_ENGINE_STREAM_BASIC") || (System.get_env("MOCK_SERVER_URL") || "") <> "/fixtures/engine_stream_basic"

      url_req = %Crawlberg.CrawlStreamRequest{url: url}
      {:ok, result} = Crawlberg.crawl_stream(engine, url_req)
      chunks = Enum.to_list(result)
      assert length(chunks) >= 3
    end
  end
end
