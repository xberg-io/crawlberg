// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:b25f293a2658b5e49df9ae7ecbdcfd96c13ada26ecf2a046c108673ffc47752a
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
const std = @import("std");
const testing = std.testing;
const crawlberg = @import("crawlberg");

// Suppress C++ global destructor aborts that break zig's --listen=- IPC
extern "c" fn signal(sig: i32, handler: usize) usize;
var _abort_handler_installed: bool = false;
fn suppress_abort() void {
    if (!_abort_handler_installed) {
        // SIGABRT = 6 on POSIX; SIG_IGN = 1
        _ = signal(6, 1);
        _abort_handler_installed = true;
    }
}

extern "c" fn setenv(name: [*:0]const u8, value: [*:0]const u8, overwrite: c_int) c_int;
fn allow_private_network() void {
    _ = setenv("CRAWLBERG_ALLOW_PRIVATE_NETWORK", "true", 1);
}

// E2e tests for category: validation

test "validation_browser_endpoint_invalid" {
    // Browser endpoint must be a valid ws:// or wss:// URL, not http://
    suppress_abort();
    allow_private_network();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const url = if (std.c.getenv("MOCK_SERVER_VALIDATION_BROWSER_ENDPOINT_INVALID")) |_pf| try std.fmt.allocPrint(allocator, "{s}", .{std.mem.span(_pf)}) else try std.fmt.allocPrint(allocator, "{s}/fixtures/validation_browser_endpoint_invalid", .{if (std.c.getenv("MOCK_SERVER_URL")) |v| std.mem.span(v) else "http://localhost:8080"});
    defer allocator.free(url);
    const _result_json = crawlberg.scrape("{\"browser\":{\"endpoint\":\"http://not-websocket:3000\",\"mode\":\"always\"}}", url) catch {
        try testing.expect(true); // Error occurred as expected
        return;
    };
    _ = _result_json;
}

test "validation_invalid_auth_config" {
    // auth object with empty username in basic auth is rejected
    suppress_abort();
    allow_private_network();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const url = if (std.c.getenv("MOCK_SERVER_VALIDATION_INVALID_AUTH_CONFIG")) |_pf| try std.fmt.allocPrint(allocator, "{s}", .{std.mem.span(_pf)}) else try std.fmt.allocPrint(allocator, "{s}/fixtures/validation_invalid_auth_config", .{if (std.c.getenv("MOCK_SERVER_URL")) |v| std.mem.span(v) else "http://localhost:8080"});
    defer allocator.free(url);
    const _result_json = crawlberg.scrape("{\"auth\":{\"password\":\"secret\",\"type\":\"basic\",\"username\":\"\"}}", url) catch {
        try testing.expect(true); // Error occurred as expected
        return;
    };
    _ = _result_json;
}

test "validation_invalid_exclude_regex" {
    // Invalid regex in exclude_paths is rejected
    suppress_abort();
    allow_private_network();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const url = if (std.c.getenv("MOCK_SERVER_VALIDATION_INVALID_EXCLUDE_REGEX")) |_pf| try std.fmt.allocPrint(allocator, "{s}", .{std.mem.span(_pf)}) else try std.fmt.allocPrint(allocator, "{s}/fixtures/validation_invalid_exclude_regex", .{if (std.c.getenv("MOCK_SERVER_URL")) |v| std.mem.span(v) else "http://localhost:8080"});
    defer allocator.free(url);
    const _result_json = crawlberg.scrape("{\"exclude_paths\":[\"(unclosed\"]}", url) catch {
        try testing.expect(true); // Error occurred as expected
        return;
    };
    _ = _result_json;
}

test "validation_invalid_include_regex" {
    // Invalid regex in include_paths is rejected
    suppress_abort();
    allow_private_network();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const url = if (std.c.getenv("MOCK_SERVER_VALIDATION_INVALID_INCLUDE_REGEX")) |_pf| try std.fmt.allocPrint(allocator, "{s}", .{std.mem.span(_pf)}) else try std.fmt.allocPrint(allocator, "{s}/fixtures/validation_invalid_include_regex", .{if (std.c.getenv("MOCK_SERVER_URL")) |v| std.mem.span(v) else "http://localhost:8080"});
    defer allocator.free(url);
    const _result_json = crawlberg.scrape("{\"include_paths\":[\"[invalid\"]}", url) catch {
        try testing.expect(true); // Error occurred as expected
        return;
    };
    _ = _result_json;
}

test "validation_invalid_proxy_url" {
    // proxy with invalid URL like 'not-a-url' is rejected
    suppress_abort();
    allow_private_network();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const url = if (std.c.getenv("MOCK_SERVER_VALIDATION_INVALID_PROXY_URL")) |_pf| try std.fmt.allocPrint(allocator, "{s}", .{std.mem.span(_pf)}) else try std.fmt.allocPrint(allocator, "{s}/fixtures/validation_invalid_proxy_url", .{if (std.c.getenv("MOCK_SERVER_URL")) |v| std.mem.span(v) else "http://localhost:8080"});
    defer allocator.free(url);
    const _result_json = crawlberg.scrape("{\"proxy\":{\"url\":\"not-a-url\"}}", url) catch {
        try testing.expect(true); // Error occurred as expected
        return;
    };
    _ = _result_json;
}

test "validation_invalid_retry_code" {
    // Retry code outside 100-599 is rejected
    suppress_abort();
    allow_private_network();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const url = if (std.c.getenv("MOCK_SERVER_VALIDATION_INVALID_RETRY_CODE")) |_pf| try std.fmt.allocPrint(allocator, "{s}", .{std.mem.span(_pf)}) else try std.fmt.allocPrint(allocator, "{s}/fixtures/validation_invalid_retry_code", .{if (std.c.getenv("MOCK_SERVER_URL")) |v| std.mem.span(v) else "http://localhost:8080"});
    defer allocator.free(url);
    const _result_json = crawlberg.scrape("{\"retry_codes\":[999]}", url) catch {
        try testing.expect(true); // Error occurred as expected
        return;
    };
    _ = _result_json;
}

test "validation_max_concurrent_zero" {
    // max_concurrent=0 is rejected as invalid config (minimum is 1)
    suppress_abort();
    allow_private_network();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const url = if (std.c.getenv("MOCK_SERVER_VALIDATION_MAX_CONCURRENT_ZERO")) |_pf| try std.fmt.allocPrint(allocator, "{s}", .{std.mem.span(_pf)}) else try std.fmt.allocPrint(allocator, "{s}/fixtures/validation_max_concurrent_zero", .{if (std.c.getenv("MOCK_SERVER_URL")) |v| std.mem.span(v) else "http://localhost:8080"});
    defer allocator.free(url);
    const _result_json = crawlberg.scrape("{\"max_concurrent\":0}", url) catch {
        try testing.expect(true); // Error occurred as expected
        return;
    };
    _ = _result_json;
}

test "validation_max_depth_too_high" {
    // max_depth=200 exceeds limit of 100
    suppress_abort();
    allow_private_network();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const url = if (std.c.getenv("MOCK_SERVER_VALIDATION_MAX_DEPTH_TOO_HIGH")) |_pf| try std.fmt.allocPrint(allocator, "{s}", .{std.mem.span(_pf)}) else try std.fmt.allocPrint(allocator, "{s}/fixtures/validation_max_depth_too_high", .{if (std.c.getenv("MOCK_SERVER_URL")) |v| std.mem.span(v) else "http://localhost:8080"});
    defer allocator.free(url);
    const _result_json = crawlberg.scrape("{\"max_depth\":200}", url) catch {
        try testing.expect(true); // Error occurred as expected
        return;
    };
    _ = _result_json;
}

test "validation_max_pages_zero" {
    // max_pages=0 is rejected as invalid config
    suppress_abort();
    allow_private_network();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const url = if (std.c.getenv("MOCK_SERVER_VALIDATION_MAX_PAGES_ZERO")) |_pf| try std.fmt.allocPrint(allocator, "{s}", .{std.mem.span(_pf)}) else try std.fmt.allocPrint(allocator, "{s}/fixtures/validation_max_pages_zero", .{if (std.c.getenv("MOCK_SERVER_URL")) |v| std.mem.span(v) else "http://localhost:8080"});
    defer allocator.free(url);
    const _result_json = crawlberg.scrape("{\"max_pages\":0}", url) catch {
        try testing.expect(true); // Error occurred as expected
        return;
    };
    _ = _result_json;
}

test "validation_max_redirects_too_high" {
    // max_redirects > 100 is rejected as invalid config
    suppress_abort();
    allow_private_network();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const url = if (std.c.getenv("MOCK_SERVER_VALIDATION_MAX_REDIRECTS_TOO_HIGH")) |_pf| try std.fmt.allocPrint(allocator, "{s}", .{std.mem.span(_pf)}) else try std.fmt.allocPrint(allocator, "{s}/fixtures/validation_max_redirects_too_high", .{if (std.c.getenv("MOCK_SERVER_URL")) |v| std.mem.span(v) else "http://localhost:8080"});
    defer allocator.free(url);
    const _result_json = crawlberg.scrape("{\"max_redirects\":200}", url) catch {
        try testing.expect(true); // Error occurred as expected
        return;
    };
    _ = _result_json;
}

test "validation_negative_body_size" {
    // max_body_size set to -1 is rejected as invalid config
    suppress_abort();
    allow_private_network();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const url = if (std.c.getenv("MOCK_SERVER_VALIDATION_NEGATIVE_BODY_SIZE")) |_pf| try std.fmt.allocPrint(allocator, "{s}", .{std.mem.span(_pf)}) else try std.fmt.allocPrint(allocator, "{s}/fixtures/validation_negative_body_size", .{if (std.c.getenv("MOCK_SERVER_URL")) |v| std.mem.span(v) else "http://localhost:8080"});
    defer allocator.free(url);
    const _result_json = crawlberg.scrape("{\"max_body_size\":0}", url) catch {
        try testing.expect(true); // Error occurred as expected
        return;
    };
    _ = _result_json;
}

test "validation_ssrf_allowlisted_loopback" {
    // SSRF allowlist permits a loopback mock server while private-network protection remains enabled
    suppress_abort();
    allow_private_network();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const url = if (std.c.getenv("MOCK_SERVER_VALIDATION_SSRF_ALLOWLISTED_LOOPBACK")) |_pf| try std.fmt.allocPrint(allocator, "{s}", .{std.mem.span(_pf)}) else try std.fmt.allocPrint(allocator, "{s}/fixtures/validation_ssrf_allowlisted_loopback", .{if (std.c.getenv("MOCK_SERVER_URL")) |v| std.mem.span(v) else "http://localhost:8080"});
    defer allocator.free(url);
    const _result_json = try crawlberg.scrape("{\"respect_robots_txt\":false,\"ssrf\":{\"allowlist\":[{\"type\":\"cidr\",\"value\":\"127.0.0.0/8\"}],\"deny_private\":true,\"max_redirects\":5},\"ssrf_deny_private_explicit\":true}", url);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expectEqual(@as(i64, 200), result.object.get("status_code").?.integer);
    try testing.expectEqualStrings("Allowlisted", std.mem.trim(u8, result.object.get("metadata").?.object.get("title").?.string, " \n\r\t"));
}

test "validation_ssrf_ipv4_mapped_ipv6_denied" {
    // SSRF policy rejects an IPv4 loopback address embedded in an IPv6 literal
    suppress_abort();
    allow_private_network();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const url = if (std.c.getenv("MOCK_SERVER_VALIDATION_SSRF_IPV4_MAPPED_IPV6_DENIED")) |_pf| try std.fmt.allocPrint(allocator, "{s}", .{std.mem.span(_pf)}) else try std.fmt.allocPrint(allocator, "{s}/fixtures/validation_ssrf_ipv4_mapped_ipv6_denied", .{if (std.c.getenv("MOCK_SERVER_URL")) |v| std.mem.span(v) else "http://localhost:8080"});
    defer allocator.free(url);
    const _result_json = crawlberg.scrape("{\"ssrf\":{\"deny_private\":true},\"ssrf_deny_private_explicit\":true}", url) catch {
        try testing.expect(true); // Error occurred as expected
        return;
    };
    _ = _result_json;
}

test "validation_ssrf_loopback_denied" {
    // scrape() rejects loopback addresses by default SSRF policy
    suppress_abort();
    allow_private_network();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const url = if (std.c.getenv("MOCK_SERVER_VALIDATION_SSRF_LOOPBACK_DENIED")) |_pf| try std.fmt.allocPrint(allocator, "{s}", .{std.mem.span(_pf)}) else try std.fmt.allocPrint(allocator, "{s}/fixtures/validation_ssrf_loopback_denied", .{if (std.c.getenv("MOCK_SERVER_URL")) |v| std.mem.span(v) else "http://localhost:8080"});
    defer allocator.free(url);
    const _result_json = crawlberg.scrape("{\"ssrf_deny_private_explicit\":true}", url) catch {
        try testing.expect(true); // Error occurred as expected
        return;
    };
    _ = _result_json;
}

test "validation_ssrf_nat64_loopback_denied" {
    // SSRF policy rejects an IPv4 loopback address embedded in the well-known NAT64 prefix
    suppress_abort();
    allow_private_network();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const url = if (std.c.getenv("MOCK_SERVER_VALIDATION_SSRF_NAT64_LOOPBACK_DENIED")) |_pf| try std.fmt.allocPrint(allocator, "{s}", .{std.mem.span(_pf)}) else try std.fmt.allocPrint(allocator, "{s}/fixtures/validation_ssrf_nat64_loopback_denied", .{if (std.c.getenv("MOCK_SERVER_URL")) |v| std.mem.span(v) else "http://localhost:8080"});
    defer allocator.free(url);
    const _result_json = crawlberg.scrape("{\"ssrf\":{\"deny_private\":true},\"ssrf_deny_private_explicit\":true}", url) catch {
        try testing.expect(true); // Error occurred as expected
        return;
    };
    _ = _result_json;
}

test "validation_timeout_zero" {
    // Zero request timeout is rejected as invalid config
    suppress_abort();
    allow_private_network();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const url = if (std.c.getenv("MOCK_SERVER_VALIDATION_TIMEOUT_ZERO")) |_pf| try std.fmt.allocPrint(allocator, "{s}", .{std.mem.span(_pf)}) else try std.fmt.allocPrint(allocator, "{s}/fixtures/validation_timeout_zero", .{if (std.c.getenv("MOCK_SERVER_URL")) |v| std.mem.span(v) else "http://localhost:8080"});
    defer allocator.free(url);
    const _result_json = crawlberg.scrape("{\"request_timeout\":0}", url) catch {
        try testing.expect(true); // Error occurred as expected
        return;
    };
    _ = _result_json;
}
