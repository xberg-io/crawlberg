// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:e1c3e46c62aa3ddf274955217909320163335ba8f69083d79fbf49f3352f0bbd
// To regenerate: alef generate
// To verify freshness: alef verify
const std = @import("std");
const testing = std.testing;
const crawlberg = @import("crawlberg");

extern "c" fn setenv(name: [*:0]const u8, value: [*:0]const u8, overwrite: c_int) c_int;
fn allow_private_network() void {
    _ = setenv("CRAWLBERG_ALLOW_PRIVATE_NETWORK", "true", 1);
}

// E2e tests for category: links

test "links_anchor_fragment" {
    // Identifies fragment-only links as anchor type
    allow_private_network();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const url = if (std.c.getenv("MOCK_SERVER_LINKS_ANCHOR_FRAGMENT")) |_pf| try std.fmt.allocPrint(allocator, "{s}", .{std.mem.span(_pf)}) else try std.fmt.allocPrint(allocator, "{s}/fixtures/links_anchor_fragment", .{if (std.c.getenv("MOCK_SERVER_URL")) |v| std.mem.span(v) else "http://localhost:8080"});
    defer allocator.free(url);
    const _result_json = try crawlberg.scrape("{}", url);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    {
        const _WildcardCheck = struct {
            fn check(_wce: std.json.Value) anyerror!void {
                {
                    const _jva0 = (_wce.object.get("link_type") orelse return error.WildcardElementFieldMissing);
                    const _jsa0 = if (_jva0 == .string) _jva0.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva0, .{});
                    defer if (_jva0 != .string) std.heap.c_allocator.free(_jsa0);
                    try testing.expect(std.mem.indexOf(u8, _jsa0, "anchor") != null);
                }
            }
        };
        var _wc_found = false;
        for (result.object.get("links").?.array.items) |_wc_item| {
            _WildcardCheck.check(_wc_item) catch continue;
            _wc_found = true;
            break;
        }
        try testing.expect(_wc_found); // no element of 'links' matched
    }
}

test "links_base_tag" {
    // Resolves relative URLs using base tag href
    allow_private_network();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const url = if (std.c.getenv("MOCK_SERVER_LINKS_BASE_TAG")) |_pf| try std.fmt.allocPrint(allocator, "{s}", .{std.mem.span(_pf)}) else try std.fmt.allocPrint(allocator, "{s}/fixtures/links_base_tag", .{if (std.c.getenv("MOCK_SERVER_URL")) |v| std.mem.span(v) else "http://localhost:8080"});
    defer allocator.free(url);
    const _result_json = try crawlberg.scrape("{}", url);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expect(result.object.get("links").?.array.items.len > @as(usize, 2));
    {
        const _WildcardCheck = struct {
            fn check(_wce: std.json.Value) anyerror!void {
                {
                    const _jv = (_wce.object.get("url") orelse return error.WildcardElementFieldMissing);
                    const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
                    defer if (_jv != .string) std.heap.c_allocator.free(_js);
                    try testing.expect(std.mem.indexOf(u8, _js, "example.com") != null);
                }
            }
        };
        var _wc_found = false;
        for (result.object.get("links").?.array.items) |_wc_item| {
            _WildcardCheck.check(_wc_item) catch continue;
            _wc_found = true;
            break;
        }
        try testing.expect(_wc_found); // no element of 'links' matched
    }
}

test "links_document_types" {
    // Detects PDF, DOCX, XLSX links as document type
    allow_private_network();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const url = if (std.c.getenv("MOCK_SERVER_LINKS_DOCUMENT_TYPES")) |_pf| try std.fmt.allocPrint(allocator, "{s}", .{std.mem.span(_pf)}) else try std.fmt.allocPrint(allocator, "{s}/fixtures/links_document_types", .{if (std.c.getenv("MOCK_SERVER_URL")) |v| std.mem.span(v) else "http://localhost:8080"});
    defer allocator.free(url);
    const _result_json = try crawlberg.scrape("{}", url);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    {
        const _WildcardCheck = struct {
            fn check(_wce: std.json.Value) anyerror!void {
                {
                    const _jva0 = (_wce.object.get("link_type") orelse return error.WildcardElementFieldMissing);
                    const _jsa0 = if (_jva0 == .string) _jva0.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jva0, .{});
                    defer if (_jva0 != .string) std.heap.c_allocator.free(_jsa0);
                    try testing.expect(std.mem.indexOf(u8, _jsa0, "document") != null);
                }
            }
        };
        var _wc_found = false;
        for (result.object.get("links").?.array.items) |_wc_item| {
            _WildcardCheck.check(_wc_item) catch continue;
            _wc_found = true;
            break;
        }
        try testing.expect(_wc_found); // no element of 'links' matched
    }
}

test "links_empty_href" {
    // Handles empty href attributes without errors
    allow_private_network();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const url = if (std.c.getenv("MOCK_SERVER_LINKS_EMPTY_HREF")) |_pf| try std.fmt.allocPrint(allocator, "{s}", .{std.mem.span(_pf)}) else try std.fmt.allocPrint(allocator, "{s}/fixtures/links_empty_href", .{if (std.c.getenv("MOCK_SERVER_URL")) |v| std.mem.span(v) else "http://localhost:8080"});
    defer allocator.free(url);
    const _result_json = try crawlberg.scrape("{}", url);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expect(result.object.get("links").?.array.items.len > @as(usize, 0));
    {
        const _WildcardCheck = struct {
            fn check(_wce: std.json.Value) anyerror!void {
                {
                    const _jv = (_wce.object.get("url") orelse return error.WildcardElementFieldMissing);
                    const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
                    defer if (_jv != .string) std.heap.c_allocator.free(_js);
                    try testing.expect(std.mem.indexOf(u8, _js, "/valid") != null);
                }
            }
        };
        var _wc_found = false;
        for (result.object.get("links").?.array.items) |_wc_item| {
            _WildcardCheck.check(_wc_item) catch continue;
            _wc_found = true;
            break;
        }
        try testing.expect(_wc_found); // no element of 'links' matched
    }
}

test "links_internal_external_classification" {
    // Correctly classifies internal vs external links by domain
    allow_private_network();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const url = if (std.c.getenv("MOCK_SERVER_LINKS_INTERNAL_EXTERNAL_CLASSIFICATION")) |_pf| try std.fmt.allocPrint(allocator, "{s}", .{std.mem.span(_pf)}) else try std.fmt.allocPrint(allocator, "{s}/fixtures/links_internal_external_classification", .{if (std.c.getenv("MOCK_SERVER_URL")) |v| std.mem.span(v) else "http://localhost:8080"});
    defer allocator.free(url);
    const _result_json = try crawlberg.scrape("{}", url);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expect(result.object.get("links").?.array.items.len > @as(usize, 4));
    {
        const _WildcardCheck = struct {
            fn check(_wce: std.json.Value) anyerror!void {
                {
                    const _ne = (_wce.object.get("url") orelse return error.WildcardElementFieldMissing);
                    try testing.expect(switch (_ne) {
                        .null => false,
                        .string => |_s| _s.len > 0,
                        .array => |_a| _a.items.len > 0,
                        .object => |_o| _o.count() > 0,
                        else => true,
                    });
                }
            }
        };
        var _wc_found = false;
        for (result.object.get("links").?.array.items) |_wc_item| {
            _WildcardCheck.check(_wc_item) catch continue;
            _wc_found = true;
            break;
        }
        try testing.expect(_wc_found); // no element of 'links' matched
    }
}

test "links_mailto_javascript_skip" {
    // Skips mailto:, javascript:, and tel: scheme links
    allow_private_network();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const url = if (std.c.getenv("MOCK_SERVER_LINKS_MAILTO_JAVASCRIPT_SKIP")) |_pf| try std.fmt.allocPrint(allocator, "{s}", .{std.mem.span(_pf)}) else try std.fmt.allocPrint(allocator, "{s}/fixtures/links_mailto_javascript_skip", .{if (std.c.getenv("MOCK_SERVER_URL")) |v| std.mem.span(v) else "http://localhost:8080"});
    defer allocator.free(url);
    const _result_json = try crawlberg.scrape("{}", url);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expect(result.object.get("links").?.array.items.len > @as(usize, 0));
    {
        const _WildcardCheck = struct {
            fn check(_wce: std.json.Value) anyerror!void {
                {
                    const _jvnc = (_wce.object.get("url") orelse return error.WildcardElementFieldMissing);
                    const _jsnc = if (_jvnc == .string) _jvnc.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jvnc, .{});
                    defer if (_jvnc != .string) std.heap.c_allocator.free(_jsnc);
                    try testing.expect(std.mem.indexOf(u8, _jsnc, "mailto:") == null);
                }
            }
        };
        var _wc_found = false;
        for (result.object.get("links").?.array.items) |_wc_item| {
            _WildcardCheck.check(_wc_item) catch continue;
            _wc_found = true;
            break;
        }
        try testing.expect(_wc_found); // no element of 'links' matched
    }
}

test "links_protocol_relative" {
    // Handles protocol-relative URLs (//example.com) correctly
    allow_private_network();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const url = if (std.c.getenv("MOCK_SERVER_LINKS_PROTOCOL_RELATIVE")) |_pf| try std.fmt.allocPrint(allocator, "{s}", .{std.mem.span(_pf)}) else try std.fmt.allocPrint(allocator, "{s}/fixtures/links_protocol_relative", .{if (std.c.getenv("MOCK_SERVER_URL")) |v| std.mem.span(v) else "http://localhost:8080"});
    defer allocator.free(url);
    const _result_json = try crawlberg.scrape("{}", url);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expect(result.object.get("links").?.array.items.len > @as(usize, 1));
    {
        const _WildcardCheck = struct {
            fn check(_wce: std.json.Value) anyerror!void {
                {
                    const _jv = (_wce.object.get("url") orelse return error.WildcardElementFieldMissing);
                    const _js = if (_jv == .string) _jv.string else try std.json.Stringify.valueAlloc(std.heap.c_allocator, _jv, .{});
                    defer if (_jv != .string) std.heap.c_allocator.free(_js);
                    try testing.expect(std.mem.indexOf(u8, _js, "//") != null);
                }
            }
        };
        var _wc_found = false;
        for (result.object.get("links").?.array.items) |_wc_item| {
            _WildcardCheck.check(_wc_item) catch continue;
            _wc_found = true;
            break;
        }
        try testing.expect(_wc_found); // no element of 'links' matched
    }
}

test "links_rel_attributes" {
    // Preserves rel=nofollow and rel=canonical attributes
    allow_private_network();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const url = if (std.c.getenv("MOCK_SERVER_LINKS_REL_ATTRIBUTES")) |_pf| try std.fmt.allocPrint(allocator, "{s}", .{std.mem.span(_pf)}) else try std.fmt.allocPrint(allocator, "{s}/fixtures/links_rel_attributes", .{if (std.c.getenv("MOCK_SERVER_URL")) |v| std.mem.span(v) else "http://localhost:8080"});
    defer allocator.free(url);
    const _result_json = try crawlberg.scrape("{}", url);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expect(result.object.get("links").?.array.items.len > @as(usize, 0));
}

test "links_relative_parent" {
    // Resolves ../ and ./ relative parent path links correctly
    allow_private_network();
    var gpa: std.heap.DebugAllocator(.{}) = .init;
    defer _ = gpa.deinit();
    const allocator = gpa.allocator();

    const url = if (std.c.getenv("MOCK_SERVER_LINKS_RELATIVE_PARENT")) |_pf| try std.fmt.allocPrint(allocator, "{s}", .{std.mem.span(_pf)}) else try std.fmt.allocPrint(allocator, "{s}/fixtures/links_relative_parent", .{if (std.c.getenv("MOCK_SERVER_URL")) |v| std.mem.span(v) else "http://localhost:8080"});
    defer allocator.free(url);
    const _result_json = try crawlberg.scrape("{}", url);
    defer std.heap.c_allocator.free(_result_json);
    var _parsed = try std.json.parseFromSlice(std.json.Value, allocator, _result_json, .{});
    defer _parsed.deinit();
    const result = &_parsed.value;
    try testing.expect(result.object.get("links").?.array.items.len > @as(usize, 3));
}
