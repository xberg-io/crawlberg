/*
 * This file is auto-generated by alef — DO NOT EDIT.
 * alef:hash:51059332f799dab15e510a890c3e364dff53dcf716a9db2b68680d7b50c17564
 * To regenerate: alef generate
 * To verify freshness: alef verify
 */
/* E2e tests for category: metadata */

#include <assert.h>
#include <stdint.h>
#include <string.h>
#include <stdio.h>
#include <stdlib.h>
#include "crawlberg.h"
#include "test_runner.h"

void test_metadata_article_times(void) {
    /* Extracts article:published_time, modified_time, author, section, and tags */
    CBERGAlefHandle config_handle = cberg_crawl_config_from_json("{}");
    assert(config_handle != 0 && "failed to parse config");
    CBERGAlefHandle engine = cberg_create_engine(config_handle);
    cberg_crawl_config_free(config_handle);
    assert(engine != 0 && "failed to create engine");
    const char* mock_per_fixture = getenv("MOCK_SERVER_METADATA_ARTICLE_TIMES");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/metadata_article_times", mock_base);
    }
    CBERGAlefHandle result = cberg_scrape(engine, url);
    assert(result != 0 && "expected call to succeed");
    uint16_t status_code = cberg_scrape_result_status_code(result);
    CBERGAlefHandle metadata_handle = cberg_scrape_result_metadata(result);
    assert(metadata_handle != 0);
    CBERGAlefHandle article_handle = cberg_page_metadata_article(metadata_handle);
    assert(article_handle != 0);
    char* article_published_time = cberg_article_metadata_published_time(article_handle);
    char* article_modified_time = cberg_article_metadata_modified_time(article_handle);
    char* article_author = cberg_article_metadata_author(article_handle);
    char* article_section = cberg_article_metadata_section(article_handle);
    char* tags_json = cberg_article_metadata_tags(article_handle);
    int article_tags_length = alef_json_array_count(tags_json);
    assert(status_code == 200 && "equals assertion failed");
    assert(article_published_time != NULL && strcmp(article_published_time, "2024-01-15T10:00:00Z") == 0 && "equals assertion failed");
    assert(article_modified_time != NULL && strcmp(article_modified_time, "2024-06-20T14:30:00Z") == 0 && "equals assertion failed");
    assert(article_author != NULL && strcmp(article_author, "Jane Developer") == 0 && "equals assertion failed");
    assert(article_section != NULL && strcmp(article_section, "Technology") == 0 && "equals assertion failed");
    assert(article_tags_length == 3 && "equals assertion failed");
    cberg_free_string(article_published_time);
    cberg_free_string(article_modified_time);
    cberg_free_string(article_author);
    cberg_free_string(article_section);
    cberg_free_string(tags_json);
    cberg_article_metadata_free(article_handle);
    cberg_page_metadata_free(metadata_handle);
    cberg_scrape_result_free(result);
    cberg_crawl_engine_handle_free(engine);
}

void test_metadata_favicons(void) {
    /* Extracts favicon link tags including apple-touch-icon */
    CBERGAlefHandle config_handle = cberg_crawl_config_from_json("{}");
    assert(config_handle != 0 && "failed to parse config");
    CBERGAlefHandle engine = cberg_create_engine(config_handle);
    cberg_crawl_config_free(config_handle);
    assert(engine != 0 && "failed to create engine");
    const char* mock_per_fixture = getenv("MOCK_SERVER_METADATA_FAVICONS");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/metadata_favicons", mock_base);
    }
    CBERGAlefHandle result = cberg_scrape(engine, url);
    assert(result != 0 && "expected call to succeed");
    uint16_t status_code = cberg_scrape_result_status_code(result);
    CBERGAlefHandle metadata_handle = cberg_scrape_result_metadata(result);
    assert(metadata_handle != 0);
    char* favicons_json = cberg_page_metadata_favicons(metadata_handle);
    int favicons_length = alef_json_array_count(favicons_json);
    assert(status_code == 200 && "equals assertion failed");
    assert(favicons_length == 5 && "equals assertion failed");
    {
        int found = 0;
        int _wc_count = alef_json_array_count(favicons_json);
        for (int _wc_i = 0; _wc_i < _wc_count; _wc_i++) {
            char* _wc_elem = alef_json_array_get_index(favicons_json, _wc_i);
            char* _wc_val = alef_json_get_string(_wc_elem, "rel");
            if (_wc_val != NULL && strstr(_wc_val, "apple-touch-icon") != NULL) { found = 1; }
            free(_wc_val);
            free(_wc_elem);
        }
        assert(found && "expected some element to contain a substring");
    }
    cberg_free_string(favicons_json);
    cberg_page_metadata_free(metadata_handle);
    cberg_scrape_result_free(result);
    cberg_crawl_engine_handle_free(engine);
}

void test_metadata_headings(void) {
    /* Extracts heading hierarchy (h1-h6) from HTML page */
    CBERGAlefHandle config_handle = cberg_crawl_config_from_json("{}");
    assert(config_handle != 0 && "failed to parse config");
    CBERGAlefHandle engine = cberg_create_engine(config_handle);
    cberg_crawl_config_free(config_handle);
    assert(engine != 0 && "failed to create engine");
    const char* mock_per_fixture = getenv("MOCK_SERVER_METADATA_HEADINGS");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/metadata_headings", mock_base);
    }
    CBERGAlefHandle result = cberg_scrape(engine, url);
    assert(result != 0 && "expected call to succeed");
    uint16_t status_code = cberg_scrape_result_status_code(result);
    CBERGAlefHandle metadata_handle = cberg_scrape_result_metadata(result);
    assert(metadata_handle != 0);
    char* headings_json = cberg_page_metadata_headings(metadata_handle);
    assert(headings_json != NULL);
    char* headings_0_json = alef_json_array_get_index(headings_json, 0);
    char* headings_0_level = alef_json_get_string(headings_0_json, "level");
    char* headings_0_text = alef_json_get_string(headings_0_json, "text");
    int headings_length = alef_json_array_count(headings_json);
    assert(status_code == 200 && "equals assertion failed");
    assert(headings_0_level != NULL && atoll(headings_0_level) == 1 && "equals assertion failed");
    assert(headings_0_text != NULL && strcmp(headings_0_text, "Primary Heading") == 0 && "equals assertion failed");
    assert(headings_length == 8 && "equals assertion failed");
    free(headings_0_level);
    free(headings_0_text);
    free(headings_0_json);
    cberg_free_string(headings_json);
    cberg_page_metadata_free(metadata_handle);
    cberg_scrape_result_free(result);
    cberg_crawl_engine_handle_free(engine);
}

void test_metadata_hreflang(void) {
    /* Extracts hreflang alternate link tags */
    CBERGAlefHandle config_handle = cberg_crawl_config_from_json("{}");
    assert(config_handle != 0 && "failed to parse config");
    CBERGAlefHandle engine = cberg_create_engine(config_handle);
    cberg_crawl_config_free(config_handle);
    assert(engine != 0 && "failed to create engine");
    const char* mock_per_fixture = getenv("MOCK_SERVER_METADATA_HREFLANG");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/metadata_hreflang", mock_base);
    }
    CBERGAlefHandle result = cberg_scrape(engine, url);
    assert(result != 0 && "expected call to succeed");
    uint16_t status_code = cberg_scrape_result_status_code(result);
    CBERGAlefHandle metadata_handle = cberg_scrape_result_metadata(result);
    assert(metadata_handle != 0);
    char* hreflangs_json = cberg_page_metadata_hreflangs(metadata_handle);
    int hreflang_length = alef_json_array_count(hreflangs_json);
    assert(status_code == 200 && "equals assertion failed");
    assert(hreflang_length == 4 && "equals assertion failed");
    {
        int found = 0;
        int _wc_count = alef_json_array_count(hreflangs_json);
        for (int _wc_i = 0; _wc_i < _wc_count; _wc_i++) {
            char* _wc_elem = alef_json_array_get_index(hreflangs_json, _wc_i);
            char* _wc_val = alef_json_get_string(_wc_elem, "lang");
            if (_wc_val != NULL && strstr(_wc_val, "en") != NULL) { found = 1; }
            free(_wc_val);
            free(_wc_elem);
        }
        assert(found && "expected some element to contain a substring");
    }
    cberg_free_string(hreflangs_json);
    cberg_page_metadata_free(metadata_handle);
    cberg_scrape_result_free(result);
    cberg_crawl_engine_handle_free(engine);
}

void test_metadata_keywords_author(void) {
    /* Extracts keywords, author, viewport, generator, theme-color, robots, lang, dir metadata */
    CBERGAlefHandle config_handle = cberg_crawl_config_from_json("{}");
    assert(config_handle != 0 && "failed to parse config");
    CBERGAlefHandle engine = cberg_create_engine(config_handle);
    cberg_crawl_config_free(config_handle);
    assert(engine != 0 && "failed to create engine");
    const char* mock_per_fixture = getenv("MOCK_SERVER_METADATA_KEYWORDS_AUTHOR");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/metadata_keywords_author", mock_base);
    }
    CBERGAlefHandle result = cberg_scrape(engine, url);
    assert(result != 0 && "expected call to succeed");
    uint16_t status_code = cberg_scrape_result_status_code(result);
    CBERGAlefHandle metadata_handle = cberg_scrape_result_metadata(result);
    assert(metadata_handle != 0);
    char* metadata_title = cberg_page_metadata_title(metadata_handle);
    char* metadata_canonical_url = cberg_page_metadata_canonical_url(metadata_handle);
    char* metadata_keywords = cberg_page_metadata_keywords(metadata_handle);
    char* metadata_author = cberg_page_metadata_author(metadata_handle);
    char* metadata_viewport = cberg_page_metadata_viewport(metadata_handle);
    char* metadata_generator = cberg_page_metadata_generator(metadata_handle);
    char* metadata_theme_color = cberg_page_metadata_theme_color(metadata_handle);
    char* metadata_robots = cberg_page_metadata_robots(metadata_handle);
    char* metadata_lang = cberg_page_metadata_html_lang(metadata_handle);
    char* metadata_dir = cberg_page_metadata_html_dir(metadata_handle);
    assert(status_code == 200 && "equals assertion failed");
    assert(metadata_title != NULL && strcmp(metadata_title, "Comprehensive Metadata Test Page") == 0 && "equals assertion failed");
    assert(metadata_canonical_url != NULL && strlen(metadata_canonical_url) > 0 && strcmp(metadata_canonical_url, "[]") != 0 && strcmp(metadata_canonical_url, "{}") != 0 && "expected non-empty value");
    assert(metadata_keywords != NULL && strlen(metadata_keywords) > 0 && strcmp(metadata_keywords, "[]") != 0 && strcmp(metadata_keywords, "{}") != 0 && "expected non-empty value");
    assert(metadata_keywords != NULL && strstr(metadata_keywords, "rust") != NULL && "expected to contain substring");
    assert(metadata_author != NULL && strcmp(metadata_author, "Jane Developer") == 0 && "equals assertion failed");
    assert(metadata_viewport != NULL && strlen(metadata_viewport) > 0 && strcmp(metadata_viewport, "[]") != 0 && strcmp(metadata_viewport, "{}") != 0 && "expected non-empty value");
    assert(metadata_generator != NULL && strcmp(metadata_generator, "crawlberg/1.0") == 0 && "equals assertion failed");
    assert(metadata_theme_color != NULL && strcmp(metadata_theme_color, "#ff6600") == 0 && "equals assertion failed");
    assert(metadata_robots != NULL && strcmp(metadata_robots, "index, follow") == 0 && "equals assertion failed");
    assert(metadata_lang != NULL && strcmp(metadata_lang, "en") == 0 && "equals assertion failed");
    assert(metadata_dir != NULL && strcmp(metadata_dir, "ltr") == 0 && "equals assertion failed");
    cberg_free_string(metadata_title);
    cberg_free_string(metadata_canonical_url);
    cberg_free_string(metadata_keywords);
    cberg_free_string(metadata_author);
    cberg_free_string(metadata_viewport);
    cberg_free_string(metadata_generator);
    cberg_free_string(metadata_theme_color);
    cberg_free_string(metadata_robots);
    cberg_free_string(metadata_lang);
    cberg_free_string(metadata_dir);
    cberg_page_metadata_free(metadata_handle);
    cberg_scrape_result_free(result);
    cberg_crawl_engine_handle_free(engine);
}

void test_metadata_og_video_audio(void) {
    /* Extracts og:video, og:audio, and og:locale:alternate metadata */
    CBERGAlefHandle config_handle = cberg_crawl_config_from_json("{}");
    assert(config_handle != 0 && "failed to parse config");
    CBERGAlefHandle engine = cberg_create_engine(config_handle);
    cberg_crawl_config_free(config_handle);
    assert(engine != 0 && "failed to create engine");
    const char* mock_per_fixture = getenv("MOCK_SERVER_METADATA_OG_VIDEO_AUDIO");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/metadata_og_video_audio", mock_base);
    }
    CBERGAlefHandle result = cberg_scrape(engine, url);
    assert(result != 0 && "expected call to succeed");
    uint16_t status_code = cberg_scrape_result_status_code(result);
    CBERGAlefHandle metadata_handle = cberg_scrape_result_metadata(result);
    assert(metadata_handle != 0);
    char* og_video = cberg_page_metadata_og_video(metadata_handle);
    char* og_audio = cberg_page_metadata_og_audio(metadata_handle);
    char* og_locale_alternates_json = cberg_page_metadata_og_locale_alternates(metadata_handle);
    int og_locale_alternate_length = alef_json_array_count(og_locale_alternates_json);
    assert(status_code == 200 && "equals assertion failed");
    assert(og_video != NULL && strcmp(og_video, "https://example.com/video.mp4") == 0 && "equals assertion failed");
    assert(og_audio != NULL && strcmp(og_audio, "https://example.com/audio.mp3") == 0 && "equals assertion failed");
    assert(og_locale_alternate_length == 2 && "equals assertion failed");
    cberg_free_string(og_video);
    cberg_free_string(og_audio);
    cberg_free_string(og_locale_alternates_json);
    cberg_page_metadata_free(metadata_handle);
    cberg_scrape_result_free(result);
    cberg_crawl_engine_handle_free(engine);
}

void test_metadata_response_headers(void) {
    /* Extracts response metadata from HTTP headers (etag, server, content-language) */
    CBERGAlefHandle config_handle = cberg_crawl_config_from_json("{}");
    assert(config_handle != 0 && "failed to parse config");
    CBERGAlefHandle engine = cberg_create_engine(config_handle);
    cberg_crawl_config_free(config_handle);
    assert(engine != 0 && "failed to create engine");
    const char* mock_per_fixture = getenv("MOCK_SERVER_METADATA_RESPONSE_HEADERS");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/metadata_response_headers", mock_base);
    }
    CBERGAlefHandle result = cberg_scrape(engine, url);
    assert(result != 0 && "expected call to succeed");
    uint16_t status_code = cberg_scrape_result_status_code(result);
    CBERGAlefHandle response_meta_handle = cberg_scrape_result_response_meta(result);
    assert(response_meta_handle != 0);
    char* response_headers_etag = cberg_response_meta_etag(response_meta_handle);
    char* response_headers_last_modified = cberg_response_meta_last_modified(response_meta_handle);
    char* response_headers_server = cberg_response_meta_server(response_meta_handle);
    char* response_headers_content_language = cberg_response_meta_content_language(response_meta_handle);
    assert(status_code == 200 && "equals assertion failed");
    assert(response_headers_etag != NULL && strstr(response_headers_etag, "abc123") != NULL && "expected to contain substring");
    assert(response_headers_last_modified != NULL && strstr(response_headers_last_modified, "01 Jan 2025") != NULL && "expected to contain substring");
    assert(response_headers_server != NULL && strstr(response_headers_server, "nginx") != NULL && "expected to contain substring");
    assert(response_headers_content_language != NULL && strcmp(response_headers_content_language, "en-US") == 0 && "equals assertion failed");
    cberg_free_string(response_headers_etag);
    cberg_free_string(response_headers_last_modified);
    cberg_free_string(response_headers_server);
    cberg_free_string(response_headers_content_language);
    cberg_response_meta_free(response_meta_handle);
    cberg_scrape_result_free(result);
    cberg_crawl_engine_handle_free(engine);
}

void test_metadata_word_count(void) {
    /* Computes word count from visible page text */
    CBERGAlefHandle config_handle = cberg_crawl_config_from_json("{}");
    assert(config_handle != 0 && "failed to parse config");
    CBERGAlefHandle engine = cberg_create_engine(config_handle);
    cberg_crawl_config_free(config_handle);
    assert(engine != 0 && "failed to create engine");
    const char* mock_per_fixture = getenv("MOCK_SERVER_METADATA_WORD_COUNT");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/metadata_word_count", mock_base);
    }
    CBERGAlefHandle result = cberg_scrape(engine, url);
    assert(result != 0 && "expected call to succeed");
    uint16_t status_code = cberg_scrape_result_status_code(result);
    CBERGAlefHandle metadata_handle = cberg_scrape_result_metadata(result);
    assert(metadata_handle != 0);
    uintptr_t metadata_word_count = cberg_page_metadata_word_count(metadata_handle);
    assert(status_code == 200 && "equals assertion failed");
    assert(metadata_word_count > 99 && "expected greater than");
    assert(metadata_word_count < 301 && "expected less than");
    cberg_page_metadata_free(metadata_handle);
    cberg_scrape_result_free(result);
    cberg_crawl_engine_handle_free(engine);
}
