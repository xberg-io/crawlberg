/*
 * This file is auto-generated by alef — DO NOT EDIT.
 * alef:hash:fd3ef8056fd9d684efe8d3c38286a11aa7b4c190b8ffe12d42fda2bad5795fac
 * To regenerate: alef generate
 * To verify freshness: alef verify
 */
/* E2e tests for category: batch */

#include <assert.h>
#include <stdint.h>
#include <string.h>
#include <stdio.h>
#include <stdlib.h>
#include "crawlberg.h"
#include "test_runner.h"

void test_batch_crawl_basic(void) {
    /* Batch crawl of 2 seed URLs with links to discover */
    CBERGAlefHandle config_handle = cberg_crawl_config_from_json("{\"max_depth\":1,\"respect_robots_txt\":false}");
    assert(config_handle != 0 && "failed to parse config");
    CBERGAlefHandle engine = cberg_create_engine(config_handle);
    cberg_crawl_config_free(config_handle);
    assert(engine != 0 && "failed to create engine");
    const char* mock_per_fixture = getenv("MOCK_SERVER_BATCH_CRAWL_BASIC");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/batch_crawl_basic", mock_base);
    }
    CBERGAlefHandle result = cberg_batch_crawl(engine, url);
    if (result != 0) cberg_batch_crawl_results_free(result);
    cberg_crawl_engine_handle_free(engine);
}

void test_batch_crawl_partial_failure(void) {
    /* Batch crawl where one seed URL returns 404 error */
    CBERGAlefHandle config_handle = cberg_crawl_config_from_json("{\"max_depth\":1,\"respect_robots_txt\":false}");
    assert(config_handle != 0 && "failed to parse config");
    CBERGAlefHandle engine = cberg_create_engine(config_handle);
    cberg_crawl_config_free(config_handle);
    assert(engine != 0 && "failed to create engine");
    const char* mock_per_fixture = getenv("MOCK_SERVER_BATCH_CRAWL_PARTIAL_FAILURE");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/batch_crawl_partial_failure", mock_base);
    }
    CBERGAlefHandle result = cberg_batch_crawl(engine, url);
    if (result != 0) cberg_batch_crawl_results_free(result);
    cberg_crawl_engine_handle_free(engine);
}

void test_batch_crawl_with_config(void) {
    /* Batch crawl with max_depth=1 config verifying pages are discovered */
    CBERGAlefHandle config_handle = cberg_crawl_config_from_json("{\"max_depth\":1,\"respect_robots_txt\":false}");
    assert(config_handle != 0 && "failed to parse config");
    CBERGAlefHandle engine = cberg_create_engine(config_handle);
    cberg_crawl_config_free(config_handle);
    assert(engine != 0 && "failed to create engine");
    const char* mock_per_fixture = getenv("MOCK_SERVER_BATCH_CRAWL_WITH_CONFIG");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/batch_crawl_with_config", mock_base);
    }
    CBERGAlefHandle result = cberg_batch_crawl(engine, url);
    if (result != 0) cberg_batch_crawl_results_free(result);
    cberg_crawl_engine_handle_free(engine);
}

void test_batch_scrape_empty_urls_error(void) {
    /* Batch scrape with empty batch_urls array returns error */
    CBERGAlefHandle config_handle = cberg_crawl_config_from_json("{}");
    if (config_handle == 0) { ALEF_TEST_PASS(); }
    CBERGAlefHandle engine = cberg_create_engine(config_handle);
    cberg_crawl_config_free(config_handle);
    if (engine == 0) { ALEF_TEST_PASS(); }
    const char* mock_per_fixture = getenv("MOCK_SERVER_BATCH_SCRAPE_EMPTY_URLS_ERROR");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/batch_scrape_empty_urls_error", mock_base);
    }
    CBERGAlefHandle result = cberg_batch_scrape(engine, url);
    if (result != 0) cberg_batch_scrape_results_free(result);
    cberg_crawl_engine_handle_free(engine);
}

void test_batch_scrape_with_config(void) {
    /* Batch scrape with aggressive preprocessing configuration */
    CBERGAlefHandle config_handle = cberg_crawl_config_from_json("{\"content\":{\"preprocessing_preset\":\"aggressive\"}}");
    assert(config_handle != 0 && "failed to parse config");
    CBERGAlefHandle engine = cberg_create_engine(config_handle);
    cberg_crawl_config_free(config_handle);
    assert(engine != 0 && "failed to create engine");
    const char* mock_per_fixture = getenv("MOCK_SERVER_BATCH_SCRAPE_WITH_CONFIG");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/batch_scrape_with_config", mock_base);
    }
    CBERGAlefHandle result = cberg_batch_scrape(engine, url);
    if (result != 0) cberg_batch_scrape_results_free(result);
    cberg_crawl_engine_handle_free(engine);
}

void test_scrape_batch_basic(void) {
    /* Batch scrape of multiple URLs all succeeding */
    CBERGAlefHandle config_handle = cberg_crawl_config_from_json("{}");
    assert(config_handle != 0 && "failed to parse config");
    CBERGAlefHandle engine = cberg_create_engine(config_handle);
    cberg_crawl_config_free(config_handle);
    assert(engine != 0 && "failed to create engine");
    const char* mock_per_fixture = getenv("MOCK_SERVER_SCRAPE_BATCH_BASIC");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/scrape_batch_basic", mock_base);
    }
    CBERGAlefHandle result = cberg_batch_scrape(engine, url);
    if (result != 0) cberg_batch_scrape_results_free(result);
    cberg_crawl_engine_handle_free(engine);
}

void test_scrape_batch_partial_failure(void) {
    /* Batch scrape with one URL failing returns partial results */
    CBERGAlefHandle config_handle = cberg_crawl_config_from_json("{}");
    assert(config_handle != 0 && "failed to parse config");
    CBERGAlefHandle engine = cberg_create_engine(config_handle);
    cberg_crawl_config_free(config_handle);
    assert(engine != 0 && "failed to create engine");
    const char* mock_per_fixture = getenv("MOCK_SERVER_SCRAPE_BATCH_PARTIAL_FAILURE");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/scrape_batch_partial_failure", mock_base);
    }
    CBERGAlefHandle result = cberg_batch_scrape(engine, url);
    if (result != 0) cberg_batch_scrape_results_free(result);
    cberg_crawl_engine_handle_free(engine);
}

void test_scrape_batch_progress(void) {
    /* Batch scrape of 2 URLs completes with 2 results */
    CBERGAlefHandle config_handle = cberg_crawl_config_from_json("{}");
    assert(config_handle != 0 && "failed to parse config");
    CBERGAlefHandle engine = cberg_create_engine(config_handle);
    cberg_crawl_config_free(config_handle);
    assert(engine != 0 && "failed to create engine");
    const char* mock_per_fixture = getenv("MOCK_SERVER_SCRAPE_BATCH_PROGRESS");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/scrape_batch_progress", mock_base);
    }
    CBERGAlefHandle result = cberg_batch_scrape(engine, url);
    if (result != 0) cberg_batch_scrape_results_free(result);
    cberg_crawl_engine_handle_free(engine);
}
