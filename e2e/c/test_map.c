/*
 * This file is auto-generated by alef — DO NOT EDIT.
 * alef:hash:6ea0384d6739fea5c39b6cb89aaf011a80ecc026e8f4ef4847e632dfc6f33329
 * To regenerate: alef generate
 * To verify freshness: alef verify
 */
/* E2e tests for category: map */

#include <assert.h>
#include <stdint.h>
#include <string.h>
#include <stdio.h>
#include <stdlib.h>
#include "crawlberg.h"
#include "test_runner.h"

void test_map_discover_urls(void) {
    /* Discovers all URLs on a site without fetching full content */
    CBERGAlefHandle config_handle = cberg_crawl_config_from_json("{\"max_depth\":0,\"respect_robots_txt\":false}");
    assert(config_handle != 0 && "failed to parse config");
    CBERGAlefHandle engine = cberg_create_engine(config_handle);
    cberg_crawl_config_free(config_handle);
    assert(engine != 0 && "failed to create engine");
    const char* mock_per_fixture = getenv("MOCK_SERVER_MAP_DISCOVER_URLS");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/map_discover_urls", mock_base);
    }
    CBERGAlefHandle result = cberg_map_urls(engine, url);
    assert(result != 0 && "expected call to succeed");
    char* urls_json = cberg_map_result_urls(result);
    int urls_length = alef_json_array_count(urls_json);
    assert(urls_length >= 3 && "expected greater than or equal");
    cberg_free_string(urls_json);
    cberg_map_result_free(result);
    cberg_crawl_engine_handle_free(engine);
}

void test_map_exclude_patterns(void) {
    /* Excludes URLs matching patterns from URL map */
    CBERGAlefHandle config_handle = cberg_crawl_config_from_json("{\"exclude_paths\":[\"/private/.*\",\"/api/.*\"],\"max_depth\":0,\"respect_robots_txt\":false}");
    assert(config_handle != 0 && "failed to parse config");
    CBERGAlefHandle engine = cberg_create_engine(config_handle);
    cberg_crawl_config_free(config_handle);
    assert(engine != 0 && "failed to create engine");
    const char* mock_per_fixture = getenv("MOCK_SERVER_MAP_EXCLUDE_PATTERNS");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/map_exclude_patterns", mock_base);
    }
    CBERGAlefHandle result = cberg_map_urls(engine, url);
    assert(result != 0 && "expected call to succeed");
    char* urls_json = cberg_map_result_urls(result);
    int urls_length = alef_json_array_count(urls_json);
    assert(urls_length == 1 && "equals assertion failed");
    cberg_free_string(urls_json);
    cberg_map_result_free(result);
    cberg_crawl_engine_handle_free(engine);
}

void test_map_include_subdomains(void) {
    /* Includes subdomain URLs in URL map discovery; page has 1 local and 1 subdomain link */
    CBERGAlefHandle config_handle = cberg_crawl_config_from_json("{\"allow_subdomains\":true,\"max_depth\":0,\"respect_robots_txt\":false}");
    assert(config_handle != 0 && "failed to parse config");
    CBERGAlefHandle engine = cberg_create_engine(config_handle);
    cberg_crawl_config_free(config_handle);
    assert(engine != 0 && "failed to create engine");
    const char* mock_per_fixture = getenv("MOCK_SERVER_MAP_INCLUDE_SUBDOMAINS");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/map_include_subdomains", mock_base);
    }
    CBERGAlefHandle result = cberg_map_urls(engine, url);
    assert(result != 0 && "expected call to succeed");
    char* urls_json = cberg_map_result_urls(result);
    int urls_length = alef_json_array_count(urls_json);
    assert(urls_length >= 2 && "expected greater than or equal");
    cberg_free_string(urls_json);
    cberg_map_result_free(result);
    cberg_crawl_engine_handle_free(engine);
}

void test_map_large_sitemap(void) {
    /* Handles large sitemap with 100+ URLs */
    CBERGAlefHandle config_handle = cberg_crawl_config_from_json("{\"respect_robots_txt\":false}");
    assert(config_handle != 0 && "failed to parse config");
    CBERGAlefHandle engine = cberg_create_engine(config_handle);
    cberg_crawl_config_free(config_handle);
    assert(engine != 0 && "failed to create engine");
    const char* mock_per_fixture = getenv("MOCK_SERVER_MAP_LARGE_SITEMAP");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/map_large_sitemap", mock_base);
    }
    CBERGAlefHandle result = cberg_map_urls(engine, url);
    assert(result != 0 && "expected call to succeed");
    char* urls_json = cberg_map_result_urls(result);
    int urls_length = alef_json_array_count(urls_json);
    assert(urls_length >= 100 && "expected greater than or equal");
    cberg_free_string(urls_json);
    cberg_map_result_free(result);
    cberg_crawl_engine_handle_free(engine);
}

void test_map_limit_pagination(void) {
    /* Limits map result count to specified maximum */
    CBERGAlefHandle config_handle = cberg_crawl_config_from_json("{\"map_limit\":5,\"max_depth\":0,\"respect_robots_txt\":false}");
    assert(config_handle != 0 && "failed to parse config");
    CBERGAlefHandle engine = cberg_create_engine(config_handle);
    cberg_crawl_config_free(config_handle);
    assert(engine != 0 && "failed to create engine");
    const char* mock_per_fixture = getenv("MOCK_SERVER_MAP_LIMIT_PAGINATION");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/map_limit_pagination", mock_base);
    }
    CBERGAlefHandle result = cberg_map_urls(engine, url);
    assert(result != 0 && "expected call to succeed");
    char* urls_json = cberg_map_result_urls(result);
    int urls_length = alef_json_array_count(urls_json);
    assert(urls_length <= 5 && "expected less than or equal");
    cberg_free_string(urls_json);
    cberg_map_result_free(result);
    cberg_crawl_engine_handle_free(engine);
}

void test_map_search_filter(void) {
    /* Filters map results by search keyword; 4 links in page but only 2 match 'blog' */
    CBERGAlefHandle config_handle = cberg_crawl_config_from_json("{\"map_search\":\"blog\",\"max_depth\":0,\"respect_robots_txt\":false}");
    assert(config_handle != 0 && "failed to parse config");
    CBERGAlefHandle engine = cberg_create_engine(config_handle);
    cberg_crawl_config_free(config_handle);
    assert(engine != 0 && "failed to create engine");
    const char* mock_per_fixture = getenv("MOCK_SERVER_MAP_SEARCH_FILTER");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/map_search_filter", mock_base);
    }
    CBERGAlefHandle result = cberg_map_urls(engine, url);
    assert(result != 0 && "expected call to succeed");
    char* urls_json = cberg_map_result_urls(result);
    int urls_length = alef_json_array_count(urls_json);
    assert(urls_length >= 2 && "expected greater than or equal");
    assert(urls_length <= 2 && "expected less than or equal");
    cberg_free_string(urls_json);
    cberg_map_result_free(result);
    cberg_crawl_engine_handle_free(engine);
}
