/*
 * This file is auto-generated by alef — DO NOT EDIT.
 * alef:hash:a6c5b7be463dfa57d6d8213b1cf37787d6280065371826c7ed156bf2799a4572
 * To regenerate: alef generate
 * To verify freshness: alef verify
 */
/* E2e tests for category: filter */

#include <assert.h>
#include <stdint.h>
#include <string.h>
#include <stdio.h>
#include <stdlib.h>
#include "crawlberg.h"
#include "test_runner.h"

void test_filter_bm25_crawl_integration(void) {
    /* BM25 filter works during multi-page crawl, keeping relevant pages */
    CBERGAlefHandle config_handle = cberg_crawl_config_from_json("{\"bm25_query\":\"rust programming\",\"bm25_threshold\":0.1,\"content_filter\":\"bm25\",\"max_concurrent\":1,\"max_depth\":1}");
    assert(config_handle != 0 && "failed to parse config");
    CBERGAlefHandle engine = cberg_create_engine(config_handle);
    cberg_crawl_config_free(config_handle);
    assert(engine != 0 && "failed to create engine");
    const char* mock_per_fixture = getenv("MOCK_SERVER_FILTER_BM25_CRAWL_INTEGRATION");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/filter_bm25_crawl_integration", mock_base);
    }
    CBERGAlefHandle result = cberg_crawl(engine, url);
    assert(result != 0 && "expected call to succeed");
    char* pages_json = cberg_crawl_result_pages(result);
    assert(pages_json != NULL);
    char* filter_remaining_contain_keyword = alef_json_get_string(pages_json, "html");
    assert(filter_remaining_contain_keyword != NULL && strstr(filter_remaining_contain_keyword, "rust") != NULL && "expected to contain substring");
    free(filter_remaining_contain_keyword);
    cberg_free_string(pages_json);
    cberg_crawl_result_free(result);
    cberg_crawl_engine_handle_free(engine);
}

void test_filter_bm25_empty_query(void) {
    /* BM25 filter with empty query passes all pages through */
    CBERGAlefHandle config_handle = cberg_crawl_config_from_json("{\"bm25_query\":\"\",\"bm25_threshold\":0.1,\"content_filter\":\"bm25\",\"max_depth\":1}");
    assert(config_handle != 0 && "failed to parse config");
    CBERGAlefHandle engine = cberg_create_engine(config_handle);
    cberg_crawl_config_free(config_handle);
    assert(engine != 0 && "failed to create engine");
    const char* mock_per_fixture = getenv("MOCK_SERVER_FILTER_BM25_EMPTY_QUERY");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/filter_bm25_empty_query", mock_base);
    }
    CBERGAlefHandle result = cberg_crawl(engine, url);
    assert(result != 0 && "expected call to succeed");
    char* pages_json = cberg_crawl_result_pages(result);
    int crawl_pages_crawled = alef_json_array_count(pages_json);
    assert(crawl_pages_crawled == 2 && "equals assertion failed");
    cberg_free_string(pages_json);
    cberg_crawl_result_free(result);
    cberg_crawl_engine_handle_free(engine);
}

void test_filter_bm25_high_threshold(void) {
    /* BM25 filter with very high threshold filters out all pages */
    CBERGAlefHandle config_handle = cberg_crawl_config_from_json("{\"bm25_query\":\"quantum physics\",\"bm25_threshold\":100.0,\"content_filter\":\"bm25\",\"max_depth\":1}");
    assert(config_handle != 0 && "failed to parse config");
    CBERGAlefHandle engine = cberg_create_engine(config_handle);
    cberg_crawl_config_free(config_handle);
    assert(engine != 0 && "failed to create engine");
    const char* mock_per_fixture = getenv("MOCK_SERVER_FILTER_BM25_HIGH_THRESHOLD");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/filter_bm25_high_threshold", mock_base);
    }
    CBERGAlefHandle result = cberg_crawl(engine, url);
    assert(result != 0 && "expected call to succeed");
    char* pages_json = cberg_crawl_result_pages(result);
    int filter_pages_after_filter = alef_json_array_count(pages_json);
    assert(filter_pages_after_filter == 0 && "equals assertion failed");
    cberg_free_string(pages_json);
    cberg_crawl_result_free(result);
    cberg_crawl_engine_handle_free(engine);
}

void test_filter_bm25_relevant_pages(void) {
    /* BM25 filter keeps only pages relevant to the query */
    CBERGAlefHandle config_handle = cberg_crawl_config_from_json("{\"bm25_query\":\"rust programming language\",\"bm25_threshold\":0.1,\"content_filter\":\"bm25\",\"max_depth\":1}");
    assert(config_handle != 0 && "failed to parse config");
    CBERGAlefHandle engine = cberg_create_engine(config_handle);
    cberg_crawl_config_free(config_handle);
    assert(engine != 0 && "failed to create engine");
    const char* mock_per_fixture = getenv("MOCK_SERVER_FILTER_BM25_RELEVANT_PAGES");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/filter_bm25_relevant_pages", mock_base);
    }
    CBERGAlefHandle result = cberg_crawl(engine, url);
    assert(result != 0 && "expected call to succeed");
    char* pages_json = cberg_crawl_result_pages(result);
    assert(pages_json != NULL);
    char* filter_remaining_contain_keyword = alef_json_get_string(pages_json, "html");
    assert(filter_remaining_contain_keyword != NULL && strstr(filter_remaining_contain_keyword, "rust") != NULL && "expected to contain substring");
    free(filter_remaining_contain_keyword);
    cberg_free_string(pages_json);
    cberg_crawl_result_free(result);
    cberg_crawl_engine_handle_free(engine);
}

void test_filter_bm25_threshold_zero(void) {
    /* BM25 filter with zero threshold passes all pages */
    CBERGAlefHandle config_handle = cberg_crawl_config_from_json("{\"bm25_query\":\"unrelated query\",\"bm25_threshold\":0.0,\"content_filter\":\"bm25\",\"max_depth\":1}");
    assert(config_handle != 0 && "failed to parse config");
    CBERGAlefHandle engine = cberg_create_engine(config_handle);
    cberg_crawl_config_free(config_handle);
    assert(engine != 0 && "failed to create engine");
    const char* mock_per_fixture = getenv("MOCK_SERVER_FILTER_BM25_THRESHOLD_ZERO");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/filter_bm25_threshold_zero", mock_base);
    }
    CBERGAlefHandle result = cberg_crawl(engine, url);
    assert(result != 0 && "expected call to succeed");
    char* pages_json = cberg_crawl_result_pages(result);
    int crawl_pages_crawled = alef_json_array_count(pages_json);
    assert(crawl_pages_crawled == 2 && "equals assertion failed");
    cberg_free_string(pages_json);
    cberg_crawl_result_free(result);
    cberg_crawl_engine_handle_free(engine);
}

void test_filter_noop_crawl_all_kept(void) {
    /* NoopFilter keeps all pages during a multi-page crawl */
    CBERGAlefHandle config_handle = cberg_crawl_config_from_json("{\"max_concurrent\":1,\"max_depth\":1}");
    assert(config_handle != 0 && "failed to parse config");
    CBERGAlefHandle engine = cberg_create_engine(config_handle);
    cberg_crawl_config_free(config_handle);
    assert(engine != 0 && "failed to create engine");
    const char* mock_per_fixture = getenv("MOCK_SERVER_FILTER_NOOP_CRAWL_ALL_KEPT");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/filter_noop_crawl_all_kept", mock_base);
    }
    CBERGAlefHandle result = cberg_crawl(engine, url);
    assert(result != 0 && "expected call to succeed");
    char* pages_json = cberg_crawl_result_pages(result);
    int filter_pages_after_filter = alef_json_array_count(pages_json);
    assert(filter_pages_after_filter == 3 && "equals assertion failed");
    cberg_free_string(pages_json);
    cberg_crawl_result_free(result);
    cberg_crawl_engine_handle_free(engine);
}

void test_filter_noop_passes_all(void) {
    /* No content filter passes all crawled pages through */
    CBERGAlefHandle config_handle = cberg_crawl_config_from_json("{\"max_depth\":1}");
    assert(config_handle != 0 && "failed to parse config");
    CBERGAlefHandle engine = cberg_create_engine(config_handle);
    cberg_crawl_config_free(config_handle);
    assert(engine != 0 && "failed to create engine");
    const char* mock_per_fixture = getenv("MOCK_SERVER_FILTER_NOOP_PASSES_ALL");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/filter_noop_passes_all", mock_base);
    }
    CBERGAlefHandle result = cberg_crawl(engine, url);
    assert(result != 0 && "expected call to succeed");
    char* pages_json = cberg_crawl_result_pages(result);
    int crawl_pages_crawled = alef_json_array_count(pages_json);
    assert(crawl_pages_crawled == 3 && "equals assertion failed");
    cberg_free_string(pages_json);
    cberg_crawl_result_free(result);
    cberg_crawl_engine_handle_free(engine);
}
