/*
 * This file is auto-generated by alef — DO NOT EDIT.
 * alef:hash:f3ca77026e45d3b1ae0c62da16e5dcfe2d30b090c04c62c7fda9c080d4cc513a
 * To regenerate: alef generate
 * To verify freshness: alef verify
 */
/* E2e tests for category: redirect */

#include <assert.h>
#include <stdint.h>
#include <string.h>
#include <stdio.h>
#include <stdlib.h>
#include "crawlberg.h"
#include "test_runner.h"

void test_redirect_301_permanent(void) {
    /* Follows 301 permanent redirect and returns final page content */
    CBERGAlefHandle config_handle = cberg_crawl_config_from_json("{\"respect_robots_txt\":false}");
    assert(config_handle != 0 && "failed to parse config");
    CBERGAlefHandle engine = cberg_create_engine(config_handle);
    cberg_crawl_config_free(config_handle);
    assert(engine != 0 && "failed to create engine");
    const char* mock_per_fixture = getenv("MOCK_SERVER_REDIRECT_301_PERMANENT");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/redirect_301_permanent", mock_base);
    }
    CBERGAlefHandle result = cberg_crawl(engine, url);
    assert(result != 0 && "expected call to succeed");
    char* final_url = cberg_crawl_result_final_url(result);
    uintptr_t redirect_count = cberg_crawl_result_redirect_count(result);
    assert(final_url != NULL && strstr(final_url, "/target") != NULL && "expected to contain substring");
    assert(redirect_count == 1 && "equals assertion failed");
    cberg_free_string(final_url);
    cberg_crawl_result_free(result);
    cberg_crawl_engine_handle_free(engine);
}

void test_redirect_302_found(void) {
    /* Follows 302 Found redirect correctly */
    CBERGAlefHandle config_handle = cberg_crawl_config_from_json("{\"respect_robots_txt\":false}");
    assert(config_handle != 0 && "failed to parse config");
    CBERGAlefHandle engine = cberg_create_engine(config_handle);
    cberg_crawl_config_free(config_handle);
    assert(engine != 0 && "failed to create engine");
    const char* mock_per_fixture = getenv("MOCK_SERVER_REDIRECT_302_FOUND");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/redirect_302_found", mock_base);
    }
    CBERGAlefHandle result = cberg_crawl(engine, url);
    assert(result != 0 && "expected call to succeed");
    char* final_url = cberg_crawl_result_final_url(result);
    uintptr_t redirect_count = cberg_crawl_result_redirect_count(result);
    assert(final_url != NULL && strstr(final_url, "/found-target") != NULL && "expected to contain substring");
    assert(redirect_count == 1 && "equals assertion failed");
    cberg_free_string(final_url);
    cberg_crawl_result_free(result);
    cberg_crawl_engine_handle_free(engine);
}

void test_redirect_303_see_other(void) {
    /* Follows 303 See Other redirect (method changes to GET) */
    CBERGAlefHandle config_handle = cberg_crawl_config_from_json("{\"respect_robots_txt\":false}");
    assert(config_handle != 0 && "failed to parse config");
    CBERGAlefHandle engine = cberg_create_engine(config_handle);
    cberg_crawl_config_free(config_handle);
    assert(engine != 0 && "failed to create engine");
    const char* mock_per_fixture = getenv("MOCK_SERVER_REDIRECT_303_SEE_OTHER");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/redirect_303_see_other", mock_base);
    }
    CBERGAlefHandle result = cberg_crawl(engine, url);
    assert(result != 0 && "expected call to succeed");
    char* final_url = cberg_crawl_result_final_url(result);
    uintptr_t redirect_count = cberg_crawl_result_redirect_count(result);
    assert(final_url != NULL && strstr(final_url, "/see-other") != NULL && "expected to contain substring");
    assert(redirect_count == 1 && "equals assertion failed");
    cberg_free_string(final_url);
    cberg_crawl_result_free(result);
    cberg_crawl_engine_handle_free(engine);
}

void test_redirect_307_temporary(void) {
    /* Follows 307 Temporary Redirect (preserves method) */
    CBERGAlefHandle config_handle = cberg_crawl_config_from_json("{\"respect_robots_txt\":false}");
    assert(config_handle != 0 && "failed to parse config");
    CBERGAlefHandle engine = cberg_create_engine(config_handle);
    cberg_crawl_config_free(config_handle);
    assert(engine != 0 && "failed to create engine");
    const char* mock_per_fixture = getenv("MOCK_SERVER_REDIRECT_307_TEMPORARY");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/redirect_307_temporary", mock_base);
    }
    CBERGAlefHandle result = cberg_crawl(engine, url);
    assert(result != 0 && "expected call to succeed");
    char* final_url = cberg_crawl_result_final_url(result);
    uintptr_t redirect_count = cberg_crawl_result_redirect_count(result);
    assert(final_url != NULL && strstr(final_url, "/temp-target") != NULL && "expected to contain substring");
    assert(redirect_count == 1 && "equals assertion failed");
    cberg_free_string(final_url);
    cberg_crawl_result_free(result);
    cberg_crawl_engine_handle_free(engine);
}

void test_redirect_308_permanent(void) {
    /* Follows 308 Permanent Redirect (preserves method) */
    CBERGAlefHandle config_handle = cberg_crawl_config_from_json("{\"respect_robots_txt\":false}");
    assert(config_handle != 0 && "failed to parse config");
    CBERGAlefHandle engine = cberg_create_engine(config_handle);
    cberg_crawl_config_free(config_handle);
    assert(engine != 0 && "failed to create engine");
    const char* mock_per_fixture = getenv("MOCK_SERVER_REDIRECT_308_PERMANENT");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/redirect_308_permanent", mock_base);
    }
    CBERGAlefHandle result = cberg_crawl(engine, url);
    assert(result != 0 && "expected call to succeed");
    char* final_url = cberg_crawl_result_final_url(result);
    uintptr_t redirect_count = cberg_crawl_result_redirect_count(result);
    assert(final_url != NULL && strstr(final_url, "/perm-target") != NULL && "expected to contain substring");
    assert(redirect_count == 1 && "equals assertion failed");
    cberg_free_string(final_url);
    cberg_crawl_result_free(result);
    cberg_crawl_engine_handle_free(engine);
}

void test_redirect_chain(void) {
    /* Follows a chain of redirects (301 -> 302 -> 200) */
    CBERGAlefHandle config_handle = cberg_crawl_config_from_json("{\"respect_robots_txt\":false}");
    assert(config_handle != 0 && "failed to parse config");
    CBERGAlefHandle engine = cberg_create_engine(config_handle);
    cberg_crawl_config_free(config_handle);
    assert(engine != 0 && "failed to create engine");
    const char* mock_per_fixture = getenv("MOCK_SERVER_REDIRECT_CHAIN");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/redirect_chain", mock_base);
    }
    CBERGAlefHandle result = cberg_crawl(engine, url);
    assert(result != 0 && "expected call to succeed");
    char* final_url = cberg_crawl_result_final_url(result);
    uintptr_t redirect_count = cberg_crawl_result_redirect_count(result);
    assert(final_url != NULL && strstr(final_url, "/step2") != NULL && "expected to contain substring");
    assert(redirect_count == 2 && "equals assertion failed");
    cberg_free_string(final_url);
    cberg_crawl_result_free(result);
    cberg_crawl_engine_handle_free(engine);
}

void test_redirect_cross_domain(void) {
    /* Reports cross-domain redirect target without following to external domain */
    CBERGAlefHandle config_handle = cberg_crawl_config_from_json("{\"respect_robots_txt\":false}");
    assert(config_handle != 0 && "failed to parse config");
    CBERGAlefHandle engine = cberg_create_engine(config_handle);
    cberg_crawl_config_free(config_handle);
    assert(engine != 0 && "failed to create engine");
    const char* mock_per_fixture = getenv("MOCK_SERVER_REDIRECT_CROSS_DOMAIN");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/redirect_cross_domain", mock_base);
    }
    CBERGAlefHandle result = cberg_crawl(engine, url);
    assert(result != 0 && "expected call to succeed");
    char* final_url = cberg_crawl_result_final_url(result);
    uintptr_t redirect_count = cberg_crawl_result_redirect_count(result);
    assert(final_url != NULL && strstr(final_url, "/external-redirect") != NULL && "expected to contain substring");
    assert(redirect_count == 1 && "equals assertion failed");
    cberg_free_string(final_url);
    cberg_crawl_result_free(result);
    cberg_crawl_engine_handle_free(engine);
}

void test_redirect_loop(void) {
    /* Detects redirect loop (A -> B -> A) and stops following without reaching a final page */
    CBERGAlefHandle config_handle = cberg_crawl_config_from_json("{\"respect_robots_txt\":false}");
    assert(config_handle != 0 && "failed to parse config");
    CBERGAlefHandle engine = cberg_create_engine(config_handle);
    cberg_crawl_config_free(config_handle);
    assert(engine != 0 && "failed to create engine");
    const char* mock_per_fixture = getenv("MOCK_SERVER_REDIRECT_LOOP");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/redirect_loop", mock_base);
    }
    CBERGAlefHandle result = cberg_crawl(engine, url);
    assert(result != 0 && "expected call to succeed");
    uintptr_t redirect_count = cberg_crawl_result_redirect_count(result);
    char* pages_json = cberg_crawl_result_pages(result);
    assert(pages_json != NULL);
    char* pages_0_json = alef_json_array_get_index(pages_json, 0);
    char* pages_0_status_code = alef_json_get_string(pages_0_json, "status_code");
    assert(redirect_count == 1 && "equals assertion failed");
    assert(pages_0_status_code != NULL && atoll(pages_0_status_code) == 302 && "equals assertion failed");
    free(pages_0_status_code);
    free(pages_0_json);
    cberg_free_string(pages_json);
    cberg_crawl_result_free(result);
    cberg_crawl_engine_handle_free(engine);
}

void test_redirect_max_exceeded(void) {
    /* Aborts when redirect count exceeds max_redirects limit */
    CBERGAlefHandle config_handle = cberg_crawl_config_from_json("{\"max_redirects\":2,\"respect_robots_txt\":false}");
    assert(config_handle != 0 && "failed to parse config");
    CBERGAlefHandle engine = cberg_create_engine(config_handle);
    cberg_crawl_config_free(config_handle);
    assert(engine != 0 && "failed to create engine");
    const char* mock_per_fixture = getenv("MOCK_SERVER_REDIRECT_MAX_EXCEEDED");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/redirect_max_exceeded", mock_base);
    }
    CBERGAlefHandle result = cberg_crawl(engine, url);
    assert(result != 0 && "expected call to succeed");
    uintptr_t redirect_count = cberg_crawl_result_redirect_count(result);
    char* pages_json = cberg_crawl_result_pages(result);
    assert(pages_json != NULL);
    char* pages_0_json = alef_json_array_get_index(pages_json, 0);
    char* pages_0_status_code = alef_json_get_string(pages_0_json, "status_code");
    assert(redirect_count == 2 && "equals assertion failed");
    assert(pages_0_status_code != NULL && atoll(pages_0_status_code) == 301 && "equals assertion failed");
    free(pages_0_status_code);
    free(pages_0_json);
    cberg_free_string(pages_json);
    cberg_crawl_result_free(result);
    cberg_crawl_engine_handle_free(engine);
}

void test_redirect_meta_refresh(void) {
    /* Follows HTML meta-refresh redirect to target page */
    CBERGAlefHandle config_handle = cberg_crawl_config_from_json("{\"respect_robots_txt\":false}");
    assert(config_handle != 0 && "failed to parse config");
    CBERGAlefHandle engine = cberg_create_engine(config_handle);
    cberg_crawl_config_free(config_handle);
    assert(engine != 0 && "failed to create engine");
    const char* mock_per_fixture = getenv("MOCK_SERVER_REDIRECT_META_REFRESH");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/redirect_meta_refresh", mock_base);
    }
    CBERGAlefHandle result = cberg_crawl(engine, url);
    assert(result != 0 && "expected call to succeed");
    char* final_url = cberg_crawl_result_final_url(result);
    uintptr_t redirect_count = cberg_crawl_result_redirect_count(result);
    assert(final_url != NULL && strstr(final_url, "/target") != NULL && "expected to contain substring");
    assert(redirect_count == 1 && "equals assertion failed");
    cberg_free_string(final_url);
    cberg_crawl_result_free(result);
    cberg_crawl_engine_handle_free(engine);
}

void test_redirect_refresh_header(void) {
    /* Handles HTTP Refresh header redirect */
    CBERGAlefHandle config_handle = cberg_crawl_config_from_json("{\"respect_robots_txt\":false}");
    assert(config_handle != 0 && "failed to parse config");
    CBERGAlefHandle engine = cberg_create_engine(config_handle);
    cberg_crawl_config_free(config_handle);
    assert(engine != 0 && "failed to create engine");
    const char* mock_per_fixture = getenv("MOCK_SERVER_REDIRECT_REFRESH_HEADER");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/redirect_refresh_header", mock_base);
    }
    CBERGAlefHandle result = cberg_crawl(engine, url);
    assert(result != 0 && "expected call to succeed");
    char* final_url = cberg_crawl_result_final_url(result);
    uintptr_t redirect_count = cberg_crawl_result_redirect_count(result);
    assert(final_url != NULL && strstr(final_url, "/refreshed") != NULL && "expected to contain substring");
    assert(redirect_count == 1 && "equals assertion failed");
    cberg_free_string(final_url);
    cberg_crawl_result_free(result);
    cberg_crawl_engine_handle_free(engine);
}

void test_redirect_to_404(void) {
    /* Redirect target returns 404 Not Found */
    CBERGAlefHandle config_handle = cberg_crawl_config_from_json("{\"respect_robots_txt\":false}");
    assert(config_handle != 0 && "failed to parse config");
    CBERGAlefHandle engine = cberg_create_engine(config_handle);
    cberg_crawl_config_free(config_handle);
    assert(engine != 0 && "failed to create engine");
    const char* mock_per_fixture = getenv("MOCK_SERVER_REDIRECT_TO_404");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/redirect_to_404", mock_base);
    }
    CBERGAlefHandle result = cberg_crawl(engine, url);
    assert(result != 0 && "expected call to succeed");
    char* final_url = cberg_crawl_result_final_url(result);
    uintptr_t redirect_count = cberg_crawl_result_redirect_count(result);
    char* error = cberg_crawl_result_error(result);
    assert(final_url != NULL && strstr(final_url, "/gone") != NULL && "expected to contain substring");
    assert(redirect_count == 1 && "equals assertion failed");
    assert(error != NULL && strstr(error, "404") != NULL && "expected to contain substring");
    cberg_free_string(final_url);
    cberg_free_string(error);
    cberg_crawl_result_free(result);
    cberg_crawl_engine_handle_free(engine);
}
