/*
 * This file is auto-generated by alef — DO NOT EDIT.
 * alef:hash:61033e7a2857d4934e440b17f03ba110aca62617c1bb1c8968475fa1d8dc7dbd
 * To regenerate: alef generate
 * To verify freshness: alef verify
 */
/* E2e tests for category: download */

#include <assert.h>
#include <stdint.h>
#include <string.h>
#include <stdio.h>
#include <stdlib.h>
#include "crawlberg.h"
#include "test_runner.h"

void test_download_basic_pdf(void) {
    /* Download a basic PDF document with download_documents enabled */
    CBERGAlefHandle config_handle = cberg_crawl_config_from_json("{\"download_documents\":true,\"respect_robots_txt\":false}");
    assert(config_handle != 0 && "failed to parse config");
    CBERGAlefHandle engine = cberg_create_engine(config_handle);
    cberg_crawl_config_free(config_handle);
    assert(engine != 0 && "failed to create engine");
    const char* mock_per_fixture = getenv("MOCK_SERVER_DOWNLOAD_BASIC_PDF");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/download_basic_pdf", mock_base);
    }
    CBERGAlefHandle result = cberg_scrape(engine, url);
    assert(result != 0 && "expected call to succeed");
    CBERGAlefHandle downloaded_document_handle = cberg_scrape_result_downloaded_document(result);
    assert(downloaded_document_handle != 0);
    char* downloaded_document_mime_type = cberg_downloaded_document_mime_type(downloaded_document_handle);
    assert(downloaded_document_mime_type != NULL && strcmp(downloaded_document_mime_type, "application/pdf") == 0 && "equals assertion failed");
    cberg_free_string(downloaded_document_mime_type);
    cberg_downloaded_document_free(downloaded_document_handle);
    cberg_scrape_result_free(result);
    cberg_crawl_engine_handle_free(engine);
}

void test_download_filename_extraction(void) {
    /* Extract filename from Content-Disposition header */
    CBERGAlefHandle config_handle = cberg_crawl_config_from_json("{\"download_documents\":true,\"respect_robots_txt\":false}");
    assert(config_handle != 0 && "failed to parse config");
    CBERGAlefHandle engine = cberg_create_engine(config_handle);
    cberg_crawl_config_free(config_handle);
    assert(engine != 0 && "failed to create engine");
    const char* mock_per_fixture = getenv("MOCK_SERVER_DOWNLOAD_FILENAME_EXTRACTION");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/download_filename_extraction", mock_base);
    }
    CBERGAlefHandle result = cberg_scrape(engine, url);
    assert(result != 0 && "expected call to succeed");
    CBERGAlefHandle downloaded_document_handle = cberg_scrape_result_downloaded_document(result);
    assert(downloaded_document_handle != 0);
    char* downloaded_document_mime_type = cberg_downloaded_document_mime_type(downloaded_document_handle);
    uint16_t status_code = cberg_scrape_result_status_code(result);
    assert(downloaded_document_mime_type != NULL && strcmp(downloaded_document_mime_type, "application/pdf") == 0 && "equals assertion failed");
    assert(status_code == 200 && "equals assertion failed");
    cberg_free_string(downloaded_document_mime_type);
    cberg_downloaded_document_free(downloaded_document_handle);
    cberg_scrape_result_free(result);
    cberg_crawl_engine_handle_free(engine);
}

void test_download_mime_filter(void) {
    /* Only download documents matching specified MIME types (PDF only, not DOCX) */
    CBERGAlefHandle config_handle = cberg_crawl_config_from_json("{\"document_mime_types\":[\"application/pdf\"],\"download_documents\":true,\"respect_robots_txt\":false}");
    assert(config_handle != 0 && "failed to parse config");
    CBERGAlefHandle engine = cberg_create_engine(config_handle);
    cberg_crawl_config_free(config_handle);
    assert(engine != 0 && "failed to create engine");
    const char* mock_per_fixture = getenv("MOCK_SERVER_DOWNLOAD_MIME_FILTER");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/download_mime_filter", mock_base);
    }
    CBERGAlefHandle result = cberg_scrape(engine, url);
    assert(result != 0 && "expected call to succeed");
    CBERGAlefHandle downloaded_document_handle = cberg_scrape_result_downloaded_document(result);
    assert(downloaded_document_handle != 0);
    char* downloaded_document_mime_type = cberg_downloaded_document_mime_type(downloaded_document_handle);
    assert(downloaded_document_mime_type != NULL && strcmp(downloaded_document_mime_type, "application/pdf") == 0 && "equals assertion failed");
    cberg_free_string(downloaded_document_mime_type);
    cberg_downloaded_document_free(downloaded_document_handle);
    cberg_scrape_result_free(result);
    cberg_crawl_engine_handle_free(engine);
}

void test_download_no_document(void) {
    /* HTML pages are not downloaded as documents even when download_documents is enabled */
    CBERGAlefHandle config_handle = cberg_crawl_config_from_json("{\"download_documents\":true,\"respect_robots_txt\":false}");
    assert(config_handle != 0 && "failed to parse config");
    CBERGAlefHandle engine = cberg_create_engine(config_handle);
    cberg_crawl_config_free(config_handle);
    assert(engine != 0 && "failed to create engine");
    const char* mock_per_fixture = getenv("MOCK_SERVER_DOWNLOAD_NO_DOCUMENT");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/download_no_document", mock_base);
    }
    CBERGAlefHandle result = cberg_scrape(engine, url);
    assert(result != 0 && "expected call to succeed");
    uint16_t status_code = cberg_scrape_result_status_code(result);
    assert(status_code == 200 && "equals assertion failed");
    cberg_scrape_result_free(result);
    cberg_crawl_engine_handle_free(engine);
}

void test_download_size_limit(void) {
    /* Reject documents exceeding the configured size limit */
    CBERGAlefHandle config_handle = cberg_crawl_config_from_json("{\"document_max_size\":100,\"download_documents\":true,\"respect_robots_txt\":false}");
    assert(config_handle != 0 && "failed to parse config");
    CBERGAlefHandle engine = cberg_create_engine(config_handle);
    cberg_crawl_config_free(config_handle);
    assert(engine != 0 && "failed to create engine");
    const char* mock_per_fixture = getenv("MOCK_SERVER_DOWNLOAD_SIZE_LIMIT");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/download_size_limit", mock_base);
    }
    CBERGAlefHandle result = cberg_scrape(engine, url);
    assert(result != 0 && "expected call to succeed");
    uint16_t status_code = cberg_scrape_result_status_code(result);
    assert(status_code == 200 && "equals assertion failed");
    cberg_scrape_result_free(result);
    cberg_crawl_engine_handle_free(engine);
}
