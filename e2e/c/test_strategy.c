/*
 * This file is auto-generated by alef — DO NOT EDIT.
 * alef:hash:8da67dfeaec67db3bdf7a6952f85fd8b596a7caf8148db67a5123dbbf9b0aea6
 * To regenerate: alef generate
 * To verify freshness: alef verify
 */
/* E2e tests for category: strategy */

#include <assert.h>
#include <stdint.h>
#include <string.h>
#include <stdio.h>
#include <stdlib.h>
#include "crawlberg.h"
#include "test_runner.h"

void test_strategy_adaptive_saturation(void) {
    /* Adaptive strategy stops early when encountering saturation (duplicate content) */
    CBERGAlefHandle config_handle = cberg_crawl_config_from_json("{\"crawl_strategy\":\"best_first\",\"max_concurrent\":1,\"max_depth\":2,\"respect_robots_txt\":false}");
    assert(config_handle != 0 && "failed to parse config");
    CBERGAlefHandle engine = cberg_create_engine(config_handle);
    cberg_crawl_config_free(config_handle);
    assert(engine != 0 && "failed to create engine");
    const char* mock_per_fixture = getenv("MOCK_SERVER_STRATEGY_ADAPTIVE_SATURATION");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/strategy_adaptive_saturation", mock_base);
    }
    CBERGAlefHandle result = cberg_crawl(engine, url);
    assert(result != 0 && "expected call to succeed");
    char* pages_json = cberg_crawl_result_pages(result);
    int crawl_pages_crawled = alef_json_array_count(pages_json);
    assert(crawl_pages_crawled >= 1 && "expected greater than or equal");
    cberg_free_string(pages_json);
    cberg_crawl_result_free(result);
    cberg_crawl_engine_handle_free(engine);
}

void test_strategy_adaptive_window(void) {
    /* Adaptive strategy crawls more pages when content is diverse */
    CBERGAlefHandle config_handle = cberg_crawl_config_from_json("{\"crawl_strategy\":\"best_first\",\"max_concurrent\":1,\"max_depth\":1,\"respect_robots_txt\":false}");
    assert(config_handle != 0 && "failed to parse config");
    CBERGAlefHandle engine = cberg_create_engine(config_handle);
    cberg_crawl_config_free(config_handle);
    assert(engine != 0 && "failed to create engine");
    const char* mock_per_fixture = getenv("MOCK_SERVER_STRATEGY_ADAPTIVE_WINDOW");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/strategy_adaptive_window", mock_base);
    }
    CBERGAlefHandle result = cberg_crawl(engine, url);
    assert(result != 0 && "expected call to succeed");
    char* pages_json = cberg_crawl_result_pages(result);
    int crawl_pages_crawled = alef_json_array_count(pages_json);
    assert(crawl_pages_crawled >= 3 && "expected greater than or equal");
    cberg_free_string(pages_json);
    cberg_crawl_result_free(result);
    cberg_crawl_engine_handle_free(engine);
}

void test_strategy_best_first_seed(void) {
    /* BestFirst strategy always processes the seed URL first */
    CBERGAlefHandle config_handle = cberg_crawl_config_from_json("{\"crawl_strategy\":\"best_first\",\"max_concurrent\":1,\"max_depth\":1}");
    assert(config_handle != 0 && "failed to parse config");
    CBERGAlefHandle engine = cberg_create_engine(config_handle);
    cberg_crawl_config_free(config_handle);
    assert(engine != 0 && "failed to create engine");
    const char* mock_per_fixture = getenv("MOCK_SERVER_STRATEGY_BEST_FIRST_SEED");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/strategy_best_first_seed", mock_base);
    }
    CBERGAlefHandle result = cberg_crawl(engine, url);
    assert(result != 0 && "expected call to succeed");
    char* pages_json = cberg_crawl_result_pages(result);
    int crawl_pages_crawled = alef_json_array_count(pages_json);
    char* pages_0_json = alef_json_array_get_index(pages_json, 0);
    char* strategy_first_page_url_contains = alef_json_get_string(pages_0_json, "url");
    assert(crawl_pages_crawled == 3 && "equals assertion failed");
    assert(strategy_first_page_url_contains != NULL && strstr(strategy_first_page_url_contains, "/page") == NULL && "expected non-null value without substring");
    free(strategy_first_page_url_contains);
    free(pages_0_json);
    cberg_free_string(pages_json);
    cberg_crawl_result_free(result);
    cberg_crawl_engine_handle_free(engine);
}

void test_strategy_bfs_default_order(void) {
    /* BFS strategy visits pages in breadth-first order */
    CBERGAlefHandle config_handle = cberg_crawl_config_from_json("{\"crawl_strategy\":\"bfs\",\"max_concurrent\":1,\"max_depth\":2}");
    assert(config_handle != 0 && "failed to parse config");
    CBERGAlefHandle engine = cberg_create_engine(config_handle);
    cberg_crawl_config_free(config_handle);
    assert(engine != 0 && "failed to create engine");
    const char* mock_per_fixture = getenv("MOCK_SERVER_STRATEGY_BFS_DEFAULT_ORDER");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/strategy_bfs_default_order", mock_base);
    }
    CBERGAlefHandle result = cberg_crawl(engine, url);
    assert(result != 0 && "expected call to succeed");
    char* pages_json = cberg_crawl_result_pages(result);
    int crawl_pages_crawled = alef_json_array_count(pages_json);
    char* pages_1_json = alef_json_array_get_index(pages_json, 1);
    char* strategy_page_1_url = alef_json_get_string(pages_1_json, "url");
    char* pages_2_json = alef_json_array_get_index(pages_json, 2);
    char* strategy_page_2_url = alef_json_get_string(pages_2_json, "url");
    char* pages_3_json = alef_json_array_get_index(pages_json, 3);
    char* strategy_page_3_url = alef_json_get_string(pages_3_json, "url");
    char* pages_4_json = alef_json_array_get_index(pages_json, 4);
    char* strategy_page_4_url = alef_json_get_string(pages_4_json, "url");
    assert(crawl_pages_crawled == 5 && "equals assertion failed");
    assert(strategy_page_1_url != NULL && strstr(strategy_page_1_url, "alpha") != NULL && "expected to contain substring");
    assert(strategy_page_2_url != NULL && strstr(strategy_page_2_url, "beta") != NULL && "expected to contain substring");
    assert(strategy_page_3_url != NULL && strstr(strategy_page_3_url, "one") != NULL && "expected to contain substring");
    assert(strategy_page_4_url != NULL && strstr(strategy_page_4_url, "two") != NULL && "expected to contain substring");
    free(strategy_page_1_url);
    free(strategy_page_2_url);
    free(strategy_page_3_url);
    free(strategy_page_4_url);
    free(pages_4_json);
    free(pages_3_json);
    free(pages_2_json);
    free(pages_1_json);
    cberg_free_string(pages_json);
    cberg_crawl_result_free(result);
    cberg_crawl_engine_handle_free(engine);
}

void test_strategy_dfs_depth_first(void) {
    /* DFS strategy visits pages in depth-first order */
    CBERGAlefHandle config_handle = cberg_crawl_config_from_json("{\"crawl_strategy\":\"dfs\",\"max_concurrent\":1,\"max_depth\":2}");
    assert(config_handle != 0 && "failed to parse config");
    CBERGAlefHandle engine = cberg_create_engine(config_handle);
    cberg_crawl_config_free(config_handle);
    assert(engine != 0 && "failed to create engine");
    const char* mock_per_fixture = getenv("MOCK_SERVER_STRATEGY_DFS_DEPTH_FIRST");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/strategy_dfs_depth_first", mock_base);
    }
    CBERGAlefHandle result = cberg_crawl(engine, url);
    assert(result != 0 && "expected call to succeed");
    char* pages_json = cberg_crawl_result_pages(result);
    int crawl_pages_crawled = alef_json_array_count(pages_json);
    char* pages_1_json = alef_json_array_get_index(pages_json, 1);
    char* strategy_page_1_url = alef_json_get_string(pages_1_json, "url");
    char* pages_2_json = alef_json_array_get_index(pages_json, 2);
    char* strategy_page_2_url = alef_json_get_string(pages_2_json, "url");
    char* pages_3_json = alef_json_array_get_index(pages_json, 3);
    char* strategy_page_3_url = alef_json_get_string(pages_3_json, "url");
    char* pages_4_json = alef_json_array_get_index(pages_json, 4);
    char* strategy_page_4_url = alef_json_get_string(pages_4_json, "url");
    assert(crawl_pages_crawled == 5 && "equals assertion failed");
    assert(strategy_page_1_url != NULL && strstr(strategy_page_1_url, "beta") != NULL && "expected to contain substring");
    assert(strategy_page_2_url != NULL && strstr(strategy_page_2_url, "two") != NULL && "expected to contain substring");
    assert(strategy_page_3_url != NULL && strstr(strategy_page_3_url, "alpha") != NULL && "expected to contain substring");
    assert(strategy_page_4_url != NULL && strstr(strategy_page_4_url, "one") != NULL && "expected to contain substring");
    free(strategy_page_1_url);
    free(strategy_page_2_url);
    free(strategy_page_3_url);
    free(strategy_page_4_url);
    free(pages_4_json);
    free(pages_3_json);
    free(pages_2_json);
    free(pages_1_json);
    cberg_free_string(pages_json);
    cberg_crawl_result_free(result);
    cberg_crawl_engine_handle_free(engine);
}
