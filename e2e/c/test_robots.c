/*
 * This file is auto-generated by alef — DO NOT EDIT.
 * alef:hash:f34d19e963b0cfaec86d57a082ffde2677ac38a48f4e4bc6283c47097450bc00
 * To regenerate: alef generate
 * To verify freshness: alef verify
 */
/* E2e tests for category: robots */

#include <assert.h>
#include <stdint.h>
#include <string.h>
#include <stdio.h>
#include <stdlib.h>
#include "crawlberg.h"
#include "test_runner.h"

void test_robots_allow_all(void) {
    /* Permissive robots.txt allows all paths */
    CBERGAlefHandle config_handle = cberg_crawl_config_from_json("{\"respect_robots_txt\":true}");
    assert(config_handle != 0 && "failed to parse config");
    CBERGAlefHandle engine = cberg_create_engine(config_handle);
    cberg_crawl_config_free(config_handle);
    assert(engine != 0 && "failed to create engine");
    const char* mock_per_fixture = getenv("MOCK_SERVER_ROBOTS_ALLOW_ALL");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/robots_allow_all", mock_base);
    }
    CBERGAlefHandle result = cberg_scrape(engine, url);
    assert(result != 0 && "expected call to succeed");
    int32_t robots_is_allowed = cberg_scrape_result_is_allowed(result);
    assert(robots_is_allowed == 1 && "equals assertion failed");
    cberg_scrape_result_free(result);
    cberg_crawl_engine_handle_free(engine);
}

void test_robots_allow_override(void) {
    /* Allow directive overrides Disallow for specific paths */
    CBERGAlefHandle config_handle = cberg_crawl_config_from_json("{\"respect_robots_txt\":true}");
    assert(config_handle != 0 && "failed to parse config");
    CBERGAlefHandle engine = cberg_create_engine(config_handle);
    cberg_crawl_config_free(config_handle);
    assert(engine != 0 && "failed to create engine");
    const char* mock_per_fixture = getenv("MOCK_SERVER_ROBOTS_ALLOW_OVERRIDE");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/robots_allow_override", mock_base);
    }
    CBERGAlefHandle result = cberg_scrape(engine, url);
    assert(result != 0 && "expected call to succeed");
    int32_t robots_is_allowed = cberg_scrape_result_is_allowed(result);
    assert(robots_is_allowed == 1 && "equals assertion failed");
    cberg_scrape_result_free(result);
    cberg_crawl_engine_handle_free(engine);
}

void test_robots_comments_handling(void) {
    /* Correctly parses robots.txt with inline and line comments */
    CBERGAlefHandle config_handle = cberg_crawl_config_from_json("{\"respect_robots_txt\":true,\"user_agent\":\"crawlberg\"}");
    assert(config_handle != 0 && "failed to parse config");
    CBERGAlefHandle engine = cberg_create_engine(config_handle);
    cberg_crawl_config_free(config_handle);
    assert(engine != 0 && "failed to create engine");
    const char* mock_per_fixture = getenv("MOCK_SERVER_ROBOTS_COMMENTS_HANDLING");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/robots_comments_handling", mock_base);
    }
    CBERGAlefHandle result = cberg_scrape(engine, url);
    assert(result != 0 && "expected call to succeed");
    int32_t robots_is_allowed = cberg_scrape_result_is_allowed(result);
    assert(robots_is_allowed == 1 && "equals assertion failed");
    cberg_scrape_result_free(result);
    cberg_crawl_engine_handle_free(engine);
}

void test_robots_crawl_delay(void) {
    /* Respects crawl-delay directive from robots.txt */
    CBERGAlefHandle config_handle = cberg_crawl_config_from_json("{\"respect_robots_txt\":true,\"user_agent\":\"crawlberg\"}");
    assert(config_handle != 0 && "failed to parse config");
    CBERGAlefHandle engine = cberg_create_engine(config_handle);
    cberg_crawl_config_free(config_handle);
    assert(engine != 0 && "failed to create engine");
    const char* mock_per_fixture = getenv("MOCK_SERVER_ROBOTS_CRAWL_DELAY");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/robots_crawl_delay", mock_base);
    }
    CBERGAlefHandle result = cberg_scrape(engine, url);
    assert(result != 0 && "expected call to succeed");
    uint64_t robots_crawl_delay = cberg_scrape_result_crawl_delay(result);
    assert((robots_crawl_delay == 0 || robots_crawl_delay == 2) && "equals assertion failed");
    cberg_scrape_result_free(result);
    cberg_crawl_engine_handle_free(engine);
}

void test_robots_crawl_disallow_blocks_child(void) {
    /* Crawl skips child URLs disallowed by robots.txt */
    CBERGAlefHandle config_handle = cberg_crawl_config_from_json("{\"max_depth\":1,\"respect_robots_txt\":true}");
    assert(config_handle != 0 && "failed to parse config");
    CBERGAlefHandle engine = cberg_create_engine(config_handle);
    cberg_crawl_config_free(config_handle);
    assert(engine != 0 && "failed to create engine");
    const char* mock_per_fixture = getenv("MOCK_SERVER_ROBOTS_CRAWL_DISALLOW_BLOCKS_CHILD");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/robots_crawl_disallow_blocks_child", mock_base);
    }
    CBERGAlefHandle result = cberg_crawl(engine, url);
    assert(result != 0 && "expected call to succeed");
    char* pages_json = cberg_crawl_result_pages(result);
    int pages_length = alef_json_array_count(pages_json);
    assert(pages_length == 2 && "equals assertion failed");
    cberg_free_string(pages_json);
    cberg_crawl_result_free(result);
    cberg_crawl_engine_handle_free(engine);
}

void test_robots_crawl_unreachable_5xx(void) {
    /* Crawl stops before any fetch when robots.txt is unreachable */
    CBERGAlefHandle config_handle = cberg_crawl_config_from_json("{\"max_depth\":1,\"respect_robots_txt\":true}");
    assert(config_handle != 0 && "failed to parse config");
    CBERGAlefHandle engine = cberg_create_engine(config_handle);
    cberg_crawl_config_free(config_handle);
    assert(engine != 0 && "failed to create engine");
    const char* mock_per_fixture = getenv("MOCK_SERVER_ROBOTS_CRAWL_UNREACHABLE_5XX");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/robots_crawl_unreachable_5xx", mock_base);
    }
    CBERGAlefHandle result = cberg_crawl(engine, url);
    assert(result != 0 && "expected call to succeed");
    char* pages_json = cberg_crawl_result_pages(result);
    int pages_length = alef_json_array_count(pages_json);
    int32_t content_was_skipped = cberg_crawl_result_was_skipped(result);
    char* error = cberg_crawl_result_error(result);
    assert(pages_length == 0 && "equals assertion failed");
    assert(content_was_skipped == 1 && "equals assertion failed");
    assert(error != NULL && strstr(error, "robots_unreachable") != NULL && "expected to contain substring");
    cberg_free_string(error);
    cberg_free_string(pages_json);
    cberg_crawl_result_free(result);
    cberg_crawl_engine_handle_free(engine);
}

void test_robots_disallow_path(void) {
    /* Robots.txt disallows specific paths */
    CBERGAlefHandle config_handle = cberg_crawl_config_from_json("{\"respect_robots_txt\":true}");
    assert(config_handle != 0 && "failed to parse config");
    CBERGAlefHandle engine = cberg_create_engine(config_handle);
    cberg_crawl_config_free(config_handle);
    assert(engine != 0 && "failed to create engine");
    const char* mock_per_fixture = getenv("MOCK_SERVER_ROBOTS_DISALLOW_PATH");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/robots_disallow_path", mock_base);
    }
    CBERGAlefHandle result = cberg_scrape(engine, url);
    assert(result != 0 && "expected call to succeed");
    int32_t robots_is_allowed = cberg_scrape_result_is_allowed(result);
    assert(robots_is_allowed == 0 && "equals assertion failed");
    cberg_scrape_result_free(result);
    cberg_crawl_engine_handle_free(engine);
}

void test_robots_meta_nofollow(void) {
    /* Detects nofollow meta robots tag and skips link extraction */
    CBERGAlefHandle config_handle = cberg_crawl_config_from_json("{\"respect_robots_txt\":true}");
    assert(config_handle != 0 && "failed to parse config");
    CBERGAlefHandle engine = cberg_create_engine(config_handle);
    cberg_crawl_config_free(config_handle);
    assert(engine != 0 && "failed to create engine");
    const char* mock_per_fixture = getenv("MOCK_SERVER_ROBOTS_META_NOFOLLOW");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/robots_meta_nofollow", mock_base);
    }
    CBERGAlefHandle result = cberg_scrape(engine, url);
    assert(result != 0 && "expected call to succeed");
    int32_t robots_nofollow_detected = cberg_scrape_result_nofollow_detected(result);
    assert(robots_nofollow_detected == 1 && "equals assertion failed");
    cberg_scrape_result_free(result);
    cberg_crawl_engine_handle_free(engine);
}

void test_robots_meta_noindex(void) {
    /* Detects noindex meta robots tag in HTML page */
    CBERGAlefHandle config_handle = cberg_crawl_config_from_json("{\"respect_robots_txt\":true}");
    assert(config_handle != 0 && "failed to parse config");
    CBERGAlefHandle engine = cberg_create_engine(config_handle);
    cberg_crawl_config_free(config_handle);
    assert(engine != 0 && "failed to create engine");
    const char* mock_per_fixture = getenv("MOCK_SERVER_ROBOTS_META_NOINDEX");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/robots_meta_noindex", mock_base);
    }
    CBERGAlefHandle result = cberg_scrape(engine, url);
    assert(result != 0 && "expected call to succeed");
    int32_t robots_noindex_detected = cberg_scrape_result_noindex_detected(result);
    assert(robots_noindex_detected == 1 && "equals assertion failed");
    cberg_scrape_result_free(result);
    cberg_crawl_engine_handle_free(engine);
}

void test_robots_missing_404(void) {
    /* Missing robots.txt (404) allows all crawling */
    CBERGAlefHandle config_handle = cberg_crawl_config_from_json("{\"respect_robots_txt\":true}");
    assert(config_handle != 0 && "failed to parse config");
    CBERGAlefHandle engine = cberg_create_engine(config_handle);
    cberg_crawl_config_free(config_handle);
    assert(engine != 0 && "failed to create engine");
    const char* mock_per_fixture = getenv("MOCK_SERVER_ROBOTS_MISSING_404");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/robots_missing_404", mock_base);
    }
    CBERGAlefHandle result = cberg_scrape(engine, url);
    assert(result != 0 && "expected call to succeed");
    int32_t robots_is_allowed = cberg_scrape_result_is_allowed(result);
    assert(robots_is_allowed == 1 && "equals assertion failed");
    cberg_scrape_result_free(result);
    cberg_crawl_engine_handle_free(engine);
}

void test_robots_multiple_user_agents(void) {
    /* Picks the most specific user-agent block from robots.txt */
    CBERGAlefHandle config_handle = cberg_crawl_config_from_json("{\"respect_robots_txt\":true,\"user_agent\":\"SpecificBot\"}");
    assert(config_handle != 0 && "failed to parse config");
    CBERGAlefHandle engine = cberg_create_engine(config_handle);
    cberg_crawl_config_free(config_handle);
    assert(engine != 0 && "failed to create engine");
    const char* mock_per_fixture = getenv("MOCK_SERVER_ROBOTS_MULTIPLE_USER_AGENTS");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/robots_multiple_user_agents", mock_base);
    }
    CBERGAlefHandle result = cberg_scrape(engine, url);
    assert(result != 0 && "expected call to succeed");
    int32_t robots_is_allowed = cberg_scrape_result_is_allowed(result);
    assert(robots_is_allowed == 1 && "equals assertion failed");
    cberg_scrape_result_free(result);
    cberg_crawl_engine_handle_free(engine);
}

void test_robots_request_rate(void) {
    /* Parses request-rate directive from robots.txt */
    CBERGAlefHandle config_handle = cberg_crawl_config_from_json("{\"respect_robots_txt\":true,\"user_agent\":\"crawlberg\"}");
    assert(config_handle != 0 && "failed to parse config");
    CBERGAlefHandle engine = cberg_create_engine(config_handle);
    cberg_crawl_config_free(config_handle);
    assert(engine != 0 && "failed to create engine");
    const char* mock_per_fixture = getenv("MOCK_SERVER_ROBOTS_REQUEST_RATE");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/robots_request_rate", mock_base);
    }
    CBERGAlefHandle result = cberg_scrape(engine, url);
    assert(result != 0 && "expected call to succeed");
    uint64_t robots_crawl_delay = cberg_scrape_result_crawl_delay(result);
    int32_t robots_is_allowed = cberg_scrape_result_is_allowed(result);
    assert((robots_crawl_delay == 0 || robots_crawl_delay == 5) && "equals assertion failed");
    assert(robots_is_allowed == 1 && "equals assertion failed");
    cberg_scrape_result_free(result);
    cberg_crawl_engine_handle_free(engine);
}

void test_robots_sitemap_directive(void) {
    /* Discovers sitemap URL from Sitemap directive in robots.txt */
    CBERGAlefHandle config_handle = cberg_crawl_config_from_json("{\"respect_robots_txt\":true}");
    assert(config_handle != 0 && "failed to parse config");
    CBERGAlefHandle engine = cberg_create_engine(config_handle);
    cberg_crawl_config_free(config_handle);
    assert(engine != 0 && "failed to create engine");
    const char* mock_per_fixture = getenv("MOCK_SERVER_ROBOTS_SITEMAP_DIRECTIVE");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/robots_sitemap_directive", mock_base);
    }
    CBERGAlefHandle result = cberg_scrape(engine, url);
    assert(result != 0 && "expected call to succeed");
    int32_t robots_is_allowed = cberg_scrape_result_is_allowed(result);
    assert(robots_is_allowed == 1 && "equals assertion failed");
    cberg_scrape_result_free(result);
    cberg_crawl_engine_handle_free(engine);
}

void test_robots_unreachable_5xx(void) {
    /* Unreachable robots.txt (503) reports is_allowed false */
    CBERGAlefHandle config_handle = cberg_crawl_config_from_json("{\"respect_robots_txt\":true}");
    assert(config_handle != 0 && "failed to parse config");
    CBERGAlefHandle engine = cberg_create_engine(config_handle);
    cberg_crawl_config_free(config_handle);
    assert(engine != 0 && "failed to create engine");
    const char* mock_per_fixture = getenv("MOCK_SERVER_ROBOTS_UNREACHABLE_5XX");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/robots_unreachable_5xx", mock_base);
    }
    CBERGAlefHandle result = cberg_scrape(engine, url);
    assert(result != 0 && "expected call to succeed");
    int32_t robots_is_allowed = cberg_scrape_result_is_allowed(result);
    assert(robots_is_allowed == 0 && "equals assertion failed");
    cberg_scrape_result_free(result);
    cberg_crawl_engine_handle_free(engine);
}

void test_robots_user_agent_specific(void) {
    /* Matches user-agent specific rules in robots.txt */
    CBERGAlefHandle config_handle = cberg_crawl_config_from_json("{\"respect_robots_txt\":true,\"user_agent\":\"CrawlbergBot\"}");
    assert(config_handle != 0 && "failed to parse config");
    CBERGAlefHandle engine = cberg_create_engine(config_handle);
    cberg_crawl_config_free(config_handle);
    assert(engine != 0 && "failed to create engine");
    const char* mock_per_fixture = getenv("MOCK_SERVER_ROBOTS_USER_AGENT_SPECIFIC");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/robots_user_agent_specific", mock_base);
    }
    CBERGAlefHandle result = cberg_scrape(engine, url);
    assert(result != 0 && "expected call to succeed");
    int32_t robots_is_allowed = cberg_scrape_result_is_allowed(result);
    assert(robots_is_allowed == 0 && "equals assertion failed");
    cberg_scrape_result_free(result);
    cberg_crawl_engine_handle_free(engine);
}

void test_robots_wildcard_paths(void) {
    /* Handles wildcard Disallow patterns in robots.txt */
    CBERGAlefHandle config_handle = cberg_crawl_config_from_json("{\"respect_robots_txt\":true}");
    assert(config_handle != 0 && "failed to parse config");
    CBERGAlefHandle engine = cberg_create_engine(config_handle);
    cberg_crawl_config_free(config_handle);
    assert(engine != 0 && "failed to create engine");
    const char* mock_per_fixture = getenv("MOCK_SERVER_ROBOTS_WILDCARD_PATHS");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/robots_wildcard_paths", mock_base);
    }
    CBERGAlefHandle result = cberg_scrape(engine, url);
    assert(result != 0 && "expected call to succeed");
    int32_t robots_is_allowed = cberg_scrape_result_is_allowed(result);
    assert(robots_is_allowed == 0 && "equals assertion failed");
    cberg_scrape_result_free(result);
    cberg_crawl_engine_handle_free(engine);
}

void test_robots_x_robots_tag(void) {
    /* Respects X-Robots-Tag HTTP header directives */
    CBERGAlefHandle config_handle = cberg_crawl_config_from_json("{\"respect_robots_txt\":true}");
    assert(config_handle != 0 && "failed to parse config");
    CBERGAlefHandle engine = cberg_create_engine(config_handle);
    cberg_crawl_config_free(config_handle);
    assert(engine != 0 && "failed to create engine");
    const char* mock_per_fixture = getenv("MOCK_SERVER_ROBOTS_X_ROBOTS_TAG");
    const char* mock_base = getenv("MOCK_SERVER_URL");
    char url[2048];
    if (mock_per_fixture && mock_per_fixture[0] != '\0') {
        snprintf(url, sizeof(url), "%s", mock_per_fixture);
    } else {
        assert(mock_base != NULL && "MOCK_SERVER_URL must be set");
        snprintf(url, sizeof(url), "%s/fixtures/robots_x_robots_tag", mock_base);
    }
    CBERGAlefHandle result = cberg_scrape(engine, url);
    assert(result != 0 && "expected call to succeed");
    char* robots_x_robots_tag = cberg_scrape_result_x_robots_tag(result);
    int32_t robots_noindex_detected = cberg_scrape_result_noindex_detected(result);
    int32_t robots_nofollow_detected = cberg_scrape_result_nofollow_detected(result);
    assert(robots_x_robots_tag != NULL && strcmp(robots_x_robots_tag, "noindex, nofollow") == 0 && "equals assertion failed");
    assert(robots_noindex_detected == 1 && "equals assertion failed");
    assert(robots_nofollow_detected == 1 && "equals assertion failed");
    cberg_free_string(robots_x_robots_tag);
    cberg_scrape_result_free(result);
    cberg_crawl_engine_handle_free(engine);
}
