// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:370c502464a76d03793075c12b7cc4f6b91af5016ac1f2d26ae93193870acd78
// To regenerate: alef generate
// To verify freshness: alef verify
package io.xberg.crawlberg.android.e2e

import org.junit.jupiter.api.Test
import kotlin.test.assertEquals
import kotlin.test.assertTrue
import kotlin.test.assertFalse
import kotlin.test.assertFailsWith
import kotlinx.coroutines.runBlocking
import io.xberg.crawlberg.android.Crawlberg

/** E2e tests for category: metadata. */
class MetadataTest {

    companion object {
        init {
            // Suite-level environment defaults from [e2e.env]. JVM OS env is
            // immutable; System.setProperty is the runtime-mutable analog. Each
            // entry uses setdefault semantics: only applied when not already set.
            if (System.getProperty("CRAWLBERG_ALLOW_PRIVATE_NETWORK") == null) {
                System.setProperty("CRAWLBERG_ALLOW_PRIVATE_NETWORK", "true")
            }
            try {
                System.loadLibrary("cberg_jni")
            } catch (e: UnsatisfiedLinkError) {
                System.err.println("Failed to load cberg_jni library: ${e.message}")
                val libPath = System.getProperty("java.library.path")
                System.err.println("java.library.path: $libPath")
                throw e
            }
        }
    }
    @Test
    fun testMetadataArticleTimes(): Unit = runBlocking {
        // Extracts article:published_time, modified_time, author, section, and tags
        val engine = Crawlberg.createEngine(null)
        val url = (System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") ?: "") + "/fixtures/metadata_article_times"
        val result = Crawlberg.scrape(engine, url)
        assertEquals(200, result.statusCode)
        assertEquals("2024-01-15T10:00:00Z", result.metadata.article?.publishedTime.orEmpty())
        assertEquals("2024-06-20T14:30:00Z", result.metadata.article?.modifiedTime.orEmpty())
        assertEquals("Jane Developer", result.metadata.article?.author.orEmpty())
        assertEquals("Technology", result.metadata.article?.section.orEmpty())
        assertEquals(3, result.metadata.article?.tags?.size!!)
    }

    @Test
    fun testMetadataFavicons(): Unit = runBlocking {
        // Extracts favicon link tags including apple-touch-icon
        val engine = Crawlberg.createEngine(null)
        val url = (System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") ?: "") + "/fixtures/metadata_favicons"
        val result = Crawlberg.scrape(engine, url)
        assertEquals(200, result.statusCode)
        assertEquals(5, result.metadata.favicons?.size!!)
        assertTrue(result.metadata.favicons.orEmpty().any { e -> e.rel.toString().contains("apple-touch-icon") }, "expected to contain: " + "apple-touch-icon")
    }

    @Test
    fun testMetadataHeadings(): Unit = runBlocking {
        // Extracts heading hierarchy (h1-h6) from HTML page
        val engine = Crawlberg.createEngine(null)
        val url = (System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") ?: "") + "/fixtures/metadata_headings"
        val result = Crawlberg.scrape(engine, url)
        assertEquals(200, result.statusCode)
        assertEquals(1, result.metadata.headings?.first()?.level!!)
        assertEquals("Primary Heading", result.metadata.headings?.first()?.text.orEmpty())
        assertEquals(8, result.metadata.headings?.size!!)
    }

    @Test
    fun testMetadataHreflang(): Unit = runBlocking {
        // Extracts hreflang alternate link tags
        val engine = Crawlberg.createEngine(null)
        val url = (System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") ?: "") + "/fixtures/metadata_hreflang"
        val result = Crawlberg.scrape(engine, url)
        assertEquals(200, result.statusCode)
        assertEquals(4, result.metadata.hreflangs?.size!!)
        assertTrue(result.metadata.hreflangs.orEmpty().any { e -> e.lang.toString().contains("en") }, "expected to contain: " + "en")
    }

    @Test
    fun testMetadataKeywordsAuthor(): Unit = runBlocking {
        // Extracts keywords, author, viewport, generator, theme-color, robots, lang, dir metadata
        val engine = Crawlberg.createEngine(null)
        val url = (System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") ?: "") + "/fixtures/metadata_keywords_author"
        val result = Crawlberg.scrape(engine, url)
        assertEquals(200, result.statusCode)
        assertEquals("Comprehensive Metadata Test Page", result.metadata.title.orEmpty())
        assertTrue(result.metadata.canonicalUrl != null, "expected non-empty value")
        assertTrue(result.metadata.keywords != null, "expected non-empty value")
        assertTrue(result.metadata.keywords.orEmpty().contains("rust"), "expected to contain: " + "rust")
        assertEquals("Jane Developer", result.metadata.author.orEmpty())
        assertTrue(result.metadata.viewport != null, "expected non-empty value")
        assertEquals("crawlberg/1.0", result.metadata.generator.orEmpty())
        assertEquals("#ff6600", result.metadata.themeColor.orEmpty())
        assertEquals("index, follow", result.metadata.robots.orEmpty())
        assertEquals("en", result.metadata.htmlLang.orEmpty())
        assertEquals("ltr", result.metadata.htmlDir.orEmpty())
    }

    @Test
    fun testMetadataOgVideoAudio(): Unit = runBlocking {
        // Extracts og:video, og:audio, and og:locale:alternate metadata
        val engine = Crawlberg.createEngine(null)
        val url = (System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") ?: "") + "/fixtures/metadata_og_video_audio"
        val result = Crawlberg.scrape(engine, url)
        assertEquals(200, result.statusCode)
        assertEquals("https://example.com/video.mp4", result.metadata.ogVideo.orEmpty())
        assertEquals("https://example.com/audio.mp3", result.metadata.ogAudio.orEmpty())
        assertEquals(2, result.metadata.ogLocaleAlternates?.size!!)
    }

    @Test
    fun testMetadataResponseHeaders(): Unit = runBlocking {
        // Extracts response metadata from HTTP headers (etag, server, content-language)
        val engine = Crawlberg.createEngine(null)
        val url = (System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") ?: "") + "/fixtures/metadata_response_headers"
        val result = Crawlberg.scrape(engine, url)
        assertEquals(200, result.statusCode)
        assertTrue(result.responseMeta?.etag.orEmpty().contains("abc123"), "expected to contain: " + "abc123")
        assertTrue(result.responseMeta?.lastModified.orEmpty().contains("01 Jan 2025"), "expected to contain: " + "01 Jan 2025")
        assertTrue(result.responseMeta?.server.orEmpty().contains("nginx"), "expected to contain: " + "nginx")
        assertEquals("en-US", result.responseMeta?.contentLanguage.orEmpty())
    }

    @Test
    fun testMetadataWordCount(): Unit = runBlocking {
        // Computes word count from visible page text
        val engine = Crawlberg.createEngine(null)
        val url = (System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") ?: "") + "/fixtures/metadata_word_count"
        val result = Crawlberg.scrape(engine, url)
        assertEquals(200, result.statusCode)
        assertTrue(result.metadata.wordCount!! > 99, "expected > 99")
        assertTrue(result.metadata.wordCount!! < 301, "expected < 301")
    }

}
