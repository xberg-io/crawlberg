// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:ce2f58cfdbb8e9d0333cf9e9dc21e53f7df91d595eae50fa8d1eaf2612f8d03d
// To regenerate: alef generate
// To verify freshness: alef verify
package io.xberg.crawlberg.android.e2e

import org.junit.jupiter.api.Test
import kotlin.test.assertEquals
import kotlin.test.assertTrue
import kotlin.test.assertFalse
import kotlin.test.assertFailsWith
import kotlinx.coroutines.runBlocking
import io.xberg.crawlberg.android.Crawlberg
import com.fasterxml.jackson.databind.ObjectMapper
import com.fasterxml.jackson.datatype.jdk8.Jdk8Module
import com.fasterxml.jackson.module.kotlin.registerKotlinModule
import kotlin.time.Duration.Companion.milliseconds
import io.xberg.crawlberg.android.CrawlConfig

/** E2e tests for category: content. */
class ContentTest {

    companion object {
        init {
            // Suite-level environment defaults from [e2e.env]. JVM OS env is
            // immutable; System.setProperty is the runtime-mutable analog. Each
            // entry uses setdefault semantics: only applied when not already set.
            if (System.getProperty("CRAWLBERG_ALLOW_PRIVATE_NETWORK") == null) {
                System.setProperty("CRAWLBERG_ALLOW_PRIVATE_NETWORK", "true")
            }
            try {
                System.loadLibrary("cberg_jni")
            } catch (e: UnsatisfiedLinkError) {
                System.err.println("Failed to load cberg_jni library: ${e.message}")
                val libPath = System.getProperty("java.library.path")
                System.err.println("java.library.path: $libPath")
                throw e
            }
        }
        private val MAPPER = ObjectMapper().registerModule(Jdk8Module()).registerModule(
            com.fasterxml.jackson.databind.module.SimpleModule()
                .addSerializer(kotlin.time.Duration::class.java, object : com.fasterxml.jackson.databind.JsonSerializer<kotlin.time.Duration>() {
                    override fun serialize(value: kotlin.time.Duration, gen: com.fasterxml.jackson.core.JsonGenerator, serializers: com.fasterxml.jackson.databind.SerializerProvider) {
                        gen.writeNumber(value.inWholeMilliseconds)
                    }
                })
                .addDeserializer(kotlin.time.Duration::class.java, object : com.fasterxml.jackson.databind.JsonDeserializer<kotlin.time.Duration>() {
                    override fun deserialize(p: com.fasterxml.jackson.core.JsonParser, ctxt: com.fasterxml.jackson.databind.DeserializationContext): kotlin.time.Duration {
                        return p.longValue.milliseconds
                    }
                })
        ).registerKotlinModule().setPropertyNamingStrategy(com.fasterxml.jackson.databind.PropertyNamingStrategies.SNAKE_CASE)
    }
    @Test
    fun testContent204NoContent(): Unit = runBlocking {
        // Handles 204 No Content response gracefully
        val engine = Crawlberg.createEngine(null)
        val url = (System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") ?: "") + "/fixtures/content_204_no_content"
        val result = Crawlberg.scrape(engine, url)
        assertEquals(204, result.statusCode)
        assertTrue(result.html.isEmpty(), "expected empty value")
    }

    @Test
    fun testContentCharsetIso8859(): Unit = runBlocking {
        // Handles ISO-8859-1 encoded page correctly
        val engineConfig = MAPPER.readValue("{\"respect_robots_txt\":false,\"ssrf\":{}}", CrawlConfig::class.java)
        val engine = Crawlberg.createEngine(engineConfig)
        val url = (System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") ?: "") + "/fixtures/content_charset_iso8859"
        val result = Crawlberg.scrape(engine, url)
        assertEquals("iso-8859-1", result.detectedCharset.orEmpty())
    }

    @Test
    fun testContentEmptyBody(): Unit = runBlocking {
        // Handles 200 response with empty body gracefully
        val engineConfig = MAPPER.readValue("{\"respect_robots_txt\":false,\"ssrf\":{}}", CrawlConfig::class.java)
        val engine = Crawlberg.createEngine(engineConfig)
        val url = (System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") ?: "") + "/fixtures/content_empty_body"
        val result = Crawlberg.scrape(engine, url)
        assertEquals(200, result.statusCode)
    }

    @Test
    fun testContentGzipCompressed(): Unit = runBlocking {
        // Handles response with Accept-Encoding gzip negotiation
        val engineConfig = MAPPER.readValue("{\"respect_robots_txt\":false,\"ssrf\":{}}", CrawlConfig::class.java)
        val engine = Crawlberg.createEngine(engineConfig)
        val url = (System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") ?: "") + "/fixtures/content_gzip_compressed"
        val result = Crawlberg.scrape(engine, url)
        assertFalse(result.html.isEmpty(), "expected non-empty value")
        assertEquals(200, result.statusCode)
    }

    @Test
    fun testContentLargePageLimit(): Unit = runBlocking {
        // Respects max body size limit and truncates or skips oversized pages
        val engineConfig = MAPPER.readValue("{\"max_body_size\":1024,\"respect_robots_txt\":false,\"ssrf\":{}}", CrawlConfig::class.java)
        val engine = Crawlberg.createEngine(engineConfig)
        val url = (System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") ?: "") + "/fixtures/content_large_page_limit"
        val result = Crawlberg.scrape(engine, url)
        assertTrue(result.bodySize < 1025, "expected < 1025")
    }

    @Test
    fun testContentMainOnly(): Unit = runBlocking {
        // Extracts content with aggressive preprocessing, excluding nav, sidebar, footer
        val engineConfig = MAPPER.readValue("{\"content\":{\"preprocessing_preset\":\"aggressive\"},\"respect_robots_txt\":false,\"ssrf\":{}}", CrawlConfig::class.java)
        val engine = Crawlberg.createEngine(engineConfig)
        val url = (System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") ?: "") + "/fixtures/content_main_only"
        val result = Crawlberg.scrape(engine, url)
    }

    @Test
    fun testContentNonAsciiBodyLimit(): Unit = runBlocking {
        // Truncates a non-ASCII body at a UTF-8 character boundary
        val engineConfig = MAPPER.readValue("{\"max_body_size\":1,\"respect_robots_txt\":false,\"ssrf\":{}}", CrawlConfig::class.java)
        val engine = Crawlberg.createEngine(engineConfig)
        val url = (System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") ?: "") + "/fixtures/content_non_ascii_body_limit"
        val result = Crawlberg.scrape(engine, url)
        assertEquals(200, result.statusCode)
        assertEquals(0, result.bodySize)
    }

    @Test
    fun testContentPdfNoExtension(): Unit = runBlocking {
        // Detects PDF content by Content-Type header when URL has no .pdf extension
        val engineConfig = MAPPER.readValue("{\"respect_robots_txt\":false,\"ssrf\":{}}", CrawlConfig::class.java)
        val engine = Crawlberg.createEngine(engineConfig)
        val url = (System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") ?: "") + "/fixtures/content_pdf_no_extension"
        val result = Crawlberg.scrape(engine, url)
        assertEquals(true, result.isPdf)
    }

    @Test
    fun testContentRemoveTags(): Unit = runBlocking {
        // Removes specified HTML elements by CSS selector before processing
        val engineConfig = MAPPER.readValue("{\"remove_tags\":[\"nav\",\"aside\",\"footer\"],\"respect_robots_txt\":false,\"ssrf\":{}}", CrawlConfig::class.java)
        val engine = Crawlberg.createEngine(engineConfig)
        val url = (System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") ?: "") + "/fixtures/content_remove_tags"
        val result = Crawlberg.scrape(engine, url)
        assertFalse(result.html.isEmpty(), "expected non-empty value")
    }

    @Test
    fun testContentUtf8Bom(): Unit = runBlocking {
        // Handles UTF-8 content with BOM marker correctly
        val engineConfig = MAPPER.readValue("{\"respect_robots_txt\":false,\"ssrf\":{}}", CrawlConfig::class.java)
        val engine = Crawlberg.createEngine(engineConfig)
        val url = (System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") ?: "") + "/fixtures/content_utf8_bom"
        val result = Crawlberg.scrape(engine, url)
        assertEquals("utf-8", result.detectedCharset.orEmpty())
        assertFalse(result.html.isEmpty(), "expected non-empty value")
    }

}
