// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:52de7999ef243ff74272ecdde568c2551f95b9e5467e0d161994595043db90a5
// To regenerate: alef generate
// To verify freshness: alef verify
package io.xberg.crawlberg.android.e2e

import org.junit.jupiter.api.Test
import kotlin.test.assertEquals
import kotlin.test.assertTrue
import kotlin.test.assertFalse
import kotlin.test.assertFailsWith
import kotlinx.coroutines.runBlocking
import io.xberg.crawlberg.android.Crawlberg
import com.fasterxml.jackson.databind.ObjectMapper
import com.fasterxml.jackson.datatype.jdk8.Jdk8Module
import com.fasterxml.jackson.module.kotlin.registerKotlinModule
import io.xberg.crawlberg.android.CrawlConfig

/** E2e tests for category: strategy. */
class StrategyTest {

    companion object {
        init {
            // Suite-level environment defaults from [e2e.env]. JVM OS env is
            // immutable; System.setProperty is the runtime-mutable analog. Each
            // entry uses setdefault semantics: only applied when not already set.
            if (System.getProperty("CRAWLBERG_ALLOW_PRIVATE_NETWORK") == null) {
                System.setProperty("CRAWLBERG_ALLOW_PRIVATE_NETWORK", "true")
            }
            try {
                System.loadLibrary("cberg_jni")
            } catch (e: UnsatisfiedLinkError) {
                System.err.println("Failed to load cberg_jni library: ${e.message}")
                val libPath = System.getProperty("java.library.path")
                System.err.println("java.library.path: $libPath")
                throw e
            }
        }
        private val MAPPER = ObjectMapper().registerModule(Jdk8Module()).registerKotlinModule().setPropertyNamingStrategy(com.fasterxml.jackson.databind.PropertyNamingStrategies.SNAKE_CASE)
    }
    @Test
    fun testStrategyAdaptiveSaturation() = runBlocking {
        // Adaptive strategy stops early when encountering saturation (duplicate content)
        val engineConfig = MAPPER.readValue("{\"crawl_strategy\":\"best_first\",\"max_concurrent\":1,\"max_depth\":2,\"respect_robots_txt\":false}", CrawlConfig::class.java)
        val engine = Crawlberg.createEngine(engineConfig)
        val url = System.getProperty("mockServer.strategy_adaptive_saturation", (System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") ?: "") + "/fixtures/strategy_adaptive_saturation")
        val result = Crawlberg.crawl(engine, url)
        assertTrue(result.pages.size >= 1, "expected >= 1")
    }

    @Test
    fun testStrategyAdaptiveWindow() = runBlocking {
        // Adaptive strategy crawls more pages when content is diverse
        val engineConfig = MAPPER.readValue("{\"crawl_strategy\":\"best_first\",\"max_concurrent\":1,\"max_depth\":1,\"respect_robots_txt\":false}", CrawlConfig::class.java)
        val engine = Crawlberg.createEngine(engineConfig)
        val url = System.getProperty("mockServer.strategy_adaptive_window", (System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") ?: "") + "/fixtures/strategy_adaptive_window")
        val result = Crawlberg.crawl(engine, url)
        assertTrue(result.pages.size >= 3, "expected >= 3")
    }

    @Test
    fun testStrategyBestFirstSeed() = runBlocking {
        // BestFirst strategy always processes the seed URL first
        val engineConfig = MAPPER.readValue("{\"crawl_strategy\":\"best_first\",\"max_concurrent\":1,\"max_depth\":1}", CrawlConfig::class.java)
        val engine = Crawlberg.createEngine(engineConfig)
        val url = System.getProperty("mockServer.strategy_best_first_seed", (System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") ?: "") + "/fixtures/strategy_best_first_seed")
        val result = Crawlberg.crawl(engine, url)
        assertEquals(3, result.pages.size)
        assertFalse(result.pages.first().url.contains("/page"), "expected NOT to contain: " + "/page")
    }

    @Test
    fun testStrategyBfsDefaultOrder() = runBlocking {
        // BFS strategy visits pages in breadth-first order
        val engineConfig = MAPPER.readValue("{\"crawl_strategy\":\"bfs\",\"max_concurrent\":1,\"max_depth\":2}", CrawlConfig::class.java)
        val engine = Crawlberg.createEngine(engineConfig)
        val url = System.getProperty("mockServer.strategy_bfs_default_order", (System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") ?: "") + "/fixtures/strategy_bfs_default_order")
        val result = Crawlberg.crawl(engine, url)
        assertEquals(5, result.pages.size)
        assertTrue(result.pages.get(1).url.contains("alpha"), "expected to contain: " + "alpha")
        assertTrue(result.pages.get(2).url.contains("beta"), "expected to contain: " + "beta")
        assertTrue(result.pages.get(3).url.contains("one"), "expected to contain: " + "one")
        assertTrue(result.pages.get(4).url.contains("two"), "expected to contain: " + "two")
    }

    @Test
    fun testStrategyDfsDepthFirst() = runBlocking {
        // DFS strategy visits pages in depth-first order
        val engineConfig = MAPPER.readValue("{\"crawl_strategy\":\"dfs\",\"max_concurrent\":1,\"max_depth\":2}", CrawlConfig::class.java)
        val engine = Crawlberg.createEngine(engineConfig)
        val url = System.getProperty("mockServer.strategy_dfs_depth_first", (System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") ?: "") + "/fixtures/strategy_dfs_depth_first")
        val result = Crawlberg.crawl(engine, url)
        assertEquals(5, result.pages.size)
        assertTrue(result.pages.get(1).url.contains("beta"), "expected to contain: " + "beta")
        assertTrue(result.pages.get(2).url.contains("two"), "expected to contain: " + "two")
        assertTrue(result.pages.get(3).url.contains("alpha"), "expected to contain: " + "alpha")
        assertTrue(result.pages.get(4).url.contains("one"), "expected to contain: " + "one")
    }

}
