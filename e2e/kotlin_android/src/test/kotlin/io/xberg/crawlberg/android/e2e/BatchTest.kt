// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:666cbf037012e286a3e0efc10c4513c3ea149510797e10ddd16c846e627deda1
// To regenerate: alef generate
// To verify freshness: alef verify
package io.xberg.crawlberg.android.e2e

import org.junit.jupiter.api.Test
import kotlin.test.assertEquals
import kotlin.test.assertTrue
import kotlin.test.assertFalse
import kotlin.test.assertFailsWith
import kotlinx.coroutines.runBlocking
import io.xberg.crawlberg.android.Crawlberg
import com.fasterxml.jackson.databind.ObjectMapper
import com.fasterxml.jackson.datatype.jdk8.Jdk8Module
import com.fasterxml.jackson.module.kotlin.registerKotlinModule
import kotlin.time.Duration.Companion.milliseconds
import io.xberg.crawlberg.android.CrawlConfig

/** E2e tests for category: batch. */
class BatchTest {

    companion object {
        init {
            // Suite-level environment defaults from [e2e.env]. JVM OS env is
            // immutable; System.setProperty is the runtime-mutable analog. Each
            // entry uses setdefault semantics: only applied when not already set.
            if (System.getProperty("CRAWLBERG_ALLOW_PRIVATE_NETWORK") == null) {
                System.setProperty("CRAWLBERG_ALLOW_PRIVATE_NETWORK", "true")
            }
            try {
                System.loadLibrary("cberg_jni")
            } catch (e: UnsatisfiedLinkError) {
                System.err.println("Failed to load cberg_jni library: ${e.message}")
                val libPath = System.getProperty("java.library.path")
                System.err.println("java.library.path: $libPath")
                throw e
            }
        }
        private val MAPPER = ObjectMapper().registerModule(Jdk8Module()).registerModule(
            com.fasterxml.jackson.databind.module.SimpleModule()
                .addSerializer(kotlin.time.Duration::class.java, object : com.fasterxml.jackson.databind.JsonSerializer<kotlin.time.Duration>() {
                    override fun serialize(value: kotlin.time.Duration, gen: com.fasterxml.jackson.core.JsonGenerator, serializers: com.fasterxml.jackson.databind.SerializerProvider) {
                        gen.writeNumber(value.inWholeMilliseconds)
                    }
                })
                .addDeserializer(kotlin.time.Duration::class.java, object : com.fasterxml.jackson.databind.JsonDeserializer<kotlin.time.Duration>() {
                    override fun deserialize(p: com.fasterxml.jackson.core.JsonParser, ctxt: com.fasterxml.jackson.databind.DeserializationContext): kotlin.time.Duration {
                        return p.longValue.milliseconds
                    }
                })
        ).registerKotlinModule().setPropertyNamingStrategy(com.fasterxml.jackson.databind.PropertyNamingStrategies.SNAKE_CASE)
    }
    @Test
    fun testBatchCrawlBasic() = runBlocking {
        // Batch crawl of 2 seed URLs with links to discover
        val engineConfig = MAPPER.readValue("{\"max_depth\":1,\"respect_robots_txt\":false,\"ssrf\":{}}", CrawlConfig::class.java)
        val engine = Crawlberg.createEngine(engineConfig)
        val urlsBase = System.getProperty("mockServer.batch_crawl_basic", System.getenv("MOCK_SERVER_BATCH_CRAWL_BASIC") ?: ((System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") ?: "") + "/fixtures/batch_crawl_basic"))
        val urls = listOf("/seed1", "/seed2").map { if (it.startsWith("http")) it else urlsBase + it }
        val result = Crawlberg.batchCrawl(engine, urls)
        assertEquals(2, result.completedCount)
        assertEquals(0, result.failedCount)
        assertEquals(2, result.totalCount)
    }

    @Test
    fun testBatchCrawlPartialFailure() = runBlocking {
        // Batch crawl where one seed URL returns 404 error
        val engineConfig = MAPPER.readValue("{\"max_depth\":1,\"respect_robots_txt\":false,\"ssrf\":{}}", CrawlConfig::class.java)
        val engine = Crawlberg.createEngine(engineConfig)
        val urlsBase = System.getProperty("mockServer.batch_crawl_partial_failure", System.getenv("MOCK_SERVER_BATCH_CRAWL_PARTIAL_FAILURE") ?: ((System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") ?: "") + "/fixtures/batch_crawl_partial_failure"))
        val urls = listOf("/good_seed", "/bad_seed").map { if (it.startsWith("http")) it else urlsBase + it }
        val result = Crawlberg.batchCrawl(engine, urls)
        assertEquals(1, result.completedCount)
        assertEquals(1, result.failedCount)
        assertEquals(2, result.totalCount)
    }

    @Test
    fun testBatchCrawlWithConfig() = runBlocking {
        // Batch crawl with max_depth=1 config verifying pages are discovered
        val engineConfig = MAPPER.readValue("{\"max_depth\":1,\"respect_robots_txt\":false,\"ssrf\":{}}", CrawlConfig::class.java)
        val engine = Crawlberg.createEngine(engineConfig)
        val urlsBase = System.getProperty("mockServer.batch_crawl_with_config", System.getenv("MOCK_SERVER_BATCH_CRAWL_WITH_CONFIG") ?: ((System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") ?: "") + "/fixtures/batch_crawl_with_config"))
        val urls = listOf("/seed1", "/seed2").map { if (it.startsWith("http")) it else urlsBase + it }
        val result = Crawlberg.batchCrawl(engine, urls)
        assertEquals(2, result.completedCount)
        assertEquals(0, result.failedCount)
    }

    @Test
    fun testBatchScrapeEmptyUrlsError() = runBlocking {
        // Batch scrape with empty batch_urls array returns error
        assertFailsWith<Exception> {
            val engine = Crawlberg.createEngine(null)
            val urlsBase = System.getProperty("mockServer.batch_scrape_empty_urls_error", System.getenv("MOCK_SERVER_BATCH_SCRAPE_EMPTY_URLS_ERROR") ?: ((System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") ?: "") + "/fixtures/batch_scrape_empty_urls_error"))
            val urls = listOf().map { if (it.startsWith("http")) it else urlsBase + it }
            Crawlberg.batchScrape(engine, urls)
        }
        Unit
    }

    @Test
    fun testBatchScrapeWithConfig() = runBlocking {
        // Batch scrape with aggressive preprocessing configuration
        val engineConfig = MAPPER.readValue("{\"content\":{\"preprocessing_preset\":\"aggressive\"},\"ssrf\":{}}", CrawlConfig::class.java)
        val engine = Crawlberg.createEngine(engineConfig)
        val urlsBase = System.getProperty("mockServer.batch_scrape_with_config", System.getenv("MOCK_SERVER_BATCH_SCRAPE_WITH_CONFIG") ?: ((System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") ?: "") + "/fixtures/batch_scrape_with_config"))
        val urls = listOf("/article1", "/article2").map { if (it.startsWith("http")) it else urlsBase + it }
        val result = Crawlberg.batchScrape(engine, urls)
        assertEquals(2, result.completedCount)
        assertEquals(0, result.failedCount)
        assertEquals(2, result.totalCount)
    }

    @Test
    fun testScrapeBatchBasic() = runBlocking {
        // Batch scrape of multiple URLs all succeeding
        val engine = Crawlberg.createEngine(null)
        val urlsBase = System.getProperty("mockServer.scrape_batch_basic", System.getenv("MOCK_SERVER_SCRAPE_BATCH_BASIC") ?: ((System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") ?: "") + "/fixtures/scrape_batch_basic"))
        val urls = listOf("/page1", "/page2", "/page3").map { if (it.startsWith("http")) it else urlsBase + it }
        val result = Crawlberg.batchScrape(engine, urls)
        assertEquals(3, result.completedCount)
        assertEquals(0, result.failedCount)
        assertEquals(3, result.totalCount)
    }

    @Test
    fun testScrapeBatchPartialFailure() = runBlocking {
        // Batch scrape with one URL failing returns partial results
        val engine = Crawlberg.createEngine(null)
        val urlsBase = System.getProperty("mockServer.scrape_batch_partial_failure", System.getenv("MOCK_SERVER_SCRAPE_BATCH_PARTIAL_FAILURE") ?: ((System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") ?: "") + "/fixtures/scrape_batch_partial_failure"))
        val urls = listOf("/good1", "/bad", "/good2").map { if (it.startsWith("http")) it else urlsBase + it }
        val result = Crawlberg.batchScrape(engine, urls)
        assertEquals(2, result.completedCount)
        assertEquals(1, result.failedCount)
        assertEquals(3, result.totalCount)
    }

    @Test
    fun testScrapeBatchProgress() = runBlocking {
        // Batch scrape of 2 URLs completes with 2 results
        val engine = Crawlberg.createEngine(null)
        val urlsBase = System.getProperty("mockServer.scrape_batch_progress", System.getenv("MOCK_SERVER_SCRAPE_BATCH_PROGRESS") ?: ((System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") ?: "") + "/fixtures/scrape_batch_progress"))
        val urls = listOf("/target", "/other").map { if (it.startsWith("http")) it else urlsBase + it }
        val result = Crawlberg.batchScrape(engine, urls)
        assertEquals(2, result.totalCount)
        assertEquals(2, result.completedCount)
    }

}
