# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:fba6285c663704bfe67f2501a92f34a5feb745415f6589feb388dc0eb968fbdf
# To regenerate: alef generate
# To verify freshness: alef verify
# frozen_string_literal: true

require 'spec_helper'
require 'crawlberg'
require 'json'

RSpec.describe 'redirect' do
  it 'redirect_301_permanent: Follows 301 permanent redirect and returns final page content' do
    engine_config = { 'respect_robots_txt' => false }
    engine = Crawlberg.create_engine(engine_config.to_json)
    url = ENV.fetch('MOCK_SERVER_REDIRECT_301_PERMANENT', nil) || "#{ENV.fetch('MOCK_SERVER_URL')}/fixtures/redirect_301_permanent"
    result = Crawlberg.crawl(engine, url)
    expect(result.final_url.to_s).to include('/target')
    expect(result.redirect_count).to eq(1)
  end

  it 'redirect_302_found: Follows 302 Found redirect correctly' do
    engine_config = { 'respect_robots_txt' => false }
    engine = Crawlberg.create_engine(engine_config.to_json)
    url = ENV.fetch('MOCK_SERVER_REDIRECT_302_FOUND', nil) || "#{ENV.fetch('MOCK_SERVER_URL')}/fixtures/redirect_302_found"
    result = Crawlberg.crawl(engine, url)
    expect(result.final_url.to_s).to include('/found-target')
    expect(result.redirect_count).to eq(1)
  end

  it 'redirect_303_see_other: Follows 303 See Other redirect (method changes to GET)' do
    engine_config = { 'respect_robots_txt' => false }
    engine = Crawlberg.create_engine(engine_config.to_json)
    url = ENV.fetch('MOCK_SERVER_REDIRECT_303_SEE_OTHER', nil) || "#{ENV.fetch('MOCK_SERVER_URL')}/fixtures/redirect_303_see_other"
    result = Crawlberg.crawl(engine, url)
    expect(result.final_url.to_s).to include('/see-other')
    expect(result.redirect_count).to eq(1)
  end

  it 'redirect_307_temporary: Follows 307 Temporary Redirect (preserves method)' do
    engine_config = { 'respect_robots_txt' => false }
    engine = Crawlberg.create_engine(engine_config.to_json)
    url = ENV.fetch('MOCK_SERVER_REDIRECT_307_TEMPORARY', nil) || "#{ENV.fetch('MOCK_SERVER_URL')}/fixtures/redirect_307_temporary"
    result = Crawlberg.crawl(engine, url)
    expect(result.final_url.to_s).to include('/temp-target')
    expect(result.redirect_count).to eq(1)
  end

  it 'redirect_308_permanent: Follows 308 Permanent Redirect (preserves method)' do
    engine_config = { 'respect_robots_txt' => false }
    engine = Crawlberg.create_engine(engine_config.to_json)
    url = ENV.fetch('MOCK_SERVER_REDIRECT_308_PERMANENT', nil) || "#{ENV.fetch('MOCK_SERVER_URL')}/fixtures/redirect_308_permanent"
    result = Crawlberg.crawl(engine, url)
    expect(result.final_url.to_s).to include('/perm-target')
    expect(result.redirect_count).to eq(1)
  end

  it 'redirect_chain: Follows a chain of redirects (301 -> 302 -> 200)' do
    engine_config = { 'respect_robots_txt' => false }
    engine = Crawlberg.create_engine(engine_config.to_json)
    url = ENV.fetch('MOCK_SERVER_REDIRECT_CHAIN', nil) || "#{ENV.fetch('MOCK_SERVER_URL')}/fixtures/redirect_chain"
    result = Crawlberg.crawl(engine, url)
    expect(result.final_url.to_s).to include('/step2')
    expect(result.redirect_count).to eq(2)
  end

  it 'redirect_cross_domain: Reports cross-domain redirect target without following to external domain' do
    engine_config = { 'respect_robots_txt' => false }
    engine = Crawlberg.create_engine(engine_config.to_json)
    url = ENV.fetch('MOCK_SERVER_REDIRECT_CROSS_DOMAIN', nil) || "#{ENV.fetch('MOCK_SERVER_URL')}/fixtures/redirect_cross_domain"
    result = Crawlberg.crawl(engine, url)
    expect(result.final_url.to_s).to include('/external-redirect')
    expect(result.redirect_count).to eq(1)
  end

  it 'redirect_loop: Detects redirect loop (A -> B -> A) and returns error' do
    # skipped: field 'is_error' not available on result type
    unresolved_assertion = 'alef resolved no assertion for fixture `redirect_loop`: 1 assertion(s) name a field path the availability oracle rejected'
    expect(unresolved_assertion).to be_nil
  end

  it 'redirect_max_exceeded: Aborts when redirect count exceeds max_redirects limit' do
    # skipped: field 'is_error' not available on result type
    unresolved_assertion = 'alef resolved no assertion for fixture `redirect_max_exceeded`: 1 assertion(s) name a field path the availability oracle rejected'
    expect(unresolved_assertion).to be_nil
  end

  it 'redirect_meta_refresh: Follows HTML meta-refresh redirect to target page' do
    engine_config = { 'respect_robots_txt' => false }
    engine = Crawlberg.create_engine(engine_config.to_json)
    url = "#{ENV.fetch('MOCK_SERVER_URL')}/fixtures/redirect_meta_refresh"
    result = Crawlberg.crawl(engine, url)
    expect(result.final_url.to_s).to include('/target')
    expect(result.redirect_count).to eq(1)
  end

  it 'redirect_refresh_header: Handles HTTP Refresh header redirect' do
    engine_config = { 'respect_robots_txt' => false }
    engine = Crawlberg.create_engine(engine_config.to_json)
    url = ENV.fetch('MOCK_SERVER_REDIRECT_REFRESH_HEADER', nil) || "#{ENV.fetch('MOCK_SERVER_URL')}/fixtures/redirect_refresh_header"
    result = Crawlberg.crawl(engine, url)
    expect(result.final_url.to_s).to include('/refreshed')
    expect(result.redirect_count).to eq(1)
  end

  it 'redirect_to_404: Redirect target returns 404 Not Found' do
    engine_config = { 'respect_robots_txt' => false }
    engine = Crawlberg.create_engine(engine_config.to_json)
    url = ENV.fetch('MOCK_SERVER_REDIRECT_TO_404', nil) || "#{ENV.fetch('MOCK_SERVER_URL')}/fixtures/redirect_to_404"
    result = Crawlberg.crawl(engine, url)
    expect(result.final_url.to_s).to include('/gone')
    expect(result.redirect_count).to eq(1)
    # skipped: field 'is_error' not available on result type
  end
end
