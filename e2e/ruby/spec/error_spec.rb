# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:8046cdb4d1009daef4facdadbb39bee3286c003d3c5bb8556246696f0a1f6e7e
# To regenerate: alef generate
# To verify freshness: alef verify
# frozen_string_literal: true

require 'spec_helper'
require 'crawlberg'
require 'json'

RSpec.describe 'error' do
  it 'error_401_unauthorized: Handles 401 Unauthorized response correctly' do
    expect do
      engine = Crawlberg.create_engine(nil)
      url = "#{ENV.fetch('MOCK_SERVER_URL')}/fixtures/error_401_unauthorized"
      Crawlberg.scrape(engine, url)
    end.to raise_error(RuntimeError) { |error|
      expect(error.message =~ /unauthorized/ || error.class.name =~ /unauthorized/).to be_truthy
    }
  end

  it 'error_403_forbidden: Handles 403 Forbidden response correctly' do
    expect do
      engine_config = { 'browser' => { 'mode' => 'never' } }
      engine = Crawlberg.create_engine(engine_config.to_json)
      url = "#{ENV.fetch('MOCK_SERVER_URL')}/fixtures/error_403_forbidden"
      Crawlberg.scrape(engine, url)
    end.to raise_error(RuntimeError) { |error|
      expect(error.message =~ /forbidden/ || error.class.name =~ /forbidden/).to be_truthy
    }
  end

  it 'error_404_page: Handles 404 response correctly' do
    expect do
      engine = Crawlberg.create_engine(nil)
      url = "#{ENV.fetch('MOCK_SERVER_URL')}/fixtures/error_404_page"
      Crawlberg.scrape(engine, url)
    end.to raise_error(RuntimeError) { |error|
      expect(error.message =~ /not_found/ || error.class.name =~ /not_found/).to be_truthy
    }
  end

  it 'error_408_request_timeout: Handles 408 Request Timeout response correctly' do
    expect do
      engine = Crawlberg.create_engine(nil)
      url = "#{ENV.fetch('MOCK_SERVER_URL')}/fixtures/error_408_request_timeout"
      Crawlberg.scrape(engine, url)
    end.to raise_error(RuntimeError) { |error|
      expect(error.message =~ /timeout/ || error.class.name =~ /timeout/).to be_truthy
    }
  end

  it 'error_410_gone: Handles 410 Gone response correctly' do
    expect do
      engine = Crawlberg.create_engine(nil)
      url = "#{ENV.fetch('MOCK_SERVER_URL')}/fixtures/error_410_gone"
      Crawlberg.scrape(engine, url)
    end.to raise_error(RuntimeError) { |error|
      expect(error.message =~ /gone/ || error.class.name =~ /gone/).to be_truthy
    }
  end

  it 'error_500_server: Handles 500 server error' do
    expect do
      engine = Crawlberg.create_engine(nil)
      url = "#{ENV.fetch('MOCK_SERVER_URL')}/fixtures/error_500_server"
      Crawlberg.scrape(engine, url)
    end.to raise_error(RuntimeError) { |error|
      expect(error.message =~ /server_error/ || error.class.name =~ /server_error/).to be_truthy
    }
  end

  it 'error_502_bad_gateway: Handles 502 Bad Gateway response correctly' do
    expect do
      engine = Crawlberg.create_engine(nil)
      url = "#{ENV.fetch('MOCK_SERVER_URL')}/fixtures/error_502_bad_gateway"
      Crawlberg.scrape(engine, url)
    end.to raise_error(RuntimeError) { |error|
      expect(error.message =~ /bad_gateway/ || error.class.name =~ /bad_gateway/).to be_truthy
    }
  end

  it 'error_browser_launch_failure: Browser launch fails when browser mode is always but browser is unavailable' do
    expect do
      engine_config = { 'browser' => { 'mode' => 'always', 'timeout' => 1 } }
      engine = Crawlberg.create_engine(engine_config.to_json)
      url = "#{ENV.fetch('MOCK_SERVER_URL')}/fixtures/error_browser_launch_failure"
      Crawlberg.scrape(engine, url)
    end.to raise_error(RuntimeError) { |error|
      expect(error.message =~ /browser/ || error.class.name =~ /browser/).to be_truthy
    }
  end

  it 'error_browser_page_timeout: Browser page load times out' do
    expect do
      engine_config = { 'browser' => { 'mode' => 'always', 'timeout' => 1 } }
      engine = Crawlberg.create_engine(engine_config.to_json)
      url = "#{ENV.fetch('MOCK_SERVER_URL')}/fixtures/error_browser_page_timeout"
      Crawlberg.scrape(engine, url)
    end.to raise_error(RuntimeError) { |error|
      expect(error.message =~ /browser_timeout/ || error.class.name =~ /browser_timeout/).to be_truthy
    }
  end

  it 'error_connection_refused: Handles connection refused error gracefully' do
    expect do
      engine = Crawlberg.create_engine(nil)
      url = "#{ENV.fetch('MOCK_SERVER_URL')}/fixtures/error_connection_refused"
      Crawlberg.scrape(engine, url)
    end.to raise_error(RuntimeError) { |error|
      expect(error.message =~ /connection/ || error.class.name =~ /connection/).to be_truthy
    }
  end

  it 'error_data_loss_truncated: Content-Length mismatch causes data loss error' do
    expect do
      engine = Crawlberg.create_engine(nil)
      url = "#{ENV.fetch('MOCK_SERVER_URL')}/fixtures/error_data_loss_truncated"
      Crawlberg.scrape(engine, url)
    end.to raise_error(RuntimeError) { |error|
      expect(error.message =~ /data_loss:/ || error.class.name =~ /data_loss:/).to be_truthy
    }
  end

  it 'error_dns_resolution: Handles DNS resolution failure gracefully' do
    expect do
      engine = Crawlberg.create_engine(nil)
      url = "#{ENV.fetch('MOCK_SERVER_URL')}/fixtures/error_dns_resolution"
      Crawlberg.scrape(engine, url)
    end.to raise_error(RuntimeError) { |error|
      expect(error.message =~ /dns/ || error.class.name =~ /dns/).to be_truthy
    }
  end

  it 'error_empty_batch_urls: Scraping a URL that cannot be found returns an error containing the URL path' do
    expect do
      engine = Crawlberg.create_engine(nil)
      url = "#{ENV.fetch('MOCK_SERVER_URL')}/fixtures/error_empty_batch_urls"
      Crawlberg.scrape(engine, url)
    end.to raise_error(RuntimeError) { |error|
      expect(error.message =~ /not_found/ || error.class.name =~ /not_found/).to be_truthy
    }
  end

  it 'error_empty_response: Handles 200 with completely empty body gracefully' do
    engine = Crawlberg.create_engine(nil)
    url = "#{ENV.fetch('MOCK_SERVER_URL')}/fixtures/error_empty_response"
    result = Crawlberg.scrape(engine, url)
    expect(result.html.nil? || result.html.empty?).to be(true)
  end

  it 'error_invalid_proxy: Proxy pointing to unreachable address causes connection error during scrape' do
    expect do
      engine_config = { 'proxy' => { 'url' => 'http://127.0.0.1:1' } }
      engine = Crawlberg.create_engine(engine_config.to_json)
      url = "#{ENV.fetch('MOCK_SERVER_URL')}/fixtures/error_invalid_proxy"
      Crawlberg.scrape(engine, url)
    end.to raise_error(RuntimeError) { |error|
      expect(error.message =~ /connection/ || error.class.name =~ /connection/).to be_truthy
    }
  end

  it 'error_partial_response: Handles incomplete or truncated HTTP response' do
    expect do
      engine = Crawlberg.create_engine(nil)
      url = "#{ENV.fetch('MOCK_SERVER_URL')}/fixtures/error_partial_response"
      Crawlberg.scrape(engine, url)
    end.to raise_error(RuntimeError) { |error|
      expect(error.message =~ /data_loss/ || error.class.name =~ /data_loss/).to be_truthy
    }
  end

  it 'error_rate_limited: Handles 429 rate limiting with Retry-After' do
    expect do
      engine = Crawlberg.create_engine(nil)
      url = "#{ENV.fetch('MOCK_SERVER_URL')}/fixtures/error_rate_limited"
      Crawlberg.scrape(engine, url)
    end.to raise_error(RuntimeError) { |error|
      expect(error.message =~ /rate_limited/ || error.class.name =~ /rate_limited/).to be_truthy
    }
  end

  it 'error_retry_503: Retries request on 503 Service Unavailable response' do
    expect do
      engine = Crawlberg.create_engine(nil)
      url = "#{ENV.fetch('MOCK_SERVER_URL')}/fixtures/error_retry_503"
      Crawlberg.scrape(engine, url)
    end.to raise_error(RuntimeError) { |error|
      expect(error.message =~ /server_error/ || error.class.name =~ /server_error/).to be_truthy
    }
  end

  it 'error_retry_backoff: Implements exponential backoff when retrying failed requests' do
    expect do
      engine = Crawlberg.create_engine(nil)
      url = "#{ENV.fetch('MOCK_SERVER_URL')}/fixtures/error_retry_backoff"
      Crawlberg.scrape(engine, url)
    end.to raise_error(RuntimeError) { |error|
      expect(error.message =~ /rate_limited/ || error.class.name =~ /rate_limited/).to be_truthy
    }
  end

  it 'error_ssl_invalid_cert: Handles SSL certificate validation error' do
    expect do
      engine = Crawlberg.create_engine(nil)
      url = "#{ENV.fetch('MOCK_SERVER_URL')}/fixtures/error_ssl_invalid_cert"
      Crawlberg.scrape(engine, url)
    end.to raise_error(RuntimeError) { |error|
      expect(error.message =~ /ssl/ || error.class.name =~ /ssl/).to be_truthy
    }
  end

  it 'error_timeout: Mock server delays response longer than request_timeout, surfacing a timeout error' do
    expect do
      engine_config = { 'request_timeout' => 500 }
      engine = Crawlberg.create_engine(engine_config.to_json)
      url = "#{ENV.fetch('MOCK_SERVER_URL')}/fixtures/error_timeout"
      Crawlberg.scrape(engine, url)
    end.to raise_error(RuntimeError) { |error|
      expect(error.message =~ /timeout/ || error.class.name =~ /timeout/).to be_truthy
    }
  end

  it 'error_unsupported_scheme: Unsupported URL scheme (gopher) is rejected' do
    expect do
      engine = Crawlberg.create_engine(nil)
      url = 'gopher://invalid.example.com/'
      Crawlberg.scrape(engine, url)
    end.to raise_error(RuntimeError) { |error|
      expect(error.message =~ /disallowed scheme: gopher/ || error.class.name =~ /disallowed scheme: gopher/).to be_truthy
    }
  end

  it 'error_waf_akamai: Akamai WAF detection returns WafBlocked error' do
    expect do
      engine_config = { 'browser' => { 'mode' => 'never' } }
      engine = Crawlberg.create_engine(engine_config.to_json)
      url = "#{ENV.fetch('MOCK_SERVER_URL')}/fixtures/error_waf_akamai"
      Crawlberg.scrape(engine, url)
    end.to raise_error(RuntimeError)
  end

  it 'error_waf_blocked: WAF challenge/block detection returns WafBlocked error' do
    expect do
      engine_config = { 'browser' => { 'mode' => 'never' } }
      engine = Crawlberg.create_engine(engine_config.to_json)
      url = "#{ENV.fetch('MOCK_SERVER_URL')}/fixtures/error_waf_blocked"
      Crawlberg.scrape(engine, url)
    end.to raise_error(RuntimeError) { |error|
      expect(error.message =~ /waf/ || error.class.name =~ /waf/).to be_truthy
    }
  end

  it 'error_waf_false_403: Detects WAF/bot protection false 403 (Cloudflare challenge page)' do
    expect do
      engine_config = { 'browser' => { 'mode' => 'never' } }
      engine = Crawlberg.create_engine(engine_config.to_json)
      url = "#{ENV.fetch('MOCK_SERVER_URL')}/fixtures/error_waf_false_403"
      Crawlberg.scrape(engine, url)
    end.to raise_error(RuntimeError) { |error|
      expect(error.message =~ /waf/ || error.class.name =~ /waf/).to be_truthy
    }
  end

  it 'error_waf_imperva: Imperva/Incapsula WAF detection' do
    expect do
      engine_config = { 'browser' => { 'mode' => 'never' } }
      engine = Crawlberg.create_engine(engine_config.to_json)
      url = "#{ENV.fetch('MOCK_SERVER_URL')}/fixtures/error_waf_imperva"
      Crawlberg.scrape(engine, url)
    end.to raise_error(RuntimeError)
  end
end
