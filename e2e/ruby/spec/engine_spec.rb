# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:c007528ea6356015a752c7a1c4bb84a07d1beb7a88376eda836ec7d8516e5a97
# To regenerate: alef generate
# To verify freshness: alef verify
# frozen_string_literal: true

require 'spec_helper'
require 'crawlberg'
require 'json'

RSpec.describe 'engine' do
  it 'engine_batch_basic: CrawlEngine with defaults batch scrapes like the free function' do
    engine = Crawlberg.create_engine(nil)
    urls_base = ENV.fetch('MOCK_SERVER_ENGINE_BATCH_BASIC', nil) || "#{ENV.fetch('MOCK_SERVER_URL')}/fixtures/engine_batch_basic"
    urls = ['/page1', '/page2'].map { |p| p.start_with?('http') ? p : "#{urls_base}#{p}" }
    result = Crawlberg.batch_scrape(engine, urls)
    expect(result.completed_count).to eq(2)
    expect(result.total_count).to eq(2)
  end

  it 'engine_crawl_basic: CrawlEngine with defaults crawls multiple pages like the free function' do
    engine_config = { 'max_depth' => 1 }
    engine = Crawlberg.create_engine(engine_config.to_json)
    url = ENV.fetch('MOCK_SERVER_ENGINE_CRAWL_BASIC', nil) || "#{ENV.fetch('MOCK_SERVER_URL')}/fixtures/engine_crawl_basic"
    result = Crawlberg.crawl(engine, url)
    expect(result.pages.length).to eq(3)
    expect(result.pages.length).to be >= 3
  end

  it 'engine_map_basic: CrawlEngine with defaults discovers URLs like the free function' do
    engine = Crawlberg.create_engine(nil)
    url = ENV.fetch('MOCK_SERVER_ENGINE_MAP_BASIC', nil) || "#{ENV.fetch('MOCK_SERVER_URL')}/fixtures/engine_map_basic"
    result = Crawlberg.map_urls(engine, url)
    expect(result.urls.length).to be >= 2
  end

  it 'engine_scrape_basic: CrawlEngine with defaults scrapes a page identically to the free function' do
    engine = Crawlberg.create_engine(nil)
    url = ENV.fetch('MOCK_SERVER_ENGINE_SCRAPE_BASIC', nil) || "#{ENV.fetch('MOCK_SERVER_URL')}/fixtures/engine_scrape_basic"
    result = Crawlberg.scrape(engine, url)
    expect(result.status_code).to eq(200)
    expect(result.content_type.to_s).to eq('text/html')
    expect(result.metadata.title.to_s).to eq('Engine Test')
    expect(result.metadata.description.to_s).to include('Testing the engine')
    expect(result.links.length).to be >= 1
    expect(result.metadata.headings.length).to be >= 1
  end

  it 'engine_stream_basic: CrawlEngine with defaults streams events like the free function' do
    # skipped: streaming assertion on unsupported field 'stream.has_page_event'
    # skipped: streaming assertion on unsupported field 'stream.has_complete_event'
    # skipped: streaming assertion on unsupported field 'stream.event_count_min'
    # skipped: streaming assertion on unsupported field 'stream_complete'; this stream's chunks carry no terminal finish_reason, so completion is not observable here
    skip 'alef rendered no runnable expectation for fixture `engine_stream_basic`: all 4 declared assertion(s) were skipped (see the markers above)'
  end
end
