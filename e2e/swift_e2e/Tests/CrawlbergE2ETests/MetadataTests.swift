// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:9d04bcf11d54c8f7cac109e78d5281936a5e239fd6307482a74ddd77cc8844bc
// To regenerate: alef generate
// To verify freshness: alef verify
// swift-format-ignore-file

import XCTest
import Foundation
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif
import Crawlberg
import RustBridge

/// E2e tests for category: metadata.
final class MetadataTests: XCTestCase {
    override class func setUp() {
        super.setUp()
        _ = "true".withCString { val in
            "CRAWLBERG_ALLOW_PRIVATE_NETWORK".withCString { key in
                setenv(key, val, 0)
            }
        }
    }

    func testMetadataArticleTimes() async throws {
        // Extracts article:published_time, modified_time, author, section, and tags
        let engineObj = try createEngine(config: nil)
        let url = AlefE2EMockServer.baseURL + "/fixtures/metadata_article_times"
        let result = try await Crawlberg.scrape(engine: engineObj, url: url)
        XCTAssertEqual(result.statusCode(), 200)
        XCTAssertEqual((result.metadata().article()?.publishedTime().toString() ?? ""), "2024-01-15T10:00:00Z")
        XCTAssertEqual((result.metadata().article()?.modifiedTime().toString() ?? ""), "2024-06-20T14:30:00Z")
        XCTAssertEqual((result.metadata().article()?.author().toString() ?? ""), "Jane Developer")
        XCTAssertEqual((result.metadata().article()?.section().toString() ?? ""), "Technology")
        XCTAssertEqual(result.metadata().article()?.tags().count, 3)
    }

    func testMetadataFavicons() async throws {
        // Extracts favicon link tags including apple-touch-icon
        let engineObj = try createEngine(config: nil)
        let url = AlefE2EMockServer.baseURL + "/fixtures/metadata_favicons"
        let result = try await Crawlberg.scrape(engine: engineObj, url: url)
        XCTAssertEqual(result.statusCode(), 200)
        // skipped: field 'favicons.length' has no countable Swift leaf (swift-bridge JSON-bridges it to RustString)
        XCTAssertTrue(result.favicons().contains(where: { $0.rel().toString().contains("apple-touch-icon") }), "expected to contain: \("apple-touch-icon")")
    }

    func testMetadataHeadings() async throws {
        // Extracts heading hierarchy (h1-h6) from HTML page
        let engineObj = try createEngine(config: nil)
        let url = AlefE2EMockServer.baseURL + "/fixtures/metadata_headings"
        let result = try await Crawlberg.scrape(engine: engineObj, url: url)
        XCTAssertEqual(result.statusCode(), 200)
        XCTAssertEqual(result.metadata().headings()?[0].level(), 1)
        XCTAssertEqual(result.metadata().headings()?[0].text().toString(), "Primary Heading")
        // skipped: field 'headings.length' has no countable Swift leaf (swift-bridge JSON-bridges it to RustString)
    }

    func testMetadataHreflang() async throws {
        // Extracts hreflang alternate link tags
        let engineObj = try createEngine(config: nil)
        let url = AlefE2EMockServer.baseURL + "/fixtures/metadata_hreflang"
        let result = try await Crawlberg.scrape(engine: engineObj, url: url)
        XCTAssertEqual(result.statusCode(), 200)
        // skipped: field 'hreflang.length' has no countable Swift leaf (swift-bridge JSON-bridges it to RustString)
        XCTAssertTrue(result.hreflang().contains(where: { $0.lang().toString().contains("en") }), "expected to contain: \("en")")
    }

    func testMetadataKeywordsAuthor() async throws {
        // Extracts keywords, author, viewport, generator, theme-color, robots, lang, dir metadata
        let engineObj = try createEngine(config: nil)
        let url = AlefE2EMockServer.baseURL + "/fixtures/metadata_keywords_author"
        let result = try await Crawlberg.scrape(engine: engineObj, url: url)
        XCTAssertEqual(result.statusCode(), 200)
        XCTAssertEqual((result.metadata().title()?.toString() ?? ""), "Comprehensive Metadata Test Page")
        XCTAssertTrue(result.metadata().canonicalUrl() != nil, "expected non-empty value")
        XCTAssertTrue(result.metadata().keywords() != nil, "expected non-empty value")
        XCTAssertTrue("rust".isEmpty || (result.metadata().keywords()?.toString() ?? "").contains("rust"), "expected to contain: \("rust")")
        XCTAssertEqual((result.metadata().author()?.toString() ?? ""), "Jane Developer")
        XCTAssertTrue(result.metadata().viewport() != nil, "expected non-empty value")
        XCTAssertEqual((result.metadata().generator()?.toString() ?? ""), "crawlberg/1.0")
        XCTAssertEqual((result.metadata().themeColor()?.toString() ?? ""), "#ff6600")
        XCTAssertEqual((result.metadata().robots()?.toString() ?? ""), "index, follow")
        XCTAssertEqual((result.metadata().htmlLang()?.toString() ?? ""), "en")
        XCTAssertEqual((result.metadata().htmlDir()?.toString() ?? ""), "ltr")
    }

    func testMetadataOgVideoAudio() async throws {
        // Extracts og:video, og:audio, and og:locale:alternate metadata
        let engineObj = try createEngine(config: nil)
        let url = AlefE2EMockServer.baseURL + "/fixtures/metadata_og_video_audio"
        let result = try await Crawlberg.scrape(engine: engineObj, url: url)
        XCTAssertEqual(result.statusCode(), 200)
        XCTAssertEqual((result.metadata().ogVideo()?.toString() ?? ""), "https://example.com/video.mp4")
        XCTAssertEqual((result.metadata().ogAudio()?.toString() ?? ""), "https://example.com/audio.mp3")
        XCTAssertEqual(result.metadata().ogLocaleAlternates()?.count, 2)
    }

    func testMetadataResponseHeaders() async throws {
        // Extracts response metadata from HTTP headers (etag, server, content-language)
        let engineObj = try createEngine(config: nil)
        let url = AlefE2EMockServer.baseURL + "/fixtures/metadata_response_headers"
        let result = try await Crawlberg.scrape(engine: engineObj, url: url)
        XCTAssertEqual(result.statusCode(), 200)
        XCTAssertTrue("abc123".isEmpty || (result.responseMeta()?.etag().toString() ?? "").contains("abc123"), "expected to contain: \("abc123")")
        XCTAssertTrue("01 Jan 2025".isEmpty || (result.responseMeta()?.lastModified().toString() ?? "").contains("01 Jan 2025"), "expected to contain: \("01 Jan 2025")")
        XCTAssertTrue("nginx".isEmpty || (result.responseMeta()?.server().toString() ?? "").contains("nginx"), "expected to contain: \("nginx")")
        XCTAssertEqual((result.responseMeta()?.contentLanguage().toString() ?? ""), "en-US")
    }

    func testMetadataWordCount() async throws {
        // Computes word count from visible page text
        let engineObj = try createEngine(config: nil)
        let url = AlefE2EMockServer.baseURL + "/fixtures/metadata_word_count"
        let result = try await Crawlberg.scrape(engine: engineObj, url: url)
        XCTAssertEqual(result.statusCode(), 200)
        XCTAssertGreaterThan((result.metadata().wordCount() ?? 0), 99)
        XCTAssertLessThan((result.metadata().wordCount() ?? 0), 301)
    }

}
