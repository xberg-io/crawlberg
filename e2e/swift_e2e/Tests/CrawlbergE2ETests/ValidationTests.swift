// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:55a8564b05cad36c8e97bbf3ef4ece9ca7c23fae4607b8f7041724ccc47c5d64
// To regenerate: alef generate
// To verify freshness: alef verify
// swift-format-ignore-file

import XCTest
import Foundation
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif
import Crawlberg
import RustBridge

/// E2e tests for category: validation.
final class ValidationTests: XCTestCase {
    override class func setUp() {
        super.setUp()
        _ = "true".withCString { val in
            "CRAWLBERG_ALLOW_PRIVATE_NETWORK".withCString { key in
                setenv(key, val, 0)
            }
        }
    }

    func testValidationBrowserEndpointInvalid() async throws {
        // Browser endpoint must be a valid ws:// or wss:// URL, not http://
        do {
            let engineConfig = try crawlConfigFromJson("{\"browser\":{\"endpoint\":\"http://not-websocket:3000\",\"mode\":\"always\"}}")
            let engineObj = try createEngine(config: engineConfig)
            let url = AlefE2EMockServer.baseURL + "/fixtures/validation_browser_endpoint_invalid"
            _ = try await Crawlberg.scrape(engine: engineObj, url: url)
            XCTFail("expected to throw")
        } catch {
            let _errorMessage = String(describing: error)
            let _errorType = String(describing: type(of: error))
            XCTAssertTrue(_errorMessage.contains("endpoint") || _errorType.contains("endpoint"), "expected error to mention \"endpoint\", got message: \(_errorMessage), type: \(_errorType)")
        }
    }

    func testValidationInvalidAuthConfig() async throws {
        // auth object with empty username in basic auth is rejected
        do {
            let engineConfig = try crawlConfigFromJson("{\"auth\":{\"password\":\"secret\",\"type\":\"basic\",\"username\":\"\"}}")
            let engineObj = try createEngine(config: engineConfig)
            let url = AlefE2EMockServer.baseURL + "/fixtures/validation_invalid_auth_config"
            _ = try await Crawlberg.scrape(engine: engineObj, url: url)
            XCTFail("expected to throw")
        } catch {
            let _errorMessage = String(describing: error)
            let _errorType = String(describing: type(of: error))
            XCTAssertTrue(_errorMessage.contains("auth") || _errorType.contains("auth"), "expected error to mention \"auth\", got message: \(_errorMessage), type: \(_errorType)")
        }
    }

    func testValidationInvalidExcludeRegex() async throws {
        // Invalid regex in exclude_paths is rejected
        do {
            let engineConfig = try crawlConfigFromJson("{\"exclude_paths\":[\"(unclosed\"]}")
            let engineObj = try createEngine(config: engineConfig)
            let url = AlefE2EMockServer.baseURL + "/fixtures/validation_invalid_exclude_regex"
            _ = try await Crawlberg.scrape(engine: engineObj, url: url)
            XCTFail("expected to throw")
        } catch {
            let _errorMessage = String(describing: error)
            let _errorType = String(describing: type(of: error))
            XCTAssertTrue(_errorMessage.contains("exclude_path") || _errorType.contains("exclude_path"), "expected error to mention \"exclude_path\", got message: \(_errorMessage), type: \(_errorType)")
        }
    }

    func testValidationInvalidIncludeRegex() async throws {
        // Invalid regex in include_paths is rejected
        do {
            let engineConfig = try crawlConfigFromJson("{\"include_paths\":[\"[invalid\"]}")
            let engineObj = try createEngine(config: engineConfig)
            let url = AlefE2EMockServer.baseURL + "/fixtures/validation_invalid_include_regex"
            _ = try await Crawlberg.scrape(engine: engineObj, url: url)
            XCTFail("expected to throw")
        } catch {
            let _errorMessage = String(describing: error)
            let _errorType = String(describing: type(of: error))
            XCTAssertTrue(_errorMessage.contains("include_path") || _errorType.contains("include_path"), "expected error to mention \"include_path\", got message: \(_errorMessage), type: \(_errorType)")
        }
    }

    func testValidationInvalidProxyUrl() async throws {
        // proxy with invalid URL like 'not-a-url' is rejected
        do {
            let engineConfig = try crawlConfigFromJson("{\"proxy\":{\"url\":\"not-a-url\"}}")
            let engineObj = try createEngine(config: engineConfig)
            let url = AlefE2EMockServer.baseURL + "/fixtures/validation_invalid_proxy_url"
            _ = try await Crawlberg.scrape(engine: engineObj, url: url)
            XCTFail("expected to throw")
        } catch {
            let _errorMessage = String(describing: error)
            let _errorType = String(describing: type(of: error))
            XCTAssertTrue(_errorMessage.contains("proxy") || _errorType.contains("proxy"), "expected error to mention \"proxy\", got message: \(_errorMessage), type: \(_errorType)")
        }
    }

    func testValidationInvalidRetryCode() async throws {
        // Retry code outside 100-599 is rejected
        do {
            let engineConfig = try crawlConfigFromJson("{\"retry_codes\":[999]}")
            let engineObj = try createEngine(config: engineConfig)
            let url = AlefE2EMockServer.baseURL + "/fixtures/validation_invalid_retry_code"
            _ = try await Crawlberg.scrape(engine: engineObj, url: url)
            XCTFail("expected to throw")
        } catch {
            let _errorMessage = String(describing: error)
            let _errorType = String(describing: type(of: error))
            XCTAssertTrue(_errorMessage.contains("retry code") || _errorType.contains("retry code"), "expected error to mention \"retry code\", got message: \(_errorMessage), type: \(_errorType)")
        }
    }

    func testValidationMaxConcurrentZero() async throws {
        // max_concurrent=0 is rejected as invalid config (minimum is 1)
        do {
            let engineConfig = try crawlConfigFromJson("{\"max_concurrent\":0}")
            let engineObj = try createEngine(config: engineConfig)
            let url = AlefE2EMockServer.baseURL + "/fixtures/validation_max_concurrent_zero"
            _ = try await Crawlberg.scrape(engine: engineObj, url: url)
            XCTFail("expected to throw")
        } catch {
            let _errorMessage = String(describing: error)
            let _errorType = String(describing: type(of: error))
            XCTAssertTrue(_errorMessage.contains("max_concurrent") || _errorType.contains("max_concurrent"), "expected error to mention \"max_concurrent\", got message: \(_errorMessage), type: \(_errorType)")
        }
    }

    func testValidationMaxDepthTooHigh() async throws {
        // max_depth=200 exceeds limit of 100
        do {
            let engineConfig = try crawlConfigFromJson("{\"max_depth\":200}")
            let engineObj = try createEngine(config: engineConfig)
            let url = AlefE2EMockServer.baseURL + "/fixtures/validation_max_depth_too_high"
            _ = try await Crawlberg.scrape(engine: engineObj, url: url)
            XCTFail("expected to throw")
        } catch {
            let _errorMessage = String(describing: error)
            let _errorType = String(describing: type(of: error))
            XCTAssertTrue(_errorMessage.contains("max_depth") || _errorType.contains("max_depth"), "expected error to mention \"max_depth\", got message: \(_errorMessage), type: \(_errorType)")
        }
    }

    func testValidationMaxPagesZero() async throws {
        // max_pages=0 is rejected as invalid config
        do {
            let engineConfig = try crawlConfigFromJson("{\"max_pages\":0}")
            let engineObj = try createEngine(config: engineConfig)
            let url = AlefE2EMockServer.baseURL + "/fixtures/validation_max_pages_zero"
            _ = try await Crawlberg.scrape(engine: engineObj, url: url)
            XCTFail("expected to throw")
        } catch {
            let _errorMessage = String(describing: error)
            let _errorType = String(describing: type(of: error))
            XCTAssertTrue(_errorMessage.contains("max_pages") || _errorType.contains("max_pages"), "expected error to mention \"max_pages\", got message: \(_errorMessage), type: \(_errorType)")
        }
    }

    func testValidationMaxRedirectsTooHigh() async throws {
        // max_redirects > 100 is rejected as invalid config
        do {
            let engineConfig = try crawlConfigFromJson("{\"max_redirects\":200}")
            let engineObj = try createEngine(config: engineConfig)
            let url = AlefE2EMockServer.baseURL + "/fixtures/validation_max_redirects_too_high"
            _ = try await Crawlberg.scrape(engine: engineObj, url: url)
            XCTFail("expected to throw")
        } catch {
            let _errorMessage = String(describing: error)
            let _errorType = String(describing: type(of: error))
            XCTAssertTrue(_errorMessage.contains("max_redirects") || _errorType.contains("max_redirects"), "expected error to mention \"max_redirects\", got message: \(_errorMessage), type: \(_errorType)")
        }
    }

    func testValidationNegativeBodySize() async throws {
        // max_body_size set to -1 is rejected as invalid config
        do {
            let engineConfig = try crawlConfigFromJson("{\"max_body_size\":0}")
            let engineObj = try createEngine(config: engineConfig)
            let url = AlefE2EMockServer.baseURL + "/fixtures/validation_negative_body_size"
            _ = try await Crawlberg.scrape(engine: engineObj, url: url)
            XCTFail("expected to throw")
        } catch {
            let _errorMessage = String(describing: error)
            let _errorType = String(describing: type(of: error))
            XCTAssertTrue(_errorMessage.contains("body_size") || _errorType.contains("body_size"), "expected error to mention \"body_size\", got message: \(_errorMessage), type: \(_errorType)")
        }
    }

    func testValidationSsrfAllowlistedLoopback() async throws {
        // SSRF allowlist permits a loopback mock server while private-network protection remains enabled
        let engineConfig = try crawlConfigFromJson("{\"respect_robots_txt\":false,\"ssrf\":{\"allowlist\":[{\"type\":\"cidr\",\"value\":\"127.0.0.0/8\"}],\"deny_private\":true,\"max_redirects\":5},\"ssrf_deny_private_explicit\":true}")
        let engineObj = try createEngine(config: engineConfig)
        let url = AlefE2EMockServer.baseURL + "/fixtures/validation_ssrf_allowlisted_loopback"
        let result = try await Crawlberg.scrape(engine: engineObj, url: url)
        XCTAssertEqual(result.statusCode(), 200)
        XCTAssertEqual((result.metadata().title()?.toString() ?? ""), "Allowlisted")
    }

    func testValidationSsrfIpv4MappedIpv6Denied() async throws {
        // SSRF policy rejects an IPv4 loopback address embedded in an IPv6 literal
        do {
            let engineConfig = try crawlConfigFromJson("{\"ssrf\":{\"deny_private\":true},\"ssrf_deny_private_explicit\":true}")
            let engineObj = try createEngine(config: engineConfig)
            let url = AlefE2EMockServer.baseURL + "/fixtures/validation_ssrf_ipv4_mapped_ipv6_denied"
            _ = try await Crawlberg.scrape(engine: engineObj, url: url)
            XCTFail("expected to throw")
        } catch {
            // success
        }
        // skipped: assertion type 'error' has no accessor for error field <none> in this backend
    }

    func testValidationSsrfLoopbackDenied() async throws {
        // scrape() rejects loopback addresses by default SSRF policy
        do {
            let engineConfig = try crawlConfigFromJson("{\"ssrf_deny_private_explicit\":true}")
            let engineObj = try createEngine(config: engineConfig)
            let url = AlefE2EMockServer.baseURL + "/fixtures/validation_ssrf_loopback_denied"
            _ = try await Crawlberg.scrape(engine: engineObj, url: url)
            XCTFail("expected to throw")
        } catch {
            // success
        }
        // skipped: assertion type 'error' has no accessor for error field <none> in this backend
    }

    func testValidationSsrfNat64LoopbackDenied() async throws {
        // SSRF policy rejects an IPv4 loopback address embedded in the well-known NAT64 prefix
        do {
            let engineConfig = try crawlConfigFromJson("{\"ssrf\":{\"deny_private\":true},\"ssrf_deny_private_explicit\":true}")
            let engineObj = try createEngine(config: engineConfig)
            let url = AlefE2EMockServer.baseURL + "/fixtures/validation_ssrf_nat64_loopback_denied"
            _ = try await Crawlberg.scrape(engine: engineObj, url: url)
            XCTFail("expected to throw")
        } catch {
            // success
        }
        // skipped: assertion type 'error' has no accessor for error field <none> in this backend
    }

    func testValidationTimeoutZero() async throws {
        // Zero request timeout is rejected as invalid config
        do {
            let engineConfig = try crawlConfigFromJson("{\"request_timeout\":0}")
            let engineObj = try createEngine(config: engineConfig)
            let url = AlefE2EMockServer.baseURL + "/fixtures/validation_timeout_zero"
            _ = try await Crawlberg.scrape(engine: engineObj, url: url)
            XCTFail("expected to throw")
        } catch {
            let _errorMessage = String(describing: error)
            let _errorType = String(describing: type(of: error))
            XCTAssertTrue(_errorMessage.contains("request_timeout") || _errorType.contains("request_timeout"), "expected error to mention \"request_timeout\", got message: \(_errorMessage), type: \(_errorType)")
        }
    }

}
