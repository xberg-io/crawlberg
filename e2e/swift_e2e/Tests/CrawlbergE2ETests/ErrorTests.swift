// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:e7a9f79309e773c029bae20553d3f0d67342f3d6d150b090ba0eae13c8f124ec
// To regenerate: alef generate
// To verify freshness: alef verify
// swift-format-ignore-file

import XCTest
import Foundation
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif
import Crawlberg
import RustBridge

/// E2e tests for category: error.
final class ErrorTests: XCTestCase {
    override class func setUp() {
        super.setUp()
        _ = "true".withCString { val in
            "CRAWLBERG_ALLOW_PRIVATE_NETWORK".withCString { key in
                setenv(key, val, 0)
            }
        }
    }

    func testError401Unauthorized() async throws {
        // Handles 401 Unauthorized response correctly
        do {
            let engineObj = try createEngine(config: nil)
            let url = AlefE2EMockServer.baseURL + "/fixtures/error_401_unauthorized"
            _ = try await Crawlberg.scrape(engine: engineObj, url: url)
            XCTFail("expected to throw")
        } catch {
            let _errorMessage = String(describing: error)
            let _errorType = String(describing: type(of: error))
            XCTAssertTrue(_errorMessage.contains("unauthorized") || _errorType.contains("unauthorized"), "expected error to mention \"unauthorized\", got message: \(_errorMessage), type: \(_errorType)")
        }
    }

    func testError403Forbidden() async throws {
        // Handles 403 Forbidden response correctly
        do {
            let engineConfig = try crawlConfigFromJson("{\"browser\":{\"mode\":\"never\"}}")
            let engineObj = try createEngine(config: engineConfig)
            let url = AlefE2EMockServer.baseURL + "/fixtures/error_403_forbidden"
            _ = try await Crawlberg.scrape(engine: engineObj, url: url)
            XCTFail("expected to throw")
        } catch {
            let _errorMessage = String(describing: error)
            let _errorType = String(describing: type(of: error))
            XCTAssertTrue(_errorMessage.contains("forbidden") || _errorType.contains("forbidden"), "expected error to mention \"forbidden\", got message: \(_errorMessage), type: \(_errorType)")
        }
    }

    func testError404Page() async throws {
        // Handles 404 response correctly
        do {
            let engineObj = try createEngine(config: nil)
            let url = AlefE2EMockServer.baseURL + "/fixtures/error_404_page"
            _ = try await Crawlberg.scrape(engine: engineObj, url: url)
            XCTFail("expected to throw")
        } catch {
            let _errorMessage = String(describing: error)
            let _errorType = String(describing: type(of: error))
            XCTAssertTrue(_errorMessage.contains("not_found") || _errorType.contains("not_found"), "expected error to mention \"not_found\", got message: \(_errorMessage), type: \(_errorType)")
        }
    }

    func testError408RequestTimeout() async throws {
        // Handles 408 Request Timeout response correctly
        do {
            let engineObj = try createEngine(config: nil)
            let url = AlefE2EMockServer.baseURL + "/fixtures/error_408_request_timeout"
            _ = try await Crawlberg.scrape(engine: engineObj, url: url)
            XCTFail("expected to throw")
        } catch {
            let _errorMessage = String(describing: error)
            let _errorType = String(describing: type(of: error))
            XCTAssertTrue(_errorMessage.contains("timeout") || _errorType.contains("timeout"), "expected error to mention \"timeout\", got message: \(_errorMessage), type: \(_errorType)")
        }
    }

    func testError410Gone() async throws {
        // Handles 410 Gone response correctly
        do {
            let engineObj = try createEngine(config: nil)
            let url = AlefE2EMockServer.baseURL + "/fixtures/error_410_gone"
            _ = try await Crawlberg.scrape(engine: engineObj, url: url)
            XCTFail("expected to throw")
        } catch {
            let _errorMessage = String(describing: error)
            let _errorType = String(describing: type(of: error))
            XCTAssertTrue(_errorMessage.contains("gone") || _errorType.contains("gone"), "expected error to mention \"gone\", got message: \(_errorMessage), type: \(_errorType)")
        }
    }

    func testError500Server() async throws {
        // Handles 500 server error
        do {
            let engineObj = try createEngine(config: nil)
            let url = AlefE2EMockServer.baseURL + "/fixtures/error_500_server"
            _ = try await Crawlberg.scrape(engine: engineObj, url: url)
            XCTFail("expected to throw")
        } catch {
            let _errorMessage = String(describing: error)
            let _errorType = String(describing: type(of: error))
            XCTAssertTrue(_errorMessage.contains("server_error") || _errorType.contains("server_error"), "expected error to mention \"server_error\", got message: \(_errorMessage), type: \(_errorType)")
        }
    }

    func testError502BadGateway() async throws {
        // Handles 502 Bad Gateway response correctly
        do {
            let engineObj = try createEngine(config: nil)
            let url = AlefE2EMockServer.baseURL + "/fixtures/error_502_bad_gateway"
            _ = try await Crawlberg.scrape(engine: engineObj, url: url)
            XCTFail("expected to throw")
        } catch {
            let _errorMessage = String(describing: error)
            let _errorType = String(describing: type(of: error))
            XCTAssertTrue(_errorMessage.contains("bad_gateway") || _errorType.contains("bad_gateway"), "expected error to mention \"bad_gateway\", got message: \(_errorMessage), type: \(_errorType)")
        }
    }

    func testErrorBrowserLaunchFailure() async throws {
        // Browser launch fails when browser mode is always but browser is unavailable
        do {
            let engineConfig = try crawlConfigFromJson("{\"browser\":{\"mode\":\"always\",\"timeout\":1}}")
            let engineObj = try createEngine(config: engineConfig)
            let url = AlefE2EMockServer.baseURL + "/fixtures/error_browser_launch_failure"
            _ = try await Crawlberg.scrape(engine: engineObj, url: url)
            XCTFail("expected to throw")
        } catch {
            let _errorMessage = String(describing: error)
            let _errorType = String(describing: type(of: error))
            XCTAssertTrue(_errorMessage.contains("browser") || _errorType.contains("browser"), "expected error to mention \"browser\", got message: \(_errorMessage), type: \(_errorType)")
        }
    }

    func testErrorBrowserPageTimeout() async throws {
        // Browser page load times out
        do {
            let engineConfig = try crawlConfigFromJson("{\"browser\":{\"mode\":\"always\",\"timeout\":1}}")
            let engineObj = try createEngine(config: engineConfig)
            let url = AlefE2EMockServer.baseURL + "/fixtures/error_browser_page_timeout"
            _ = try await Crawlberg.scrape(engine: engineObj, url: url)
            XCTFail("expected to throw")
        } catch {
            let _errorMessage = String(describing: error)
            let _errorType = String(describing: type(of: error))
            XCTAssertTrue(_errorMessage.contains("browser_timeout") || _errorType.contains("browser_timeout"), "expected error to mention \"browser_timeout\", got message: \(_errorMessage), type: \(_errorType)")
        }
    }

    func testErrorConnectionRefused() async throws {
        // Handles connection refused error gracefully
        do {
            let engineObj = try createEngine(config: nil)
            let url = AlefE2EMockServer.baseURL + "/fixtures/error_connection_refused"
            _ = try await Crawlberg.scrape(engine: engineObj, url: url)
            XCTFail("expected to throw")
        } catch {
            let _errorMessage = String(describing: error)
            let _errorType = String(describing: type(of: error))
            XCTAssertTrue(_errorMessage.contains("connection") || _errorType.contains("connection"), "expected error to mention \"connection\", got message: \(_errorMessage), type: \(_errorType)")
        }
    }

    func testErrorDataLossTruncated() async throws {
        // Content-Length mismatch causes data loss error
        do {
            let engineObj = try createEngine(config: nil)
            let url = AlefE2EMockServer.baseURL + "/fixtures/error_data_loss_truncated"
            _ = try await Crawlberg.scrape(engine: engineObj, url: url)
            XCTFail("expected to throw")
        } catch {
            let _errorMessage = String(describing: error)
            let _errorType = String(describing: type(of: error))
            XCTAssertTrue(_errorMessage.contains("data_loss:") || _errorType.contains("data_loss:"), "expected error to mention \"data_loss:\", got message: \(_errorMessage), type: \(_errorType)")
        }
    }

    func testErrorDnsResolution() async throws {
        // Handles DNS resolution failure gracefully
        do {
            let engineObj = try createEngine(config: nil)
            let url = AlefE2EMockServer.baseURL + "/fixtures/error_dns_resolution"
            _ = try await Crawlberg.scrape(engine: engineObj, url: url)
            XCTFail("expected to throw")
        } catch {
            let _errorMessage = String(describing: error)
            let _errorType = String(describing: type(of: error))
            XCTAssertTrue(_errorMessage.contains("dns") || _errorType.contains("dns"), "expected error to mention \"dns\", got message: \(_errorMessage), type: \(_errorType)")
        }
    }

    func testErrorEmptyBatchUrls() async throws {
        // Scraping a URL that cannot be found returns an error containing the URL path
        do {
            let engineObj = try createEngine(config: nil)
            let url = AlefE2EMockServer.baseURL + "/fixtures/error_empty_batch_urls"
            _ = try await Crawlberg.scrape(engine: engineObj, url: url)
            XCTFail("expected to throw")
        } catch {
            let _errorMessage = String(describing: error)
            let _errorType = String(describing: type(of: error))
            XCTAssertTrue(_errorMessage.contains("not_found") || _errorType.contains("not_found"), "expected error to mention \"not_found\", got message: \(_errorMessage), type: \(_errorType)")
        }
    }

    func testErrorEmptyResponse() async throws {
        // Handles 200 with completely empty body gracefully
        let engineObj = try createEngine(config: nil)
        let url = AlefE2EMockServer.baseURL + "/fixtures/error_empty_response"
        let result = try await Crawlberg.scrape(engine: engineObj, url: url)
        XCTAssertEqual(result.html().toString().count, 0, "expected empty value")
    }

    func testErrorInvalidProxy() async throws {
        // Proxy pointing to unreachable address causes connection error during scrape
        do {
            let engineConfig = try crawlConfigFromJson("{\"proxy\":{\"url\":\"http://127.0.0.1:1\"}}")
            let engineObj = try createEngine(config: engineConfig)
            let url = AlefE2EMockServer.baseURL + "/fixtures/error_invalid_proxy"
            _ = try await Crawlberg.scrape(engine: engineObj, url: url)
            XCTFail("expected to throw")
        } catch {
            let _errorMessage = String(describing: error)
            let _errorType = String(describing: type(of: error))
            XCTAssertTrue(_errorMessage.contains("connection") || _errorType.contains("connection"), "expected error to mention \"connection\", got message: \(_errorMessage), type: \(_errorType)")
        }
    }

    func testErrorPartialResponse() async throws {
        // Handles incomplete or truncated HTTP response
        do {
            let engineObj = try createEngine(config: nil)
            let url = AlefE2EMockServer.baseURL + "/fixtures/error_partial_response"
            _ = try await Crawlberg.scrape(engine: engineObj, url: url)
            XCTFail("expected to throw")
        } catch {
            let _errorMessage = String(describing: error)
            let _errorType = String(describing: type(of: error))
            XCTAssertTrue(_errorMessage.contains("data_loss") || _errorType.contains("data_loss"), "expected error to mention \"data_loss\", got message: \(_errorMessage), type: \(_errorType)")
        }
    }

    func testErrorRateLimited() async throws {
        // Handles 429 rate limiting with Retry-After
        do {
            let engineObj = try createEngine(config: nil)
            let url = AlefE2EMockServer.baseURL + "/fixtures/error_rate_limited"
            _ = try await Crawlberg.scrape(engine: engineObj, url: url)
            XCTFail("expected to throw")
        } catch {
            let _errorMessage = String(describing: error)
            let _errorType = String(describing: type(of: error))
            XCTAssertTrue(_errorMessage.contains("rate_limited") || _errorType.contains("rate_limited"), "expected error to mention \"rate_limited\", got message: \(_errorMessage), type: \(_errorType)")
        }
    }

    func testErrorRetry503() async throws {
        // Retries request on 503 Service Unavailable response
        do {
            let engineObj = try createEngine(config: nil)
            let url = AlefE2EMockServer.baseURL + "/fixtures/error_retry_503"
            _ = try await Crawlberg.scrape(engine: engineObj, url: url)
            XCTFail("expected to throw")
        } catch {
            let _errorMessage = String(describing: error)
            let _errorType = String(describing: type(of: error))
            XCTAssertTrue(_errorMessage.contains("server_error") || _errorType.contains("server_error"), "expected error to mention \"server_error\", got message: \(_errorMessage), type: \(_errorType)")
        }
    }

    func testErrorRetryBackoff() async throws {
        // Implements exponential backoff when retrying failed requests
        do {
            let engineObj = try createEngine(config: nil)
            let url = AlefE2EMockServer.baseURL + "/fixtures/error_retry_backoff"
            _ = try await Crawlberg.scrape(engine: engineObj, url: url)
            XCTFail("expected to throw")
        } catch {
            let _errorMessage = String(describing: error)
            let _errorType = String(describing: type(of: error))
            XCTAssertTrue(_errorMessage.contains("rate_limited") || _errorType.contains("rate_limited"), "expected error to mention \"rate_limited\", got message: \(_errorMessage), type: \(_errorType)")
        }
    }

    func testErrorSslInvalidCert() async throws {
        // Handles SSL certificate validation error
        do {
            let engineObj = try createEngine(config: nil)
            let url = AlefE2EMockServer.baseURL + "/fixtures/error_ssl_invalid_cert"
            _ = try await Crawlberg.scrape(engine: engineObj, url: url)
            XCTFail("expected to throw")
        } catch {
            let _errorMessage = String(describing: error)
            let _errorType = String(describing: type(of: error))
            XCTAssertTrue(_errorMessage.contains("ssl") || _errorType.contains("ssl"), "expected error to mention \"ssl\", got message: \(_errorMessage), type: \(_errorType)")
        }
    }

    func testErrorTimeout() async throws {
        // Mock server delays response longer than request_timeout, surfacing a timeout error
        do {
            let engineConfig = try crawlConfigFromJson("{\"request_timeout\":500}")
            let engineObj = try createEngine(config: engineConfig)
            let url = AlefE2EMockServer.baseURL + "/fixtures/error_timeout"
            _ = try await Crawlberg.scrape(engine: engineObj, url: url)
            XCTFail("expected to throw")
        } catch {
            let _errorMessage = String(describing: error)
            let _errorType = String(describing: type(of: error))
            XCTAssertTrue(_errorMessage.contains("timeout") || _errorType.contains("timeout"), "expected error to mention \"timeout\", got message: \(_errorMessage), type: \(_errorType)")
        }
    }

    func testErrorUnsupportedScheme() async throws {
        // Unsupported URL scheme (gopher) is rejected
        do {
            let engineObj = try createEngine(config: nil)
            let url = "gopher://invalid.example.com/"
            _ = try await Crawlberg.scrape(engine: engineObj, url: url)
            XCTFail("expected to throw")
        } catch {
            let _errorMessage = String(describing: error)
            let _errorType = String(describing: type(of: error))
            XCTAssertTrue(_errorMessage.contains("disallowed scheme: gopher") || _errorType.contains("disallowed scheme: gopher"), "expected error to mention \"disallowed scheme: gopher\", got message: \(_errorMessage), type: \(_errorType)")
        }
    }

    func testErrorWafAkamai() async throws {
        // Akamai WAF detection returns WafBlocked error
        do {
            let engineConfig = try crawlConfigFromJson("{\"browser\":{\"mode\":\"never\"}}")
            let engineObj = try createEngine(config: engineConfig)
            let url = AlefE2EMockServer.baseURL + "/fixtures/error_waf_akamai"
            _ = try await Crawlberg.scrape(engine: engineObj, url: url)
            XCTFail("expected to throw")
        } catch {
            // success
        }
    }

    func testErrorWafBlocked() async throws {
        // WAF challenge/block detection returns WafBlocked error
        do {
            let engineConfig = try crawlConfigFromJson("{\"browser\":{\"mode\":\"never\"}}")
            let engineObj = try createEngine(config: engineConfig)
            let url = AlefE2EMockServer.baseURL + "/fixtures/error_waf_blocked"
            _ = try await Crawlberg.scrape(engine: engineObj, url: url)
            XCTFail("expected to throw")
        } catch {
            let _errorMessage = String(describing: error)
            let _errorType = String(describing: type(of: error))
            XCTAssertTrue(_errorMessage.contains("waf") || _errorType.contains("waf"), "expected error to mention \"waf\", got message: \(_errorMessage), type: \(_errorType)")
        }
    }

    func testErrorWafFalse403() async throws {
        // Detects WAF/bot protection false 403 (Cloudflare challenge page)
        do {
            let engineConfig = try crawlConfigFromJson("{\"browser\":{\"mode\":\"never\"}}")
            let engineObj = try createEngine(config: engineConfig)
            let url = AlefE2EMockServer.baseURL + "/fixtures/error_waf_false_403"
            _ = try await Crawlberg.scrape(engine: engineObj, url: url)
            XCTFail("expected to throw")
        } catch {
            let _errorMessage = String(describing: error)
            let _errorType = String(describing: type(of: error))
            XCTAssertTrue(_errorMessage.contains("waf") || _errorType.contains("waf"), "expected error to mention \"waf\", got message: \(_errorMessage), type: \(_errorType)")
        }
    }

    func testErrorWafImperva() async throws {
        // Imperva/Incapsula WAF detection
        do {
            let engineConfig = try crawlConfigFromJson("{\"browser\":{\"mode\":\"never\"}}")
            let engineObj = try createEngine(config: engineConfig)
            let url = AlefE2EMockServer.baseURL + "/fixtures/error_waf_imperva"
            _ = try await Crawlberg.scrape(engine: engineObj, url: url)
            XCTFail("expected to throw")
        } catch {
            // success
        }
    }

}
