// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:9ff7a9b8c0bd359476d1c76a8c03769d326d18c4072e444ed4daf29f49ec2875
// To regenerate: alef generate
// To verify freshness: alef verify --exit-code
// swift-format-ignore-file

import XCTest
import Foundation
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif
import Crawlberg
import RustBridge

/// E2e tests for category: proxy.
final class ProxyTests: XCTestCase {
    override class func setUp() {
        super.setUp()
        _ = "true".withCString { val in
            "CRAWLBERG_ALLOW_PRIVATE_NETWORK".withCString { key in
                setenv(key, val, 0)
            }
        }
    }

    func testProxyAuthenticated() async throws {
        // Proxy with username and password credentials authenticates successfully
        let engineConfig = try crawlConfigFromJson("{\"proxy\":{\"password\":\"proxypass\",\"url\":\"http://127.0.0.1:8889\",\"username\":\"proxyuser\"},\"respect_robots_txt\":false}")
        let engineObj = try createEngine(config: engineConfig)
        let url = AlefE2EMockServer.baseURL + "/fixtures/proxy_authenticated"
        let result = try await Crawlberg.crawl(engine: engineObj, url: url)
        XCTAssertEqual(result.pages().count, 0)
    }

    func testProxyBasicSuccess() async throws {
        // Configure proxy URL and successfully crawl through it
        let engineConfig = try crawlConfigFromJson("{\"proxy\":{\"url\":\"http://127.0.0.1:8888\"},\"respect_robots_txt\":false}")
        let engineObj = try createEngine(config: engineConfig)
        let url = AlefE2EMockServer.baseURL + "/fixtures/proxy_basic_success"
        let result = try await Crawlberg.crawl(engine: engineObj, url: url)
        XCTAssertEqual(result.pages().count, 0)
    }

}
