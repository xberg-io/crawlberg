// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:d2cf583848114c999427787530631b0ccb57ff21c082cca9dc5ce8fe614f4454
// To regenerate: alef generate
// To verify freshness: alef verify
// swift-format-ignore-file

import XCTest
import Foundation
#if canImport(FoundationNetworking)
import FoundationNetworking
#endif
import Crawlberg
import RustBridge

/// E2e tests for category: strategy.
final class StrategyTests: XCTestCase {
    override class func setUp() {
        super.setUp()
        _ = "true".withCString { val in
            "CRAWLBERG_ALLOW_PRIVATE_NETWORK".withCString { key in
                setenv(key, val, 0)
            }
        }
    }

    func testStrategyAdaptiveSaturation() async throws {
        // Adaptive strategy stops early when encountering saturation (duplicate content)
        let engineConfig = try crawlConfigFromJson("{\"crawl_strategy\":\"best_first\",\"max_concurrent\":1,\"max_depth\":2,\"respect_robots_txt\":false}")
        let engineObj = try createEngine(config: engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_STRATEGY_ADAPTIVE_SATURATION"] ?? (AlefE2EMockServer.baseURL + "/fixtures/strategy_adaptive_saturation")
        let result = try await Crawlberg.crawl(engine: engineObj, url: url)
        XCTAssertGreaterThanOrEqual(result.pages().count, 1)
    }

    func testStrategyAdaptiveWindow() async throws {
        // Adaptive strategy crawls more pages when content is diverse
        let engineConfig = try crawlConfigFromJson("{\"crawl_strategy\":\"best_first\",\"max_concurrent\":1,\"max_depth\":1,\"respect_robots_txt\":false}")
        let engineObj = try createEngine(config: engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_STRATEGY_ADAPTIVE_WINDOW"] ?? (AlefE2EMockServer.baseURL + "/fixtures/strategy_adaptive_window")
        let result = try await Crawlberg.crawl(engine: engineObj, url: url)
        XCTAssertGreaterThanOrEqual(result.pages().count, 3)
    }

    func testStrategyBestFirstSeed() async throws {
        // BestFirst strategy always processes the seed URL first
        let engineConfig = try crawlConfigFromJson("{\"crawl_strategy\":\"best_first\",\"max_concurrent\":1,\"max_depth\":1}")
        let engineObj = try createEngine(config: engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_STRATEGY_BEST_FIRST_SEED"] ?? (AlefE2EMockServer.baseURL + "/fixtures/strategy_best_first_seed")
        let result = try await Crawlberg.crawl(engine: engineObj, url: url)
        XCTAssertEqual(result.pages().count, 3)
        let _vec_pages_not_contains_11ffe89 = result.pages()
        XCTAssertFalse(_vec_pages_not_contains_11ffe89[0].url().toString().contains("/page"), "expected NOT to contain: \("/page")")
    }

    func testStrategyBfsDefaultOrder() async throws {
        // BFS strategy visits pages in breadth-first order
        let engineConfig = try crawlConfigFromJson("{\"crawl_strategy\":\"bfs\",\"max_concurrent\":1,\"max_depth\":2}")
        let engineObj = try createEngine(config: engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_STRATEGY_BFS_DEFAULT_ORDER"] ?? (AlefE2EMockServer.baseURL + "/fixtures/strategy_bfs_default_order")
        let result = try await Crawlberg.crawl(engine: engineObj, url: url)
        XCTAssertEqual(result.pages().count, 5)
        let _vec_pages_contains_a62dc256 = result.pages()
        XCTAssertTrue("alpha".isEmpty || _vec_pages_contains_a62dc256[1].url().toString().contains("alpha"), "expected to contain: \("alpha")")
        let _vec_pages_contains_b5624b1a = result.pages()
        XCTAssertTrue("beta".isEmpty || _vec_pages_contains_b5624b1a[2].url().toString().contains("beta"), "expected to contain: \("beta")")
        let _vec_pages_contains_8177c6fb = result.pages()
        XCTAssertTrue("one".isEmpty || _vec_pages_contains_8177c6fb[3].url().toString().contains("one"), "expected to contain: \("one")")
        let _vec_pages_contains_8f48d049 = result.pages()
        XCTAssertTrue("two".isEmpty || _vec_pages_contains_8f48d049[4].url().toString().contains("two"), "expected to contain: \("two")")
    }

    func testStrategyDfsDepthFirst() async throws {
        // DFS strategy visits pages in depth-first order
        let engineConfig = try crawlConfigFromJson("{\"crawl_strategy\":\"dfs\",\"max_concurrent\":1,\"max_depth\":2}")
        let engineObj = try createEngine(config: engineConfig)
        let url = ProcessInfo.processInfo.environment["MOCK_SERVER_STRATEGY_DFS_DEPTH_FIRST"] ?? (AlefE2EMockServer.baseURL + "/fixtures/strategy_dfs_depth_first")
        let result = try await Crawlberg.crawl(engine: engineObj, url: url)
        XCTAssertEqual(result.pages().count, 5)
        let _vec_pages_contains_28222dcf = result.pages()
        XCTAssertTrue("beta".isEmpty || _vec_pages_contains_28222dcf[1].url().toString().contains("beta"), "expected to contain: \("beta")")
        let _vec_pages_contains_1ab0e7eb = result.pages()
        XCTAssertTrue("two".isEmpty || _vec_pages_contains_1ab0e7eb[2].url().toString().contains("two"), "expected to contain: \("two")")
        let _vec_pages_contains_1c8a308d = result.pages()
        XCTAssertTrue("alpha".isEmpty || _vec_pages_contains_1c8a308d[3].url().toString().contains("alpha"), "expected to contain: \("alpha")")
        let _vec_pages_contains_c2892592 = result.pages()
        XCTAssertTrue("one".isEmpty || _vec_pages_contains_c2892592[4].url().toString().contains("one"), "expected to contain: \("one")")
    }

}
