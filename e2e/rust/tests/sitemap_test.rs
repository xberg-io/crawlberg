// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:c86b148a39c7fa0c4a49551b3b22d7ba3fb7680fd74bc76a013cc7b644d10f34
// To regenerate: alef generate
// To verify freshness: alef verify
//! E2e tests for category: sitemap

use crawlberg::CrawlConfig;
use crawlberg::create_engine;
use crawlberg::map_urls;
mod common;
mod mock_server;
#[allow(unused_imports)]
use mock_server::{MockRoute, MockServer};

#[test]
fn test_sitemap_basic() {
    common::runtime().block_on(async {
        // Parses a standard urlset sitemap
        let engine = create_engine(None).expect("handle creation should succeed");
        let url = std::env::var("MOCK_SERVER_SITEMAP_BASIC").unwrap_or_else(|_| {
            let _ = common::mock_server_url();
            std::env::var("MOCK_SERVER_SITEMAP_BASIC")
                .unwrap_or_else(|_| format!("{}/fixtures/{}", common::mock_server_url(), "sitemap_basic"))
        });
        let result = map_urls(&engine, &url).await.expect("call failed");
        assert_eq!(result.urls.len(), 4, "equals assertion failed");
    });
}

#[test]
fn test_sitemap_compressed_gzip() {
    common::runtime().block_on(async {
        // Parses a gzip-compressed sitemap file
        let engine_config: CrawlConfig =
            serde_json::from_str("{\"respect_robots_txt\":false}").expect("config should parse");
        let engine = create_engine(Some(engine_config)).expect("handle creation should succeed");
        let url = std::env::var("MOCK_SERVER_SITEMAP_COMPRESSED_GZIP").unwrap_or_else(|_| {
            let _ = common::mock_server_url();
            std::env::var("MOCK_SERVER_SITEMAP_COMPRESSED_GZIP")
                .unwrap_or_else(|_| format!("{}/fixtures/{}", common::mock_server_url(), "sitemap_compressed_gzip"))
        });
        let result = map_urls(&engine, &url).await.expect("call failed");
        assert_eq!(result.urls.len(), 3, "equals assertion failed");
    });
}

#[test]
fn test_sitemap_empty() {
    common::runtime().block_on(async {
        // Handles empty sitemap gracefully
        let engine = create_engine(None).expect("handle creation should succeed");
        let url = std::env::var("MOCK_SERVER_SITEMAP_EMPTY").unwrap_or_else(|_| {
            let _ = common::mock_server_url();
            std::env::var("MOCK_SERVER_SITEMAP_EMPTY")
                .unwrap_or_else(|_| format!("{}/fixtures/{}", common::mock_server_url(), "sitemap_empty"))
        });
        let result = map_urls(&engine, &url).await.expect("call failed");
        assert_eq!(result.urls.len(), 0, "equals assertion failed");
    });
}

#[test]
fn test_sitemap_from_robots_txt() {
    common::runtime().block_on(async {
        // Discovers sitemap via robots.txt Sitemap directive
        let engine_config: CrawlConfig =
            serde_json::from_str("{\"respect_robots_txt\":true}").expect("config should parse");
        let engine = create_engine(Some(engine_config)).expect("handle creation should succeed");
        let url = std::env::var("MOCK_SERVER_SITEMAP_FROM_ROBOTS_TXT").unwrap_or_else(|_| {
            let _ = common::mock_server_url();
            std::env::var("MOCK_SERVER_SITEMAP_FROM_ROBOTS_TXT")
                .unwrap_or_else(|_| format!("{}/fixtures/{}", common::mock_server_url(), "sitemap_from_robots_txt"))
        });
        let result = map_urls(&engine, &url).await.expect("call failed");
        assert_eq!(result.urls.len(), 4, "equals assertion failed");
    });
}

#[test]
fn test_sitemap_index() {
    common::runtime().block_on(async {
        // Follows sitemap index to discover child sitemaps
        let engine = create_engine(None).expect("handle creation should succeed");
        let url = std::env::var("MOCK_SERVER_SITEMAP_INDEX").unwrap_or_else(|_| {
            let _ = common::mock_server_url();
            std::env::var("MOCK_SERVER_SITEMAP_INDEX")
                .unwrap_or_else(|_| format!("{}/fixtures/{}", common::mock_server_url(), "sitemap_index"))
        });
        let result = map_urls(&engine, &url).await.expect("call failed");
        assert_eq!(result.urls.len(), 3, "equals assertion failed");
    });
}

#[test]
fn test_sitemap_lastmod_filter() {
    common::runtime().block_on(async {
        // Filters sitemap URLs by lastmod date
        let engine_config: CrawlConfig =
            serde_json::from_str("{\"respect_robots_txt\":false}").expect("config should parse");
        let engine = create_engine(Some(engine_config)).expect("handle creation should succeed");
        let url = std::env::var("MOCK_SERVER_SITEMAP_LASTMOD_FILTER").unwrap_or_else(|_| {
            let _ = common::mock_server_url();
            std::env::var("MOCK_SERVER_SITEMAP_LASTMOD_FILTER")
                .unwrap_or_else(|_| format!("{}/fixtures/{}", common::mock_server_url(), "sitemap_lastmod_filter"))
        });
        let result = map_urls(&engine, &url).await.expect("call failed");
        assert_eq!(result.urls.len(), 4, "equals assertion failed");
    });
}

#[test]
fn test_sitemap_only_mode() {
    common::runtime().block_on(async {
        // Uses sitemap URLs exclusively without following page links
        let engine_config: CrawlConfig =
            serde_json::from_str("{\"respect_robots_txt\":false}").expect("config should parse");
        let engine = create_engine(Some(engine_config)).expect("handle creation should succeed");
        let url = std::env::var("MOCK_SERVER_SITEMAP_ONLY_MODE").unwrap_or_else(|_| {
            let _ = common::mock_server_url();
            std::env::var("MOCK_SERVER_SITEMAP_ONLY_MODE")
                .unwrap_or_else(|_| format!("{}/fixtures/{}", common::mock_server_url(), "sitemap_only_mode"))
        });
        let result = map_urls(&engine, &url).await.expect("call failed");
        assert_eq!(result.urls.len(), 4, "equals assertion failed");
    });
}

#[test]
fn test_sitemap_xhtml_links() {
    common::runtime().block_on(async {
        // Parses sitemap with XHTML namespace alternate links
        let engine_config: CrawlConfig =
            serde_json::from_str("{\"respect_robots_txt\":false}").expect("config should parse");
        let engine = create_engine(Some(engine_config)).expect("handle creation should succeed");
        let url = std::env::var("MOCK_SERVER_SITEMAP_XHTML_LINKS").unwrap_or_else(|_| {
            let _ = common::mock_server_url();
            std::env::var("MOCK_SERVER_SITEMAP_XHTML_LINKS")
                .unwrap_or_else(|_| format!("{}/fixtures/{}", common::mock_server_url(), "sitemap_xhtml_links"))
        });
        let result = map_urls(&engine, &url).await.expect("call failed");
        assert_eq!(result.urls.len(), 2, "equals assertion failed");
    });
}
