// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:8cd0fdca3e6e333b1a038bdf775b835e32c38ad2ced5994c3464512e12f31139
// To regenerate: alef generate
// To verify freshness: alef verify
//! E2e tests for category: concurrent

use crawlberg::CrawlConfig;
use crawlberg::crawl;
use crawlberg::create_engine;
mod common;
mod mock_server;
#[allow(unused_imports)]
use mock_server::{MockRoute, MockServer};

#[test]
fn test_concurrent_basic() {
    common::runtime().block_on(async {
        // Concurrent crawling fetches all pages with max_concurrent workers
        let engine_config: CrawlConfig =
            serde_json::from_str("{\"max_concurrent\":3,\"max_depth\":1}").expect("config should parse");
        let engine = create_engine(Some(engine_config)).expect("handle creation should succeed");
        let url = std::env::var("MOCK_SERVER_CONCURRENT_BASIC").unwrap_or_else(|_| {
            let _ = common::mock_server_url();
            std::env::var("MOCK_SERVER_CONCURRENT_BASIC")
                .unwrap_or_else(|_| format!("{}/fixtures/{}", common::mock_server_url(), "concurrent_basic"))
        });
        let result = crawl(&engine, &url).await.expect("call failed");
        assert_eq!(result.pages.len(), 6, "equals assertion failed");
        assert!(result.pages.len() >= 6, "expected >= 6");
    });
}

#[test]
fn test_concurrent_depth_two_fan_out() {
    common::runtime().block_on(async {
        // Concurrent depth=2 crawl correctly fans out and deduplicates across levels
        let engine_config: CrawlConfig =
            serde_json::from_str("{\"max_concurrent\":3,\"max_depth\":2}").expect("config should parse");
        let engine = create_engine(Some(engine_config)).expect("handle creation should succeed");
        let url = std::env::var("MOCK_SERVER_CONCURRENT_DEPTH_TWO_FAN_OUT").unwrap_or_else(|_| {
            let _ = common::mock_server_url();
            std::env::var("MOCK_SERVER_CONCURRENT_DEPTH_TWO_FAN_OUT").unwrap_or_else(|_| {
                format!(
                    "{}/fixtures/{}",
                    common::mock_server_url(),
                    "concurrent_depth_two_fan_out"
                )
            })
        });
        let result = crawl(&engine, &url).await.expect("call failed");
        assert_eq!(result.pages.len(), 4, "equals assertion failed");
    });
}

#[test]
fn test_concurrent_max_pages_exact() {
    common::runtime().block_on(async {
        // Concurrent crawling does not exceed max_pages limit even with high concurrency
        let engine_config: CrawlConfig = serde_json::from_str("{\"max_concurrent\":5,\"max_depth\":1,\"max_pages\":3}")
            .expect("config should parse");
        let engine = create_engine(Some(engine_config)).expect("handle creation should succeed");
        let url = std::env::var("MOCK_SERVER_CONCURRENT_MAX_PAGES_EXACT").unwrap_or_else(|_| {
            let _ = common::mock_server_url();
            std::env::var("MOCK_SERVER_CONCURRENT_MAX_PAGES_EXACT").unwrap_or_else(|_| {
                format!(
                    "{}/fixtures/{}",
                    common::mock_server_url(),
                    "concurrent_max_pages_exact"
                )
            })
        });
        let result = crawl(&engine, &url).await.expect("call failed");
        assert!(result.pages.len() <= 3, "expected <= 3");
    });
}

#[test]
fn test_concurrent_partial_errors() {
    common::runtime().block_on(async {
        // Concurrent crawl handles partial failures gracefully
        let engine_config: CrawlConfig =
            serde_json::from_str("{\"max_concurrent\":3,\"max_depth\":1}").expect("config should parse");
        let engine = create_engine(Some(engine_config)).expect("handle creation should succeed");
        let url = std::env::var("MOCK_SERVER_CONCURRENT_PARTIAL_ERRORS").unwrap_or_else(|_| {
            let _ = common::mock_server_url();
            std::env::var("MOCK_SERVER_CONCURRENT_PARTIAL_ERRORS")
                .unwrap_or_else(|_| format!("{}/fixtures/{}", common::mock_server_url(), "concurrent_partial_errors"))
        });
        let result = crawl(&engine, &url).await.expect("call failed");
        assert!(result.pages.len() >= 2, "expected >= 2");
    });
}

#[test]
fn test_concurrent_respects_max_pages() {
    common::runtime().block_on(async {
        // Concurrent crawling respects max_pages limit
        let engine_config: CrawlConfig = serde_json::from_str("{\"max_concurrent\":2,\"max_depth\":1,\"max_pages\":3}")
            .expect("config should parse");
        let engine = create_engine(Some(engine_config)).expect("handle creation should succeed");
        let url = std::env::var("MOCK_SERVER_CONCURRENT_RESPECTS_MAX_PAGES").unwrap_or_else(|_| {
            let _ = common::mock_server_url();
            std::env::var("MOCK_SERVER_CONCURRENT_RESPECTS_MAX_PAGES").unwrap_or_else(|_| {
                format!(
                    "{}/fixtures/{}",
                    common::mock_server_url(),
                    "concurrent_respects_max_pages"
                )
            })
        });
        let result = crawl(&engine, &url).await.expect("call failed");
        assert!(result.pages.len() <= 3, "expected <= 3");
    });
}
