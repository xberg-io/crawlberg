// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:6a7efd9fd204dc19fceb7b814b6a17b413832b07ab3435d4b7b55fc09d11a93e
// To regenerate: alef generate
// To verify freshness: alef verify
//! E2e tests for category: markdown

use crawlberg::CrawlConfig;
use crawlberg::create_engine;
use crawlberg::{crawl, scrape};
mod common;
mod mock_server;
#[allow(unused_imports)]
use mock_server::{MockRoute, MockServer};

#[test]
fn test_citations_balanced_parens() {
    common::runtime().block_on(async {
        // Citations correctly handle links inside parentheses with balanced rendering
        let engine = create_engine(None).expect("handle creation should succeed");
        let url = std::env::var("MOCK_SERVER_CITATIONS_BALANCED_PARENS").unwrap_or_else(|_| {
            let _ = common::mock_server_url();
            std::env::var("MOCK_SERVER_CITATIONS_BALANCED_PARENS")
                .unwrap_or_else(|_| format!("{}/fixtures/{}", common::mock_server_url(), "citations_balanced_parens"))
        });
        let result = scrape(&engine, &url).await.expect("call failed");
        assert_eq!(result.status_code, 200, "equals assertion failed");
        assert!(
            !result.markdown.as_ref().unwrap().content.is_empty(),
            "expected non-empty value"
        );
        assert!(
            result.markdown.as_ref().unwrap().content.contains(r#"("#),
            "expected to contain: {}",
            r#"("#
        );
    });
}

#[test]
fn test_citations_duplicate_urls() {
    common::runtime().block_on(async {
        // Citations deduplicates multiple links to the same URL
        let engine = create_engine(None).expect("handle creation should succeed");
        let url = std::env::var("MOCK_SERVER_CITATIONS_DUPLICATE_URLS").unwrap_or_else(|_| {
            let _ = common::mock_server_url();
            std::env::var("MOCK_SERVER_CITATIONS_DUPLICATE_URLS")
                .unwrap_or_else(|_| format!("{}/fixtures/{}", common::mock_server_url(), "citations_duplicate_urls"))
        });
        let result = scrape(&engine, &url).await.expect("call failed");
        assert_eq!(result.status_code, 200, "equals assertion failed");
        assert!(
            !result.markdown.as_ref().unwrap().content.is_empty(),
            "expected non-empty value"
        );
        assert!(result.markdown.as_ref().unwrap().citations, "expected true");
    });
}

#[test]
fn test_markdown_basic_conversion() {
    common::runtime().block_on(async {
        // HTML is always converted to markdown alongside raw HTML
        let engine = create_engine(None).expect("handle creation should succeed");
        let url = std::env::var("MOCK_SERVER_MARKDOWN_BASIC_CONVERSION").unwrap_or_else(|_| {
            let _ = common::mock_server_url();
            std::env::var("MOCK_SERVER_MARKDOWN_BASIC_CONVERSION")
                .unwrap_or_else(|_| format!("{}/fixtures/{}", common::mock_server_url(), "markdown_basic_conversion"))
        });
        let result = scrape(&engine, &url).await.expect("call failed");
        let _metadata_title = result
            .metadata
            .title
            .as_ref()
            .map(|v| v.to_string())
            .unwrap_or_default();
        assert_eq!(result.status_code, 200, "equals assertion failed");
        assert_eq!(_metadata_title.to_string(), r#"Test"#, "equals assertion failed");
        assert!(!result.html.is_empty(), "expected non-empty value");
        assert!(
            !result.markdown.as_ref().unwrap().content.is_empty(),
            "expected non-empty value"
        );
        assert!(
            result.markdown.as_ref().unwrap().content.contains(r#"Hello World"#),
            "expected to contain: {}",
            r#"Hello World"#
        );
    });
}

#[test]
fn test_markdown_crawl_all_pages() {
    common::runtime().block_on(async {
        // All crawled pages have markdown field populated
        let engine_config: CrawlConfig = serde_json::from_str("{\"max_depth\":1}").expect("config should parse");
        let engine = create_engine(Some(engine_config)).expect("handle creation should succeed");
        let url = std::env::var("MOCK_SERVER_MARKDOWN_CRAWL_ALL_PAGES").unwrap_or_else(|_| {
            let _ = common::mock_server_url();
            std::env::var("MOCK_SERVER_MARKDOWN_CRAWL_ALL_PAGES")
                .unwrap_or_else(|_| format!("{}/fixtures/{}", common::mock_server_url(), "markdown_crawl_all_pages"))
        });
        let result = crawl(&engine, &url).await.expect("call failed");
        assert_eq!(result.pages.len(), 2, "equals assertion failed");
    });
}

#[test]
fn test_markdown_fit_content() {
    common::runtime().block_on(async {
        // Fit markdown removes navigation and boilerplate content
        let engine = create_engine(None).expect("handle creation should succeed");
        let url = std::env::var("MOCK_SERVER_MARKDOWN_FIT_CONTENT").unwrap_or_else(|_| {
            let _ = common::mock_server_url();
            std::env::var("MOCK_SERVER_MARKDOWN_FIT_CONTENT")
                .unwrap_or_else(|_| format!("{}/fixtures/{}", common::mock_server_url(), "markdown_fit_content"))
        });
        let result = scrape(&engine, &url).await.expect("call failed");
        assert_eq!(result.status_code, 200, "equals assertion failed");
        assert!(
            !result.markdown.as_ref().unwrap().content.is_empty(),
            "expected non-empty value"
        );
    });
}

#[test]
fn test_markdown_headings_and_paragraphs() {
    common::runtime().block_on(async {
        // Markdown conversion preserves heading hierarchy and paragraph text
        let engine = create_engine(None).expect("handle creation should succeed");
        let url = std::env::var("MOCK_SERVER_MARKDOWN_HEADINGS_AND_PARAGRAPHS").unwrap_or_else(|_| {
            let _ = common::mock_server_url();
            std::env::var("MOCK_SERVER_MARKDOWN_HEADINGS_AND_PARAGRAPHS").unwrap_or_else(|_| {
                format!(
                    "{}/fixtures/{}",
                    common::mock_server_url(),
                    "markdown_headings_and_paragraphs"
                )
            })
        });
        let result = scrape(&engine, &url).await.expect("call failed");
        assert!(
            !result.markdown.as_ref().unwrap().content.is_empty(),
            "expected non-empty value"
        );
        assert!(
            result.markdown.as_ref().unwrap().content.contains(r#"Main Title"#),
            "expected to contain: {}",
            r#"Main Title"#
        );
    });
}

#[test]
fn test_markdown_links_converted() {
    common::runtime().block_on(async {
        // HTML links are converted to markdown link syntax
        let engine = create_engine(None).expect("handle creation should succeed");
        let url = std::env::var("MOCK_SERVER_MARKDOWN_LINKS_CONVERTED").unwrap_or_else(|_| {
            let _ = common::mock_server_url();
            std::env::var("MOCK_SERVER_MARKDOWN_LINKS_CONVERTED")
                .unwrap_or_else(|_| format!("{}/fixtures/{}", common::mock_server_url(), "markdown_links_converted"))
        });
        let result = scrape(&engine, &url).await.expect("call failed");
        assert_eq!(result.status_code, 200, "equals assertion failed");
        assert!(!result.html.is_empty(), "expected non-empty value");
        assert!(
            !result.markdown.as_ref().unwrap().content.is_empty(),
            "expected non-empty value"
        );
        assert!(
            result.markdown.as_ref().unwrap().content.contains(r#"Example"#),
            "expected to contain: {}",
            r#"Example"#
        );
    });
}

#[test]
fn test_markdown_with_citations() {
    common::runtime().block_on(async {
        // Markdown includes citation conversion with numbered references
        let engine = create_engine(None).expect("handle creation should succeed");
        let url = std::env::var("MOCK_SERVER_MARKDOWN_WITH_CITATIONS").unwrap_or_else(|_| {
            let _ = common::mock_server_url();
            std::env::var("MOCK_SERVER_MARKDOWN_WITH_CITATIONS")
                .unwrap_or_else(|_| format!("{}/fixtures/{}", common::mock_server_url(), "markdown_with_citations"))
        });
        let result = scrape(&engine, &url).await.expect("call failed");
        assert_eq!(result.status_code, 200, "equals assertion failed");
        assert!(
            !result.markdown.as_ref().unwrap().content.is_empty(),
            "expected non-empty value"
        );
    });
}
