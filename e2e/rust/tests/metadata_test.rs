// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:9d087fc48fa42da653674b8105f58d0c61f360d4c1a40df4f9fbd0e803a32b31
// To regenerate: alef generate
// To verify freshness: alef verify
//! E2e tests for category: metadata

use crawlberg::create_engine;
use crawlberg::scrape;
mod common;
mod mock_server;
#[allow(unused_imports)]
use mock_server::{MockRoute, MockServer};

#[tokio::test]
async fn test_metadata_article_times() {
    // Extracts article:published_time, modified_time, author, section, and tags
    let engine = create_engine(None).expect("handle creation should succeed");
    let url = std::env::var("MOCK_SERVER_METADATA_ARTICLE_TIMES").unwrap_or_else(|_| {
        let _ = common::mock_server_url();
        std::env::var("MOCK_SERVER_METADATA_ARTICLE_TIMES")
            .unwrap_or_else(|_| format!("{}/fixtures/{}", common::mock_server_url(), "metadata_article_times"))
    });
    let result = scrape(&engine, &url).await.expect("call failed");
    let _metadata_article_published_time = result
        .metadata
        .article
        .as_ref()
        .unwrap()
        .published_time
        .as_ref()
        .map(|v| v.to_string())
        .unwrap_or_default();
    let _metadata_article_modified_time = result
        .metadata
        .article
        .as_ref()
        .unwrap()
        .modified_time
        .as_ref()
        .map(|v| v.to_string())
        .unwrap_or_default();
    let _metadata_article_author = result
        .metadata
        .article
        .as_ref()
        .unwrap()
        .author
        .as_ref()
        .map(|v| v.to_string())
        .unwrap_or_default();
    let _metadata_article_section = result
        .metadata
        .article
        .as_ref()
        .unwrap()
        .section
        .as_ref()
        .map(|v| v.to_string())
        .unwrap_or_default();
    assert_eq!(result.status_code, 200, "equals assertion failed");
    assert_eq!(
        _metadata_article_published_time.to_string(),
        r#"2024-01-15T10:00:00Z"#,
        "equals assertion failed"
    );
    assert_eq!(
        _metadata_article_modified_time.to_string(),
        r#"2024-06-20T14:30:00Z"#,
        "equals assertion failed"
    );
    assert_eq!(
        _metadata_article_author.to_string(),
        r#"Jane Developer"#,
        "equals assertion failed"
    );
    assert_eq!(
        _metadata_article_section.to_string(),
        r#"Technology"#,
        "equals assertion failed"
    );
    assert_eq!(
        result.metadata.article.as_ref().unwrap().tags.len(),
        3,
        "equals assertion failed"
    );
}

#[tokio::test]
async fn test_metadata_favicons() {
    // Extracts favicon link tags including apple-touch-icon
    let engine = create_engine(None).expect("handle creation should succeed");
    let url = std::env::var("MOCK_SERVER_METADATA_FAVICONS").unwrap_or_else(|_| {
        let _ = common::mock_server_url();
        std::env::var("MOCK_SERVER_METADATA_FAVICONS")
            .unwrap_or_else(|_| format!("{}/fixtures/{}", common::mock_server_url(), "metadata_favicons"))
    });
    let result = scrape(&engine, &url).await.expect("call failed");
    assert_eq!(result.status_code, 200, "equals assertion failed");
    assert_eq!(
        result.metadata.favicons.as_ref().unwrap().len(),
        5,
        "equals assertion failed"
    );
    assert!(
        result
            .metadata
            .favicons
            .as_ref()
            .is_some_and(|v| v.iter().any(|e| e.rel.to_string().contains(r#"apple-touch-icon"#))),
        "expected some element of favicons[].rel to contain: {}",
        r#"apple-touch-icon"#
    );
}

#[tokio::test]
async fn test_metadata_headings() {
    // Extracts heading hierarchy (h1-h6) from HTML page
    let engine = create_engine(None).expect("handle creation should succeed");
    let url = std::env::var("MOCK_SERVER_METADATA_HEADINGS").unwrap_or_else(|_| {
        let _ = common::mock_server_url();
        std::env::var("MOCK_SERVER_METADATA_HEADINGS")
            .unwrap_or_else(|_| format!("{}/fixtures/{}", common::mock_server_url(), "metadata_headings"))
    });
    let result = scrape(&engine, &url).await.expect("call failed");
    assert_eq!(result.status_code, 200, "equals assertion failed");
    assert_eq!(
        result.metadata.headings.as_ref().unwrap()[0].level,
        1,
        "equals assertion failed"
    );
    assert_eq!(
        result.metadata.headings.as_ref().unwrap()[0].text.to_string(),
        r#"Primary Heading"#,
        "equals assertion failed"
    );
    assert_eq!(
        result.metadata.headings.as_ref().unwrap().len(),
        8,
        "equals assertion failed"
    );
}

#[tokio::test]
async fn test_metadata_hreflang() {
    // Extracts hreflang alternate link tags
    let engine = create_engine(None).expect("handle creation should succeed");
    let url = std::env::var("MOCK_SERVER_METADATA_HREFLANG").unwrap_or_else(|_| {
        let _ = common::mock_server_url();
        std::env::var("MOCK_SERVER_METADATA_HREFLANG")
            .unwrap_or_else(|_| format!("{}/fixtures/{}", common::mock_server_url(), "metadata_hreflang"))
    });
    let result = scrape(&engine, &url).await.expect("call failed");
    assert_eq!(result.status_code, 200, "equals assertion failed");
    assert_eq!(
        result.metadata.hreflangs.as_ref().unwrap().len(),
        4,
        "equals assertion failed"
    );
    assert!(
        result
            .metadata
            .hreflangs
            .as_ref()
            .is_some_and(|v| v.iter().any(|e| e.lang.to_string().contains(r#"en"#))),
        "expected some element of hreflang[].lang to contain: {}",
        r#"en"#
    );
}

#[tokio::test]
async fn test_metadata_keywords_author() {
    // Extracts keywords, author, viewport, generator, theme-color, robots, lang, dir metadata
    let engine = create_engine(None).expect("handle creation should succeed");
    let url = std::env::var("MOCK_SERVER_METADATA_KEYWORDS_AUTHOR").unwrap_or_else(|_| {
        let _ = common::mock_server_url();
        std::env::var("MOCK_SERVER_METADATA_KEYWORDS_AUTHOR")
            .unwrap_or_else(|_| format!("{}/fixtures/{}", common::mock_server_url(), "metadata_keywords_author"))
    });
    let result = scrape(&engine, &url).await.expect("call failed");
    let _metadata_title = result
        .metadata
        .title
        .as_ref()
        .map(|v| v.to_string())
        .unwrap_or_default();
    let _metadata_keywords = result
        .metadata
        .keywords
        .as_ref()
        .map(|v| v.to_string())
        .unwrap_or_default();
    let _metadata_author = result
        .metadata
        .author
        .as_ref()
        .map(|v| v.to_string())
        .unwrap_or_default();
    let _metadata_generator = result
        .metadata
        .generator
        .as_ref()
        .map(|v| v.to_string())
        .unwrap_or_default();
    let _metadata_theme_color = result
        .metadata
        .theme_color
        .as_ref()
        .map(|v| v.to_string())
        .unwrap_or_default();
    let _metadata_robots = result
        .metadata
        .robots
        .as_ref()
        .map(|v| v.to_string())
        .unwrap_or_default();
    let _metadata_html_lang = result
        .metadata
        .html_lang
        .as_ref()
        .map(|v| v.to_string())
        .unwrap_or_default();
    let _metadata_html_dir = result
        .metadata
        .html_dir
        .as_ref()
        .map(|v| v.to_string())
        .unwrap_or_default();
    assert_eq!(result.status_code, 200, "equals assertion failed");
    assert_eq!(
        _metadata_title.to_string(),
        r#"Comprehensive Metadata Test Page"#,
        "equals assertion failed"
    );
    assert!(
        result.metadata.canonical_url.is_some(),
        "expected metadata.canonical_url to be present"
    );
    assert!(!_metadata_keywords.is_empty(), "expected non-empty value");
    assert!(
        _metadata_keywords.contains(r#"rust"#),
        "expected to contain: {}",
        r#"rust"#
    );
    assert_eq!(
        _metadata_author.to_string(),
        r#"Jane Developer"#,
        "equals assertion failed"
    );
    assert!(
        result.metadata.viewport.is_some(),
        "expected metadata.viewport to be present"
    );
    assert_eq!(
        _metadata_generator.to_string(),
        r#"crawlberg/1.0"#,
        "equals assertion failed"
    );
    assert_eq!(
        _metadata_theme_color.to_string(),
        r#"#ff6600"#,
        "equals assertion failed"
    );
    assert_eq!(
        _metadata_robots.to_string(),
        r#"index, follow"#,
        "equals assertion failed"
    );
    assert_eq!(_metadata_html_lang.to_string(), r#"en"#, "equals assertion failed");
    assert_eq!(_metadata_html_dir.to_string(), r#"ltr"#, "equals assertion failed");
}

#[tokio::test]
async fn test_metadata_og_video_audio() {
    // Extracts og:video, og:audio, and og:locale:alternate metadata
    let engine = create_engine(None).expect("handle creation should succeed");
    let url = std::env::var("MOCK_SERVER_METADATA_OG_VIDEO_AUDIO").unwrap_or_else(|_| {
        let _ = common::mock_server_url();
        std::env::var("MOCK_SERVER_METADATA_OG_VIDEO_AUDIO")
            .unwrap_or_else(|_| format!("{}/fixtures/{}", common::mock_server_url(), "metadata_og_video_audio"))
    });
    let result = scrape(&engine, &url).await.expect("call failed");
    let _metadata_og_video = result
        .metadata
        .og_video
        .as_ref()
        .map(|v| v.to_string())
        .unwrap_or_default();
    let _metadata_og_audio = result
        .metadata
        .og_audio
        .as_ref()
        .map(|v| v.to_string())
        .unwrap_or_default();
    assert_eq!(result.status_code, 200, "equals assertion failed");
    assert_eq!(
        _metadata_og_video.to_string(),
        r#"https://example.com/video.mp4"#,
        "equals assertion failed"
    );
    assert_eq!(
        _metadata_og_audio.to_string(),
        r#"https://example.com/audio.mp3"#,
        "equals assertion failed"
    );
    assert_eq!(
        result.metadata.og_locale_alternates.as_ref().unwrap().len(),
        2,
        "equals assertion failed"
    );
}

#[tokio::test]
async fn test_metadata_response_headers() {
    // Extracts response metadata from HTTP headers (etag, server, content-language)
    let engine = create_engine(None).expect("handle creation should succeed");
    let url = std::env::var("MOCK_SERVER_METADATA_RESPONSE_HEADERS").unwrap_or_else(|_| {
        let _ = common::mock_server_url();
        std::env::var("MOCK_SERVER_METADATA_RESPONSE_HEADERS")
            .unwrap_or_else(|_| format!("{}/fixtures/{}", common::mock_server_url(), "metadata_response_headers"))
    });
    let result = scrape(&engine, &url).await.expect("call failed");
    let _response_meta_etag = result
        .response_meta
        .as_ref()
        .unwrap()
        .etag
        .as_ref()
        .map(|v| v.to_string())
        .unwrap_or_default();
    let _response_meta_last_modified = result
        .response_meta
        .as_ref()
        .unwrap()
        .last_modified
        .as_ref()
        .map(|v| v.to_string())
        .unwrap_or_default();
    let _response_meta_server = result
        .response_meta
        .as_ref()
        .unwrap()
        .server
        .as_ref()
        .map(|v| v.to_string())
        .unwrap_or_default();
    let _response_meta_content_language = result
        .response_meta
        .as_ref()
        .unwrap()
        .content_language
        .as_ref()
        .map(|v| v.to_string())
        .unwrap_or_default();
    assert_eq!(result.status_code, 200, "equals assertion failed");
    assert!(
        _response_meta_etag.contains(r#"abc123"#),
        "expected to contain: {}",
        r#"abc123"#
    );
    assert!(
        _response_meta_last_modified.contains(r#"01 Jan 2025"#),
        "expected to contain: {}",
        r#"01 Jan 2025"#
    );
    assert!(
        _response_meta_server.contains(r#"nginx"#),
        "expected to contain: {}",
        r#"nginx"#
    );
    assert_eq!(
        _response_meta_content_language.to_string(),
        r#"en-US"#,
        "equals assertion failed"
    );
}

#[tokio::test]
async fn test_metadata_word_count() {
    // Computes word count from visible page text
    let engine = create_engine(None).expect("handle creation should succeed");
    let url = std::env::var("MOCK_SERVER_METADATA_WORD_COUNT").unwrap_or_else(|_| {
        let _ = common::mock_server_url();
        std::env::var("MOCK_SERVER_METADATA_WORD_COUNT")
            .unwrap_or_else(|_| format!("{}/fixtures/{}", common::mock_server_url(), "metadata_word_count"))
    });
    let result = scrape(&engine, &url).await.expect("call failed");
    assert_eq!(result.status_code, 200, "equals assertion failed");
    assert!(result.metadata.word_count.unwrap_or(0) > 99, "expected > 99");
    assert!(result.metadata.word_count.unwrap_or(0) < 301, "expected < 301");
}
