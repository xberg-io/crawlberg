// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:8cfb6bf48cef4590e46f89e2e8daace85c9f5b374f3a6a9bab113d0dbd19b084
// To regenerate: alef generate
// To verify freshness: alef verify
//! E2e tests for category: error

use crawlberg::CrawlConfig;
use crawlberg::create_engine;
use crawlberg::scrape;
mod common;
mod mock_server;
#[allow(unused_imports)]
use mock_server::{MockRoute, MockServer};

#[test]
fn test_error_401_unauthorized() {
    common::runtime().block_on(async {
        // Handles 401 Unauthorized response correctly
        let mock_route = MockRoute {
            path: "/",
            method: "POST",
            status: 401,
            body: r#""<html><body><h1>401 Unauthorized</h1></body></html>""#.to_string(),
            is_streaming: false,
            stream_chunks: vec![],
            headers: vec![
                (r#"content-type"#.to_string(), r#"text/html; charset=utf-8"#.to_string()),
                (r#"www-authenticate"#.to_string(), r#"Basic realm="test""#.to_string()),
            ],
            delay_ms: None,
        };
        let _mock_server = MockServer::start(vec![mock_route]).await;
        let engine_result = create_engine(None);
        let url = std::env::var("MOCK_SERVER_ERROR_401_UNAUTHORIZED").unwrap_or_else(|_| {
            let _ = common::mock_server_url();
            std::env::var("MOCK_SERVER_ERROR_401_UNAUTHORIZED")
                .unwrap_or_else(|_| format!("{}/fixtures/{}", common::mock_server_url(), "error_401_unauthorized"))
        });
        let result = match engine_result {
            Err(e) => Err(e),
            Ok(engine) => scrape(&engine, &url).await,
        };
        assert!(result.is_err(), "expected call to fail");
        assert!(result.is_err(), "expected call to fail");
        {
            let __e = result.as_ref().err().unwrap();
            assert!(
                format!("{:?}", __e).contains("unauthorized") || __e.to_string().contains("unauthorized"),
                "error message mismatch"
            );
        }
    });
}

#[test]
fn test_error_403_forbidden() {
    common::runtime().block_on(async {
        // Handles 403 Forbidden response correctly
        let mock_route = MockRoute {
            path: "/",
            method: "POST",
            status: 403,
            body: r#""<html><body><h1>403 Forbidden</h1></body></html>""#.to_string(),
            is_streaming: false,
            stream_chunks: vec![],
            headers: vec![(r#"content-type"#.to_string(), r#"text/html; charset=utf-8"#.to_string())],
            delay_ms: None,
        };
        let _mock_server = MockServer::start(vec![mock_route]).await;
        let engine_config: CrawlConfig =
            serde_json::from_str("{\"browser\":{\"mode\":\"never\"}}").expect("config should parse");
        let engine_result = create_engine(Some(engine_config));
        let url = std::env::var("MOCK_SERVER_ERROR_403_FORBIDDEN").unwrap_or_else(|_| {
            let _ = common::mock_server_url();
            std::env::var("MOCK_SERVER_ERROR_403_FORBIDDEN")
                .unwrap_or_else(|_| format!("{}/fixtures/{}", common::mock_server_url(), "error_403_forbidden"))
        });
        let result = match engine_result {
            Err(e) => Err(e),
            Ok(engine) => scrape(&engine, &url).await,
        };
        assert!(result.is_err(), "expected call to fail");
        assert!(result.is_err(), "expected call to fail");
        {
            let __e = result.as_ref().err().unwrap();
            assert!(
                format!("{:?}", __e).contains("forbidden") || __e.to_string().contains("forbidden"),
                "error message mismatch"
            );
        }
    });
}

#[test]
fn test_error_404_page() {
    common::runtime().block_on(async {
        // Handles 404 response correctly
        let mock_route = MockRoute {
            path: "/",
            method: "POST",
            status: 404,
            body: r#""<html><body><h1>404 Not Found</h1></body></html>""#.to_string(),
            is_streaming: false,
            stream_chunks: vec![],
            headers: vec![(r#"content-type"#.to_string(), r#"text/html; charset=utf-8"#.to_string())],
            delay_ms: None,
        };
        let _mock_server = MockServer::start(vec![mock_route]).await;
        let engine_result = create_engine(None);
        let url = std::env::var("MOCK_SERVER_ERROR_404_PAGE").unwrap_or_else(|_| {
            let _ = common::mock_server_url();
            std::env::var("MOCK_SERVER_ERROR_404_PAGE")
                .unwrap_or_else(|_| format!("{}/fixtures/{}", common::mock_server_url(), "error_404_page"))
        });
        let result = match engine_result {
            Err(e) => Err(e),
            Ok(engine) => scrape(&engine, &url).await,
        };
        assert!(result.is_err(), "expected call to fail");
        assert!(result.is_err(), "expected call to fail");
        {
            let __e = result.as_ref().err().unwrap();
            assert!(
                format!("{:?}", __e).contains("not_found") || __e.to_string().contains("not_found"),
                "error message mismatch"
            );
        }
    });
}

#[test]
fn test_error_408_request_timeout() {
    common::runtime().block_on(async {
        // Handles 408 Request Timeout response correctly
        let mock_route = MockRoute {
            path: "/",
            method: "POST",
            status: 408,
            body: r#""<html><body><h1>408 Request Timeout</h1></body></html>""#.to_string(),
            is_streaming: false,
            stream_chunks: vec![],
            headers: vec![(r#"content-type"#.to_string(), r#"text/html; charset=utf-8"#.to_string())],
            delay_ms: None,
        };
        let _mock_server = MockServer::start(vec![mock_route]).await;
        let engine_result = create_engine(None);
        let url = std::env::var("MOCK_SERVER_ERROR_408_REQUEST_TIMEOUT").unwrap_or_else(|_| {
            let _ = common::mock_server_url();
            std::env::var("MOCK_SERVER_ERROR_408_REQUEST_TIMEOUT")
                .unwrap_or_else(|_| format!("{}/fixtures/{}", common::mock_server_url(), "error_408_request_timeout"))
        });
        let result = match engine_result {
            Err(e) => Err(e),
            Ok(engine) => scrape(&engine, &url).await,
        };
        assert!(result.is_err(), "expected call to fail");
        assert!(result.is_err(), "expected call to fail");
        {
            let __e = result.as_ref().err().unwrap();
            assert!(
                format!("{:?}", __e).contains("timeout") || __e.to_string().contains("timeout"),
                "error message mismatch"
            );
        }
    });
}

#[test]
fn test_error_410_gone() {
    common::runtime().block_on(async {
        // Handles 410 Gone response correctly
        let mock_route = MockRoute {
            path: "/",
            method: "POST",
            status: 410,
            body: r#""<html><body><h1>410 Gone</h1></body></html>""#.to_string(),
            is_streaming: false,
            stream_chunks: vec![],
            headers: vec![(r#"content-type"#.to_string(), r#"text/html; charset=utf-8"#.to_string())],
            delay_ms: None,
        };
        let _mock_server = MockServer::start(vec![mock_route]).await;
        let engine_result = create_engine(None);
        let url = std::env::var("MOCK_SERVER_ERROR_410_GONE").unwrap_or_else(|_| {
            let _ = common::mock_server_url();
            std::env::var("MOCK_SERVER_ERROR_410_GONE")
                .unwrap_or_else(|_| format!("{}/fixtures/{}", common::mock_server_url(), "error_410_gone"))
        });
        let result = match engine_result {
            Err(e) => Err(e),
            Ok(engine) => scrape(&engine, &url).await,
        };
        assert!(result.is_err(), "expected call to fail");
        assert!(result.is_err(), "expected call to fail");
        {
            let __e = result.as_ref().err().unwrap();
            assert!(
                format!("{:?}", __e).contains("gone") || __e.to_string().contains("gone"),
                "error message mismatch"
            );
        }
    });
}

#[test]
fn test_error_500_server() {
    common::runtime().block_on(async {
        // Handles 500 server error
        let mock_route = MockRoute {
            path: "/",
            method: "POST",
            status: 500,
            body: r#""<html><body><h1>Internal Server Error</h1></body></html>""#.to_string(),
            is_streaming: false,
            stream_chunks: vec![],
            headers: vec![(r#"content-type"#.to_string(), r#"text/html; charset=utf-8"#.to_string())],
            delay_ms: None,
        };
        let _mock_server = MockServer::start(vec![mock_route]).await;
        let engine_result = create_engine(None);
        let url = std::env::var("MOCK_SERVER_ERROR_500_SERVER").unwrap_or_else(|_| {
            let _ = common::mock_server_url();
            std::env::var("MOCK_SERVER_ERROR_500_SERVER")
                .unwrap_or_else(|_| format!("{}/fixtures/{}", common::mock_server_url(), "error_500_server"))
        });
        let result = match engine_result {
            Err(e) => Err(e),
            Ok(engine) => scrape(&engine, &url).await,
        };
        assert!(result.is_err(), "expected call to fail");
        assert!(result.is_err(), "expected call to fail");
        {
            let __e = result.as_ref().err().unwrap();
            assert!(
                format!("{:?}", __e).contains("server_error") || __e.to_string().contains("server_error"),
                "error message mismatch"
            );
        }
    });
}

#[test]
fn test_error_502_bad_gateway() {
    common::runtime().block_on(async {
        // Handles 502 Bad Gateway response correctly
        let mock_route = MockRoute {
            path: "/",
            method: "POST",
            status: 502,
            body: r#""<html><body><h1>502 Bad Gateway</h1></body></html>""#.to_string(),
            is_streaming: false,
            stream_chunks: vec![],
            headers: vec![(r#"content-type"#.to_string(), r#"text/html; charset=utf-8"#.to_string())],
            delay_ms: None,
        };
        let _mock_server = MockServer::start(vec![mock_route]).await;
        let engine_result = create_engine(None);
        let url = std::env::var("MOCK_SERVER_ERROR_502_BAD_GATEWAY").unwrap_or_else(|_| {
            let _ = common::mock_server_url();
            std::env::var("MOCK_SERVER_ERROR_502_BAD_GATEWAY")
                .unwrap_or_else(|_| format!("{}/fixtures/{}", common::mock_server_url(), "error_502_bad_gateway"))
        });
        let result = match engine_result {
            Err(e) => Err(e),
            Ok(engine) => scrape(&engine, &url).await,
        };
        assert!(result.is_err(), "expected call to fail");
        assert!(result.is_err(), "expected call to fail");
        {
            let __e = result.as_ref().err().unwrap();
            assert!(
                format!("{:?}", __e).contains("bad_gateway") || __e.to_string().contains("bad_gateway"),
                "error message mismatch"
            );
        }
    });
}

#[test]
fn test_error_browser_launch_failure() {
    common::runtime().block_on(async {
        // Browser launch fails when browser mode is always but browser is unavailable
        let engine_config: CrawlConfig =
            serde_json::from_str("{\"browser\":{\"mode\":\"always\",\"timeout\":1}}").expect("config should parse");
        let engine_result = create_engine(Some(engine_config));
        let url = std::env::var("MOCK_SERVER_ERROR_BROWSER_LAUNCH_FAILURE").unwrap_or_else(|_| {
            let _ = common::mock_server_url();
            std::env::var("MOCK_SERVER_ERROR_BROWSER_LAUNCH_FAILURE").unwrap_or_else(|_| {
                format!(
                    "{}/fixtures/{}",
                    common::mock_server_url(),
                    "error_browser_launch_failure"
                )
            })
        });
        let result = match engine_result {
            Err(e) => Err(e),
            Ok(engine) => scrape(&engine, &url).await,
        };
        assert!(result.is_err(), "expected call to fail");
        assert!(result.is_err(), "expected call to fail");
        {
            let __e = result.as_ref().err().unwrap();
            assert!(
                format!("{:?}", __e).contains("browser") || __e.to_string().contains("browser"),
                "error message mismatch"
            );
        }
    });
}

#[test]
fn test_error_browser_page_timeout() {
    common::runtime().block_on(async {
        // Browser page load times out
        let engine_config: CrawlConfig =
            serde_json::from_str("{\"browser\":{\"mode\":\"always\",\"timeout\":1}}").expect("config should parse");
        let engine_result = create_engine(Some(engine_config));
        let url = std::env::var("MOCK_SERVER_ERROR_BROWSER_PAGE_TIMEOUT").unwrap_or_else(|_| {
            let _ = common::mock_server_url();
            std::env::var("MOCK_SERVER_ERROR_BROWSER_PAGE_TIMEOUT").unwrap_or_else(|_| {
                format!(
                    "{}/fixtures/{}",
                    common::mock_server_url(),
                    "error_browser_page_timeout"
                )
            })
        });
        let result = match engine_result {
            Err(e) => Err(e),
            Ok(engine) => scrape(&engine, &url).await,
        };
        assert!(result.is_err(), "expected call to fail");
        assert!(result.is_err(), "expected call to fail");
        {
            let __e = result.as_ref().err().unwrap();
            assert!(
                format!("{:?}", __e).contains("browser_timeout") || __e.to_string().contains("browser_timeout"),
                "error message mismatch"
            );
        }
    });
}

#[test]
fn test_error_connection_refused() {
    common::runtime().block_on(async {
        // Handles connection refused error gracefully
        let mock_route = MockRoute {
            path: "/",
            method: "POST",
            status: 302,
            body: r#""""#.to_string(),
            is_streaming: false,
            stream_chunks: vec![],
            headers: vec![(r#"location"#.to_string(), r#"http://127.0.0.1:1/"#.to_string())],
            delay_ms: None,
        };
        let _mock_server = MockServer::start(vec![mock_route]).await;
        let engine_result = create_engine(None);
        let url = std::env::var("MOCK_SERVER_ERROR_CONNECTION_REFUSED").unwrap_or_else(|_| {
            let _ = common::mock_server_url();
            std::env::var("MOCK_SERVER_ERROR_CONNECTION_REFUSED")
                .unwrap_or_else(|_| format!("{}/fixtures/{}", common::mock_server_url(), "error_connection_refused"))
        });
        let result = match engine_result {
            Err(e) => Err(e),
            Ok(engine) => scrape(&engine, &url).await,
        };
        assert!(result.is_err(), "expected call to fail");
        assert!(result.is_err(), "expected call to fail");
        {
            let __e = result.as_ref().err().unwrap();
            assert!(
                format!("{:?}", __e).contains("connection") || __e.to_string().contains("connection"),
                "error message mismatch"
            );
        }
    });
}

#[test]
fn test_error_data_loss_truncated() {
    common::runtime().block_on(async {
        // Content-Length mismatch causes data loss error
        let mock_route = MockRoute {
            path: "/",
            method: "POST",
            status: 200,
            body: r#""<html><body>Incomplete content""#.to_string(),
            is_streaming: false,
            stream_chunks: vec![],
            headers: vec![
                (r#"connection"#.to_string(), r#"close"#.to_string()),
                (r#"content-length"#.to_string(), r#"500000"#.to_string()),
                (r#"content-type"#.to_string(), r#"text/html; charset=utf-8"#.to_string()),
            ],
            delay_ms: None,
        };
        let _mock_server = MockServer::start(vec![mock_route]).await;
        let engine_result = create_engine(None);
        let url = std::env::var("MOCK_SERVER_ERROR_DATA_LOSS_TRUNCATED").unwrap_or_else(|_| {
            let _ = common::mock_server_url();
            std::env::var("MOCK_SERVER_ERROR_DATA_LOSS_TRUNCATED")
                .unwrap_or_else(|_| format!("{}/fixtures/{}", common::mock_server_url(), "error_data_loss_truncated"))
        });
        let result = match engine_result {
            Err(e) => Err(e),
            Ok(engine) => scrape(&engine, &url).await,
        };
        assert!(result.is_err(), "expected call to fail");
        assert!(result.is_err(), "expected call to fail");
        {
            let __e = result.as_ref().err().unwrap();
            assert!(
                format!("{:?}", __e).contains("data_loss:") || __e.to_string().contains("data_loss:"),
                "error message mismatch"
            );
        }
    });
}

#[test]
fn test_error_dns_resolution() {
    common::runtime().block_on(async {
        // Handles DNS resolution failure gracefully
        let mock_route = MockRoute {
            path: "/",
            method: "POST",
            status: 302,
            body: r#""""#.to_string(),
            is_streaming: false,
            stream_chunks: vec![],
            headers: vec![(
                r#"location"#.to_string(),
                r#"http://this-hostname-does-not-exist-crawlberg-test.invalid/"#.to_string(),
            )],
            delay_ms: None,
        };
        let _mock_server = MockServer::start(vec![mock_route]).await;
        let engine_result = create_engine(None);
        let url = std::env::var("MOCK_SERVER_ERROR_DNS_RESOLUTION").unwrap_or_else(|_| {
            let _ = common::mock_server_url();
            std::env::var("MOCK_SERVER_ERROR_DNS_RESOLUTION")
                .unwrap_or_else(|_| format!("{}/fixtures/{}", common::mock_server_url(), "error_dns_resolution"))
        });
        let result = match engine_result {
            Err(e) => Err(e),
            Ok(engine) => scrape(&engine, &url).await,
        };
        assert!(result.is_err(), "expected call to fail");
        assert!(result.is_err(), "expected call to fail");
        {
            let __e = result.as_ref().err().unwrap();
            assert!(
                format!("{:?}", __e).contains("dns") || __e.to_string().contains("dns"),
                "error message mismatch"
            );
        }
    });
}

#[test]
fn test_error_empty_batch_urls() {
    common::runtime().block_on(async {
        // Scraping a URL that cannot be found returns an error containing the URL path
        let engine_result = create_engine(None);
        let url = std::env::var("MOCK_SERVER_ERROR_EMPTY_BATCH_URLS").unwrap_or_else(|_| {
            let _ = common::mock_server_url();
            std::env::var("MOCK_SERVER_ERROR_EMPTY_BATCH_URLS")
                .unwrap_or_else(|_| format!("{}/fixtures/{}", common::mock_server_url(), "error_empty_batch_urls"))
        });
        let result = match engine_result {
            Err(e) => Err(e),
            Ok(engine) => scrape(&engine, &url).await,
        };
        assert!(result.is_err(), "expected call to fail");
        {
            let __e = result.as_ref().err().unwrap();
            assert!(
                format!("{:?}", __e).contains("not_found") || __e.to_string().contains("not_found"),
                "error message mismatch"
            );
        }
    });
}

#[test]
fn test_error_empty_response() {
    common::runtime().block_on(async {
        // Handles 200 with completely empty body gracefully
        let mock_route = MockRoute {
            path: "/",
            method: "POST",
            status: 200,
            body: r#""""#.to_string(),
            is_streaming: false,
            stream_chunks: vec![],
            headers: vec![],
            delay_ms: None,
        };
        let _mock_server = MockServer::start(vec![mock_route]).await;
        let engine = create_engine(None).expect("handle creation should succeed");
        let url = std::env::var("MOCK_SERVER_ERROR_EMPTY_RESPONSE").unwrap_or_else(|_| {
            let _ = common::mock_server_url();
            std::env::var("MOCK_SERVER_ERROR_EMPTY_RESPONSE")
                .unwrap_or_else(|_| format!("{}/fixtures/{}", common::mock_server_url(), "error_empty_response"))
        });
        let result = scrape(&engine, &url).await.expect("call failed");
        assert!(result.html.is_empty(), "expected empty value");
    });
}

#[test]
fn test_error_invalid_proxy() {
    common::runtime().block_on(async {
        // Proxy pointing to unreachable address causes connection error during scrape
        let engine_config: CrawlConfig =
            serde_json::from_str("{\"proxy\":{\"url\":\"http://127.0.0.1:1\"}}").expect("config should parse");
        let engine_result = create_engine(Some(engine_config));
        let url = std::env::var("MOCK_SERVER_ERROR_INVALID_PROXY").unwrap_or_else(|_| {
            let _ = common::mock_server_url();
            std::env::var("MOCK_SERVER_ERROR_INVALID_PROXY")
                .unwrap_or_else(|_| format!("{}/fixtures/{}", common::mock_server_url(), "error_invalid_proxy"))
        });
        let result = match engine_result {
            Err(e) => Err(e),
            Ok(engine) => scrape(&engine, &url).await,
        };
        assert!(result.is_err(), "expected call to fail");
        assert!(result.is_err(), "expected call to fail");
        {
            let __e = result.as_ref().err().unwrap();
            assert!(
                format!("{:?}", __e).contains("connection") || __e.to_string().contains("connection"),
                "error message mismatch"
            );
        }
    });
}

#[test]
fn test_error_partial_response() {
    common::runtime().block_on(async {
        // Handles incomplete or truncated HTTP response
        let mock_route = MockRoute {
            path: "/",
            method: "POST",
            status: 200,
            body: r#""<html><body><h1>Truncated""#.to_string(),
            is_streaming: false,
            stream_chunks: vec![],
            headers: vec![
                (r#"connection"#.to_string(), r#"close"#.to_string()),
                (r#"content-length"#.to_string(), r#"99999"#.to_string()),
                (r#"content-type"#.to_string(), r#"text/html; charset=utf-8"#.to_string()),
            ],
            delay_ms: None,
        };
        let _mock_server = MockServer::start(vec![mock_route]).await;
        let engine_result = create_engine(None);
        let url = std::env::var("MOCK_SERVER_ERROR_PARTIAL_RESPONSE").unwrap_or_else(|_| {
            let _ = common::mock_server_url();
            std::env::var("MOCK_SERVER_ERROR_PARTIAL_RESPONSE")
                .unwrap_or_else(|_| format!("{}/fixtures/{}", common::mock_server_url(), "error_partial_response"))
        });
        let result = match engine_result {
            Err(e) => Err(e),
            Ok(engine) => scrape(&engine, &url).await,
        };
        assert!(result.is_err(), "expected call to fail");
        assert!(result.is_err(), "expected call to fail");
        {
            let __e = result.as_ref().err().unwrap();
            assert!(
                format!("{:?}", __e).contains("data_loss") || __e.to_string().contains("data_loss"),
                "error message mismatch"
            );
        }
    });
}

#[test]
fn test_error_rate_limited() {
    common::runtime().block_on(async {
        // Handles 429 rate limiting with Retry-After
        let mock_route = MockRoute {
            path: "/",
            method: "POST",
            status: 429,
            body: r#""<html><body><h1>Too Many Requests</h1></body></html>""#.to_string(),
            is_streaming: false,
            stream_chunks: vec![],
            headers: vec![
                (r#"content-type"#.to_string(), r#"text/html"#.to_string()),
                (r#"retry-after"#.to_string(), r#"60"#.to_string()),
            ],
            delay_ms: None,
        };
        let _mock_server = MockServer::start(vec![mock_route]).await;
        let engine_result = create_engine(None);
        let url = std::env::var("MOCK_SERVER_ERROR_RATE_LIMITED").unwrap_or_else(|_| {
            let _ = common::mock_server_url();
            std::env::var("MOCK_SERVER_ERROR_RATE_LIMITED")
                .unwrap_or_else(|_| format!("{}/fixtures/{}", common::mock_server_url(), "error_rate_limited"))
        });
        let result = match engine_result {
            Err(e) => Err(e),
            Ok(engine) => scrape(&engine, &url).await,
        };
        assert!(result.is_err(), "expected call to fail");
        assert!(result.is_err(), "expected call to fail");
        {
            let __e = result.as_ref().err().unwrap();
            assert!(
                format!("{:?}", __e).contains("rate_limited") || __e.to_string().contains("rate_limited"),
                "error message mismatch"
            );
        }
    });
}

#[test]
fn test_error_retry_503() {
    common::runtime().block_on(async {
        // Retries request on 503 Service Unavailable response
        let mock_route = MockRoute {
            path: "/",
            method: "POST",
            status: 503,
            body: r#""<html><body>Service Unavailable</body></html>""#.to_string(),
            is_streaming: false,
            stream_chunks: vec![],
            headers: vec![(r#"content-type"#.to_string(), r#"text/html"#.to_string())],
            delay_ms: None,
        };
        let _mock_server = MockServer::start(vec![mock_route]).await;
        let engine_result = create_engine(None);
        let url = std::env::var("MOCK_SERVER_ERROR_RETRY_503").unwrap_or_else(|_| {
            let _ = common::mock_server_url();
            std::env::var("MOCK_SERVER_ERROR_RETRY_503")
                .unwrap_or_else(|_| format!("{}/fixtures/{}", common::mock_server_url(), "error_retry_503"))
        });
        let result = match engine_result {
            Err(e) => Err(e),
            Ok(engine) => scrape(&engine, &url).await,
        };
        assert!(result.is_err(), "expected call to fail");
        assert!(result.is_err(), "expected call to fail");
        {
            let __e = result.as_ref().err().unwrap();
            assert!(
                format!("{:?}", __e).contains("server_error") || __e.to_string().contains("server_error"),
                "error message mismatch"
            );
        }
    });
}

#[test]
fn test_error_retry_backoff() {
    common::runtime().block_on(async {
        // Implements exponential backoff when retrying failed requests
        let mock_route = MockRoute {
            path: "/",
            method: "POST",
            status: 429,
            body: r#""<html><body>Too Many Requests</body></html>""#.to_string(),
            is_streaming: false,
            stream_chunks: vec![],
            headers: vec![
                (r#"content-type"#.to_string(), r#"text/html"#.to_string()),
                (r#"retry-after"#.to_string(), r#"1"#.to_string()),
            ],
            delay_ms: None,
        };
        let _mock_server = MockServer::start(vec![mock_route]).await;
        let engine_result = create_engine(None);
        let url = std::env::var("MOCK_SERVER_ERROR_RETRY_BACKOFF").unwrap_or_else(|_| {
            let _ = common::mock_server_url();
            std::env::var("MOCK_SERVER_ERROR_RETRY_BACKOFF")
                .unwrap_or_else(|_| format!("{}/fixtures/{}", common::mock_server_url(), "error_retry_backoff"))
        });
        let result = match engine_result {
            Err(e) => Err(e),
            Ok(engine) => scrape(&engine, &url).await,
        };
        assert!(result.is_err(), "expected call to fail");
        assert!(result.is_err(), "expected call to fail");
        {
            let __e = result.as_ref().err().unwrap();
            assert!(
                format!("{:?}", __e).contains("rate_limited") || __e.to_string().contains("rate_limited"),
                "error message mismatch"
            );
        }
    });
}

#[test]
fn test_error_ssl_invalid_cert() {
    common::runtime().block_on(async {
        // Handles SSL certificate validation error
        let mock_route = MockRoute {
            path: "/",
            method: "POST",
            status: 302,
            body: r#""""#.to_string(),
            is_streaming: false,
            stream_chunks: vec![],
            headers: vec![(
                r#"location"#.to_string(),
                r#"https://self-signed.badssl.com/"#.to_string(),
            )],
            delay_ms: None,
        };
        let _mock_server = MockServer::start(vec![mock_route]).await;
        let engine_result = create_engine(None);
        let url = std::env::var("MOCK_SERVER_ERROR_SSL_INVALID_CERT").unwrap_or_else(|_| {
            let _ = common::mock_server_url();
            std::env::var("MOCK_SERVER_ERROR_SSL_INVALID_CERT")
                .unwrap_or_else(|_| format!("{}/fixtures/{}", common::mock_server_url(), "error_ssl_invalid_cert"))
        });
        let result = match engine_result {
            Err(e) => Err(e),
            Ok(engine) => scrape(&engine, &url).await,
        };
        assert!(result.is_err(), "expected call to fail");
        assert!(result.is_err(), "expected call to fail");
        {
            let __e = result.as_ref().err().unwrap();
            assert!(
                format!("{:?}", __e).contains("ssl") || __e.to_string().contains("ssl"),
                "error message mismatch"
            );
        }
    });
}

#[test]
fn test_error_timeout() {
    common::runtime().block_on(async {
        // Mock server delays response longer than request_timeout, surfacing a timeout error
        let engine_config: CrawlConfig =
            serde_json::from_str("{\"request_timeout\":500}").expect("config should parse");
        let engine_result = create_engine(Some(engine_config));
        let url = std::env::var("MOCK_SERVER_ERROR_TIMEOUT").unwrap_or_else(|_| {
            let _ = common::mock_server_url();
            std::env::var("MOCK_SERVER_ERROR_TIMEOUT")
                .unwrap_or_else(|_| format!("{}/fixtures/{}", common::mock_server_url(), "error_timeout"))
        });
        let result = match engine_result {
            Err(e) => Err(e),
            Ok(engine) => scrape(&engine, &url).await,
        };
        assert!(result.is_err(), "expected call to fail");
        assert!(result.is_err(), "expected call to fail");
        {
            let __e = result.as_ref().err().unwrap();
            assert!(
                format!("{:?}", __e).contains("timeout") || __e.to_string().contains("timeout"),
                "error message mismatch"
            );
        }
    });
}

#[test]
fn test_error_unsupported_scheme() {
    common::runtime().block_on(async {
        // Unsupported URL scheme (gopher) is rejected
        let engine_result = create_engine(None);
        let url = r#"gopher://invalid.example.com/"#.to_string();
        let result = match engine_result {
            Err(e) => Err(e),
            Ok(engine) => scrape(&engine, &url).await,
        };
        assert!(result.is_err(), "expected call to fail");
        assert!(result.is_err(), "expected call to fail");
        {
            let __e = result.as_ref().err().unwrap();
            assert!(
                format!("{:?}", __e).contains("disallowed scheme: gopher")
                    || __e.to_string().contains("disallowed scheme: gopher"),
                "error message mismatch"
            );
        }
    });
}

#[test]
fn test_error_waf_akamai() {
    common::runtime().block_on(async {
        // Akamai WAF detection returns WafBlocked error
        let mock_route = MockRoute {
            path: "/",
            method: "POST",
            status: 403,
            body: r#""<html><body>Access Denied. Reference #18.abc123</body></html>""#.to_string(),
            is_streaming: false,
            stream_chunks: vec![],
            headers: vec![
                (r#"content-type"#.to_string(), r#"text/html"#.to_string()),
                (r#"server"#.to_string(), r#"AkamaiGHost"#.to_string()),
            ],
            delay_ms: None,
        };
        let _mock_server = MockServer::start(vec![mock_route]).await;
        let engine_config: CrawlConfig =
            serde_json::from_str("{\"browser\":{\"mode\":\"never\"}}").expect("config should parse");
        let engine_result = create_engine(Some(engine_config));
        let url = std::env::var("MOCK_SERVER_ERROR_WAF_AKAMAI").unwrap_or_else(|_| {
            let _ = common::mock_server_url();
            std::env::var("MOCK_SERVER_ERROR_WAF_AKAMAI")
                .unwrap_or_else(|_| format!("{}/fixtures/{}", common::mock_server_url(), "error_waf_akamai"))
        });
        let result = match engine_result {
            Err(e) => Err(e),
            Ok(engine) => scrape(&engine, &url).await,
        };
        assert!(result.is_err(), "expected call to fail");
    });
}

#[test]
fn test_error_waf_blocked() {
    common::runtime().block_on(async {
    // WAF challenge/block detection returns WafBlocked error
    let mock_route = MockRoute {
        path: "/",
        method: "POST",
        status: 403,
        body: r#""<html><body><h1>403 Forbidden</h1><p>You have been blocked by our security system.</p></body></html>""#.to_string(),
        is_streaming: false,
        stream_chunks: vec![],
        headers: vec![
            (r#"content-type"#.to_string(), r#"text/html; charset=utf-8"#.to_string()),
            (r#"server"#.to_string(), r#"Cloudflare"#.to_string()),
        ],
        delay_ms: None,
    };
    let _mock_server = MockServer::start(vec![mock_route]).await;
    let engine_config: CrawlConfig = serde_json::from_str("{\"browser\":{\"mode\":\"never\"}}").expect("config should parse");
    let engine_result = create_engine(Some(engine_config));
    let url = std::env::var("MOCK_SERVER_ERROR_WAF_BLOCKED").unwrap_or_else(|_| { let _ = common::mock_server_url(); std::env::var("MOCK_SERVER_ERROR_WAF_BLOCKED").unwrap_or_else(|_| format!("{}/fixtures/{}", common::mock_server_url(), "error_waf_blocked")) });
    let result = match engine_result {
        Err(e) => Err(e),
        Ok(engine) => {
            scrape(&engine, &url).await
        }
    };
    assert!(result.is_err(), "expected call to fail");
    assert!(result.is_err(), "expected call to fail");
    { let __e = result.as_ref().err().unwrap(); assert!(format!("{:?}", __e).contains("waf") || __e.to_string().contains("waf"), "error message mismatch"); }
    });
}

#[test]
fn test_error_waf_false_403() {
    common::runtime().block_on(async {
    // Detects WAF/bot protection false 403 (Cloudflare challenge page)
    let mock_route = MockRoute {
        path: "/",
        method: "POST",
        status: 403,
        body: r#""<html><head><title>Just a moment...</title></head><body><div class=\"cf-browser-verification\"></div></body></html>""#.to_string(),
        is_streaming: false,
        stream_chunks: vec![],
        headers: vec![
            (r#"content-type"#.to_string(), r#"text/html; charset=utf-8"#.to_string()),
            (r#"server"#.to_string(), r#"cloudflare"#.to_string()),
        ],
        delay_ms: None,
    };
    let _mock_server = MockServer::start(vec![mock_route]).await;
    let engine_config: CrawlConfig = serde_json::from_str("{\"browser\":{\"mode\":\"never\"}}").expect("config should parse");
    let engine_result = create_engine(Some(engine_config));
    let url = std::env::var("MOCK_SERVER_ERROR_WAF_FALSE_403").unwrap_or_else(|_| { let _ = common::mock_server_url(); std::env::var("MOCK_SERVER_ERROR_WAF_FALSE_403").unwrap_or_else(|_| format!("{}/fixtures/{}", common::mock_server_url(), "error_waf_false_403")) });
    let result = match engine_result {
        Err(e) => Err(e),
        Ok(engine) => {
            scrape(&engine, &url).await
        }
    };
    assert!(result.is_err(), "expected call to fail");
    assert!(result.is_err(), "expected call to fail");
    { let __e = result.as_ref().err().unwrap(); assert!(format!("{:?}", __e).contains("waf") || __e.to_string().contains("waf"), "error message mismatch"); }
    });
}

#[test]
fn test_error_waf_imperva() {
    common::runtime().block_on(async {
        // Imperva/Incapsula WAF detection
        let mock_route = MockRoute {
            path: "/",
            method: "POST",
            status: 403,
            body: r#""<html><body>Powered by Incapsula. _incap_ses_ cookie required.</body></html>""#.to_string(),
            is_streaming: false,
            stream_chunks: vec![],
            headers: vec![(r#"content-type"#.to_string(), r#"text/html"#.to_string())],
            delay_ms: None,
        };
        let _mock_server = MockServer::start(vec![mock_route]).await;
        let engine_config: CrawlConfig =
            serde_json::from_str("{\"browser\":{\"mode\":\"never\"}}").expect("config should parse");
        let engine_result = create_engine(Some(engine_config));
        let url = std::env::var("MOCK_SERVER_ERROR_WAF_IMPERVA").unwrap_or_else(|_| {
            let _ = common::mock_server_url();
            std::env::var("MOCK_SERVER_ERROR_WAF_IMPERVA")
                .unwrap_or_else(|_| format!("{}/fixtures/{}", common::mock_server_url(), "error_waf_imperva"))
        });
        let result = match engine_result {
            Err(e) => Err(e),
            Ok(engine) => scrape(&engine, &url).await,
        };
        assert!(result.is_err(), "expected call to fail");
    });
}
