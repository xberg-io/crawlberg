// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:2df9d68eaeea209254a60a0ab74d168129b8c1b38ffcd7edb1e8ee172baaade4
// To regenerate: alef generate
// To verify freshness: alef verify
//! E2e tests for category: links

use crawlberg::create_engine;
use crawlberg::scrape;
mod common;
mod mock_server;
#[allow(unused_imports)]
use mock_server::{MockRoute, MockServer};

#[tokio::test]
async fn test_links_anchor_fragment() {
    // Identifies fragment-only links as anchor type
    let engine = create_engine(None).expect("handle creation should succeed");
    let url = std::env::var("MOCK_SERVER_LINKS_ANCHOR_FRAGMENT").unwrap_or_else(|_| {
        let _ = common::mock_server_url();
        std::env::var("MOCK_SERVER_LINKS_ANCHOR_FRAGMENT")
            .unwrap_or_else(|_| format!("{}/fixtures/{}", common::mock_server_url(), "links_anchor_fragment"))
    });
    let result = scrape(&engine, &url).await.expect("call failed");
    assert!(
        result
            .links
            .iter()
            .any(|e| format!("{:?}", e.link_type).contains(r#"anchor"#)),
        "expected some element of links[].link_type to contain: {}",
        r#"anchor"#
    );
}

#[tokio::test]
async fn test_links_base_tag() {
    // Resolves relative URLs using base tag href
    let engine = create_engine(None).expect("handle creation should succeed");
    let url = std::env::var("MOCK_SERVER_LINKS_BASE_TAG").unwrap_or_else(|_| {
        let _ = common::mock_server_url();
        std::env::var("MOCK_SERVER_LINKS_BASE_TAG")
            .unwrap_or_else(|_| format!("{}/fixtures/{}", common::mock_server_url(), "links_base_tag"))
    });
    let result = scrape(&engine, &url).await.expect("call failed");
    assert!(result.links.len() > 2, "expected > 2");
    assert!(
        result
            .links
            .iter()
            .any(|e| e.url.to_string().contains(r#"example.com"#)),
        "expected some element of links[].url to contain: {}",
        r#"example.com"#
    );
}

#[tokio::test]
async fn test_links_document_types() {
    // Detects PDF, DOCX, XLSX links as document type
    let engine = create_engine(None).expect("handle creation should succeed");
    let url = std::env::var("MOCK_SERVER_LINKS_DOCUMENT_TYPES").unwrap_or_else(|_| {
        let _ = common::mock_server_url();
        std::env::var("MOCK_SERVER_LINKS_DOCUMENT_TYPES")
            .unwrap_or_else(|_| format!("{}/fixtures/{}", common::mock_server_url(), "links_document_types"))
    });
    let result = scrape(&engine, &url).await.expect("call failed");
    assert!(
        result
            .links
            .iter()
            .any(|e| format!("{:?}", e.link_type).contains(r#"document"#)),
        "expected some element of links[].link_type to contain: {}",
        r#"document"#
    );
}

#[tokio::test]
async fn test_links_empty_href() {
    // Handles empty href attributes without errors
    let engine = create_engine(None).expect("handle creation should succeed");
    let url = std::env::var("MOCK_SERVER_LINKS_EMPTY_HREF").unwrap_or_else(|_| {
        let _ = common::mock_server_url();
        std::env::var("MOCK_SERVER_LINKS_EMPTY_HREF")
            .unwrap_or_else(|_| format!("{}/fixtures/{}", common::mock_server_url(), "links_empty_href"))
    });
    let result = scrape(&engine, &url).await.expect("call failed");
    assert!(!result.links.is_empty(), "expected > 0");
    assert!(
        result.links.iter().any(|e| e.url.to_string().contains(r#"/valid"#)),
        "expected some element of links[].url to contain: {}",
        r#"/valid"#
    );
}

#[tokio::test]
async fn test_links_internal_external_classification() {
    // Correctly classifies internal vs external links by domain
    let engine = create_engine(None).expect("handle creation should succeed");
    let url = std::env::var("MOCK_SERVER_LINKS_INTERNAL_EXTERNAL_CLASSIFICATION").unwrap_or_else(|_| {
        let _ = common::mock_server_url();
        std::env::var("MOCK_SERVER_LINKS_INTERNAL_EXTERNAL_CLASSIFICATION").unwrap_or_else(|_| {
            format!(
                "{}/fixtures/{}",
                common::mock_server_url(),
                "links_internal_external_classification"
            )
        })
    });
    let result = scrape(&engine, &url).await.expect("call failed");
    assert!(result.links.len() > 4, "expected > 4");
    assert!(
        result.links.iter().any(|e| !e.url.to_string().is_empty()),
        "expected some element of links[].url to be non-empty"
    );
}

#[tokio::test]
async fn test_links_mailto_javascript_skip() {
    // Skips mailto:, javascript:, and tel: scheme links
    let engine = create_engine(None).expect("handle creation should succeed");
    let url = std::env::var("MOCK_SERVER_LINKS_MAILTO_JAVASCRIPT_SKIP").unwrap_or_else(|_| {
        let _ = common::mock_server_url();
        std::env::var("MOCK_SERVER_LINKS_MAILTO_JAVASCRIPT_SKIP").unwrap_or_else(|_| {
            format!(
                "{}/fixtures/{}",
                common::mock_server_url(),
                "links_mailto_javascript_skip"
            )
        })
    });
    let result = scrape(&engine, &url).await.expect("call failed");
    assert!(!result.links.is_empty(), "expected > 0");
    assert!(
        !result.links.iter().any(|e| e.url.to_string().contains(r#"mailto:"#)),
        "expected no element of links[].url to contain: {}",
        r#"mailto:"#
    );
}

#[tokio::test]
async fn test_links_protocol_relative() {
    // Handles protocol-relative URLs (//example.com) correctly
    let engine = create_engine(None).expect("handle creation should succeed");
    let url = std::env::var("MOCK_SERVER_LINKS_PROTOCOL_RELATIVE").unwrap_or_else(|_| {
        let _ = common::mock_server_url();
        std::env::var("MOCK_SERVER_LINKS_PROTOCOL_RELATIVE")
            .unwrap_or_else(|_| format!("{}/fixtures/{}", common::mock_server_url(), "links_protocol_relative"))
    });
    let result = scrape(&engine, &url).await.expect("call failed");
    assert!(result.links.len() > 1, "expected > 1");
    assert!(
        result
            .links
            .iter()
            .any(|e| e.url.to_string().contains(r#"http://cdn.example.com/resource"#)),
        "expected some element of links[].url to contain: {}",
        r#"http://cdn.example.com/resource"#
    );
    assert!(
        result
            .links
            .iter()
            .any(|e| e.url.to_string().contains(r#"http://images.example.com/photo.jpg"#)),
        "expected some element of links[].url to contain: {}",
        r#"http://images.example.com/photo.jpg"#
    );
}

#[tokio::test]
async fn test_links_rel_attributes() {
    // Preserves rel=nofollow and rel=canonical attributes
    let engine = create_engine(None).expect("handle creation should succeed");
    let url = std::env::var("MOCK_SERVER_LINKS_REL_ATTRIBUTES").unwrap_or_else(|_| {
        let _ = common::mock_server_url();
        std::env::var("MOCK_SERVER_LINKS_REL_ATTRIBUTES")
            .unwrap_or_else(|_| format!("{}/fixtures/{}", common::mock_server_url(), "links_rel_attributes"))
    });
    let result = scrape(&engine, &url).await.expect("call failed");
    assert!(!result.links.is_empty(), "expected > 0");
}

#[tokio::test]
async fn test_links_relative_parent() {
    // Resolves ../ and ./ relative parent path links correctly
    let engine = create_engine(None).expect("handle creation should succeed");
    let url = std::env::var("MOCK_SERVER_LINKS_RELATIVE_PARENT").unwrap_or_else(|_| {
        let _ = common::mock_server_url();
        std::env::var("MOCK_SERVER_LINKS_RELATIVE_PARENT")
            .unwrap_or_else(|_| format!("{}/fixtures/{}", common::mock_server_url(), "links_relative_parent"))
    });
    let result = scrape(&engine, &url).await.expect("call failed");
    assert!(result.links.len() > 3, "expected > 3");
}
