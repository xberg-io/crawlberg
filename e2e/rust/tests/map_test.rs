// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:7f698325d8c3539b4fd2b753bba12eed294427d40a41dc74c71de4e9eab1f426
// To regenerate: alef generate
// To verify freshness: alef verify
//! E2e tests for category: map

use crawlberg::CrawlConfig;
use crawlberg::create_engine;
use crawlberg::map_urls;
mod common;
mod mock_server;
#[allow(unused_imports)]
use mock_server::{MockRoute, MockServer};

#[test]
fn test_map_discover_urls() {
    common::runtime().block_on(async {
        // Discovers all URLs on a site without fetching full content
        let engine_config: CrawlConfig =
            serde_json::from_str("{\"max_depth\":0,\"respect_robots_txt\":false}").expect("config should parse");
        let engine = create_engine(Some(engine_config)).expect("handle creation should succeed");
        let url = std::env::var("MOCK_SERVER_MAP_DISCOVER_URLS").unwrap_or_else(|_| {
            let _ = common::mock_server_url();
            std::env::var("MOCK_SERVER_MAP_DISCOVER_URLS")
                .unwrap_or_else(|_| format!("{}/fixtures/{}", common::mock_server_url(), "map_discover_urls"))
        });
        let result = map_urls(&engine, &url).await.expect("call failed");
        assert!(result.urls.len() >= 3, "expected >= 3");
    });
}

#[test]
fn test_map_exclude_patterns() {
    common::runtime().block_on(async {
        // Excludes URLs matching patterns from URL map
        let engine_config: CrawlConfig = serde_json::from_str(
            "{\"exclude_paths\":[\"/private/.*\",\"/api/.*\"],\"max_depth\":0,\"respect_robots_txt\":false}",
        )
        .expect("config should parse");
        let engine = create_engine(Some(engine_config)).expect("handle creation should succeed");
        let url = std::env::var("MOCK_SERVER_MAP_EXCLUDE_PATTERNS").unwrap_or_else(|_| {
            let _ = common::mock_server_url();
            std::env::var("MOCK_SERVER_MAP_EXCLUDE_PATTERNS")
                .unwrap_or_else(|_| format!("{}/fixtures/{}", common::mock_server_url(), "map_exclude_patterns"))
        });
        let result = map_urls(&engine, &url).await.expect("call failed");
        assert_eq!(result.urls.len(), 1, "equals assertion failed");
    });
}

#[test]
fn test_map_include_subdomains() {
    common::runtime().block_on(async {
        // Includes subdomain URLs in URL map discovery; page has 1 local and 1 subdomain link
        let engine_config: CrawlConfig =
            serde_json::from_str("{\"allow_subdomains\":true,\"max_depth\":0,\"respect_robots_txt\":false}")
                .expect("config should parse");
        let engine = create_engine(Some(engine_config)).expect("handle creation should succeed");
        let url = std::env::var("MOCK_SERVER_MAP_INCLUDE_SUBDOMAINS").unwrap_or_else(|_| {
            let _ = common::mock_server_url();
            std::env::var("MOCK_SERVER_MAP_INCLUDE_SUBDOMAINS")
                .unwrap_or_else(|_| format!("{}/fixtures/{}", common::mock_server_url(), "map_include_subdomains"))
        });
        let result = map_urls(&engine, &url).await.expect("call failed");
        assert!(result.urls.len() >= 2, "expected >= 2");
    });
}

#[test]
fn test_map_large_sitemap() {
    common::runtime().block_on(async {
        // Handles large sitemap with 100+ URLs
        let engine_config: CrawlConfig =
            serde_json::from_str("{\"respect_robots_txt\":false}").expect("config should parse");
        let engine = create_engine(Some(engine_config)).expect("handle creation should succeed");
        let url = std::env::var("MOCK_SERVER_MAP_LARGE_SITEMAP").unwrap_or_else(|_| {
            let _ = common::mock_server_url();
            std::env::var("MOCK_SERVER_MAP_LARGE_SITEMAP")
                .unwrap_or_else(|_| format!("{}/fixtures/{}", common::mock_server_url(), "map_large_sitemap"))
        });
        let result = map_urls(&engine, &url).await.expect("call failed");
        assert!(result.urls.len() >= 100, "expected >= 100");
    });
}

#[test]
fn test_map_limit_pagination() {
    common::runtime().block_on(async {
        // Limits map result count to specified maximum
        let engine_config: CrawlConfig =
            serde_json::from_str("{\"map_limit\":5,\"max_depth\":0,\"respect_robots_txt\":false}")
                .expect("config should parse");
        let engine = create_engine(Some(engine_config)).expect("handle creation should succeed");
        let url = std::env::var("MOCK_SERVER_MAP_LIMIT_PAGINATION").unwrap_or_else(|_| {
            let _ = common::mock_server_url();
            std::env::var("MOCK_SERVER_MAP_LIMIT_PAGINATION")
                .unwrap_or_else(|_| format!("{}/fixtures/{}", common::mock_server_url(), "map_limit_pagination"))
        });
        let result = map_urls(&engine, &url).await.expect("call failed");
        assert!(result.urls.len() <= 5, "expected <= 5");
    });
}

#[test]
fn test_map_search_filter() {
    common::runtime().block_on(async {
        // Filters map results by search keyword; 4 links in page but only 2 match 'blog'
        let engine_config: CrawlConfig =
            serde_json::from_str("{\"map_search\":\"blog\",\"max_depth\":0,\"respect_robots_txt\":false}")
                .expect("config should parse");
        let engine = create_engine(Some(engine_config)).expect("handle creation should succeed");
        let url = std::env::var("MOCK_SERVER_MAP_SEARCH_FILTER").unwrap_or_else(|_| {
            let _ = common::mock_server_url();
            std::env::var("MOCK_SERVER_MAP_SEARCH_FILTER")
                .unwrap_or_else(|_| format!("{}/fixtures/{}", common::mock_server_url(), "map_search_filter"))
        });
        let result = map_urls(&engine, &url).await.expect("call failed");
        assert!(result.urls.len() >= 2, "expected >= 2");
        assert!(result.urls.len() <= 2, "expected <= 2");
    });
}
