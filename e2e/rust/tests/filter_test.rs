// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:0c1a6685baeacdf6458cdffb68bfe1f39c13e64072329f1b64d470d2e25c4e39
// To regenerate: alef generate
// To verify freshness: alef verify
//! E2e tests for category: filter

use crawlberg::CrawlConfig;
use crawlberg::crawl;
use crawlberg::create_engine;
mod common;
mod mock_server;
#[allow(unused_imports)]
use mock_server::{MockRoute, MockServer};

#[tokio::test]
async fn test_filter_bm25_crawl_integration() {
    // BM25 filter works during multi-page crawl, keeping relevant pages
    let engine_config: CrawlConfig = serde_json::from_str("{\"bm25_query\":\"rust programming\",\"bm25_threshold\":0.1,\"content_filter\":\"bm25\",\"max_concurrent\":1,\"max_depth\":1}").expect("config should parse");
    let engine = create_engine(Some(engine_config)).expect("handle creation should succeed");
    let url = std::env::var("MOCK_SERVER_FILTER_BM25_CRAWL_INTEGRATION").unwrap_or_else(|_| {
        let _ = common::mock_server_url();
        std::env::var("MOCK_SERVER_FILTER_BM25_CRAWL_INTEGRATION").unwrap_or_else(|_| {
            format!(
                "{}/fixtures/{}",
                common::mock_server_url(),
                "filter_bm25_crawl_integration"
            )
        })
    });
    let result = crawl(&engine, &url).await.expect("call failed");
    assert!(
        result.pages[0].html.contains(r#"rust"#),
        "expected to contain: {}",
        r#"rust"#
    );
}

#[tokio::test]
async fn test_filter_bm25_empty_query() {
    // BM25 filter with empty query passes all pages through
    let engine_config: CrawlConfig = serde_json::from_str(
        "{\"bm25_query\":\"\",\"bm25_threshold\":0.1,\"content_filter\":\"bm25\",\"max_depth\":1}",
    )
    .expect("config should parse");
    let engine = create_engine(Some(engine_config)).expect("handle creation should succeed");
    let url = std::env::var("MOCK_SERVER_FILTER_BM25_EMPTY_QUERY").unwrap_or_else(|_| {
        let _ = common::mock_server_url();
        std::env::var("MOCK_SERVER_FILTER_BM25_EMPTY_QUERY")
            .unwrap_or_else(|_| format!("{}/fixtures/{}", common::mock_server_url(), "filter_bm25_empty_query"))
    });
    let result = crawl(&engine, &url).await.expect("call failed");
    assert_eq!(result.pages.len(), 2, "equals assertion failed");
}

#[tokio::test]
async fn test_filter_bm25_high_threshold() {
    // BM25 filter with very high threshold filters out all pages
    let engine_config: CrawlConfig = serde_json::from_str(
        "{\"bm25_query\":\"quantum physics\",\"bm25_threshold\":100.0,\"content_filter\":\"bm25\",\"max_depth\":1}",
    )
    .expect("config should parse");
    let engine = create_engine(Some(engine_config)).expect("handle creation should succeed");
    let url = std::env::var("MOCK_SERVER_FILTER_BM25_HIGH_THRESHOLD").unwrap_or_else(|_| {
        let _ = common::mock_server_url();
        std::env::var("MOCK_SERVER_FILTER_BM25_HIGH_THRESHOLD").unwrap_or_else(|_| {
            format!(
                "{}/fixtures/{}",
                common::mock_server_url(),
                "filter_bm25_high_threshold"
            )
        })
    });
    let result = crawl(&engine, &url).await.expect("call failed");
    assert_eq!(result.pages.len(), 0, "equals assertion failed");
}

#[tokio::test]
async fn test_filter_bm25_relevant_pages() {
    // BM25 filter keeps only pages relevant to the query
    let engine_config: CrawlConfig = serde_json::from_str("{\"bm25_query\":\"rust programming language\",\"bm25_threshold\":0.1,\"content_filter\":\"bm25\",\"max_depth\":1}").expect("config should parse");
    let engine = create_engine(Some(engine_config)).expect("handle creation should succeed");
    let url = std::env::var("MOCK_SERVER_FILTER_BM25_RELEVANT_PAGES").unwrap_or_else(|_| {
        let _ = common::mock_server_url();
        std::env::var("MOCK_SERVER_FILTER_BM25_RELEVANT_PAGES").unwrap_or_else(|_| {
            format!(
                "{}/fixtures/{}",
                common::mock_server_url(),
                "filter_bm25_relevant_pages"
            )
        })
    });
    let result = crawl(&engine, &url).await.expect("call failed");
    assert!(
        result.pages[0].html.contains(r#"rust"#),
        "expected to contain: {}",
        r#"rust"#
    );
}

#[tokio::test]
async fn test_filter_bm25_threshold_zero() {
    // BM25 filter with zero threshold passes all pages
    let engine_config: CrawlConfig = serde_json::from_str(
        "{\"bm25_query\":\"unrelated query\",\"bm25_threshold\":0.0,\"content_filter\":\"bm25\",\"max_depth\":1}",
    )
    .expect("config should parse");
    let engine = create_engine(Some(engine_config)).expect("handle creation should succeed");
    let url = std::env::var("MOCK_SERVER_FILTER_BM25_THRESHOLD_ZERO").unwrap_or_else(|_| {
        let _ = common::mock_server_url();
        std::env::var("MOCK_SERVER_FILTER_BM25_THRESHOLD_ZERO").unwrap_or_else(|_| {
            format!(
                "{}/fixtures/{}",
                common::mock_server_url(),
                "filter_bm25_threshold_zero"
            )
        })
    });
    let result = crawl(&engine, &url).await.expect("call failed");
    assert_eq!(result.pages.len(), 2, "equals assertion failed");
}

#[tokio::test]
async fn test_filter_noop_crawl_all_kept() {
    // NoopFilter keeps all pages during a multi-page crawl
    let engine_config: CrawlConfig =
        serde_json::from_str("{\"max_concurrent\":1,\"max_depth\":1}").expect("config should parse");
    let engine = create_engine(Some(engine_config)).expect("handle creation should succeed");
    let url = std::env::var("MOCK_SERVER_FILTER_NOOP_CRAWL_ALL_KEPT").unwrap_or_else(|_| {
        let _ = common::mock_server_url();
        std::env::var("MOCK_SERVER_FILTER_NOOP_CRAWL_ALL_KEPT").unwrap_or_else(|_| {
            format!(
                "{}/fixtures/{}",
                common::mock_server_url(),
                "filter_noop_crawl_all_kept"
            )
        })
    });
    let result = crawl(&engine, &url).await.expect("call failed");
    assert_eq!(result.pages.len(), 3, "equals assertion failed");
}

#[tokio::test]
async fn test_filter_noop_passes_all() {
    // No content filter passes all crawled pages through
    let engine_config: CrawlConfig = serde_json::from_str("{\"max_depth\":1}").expect("config should parse");
    let engine = create_engine(Some(engine_config)).expect("handle creation should succeed");
    let url = std::env::var("MOCK_SERVER_FILTER_NOOP_PASSES_ALL").unwrap_or_else(|_| {
        let _ = common::mock_server_url();
        std::env::var("MOCK_SERVER_FILTER_NOOP_PASSES_ALL")
            .unwrap_or_else(|_| format!("{}/fixtures/{}", common::mock_server_url(), "filter_noop_passes_all"))
    });
    let result = crawl(&engine, &url).await.expect("call failed");
    assert_eq!(result.pages.len(), 3, "equals assertion failed");
}
