// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:cf877997879ab0cb5f48271e5c99be5398b0dab3b273c3410db6ee3588b89dcb
// To regenerate: alef generate
// To verify freshness: alef verify

using System;
using System.Collections.Generic;
using System.Linq;
using System.Net.Http;
using System.Text;
using System.Text.Json;
using System.Text.Json.Serialization;
using System.Threading.Tasks;
using Xunit;
using Crawlberg;
using static Crawlberg.CrawlbergConverter;

namespace Crawlberg
{
    /// <summary>E2e tests for category: validation.</summary>
    public class ValidationTests
    {
            private static readonly JsonSerializerOptions ConfigOptions = new() { Converters = { new JsonStringEnumConverter(JsonNamingPolicy.SnakeCaseLower) }, DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingDefault };

        [Fact]
        public async Task Test_ValidationBrowserEndpointInvalid()
        {
            // Browser endpoint must be a valid ws:// or wss:// URL, not http://
            var thrown = await Assert.ThrowsAnyAsync<CrawlbergException>(async () =>
            {
                var engineConfig = JsonSerializer.Deserialize<CrawlConfig>("{\"browser\":{\"endpoint\":\"http://not-websocket:3000\",\"mode\":\"always\"}}", ConfigOptions)!;
                var engine = CrawlbergConverter.CreateEngine(engineConfig);
                var url = Environment.GetEnvironmentVariable("MOCK_SERVER_URL") + "/fixtures/validation_browser_endpoint_invalid";
                await CrawlbergConverter.ScrapeAsync(engine, url);
            });
            Assert.True(thrown.Message != null && thrown.Message.Contains("endpoint") || thrown.GetType().Name.Contains("endpoint"), "expected error to match: endpoint");
        }

        [Fact]
        public async Task Test_ValidationInvalidAuthConfig()
        {
            // auth object with empty username in basic auth is rejected
            var thrown = await Assert.ThrowsAnyAsync<CrawlbergException>(async () =>
            {
                var engineConfig = JsonSerializer.Deserialize<CrawlConfig>("{\"auth\":{\"password\":\"secret\",\"type\":\"basic\",\"username\":\"\"}}", ConfigOptions)!;
                var engine = CrawlbergConverter.CreateEngine(engineConfig);
                var url = Environment.GetEnvironmentVariable("MOCK_SERVER_URL") + "/fixtures/validation_invalid_auth_config";
                await CrawlbergConverter.ScrapeAsync(engine, url);
            });
            Assert.True(thrown.Message != null && thrown.Message.Contains("auth") || thrown.GetType().Name.Contains("auth"), "expected error to match: auth");
        }

        [Fact]
        public async Task Test_ValidationInvalidExcludeRegex()
        {
            // Invalid regex in exclude_paths is rejected
            var thrown = await Assert.ThrowsAnyAsync<CrawlbergException>(async () =>
            {
                var engineConfig = JsonSerializer.Deserialize<CrawlConfig>("{\"exclude_paths\":[\"(unclosed\"]}", ConfigOptions)!;
                var engine = CrawlbergConverter.CreateEngine(engineConfig);
                var url = Environment.GetEnvironmentVariable("MOCK_SERVER_URL") + "/fixtures/validation_invalid_exclude_regex";
                await CrawlbergConverter.ScrapeAsync(engine, url);
            });
            Assert.True(thrown.Message != null && thrown.Message.Contains("exclude_path") || thrown.GetType().Name.Contains("exclude_path"), "expected error to match: exclude_path");
        }

        [Fact]
        public async Task Test_ValidationInvalidIncludeRegex()
        {
            // Invalid regex in include_paths is rejected
            var thrown = await Assert.ThrowsAnyAsync<CrawlbergException>(async () =>
            {
                var engineConfig = JsonSerializer.Deserialize<CrawlConfig>("{\"include_paths\":[\"[invalid\"]}", ConfigOptions)!;
                var engine = CrawlbergConverter.CreateEngine(engineConfig);
                var url = Environment.GetEnvironmentVariable("MOCK_SERVER_URL") + "/fixtures/validation_invalid_include_regex";
                await CrawlbergConverter.ScrapeAsync(engine, url);
            });
            Assert.True(thrown.Message != null && thrown.Message.Contains("include_path") || thrown.GetType().Name.Contains("include_path"), "expected error to match: include_path");
        }

        [Fact]
        public async Task Test_ValidationInvalidProxyUrl()
        {
            // proxy with invalid URL like 'not-a-url' is rejected
            var thrown = await Assert.ThrowsAnyAsync<CrawlbergException>(async () =>
            {
                var engineConfig = JsonSerializer.Deserialize<CrawlConfig>("{\"proxy\":{\"url\":\"not-a-url\"}}", ConfigOptions)!;
                var engine = CrawlbergConverter.CreateEngine(engineConfig);
                var url = Environment.GetEnvironmentVariable("MOCK_SERVER_URL") + "/fixtures/validation_invalid_proxy_url";
                await CrawlbergConverter.ScrapeAsync(engine, url);
            });
            Assert.True(thrown.Message != null && thrown.Message.Contains("proxy") || thrown.GetType().Name.Contains("proxy"), "expected error to match: proxy");
        }

        [Fact]
        public async Task Test_ValidationInvalidRetryCode()
        {
            // Retry code outside 100-599 is rejected
            var thrown = await Assert.ThrowsAnyAsync<CrawlbergException>(async () =>
            {
                var engineConfig = JsonSerializer.Deserialize<CrawlConfig>("{\"retry_codes\":[999]}", ConfigOptions)!;
                var engine = CrawlbergConverter.CreateEngine(engineConfig);
                var url = Environment.GetEnvironmentVariable("MOCK_SERVER_URL") + "/fixtures/validation_invalid_retry_code";
                await CrawlbergConverter.ScrapeAsync(engine, url);
            });
            Assert.True(thrown.Message != null && thrown.Message.Contains("retry code") || thrown.GetType().Name.Contains("retry code"), "expected error to match: retry code");
        }

        [Fact]
        public async Task Test_ValidationMaxConcurrentZero()
        {
            // max_concurrent=0 is rejected as invalid config (minimum is 1)
            var thrown = await Assert.ThrowsAnyAsync<CrawlbergException>(async () =>
            {
                var engineConfig = JsonSerializer.Deserialize<CrawlConfig>("{\"max_concurrent\":0}", ConfigOptions)!;
                var engine = CrawlbergConverter.CreateEngine(engineConfig);
                var url = Environment.GetEnvironmentVariable("MOCK_SERVER_URL") + "/fixtures/validation_max_concurrent_zero";
                await CrawlbergConverter.ScrapeAsync(engine, url);
            });
            Assert.True(thrown.Message != null && thrown.Message.Contains("max_concurrent") || thrown.GetType().Name.Contains("max_concurrent"), "expected error to match: max_concurrent");
        }

        [Fact]
        public async Task Test_ValidationMaxDepthTooHigh()
        {
            // max_depth=200 exceeds limit of 100
            var thrown = await Assert.ThrowsAnyAsync<CrawlbergException>(async () =>
            {
                var engineConfig = JsonSerializer.Deserialize<CrawlConfig>("{\"max_depth\":200}", ConfigOptions)!;
                var engine = CrawlbergConverter.CreateEngine(engineConfig);
                var url = Environment.GetEnvironmentVariable("MOCK_SERVER_URL") + "/fixtures/validation_max_depth_too_high";
                await CrawlbergConverter.ScrapeAsync(engine, url);
            });
            Assert.True(thrown.Message != null && thrown.Message.Contains("max_depth") || thrown.GetType().Name.Contains("max_depth"), "expected error to match: max_depth");
        }

        [Fact]
        public async Task Test_ValidationMaxPagesZero()
        {
            // max_pages=0 is rejected as invalid config
            var thrown = await Assert.ThrowsAnyAsync<CrawlbergException>(async () =>
            {
                var engineConfig = JsonSerializer.Deserialize<CrawlConfig>("{\"max_pages\":0}", ConfigOptions)!;
                var engine = CrawlbergConverter.CreateEngine(engineConfig);
                var url = Environment.GetEnvironmentVariable("MOCK_SERVER_URL") + "/fixtures/validation_max_pages_zero";
                await CrawlbergConverter.ScrapeAsync(engine, url);
            });
            Assert.True(thrown.Message != null && thrown.Message.Contains("max_pages") || thrown.GetType().Name.Contains("max_pages"), "expected error to match: max_pages");
        }

        [Fact]
        public async Task Test_ValidationMaxRedirectsTooHigh()
        {
            // max_redirects > 100 is rejected as invalid config
            var thrown = await Assert.ThrowsAnyAsync<CrawlbergException>(async () =>
            {
                var engineConfig = JsonSerializer.Deserialize<CrawlConfig>("{\"max_redirects\":200}", ConfigOptions)!;
                var engine = CrawlbergConverter.CreateEngine(engineConfig);
                var url = Environment.GetEnvironmentVariable("MOCK_SERVER_URL") + "/fixtures/validation_max_redirects_too_high";
                await CrawlbergConverter.ScrapeAsync(engine, url);
            });
            Assert.True(thrown.Message != null && thrown.Message.Contains("max_redirects") || thrown.GetType().Name.Contains("max_redirects"), "expected error to match: max_redirects");
        }

        [Fact]
        public async Task Test_ValidationNegativeBodySize()
        {
            // max_body_size set to -1 is rejected as invalid config
            var thrown = await Assert.ThrowsAnyAsync<CrawlbergException>(async () =>
            {
                var engineConfig = JsonSerializer.Deserialize<CrawlConfig>("{\"max_body_size\":0}", ConfigOptions)!;
                var engine = CrawlbergConverter.CreateEngine(engineConfig);
                var url = Environment.GetEnvironmentVariable("MOCK_SERVER_URL") + "/fixtures/validation_negative_body_size";
                await CrawlbergConverter.ScrapeAsync(engine, url);
            });
            Assert.True(thrown.Message != null && thrown.Message.Contains("body_size") || thrown.GetType().Name.Contains("body_size"), "expected error to match: body_size");
        }

        [Fact]
        public async Task Test_ValidationSsrfAllowlistedLoopback()
        {
            // SSRF allowlist permits a loopback mock server while private-network protection remains enabled
            var engineConfig = JsonSerializer.Deserialize<CrawlConfig>("{\"respect_robots_txt\":false,\"ssrf\":{\"allowlist\":[{\"type\":\"cidr\",\"value\":\"127.0.0.0/8\"}],\"deny_private\":true,\"max_redirects\":5},\"ssrf_deny_private_explicit\":true}", ConfigOptions)!;
            var engine = CrawlbergConverter.CreateEngine(engineConfig);
            var url = Environment.GetEnvironmentVariable("MOCK_SERVER_URL") + "/fixtures/validation_ssrf_allowlisted_loopback";
            var result = await CrawlbergConverter.ScrapeAsync(engine, url);
            Assert.True(result.StatusCode == 200);
            Assert.Equal("Allowlisted", result.Metadata.Title);

        }

        [Fact]
        public async Task Test_ValidationSsrfIpv4MappedIpv6Denied()
        {
            // SSRF policy rejects an IPv4 loopback address embedded in an IPv6 literal
            await Assert.ThrowsAnyAsync<CrawlbergException>(async () =>
            {
                var engineConfig = JsonSerializer.Deserialize<CrawlConfig>("{\"ssrf\":{\"deny_private\":true},\"ssrf_deny_private_explicit\":true}", ConfigOptions)!;
                var engine = CrawlbergConverter.CreateEngine(engineConfig);
                var url = Environment.GetEnvironmentVariable("MOCK_SERVER_URL") + "/fixtures/validation_ssrf_ipv4_mapped_ipv6_denied";
                await CrawlbergConverter.ScrapeAsync(engine, url);
            });
            // skipped: assertion type 'error' has no accessor for error field <none> in this backend
        }

        [Fact]
        public async Task Test_ValidationSsrfLoopbackDenied()
        {
            // scrape() rejects loopback addresses by default SSRF policy
            await Assert.ThrowsAnyAsync<CrawlbergException>(async () =>
            {
                var engineConfig = JsonSerializer.Deserialize<CrawlConfig>("{\"ssrf_deny_private_explicit\":true}", ConfigOptions)!;
                var engine = CrawlbergConverter.CreateEngine(engineConfig);
                var url = Environment.GetEnvironmentVariable("MOCK_SERVER_URL") + "/fixtures/validation_ssrf_loopback_denied";
                await CrawlbergConverter.ScrapeAsync(engine, url);
            });
            // skipped: assertion type 'error' has no accessor for error field <none> in this backend
        }

        [Fact]
        public async Task Test_ValidationSsrfNat64LoopbackDenied()
        {
            // SSRF policy rejects an IPv4 loopback address embedded in the well-known NAT64 prefix
            await Assert.ThrowsAnyAsync<CrawlbergException>(async () =>
            {
                var engineConfig = JsonSerializer.Deserialize<CrawlConfig>("{\"ssrf\":{\"deny_private\":true},\"ssrf_deny_private_explicit\":true}", ConfigOptions)!;
                var engine = CrawlbergConverter.CreateEngine(engineConfig);
                var url = Environment.GetEnvironmentVariable("MOCK_SERVER_URL") + "/fixtures/validation_ssrf_nat64_loopback_denied";
                await CrawlbergConverter.ScrapeAsync(engine, url);
            });
            // skipped: assertion type 'error' has no accessor for error field <none> in this backend
        }

        [Fact]
        public async Task Test_ValidationTimeoutZero()
        {
            // Zero request timeout is rejected as invalid config
            var thrown = await Assert.ThrowsAnyAsync<CrawlbergException>(async () =>
            {
                var engineConfig = JsonSerializer.Deserialize<CrawlConfig>("{\"request_timeout\":0}", ConfigOptions)!;
                var engine = CrawlbergConverter.CreateEngine(engineConfig);
                var url = Environment.GetEnvironmentVariable("MOCK_SERVER_URL") + "/fixtures/validation_timeout_zero";
                await CrawlbergConverter.ScrapeAsync(engine, url);
            });
            Assert.True(thrown.Message != null && thrown.Message.Contains("request_timeout") || thrown.GetType().Name.Contains("request_timeout"), "expected error to match: request_timeout");
        }


    }
}
