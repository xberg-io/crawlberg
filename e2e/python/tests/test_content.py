# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:760face2a4330247281117ab60e021e727a08ae40f8e0dd152532e439e9e17f9
# To regenerate: alef generate
# To verify freshness: alef verify

"""E2e tests for category: content."""

import os
import pytest  # noqa: F401
from crawlberg import scrape, create_engine, CrawlConfig


@pytest.mark.asyncio
async def test_content_204_no_content() -> None:
    """Handles 204 No Content response gracefully."""
    engine = create_engine(None)
    url = os.environ["MOCK_SERVER_URL"] + "/fixtures/content_204_no_content"

    result = await scrape(engine, url)
    assert result.status_code == 204  # noqa: S101
    assert not result.html  # noqa: S101


@pytest.mark.asyncio
async def test_content_charset_iso8859() -> None:
    """Handles ISO-8859-1 encoded page correctly."""
    engine_config = CrawlConfig(respect_robots_txt=False)
    engine = create_engine(engine_config)
    url = os.environ["MOCK_SERVER_URL"] + "/fixtures/content_charset_iso8859"

    result = await scrape(engine, url)
    assert result.detected_charset == "iso-8859-1"  # noqa: S101


@pytest.mark.asyncio
async def test_content_empty_body() -> None:
    """Handles 200 response with empty body gracefully."""
    engine_config = CrawlConfig(respect_robots_txt=False)
    engine = create_engine(engine_config)
    url = os.environ["MOCK_SERVER_URL"] + "/fixtures/content_empty_body"

    result = await scrape(engine, url)
    assert result.status_code == 200  # noqa: S101


@pytest.mark.asyncio
async def test_content_gzip_compressed() -> None:
    """Handles response with Accept-Encoding gzip negotiation."""
    engine_config = CrawlConfig(respect_robots_txt=False)
    engine = create_engine(engine_config)
    url = os.environ["MOCK_SERVER_URL"] + "/fixtures/content_gzip_compressed"

    result = await scrape(engine, url)
    assert result.html is not None and (not hasattr(result.html, "__len__") or len(result.html) > 0)  # noqa: S101
    assert result.status_code == 200  # noqa: S101


@pytest.mark.asyncio
async def test_content_large_page_limit() -> None:
    """Respects max body size limit and truncates or skips oversized pages."""
    engine_config = CrawlConfig(max_body_size=1024, respect_robots_txt=False)
    engine = create_engine(engine_config)
    url = os.environ["MOCK_SERVER_URL"] + "/fixtures/content_large_page_limit"

    result = await scrape(engine, url)
    assert result.body_size < 1025  # noqa: S101


@pytest.mark.asyncio
async def test_content_non_ascii_body_limit() -> None:
    """Truncates a non-ASCII body at a UTF-8 character boundary."""
    engine_config = CrawlConfig(max_body_size=1, respect_robots_txt=False)
    engine = create_engine(engine_config)
    url = os.environ["MOCK_SERVER_URL"] + "/fixtures/content_non_ascii_body_limit"

    result = await scrape(engine, url)
    assert result.status_code == 200  # noqa: S101
    assert result.body_size == 0  # noqa: S101


@pytest.mark.asyncio
async def test_content_pdf_no_extension() -> None:
    """Detects PDF content by Content-Type header when URL has no .pdf extension."""
    engine_config = CrawlConfig(respect_robots_txt=False)
    engine = create_engine(engine_config)
    url = os.environ["MOCK_SERVER_URL"] + "/fixtures/content_pdf_no_extension"

    result = await scrape(engine, url)
    assert result.is_pdf is True  # noqa: S101


@pytest.mark.asyncio
async def test_content_remove_tags() -> None:
    """Removes specified HTML elements by CSS selector before processing."""
    engine_config = CrawlConfig(remove_tags=["nav", "aside", "footer"], respect_robots_txt=False)
    engine = create_engine(engine_config)
    url = os.environ["MOCK_SERVER_URL"] + "/fixtures/content_remove_tags"

    result = await scrape(engine, url)
    assert result.html is not None and (not hasattr(result.html, "__len__") or len(result.html) > 0)  # noqa: S101


@pytest.mark.asyncio
async def test_content_utf8_bom() -> None:
    """Handles UTF-8 content with BOM marker correctly."""
    engine_config = CrawlConfig(respect_robots_txt=False)
    engine = create_engine(engine_config)
    url = os.environ["MOCK_SERVER_URL"] + "/fixtures/content_utf8_bom"

    result = await scrape(engine, url)
    assert result.detected_charset == "utf-8"  # noqa: S101
    assert result.html is not None and (not hasattr(result.html, "__len__") or len(result.html) > 0)  # noqa: S101
