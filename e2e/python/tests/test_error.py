# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:07983efd8fd3e91a24682e26cde1e0ff0f6db0e2ea78c1de7718bb7bd79a093d
# To regenerate: alef generate
# To verify freshness: alef verify

"""E2e tests for category: error."""

import os

import pytest
from crawlberg import scrape, create_engine, CrawlConfig, BrowserConfig, ProxyConfig


@pytest.mark.asyncio
async def test_error_401_unauthorized() -> None:
    """Handles 401 Unauthorized response correctly."""
    with pytest.raises(Exception) as exc_info:
        engine = create_engine(None)
        url = os.environ["MOCK_SERVER_URL"] + "/fixtures/error_401_unauthorized"
        await scrape(engine, url)
    assert "unauthorized" in str(exc_info.value) or "unauthorized" in type(exc_info.value).__name__


@pytest.mark.asyncio
async def test_error_403_forbidden() -> None:
    """Handles 403 Forbidden response correctly."""
    with pytest.raises(Exception) as exc_info:
        engine_config = CrawlConfig(browser=BrowserConfig(mode="never"))
        engine = create_engine(engine_config)
        url = os.environ["MOCK_SERVER_URL"] + "/fixtures/error_403_forbidden"
        await scrape(engine, url)
    assert "forbidden" in str(exc_info.value) or "forbidden" in type(exc_info.value).__name__


@pytest.mark.asyncio
async def test_error_404_page() -> None:
    """Handles 404 response correctly."""
    with pytest.raises(Exception) as exc_info:
        engine = create_engine(None)
        url = os.environ["MOCK_SERVER_URL"] + "/fixtures/error_404_page"
        await scrape(engine, url)
    assert "not_found" in str(exc_info.value) or "not_found" in type(exc_info.value).__name__


@pytest.mark.asyncio
async def test_error_408_request_timeout() -> None:
    """Handles 408 Request Timeout response correctly."""
    with pytest.raises(Exception) as exc_info:
        engine = create_engine(None)
        url = os.environ["MOCK_SERVER_URL"] + "/fixtures/error_408_request_timeout"
        await scrape(engine, url)
    assert "timeout" in str(exc_info.value) or "timeout" in type(exc_info.value).__name__


@pytest.mark.asyncio
async def test_error_410_gone() -> None:
    """Handles 410 Gone response correctly."""
    with pytest.raises(Exception) as exc_info:
        engine = create_engine(None)
        url = os.environ["MOCK_SERVER_URL"] + "/fixtures/error_410_gone"
        await scrape(engine, url)
    assert "gone" in str(exc_info.value) or "gone" in type(exc_info.value).__name__


@pytest.mark.asyncio
async def test_error_500_server() -> None:
    """Handles 500 server error."""
    with pytest.raises(Exception) as exc_info:
        engine = create_engine(None)
        url = os.environ["MOCK_SERVER_URL"] + "/fixtures/error_500_server"
        await scrape(engine, url)
    assert "server_error" in str(exc_info.value) or "server_error" in type(exc_info.value).__name__


@pytest.mark.asyncio
async def test_error_502_bad_gateway() -> None:
    """Handles 502 Bad Gateway response correctly."""
    with pytest.raises(Exception) as exc_info:
        engine = create_engine(None)
        url = os.environ["MOCK_SERVER_URL"] + "/fixtures/error_502_bad_gateway"
        await scrape(engine, url)
    assert "bad_gateway" in str(exc_info.value) or "bad_gateway" in type(exc_info.value).__name__


@pytest.mark.asyncio
async def test_error_browser_launch_failure() -> None:
    """Browser launch fails when browser mode is always but browser is unavailable."""
    with pytest.raises(Exception) as exc_info:
        engine_config = CrawlConfig(browser=BrowserConfig(mode="always", timeout=1))
        engine = create_engine(engine_config)
        url = os.environ["MOCK_SERVER_URL"] + "/fixtures/error_browser_launch_failure"
        await scrape(engine, url)
    assert "browser" in str(exc_info.value) or "browser" in type(exc_info.value).__name__


@pytest.mark.asyncio
async def test_error_browser_page_timeout() -> None:
    """Browser page load times out."""
    with pytest.raises(Exception) as exc_info:
        engine_config = CrawlConfig(browser=BrowserConfig(mode="always", timeout=1))
        engine = create_engine(engine_config)
        url = os.environ["MOCK_SERVER_URL"] + "/fixtures/error_browser_page_timeout"
        await scrape(engine, url)
    assert "browser_timeout" in str(exc_info.value) or "browser_timeout" in type(exc_info.value).__name__


@pytest.mark.asyncio
async def test_error_connection_refused() -> None:
    """Handles connection refused error gracefully."""
    with pytest.raises(Exception) as exc_info:
        engine = create_engine(None)
        url = os.environ["MOCK_SERVER_URL"] + "/fixtures/error_connection_refused"
        await scrape(engine, url)
    assert "connection" in str(exc_info.value) or "connection" in type(exc_info.value).__name__


@pytest.mark.asyncio
async def test_error_data_loss_truncated() -> None:
    """Content-Length mismatch causes data loss error."""
    with pytest.raises(Exception) as exc_info:
        engine = create_engine(None)
        url = os.environ["MOCK_SERVER_URL"] + "/fixtures/error_data_loss_truncated"
        await scrape(engine, url)
    assert "data_loss:" in str(exc_info.value) or "data_loss:" in type(exc_info.value).__name__


@pytest.mark.asyncio
async def test_error_dns_resolution() -> None:
    """Handles DNS resolution failure gracefully."""
    with pytest.raises(Exception) as exc_info:
        engine = create_engine(None)
        url = os.environ["MOCK_SERVER_URL"] + "/fixtures/error_dns_resolution"
        await scrape(engine, url)
    assert "dns" in str(exc_info.value) or "dns" in type(exc_info.value).__name__


@pytest.mark.asyncio
async def test_error_empty_batch_urls() -> None:
    """Scraping a URL that cannot be found returns an error containing the URL path."""
    with pytest.raises(Exception) as exc_info:
        engine = create_engine(None)
        url = os.environ["MOCK_SERVER_URL"] + "/fixtures/error_empty_batch_urls"
        await scrape(engine, url)
    assert "not_found" in str(exc_info.value) or "not_found" in type(exc_info.value).__name__


@pytest.mark.asyncio
async def test_error_empty_response() -> None:
    """Handles 200 with completely empty body gracefully."""
    engine = create_engine(None)
    url = os.environ["MOCK_SERVER_URL"] + "/fixtures/error_empty_response"

    result = await scrape(engine, url)
    assert not result["html"]


@pytest.mark.asyncio
async def test_error_invalid_proxy() -> None:
    """Proxy pointing to unreachable address causes connection error during scrape."""
    with pytest.raises(Exception) as exc_info:
        engine_config = CrawlConfig(proxy=ProxyConfig(url="http://127.0.0.1:1"))
        engine = create_engine(engine_config)
        url = os.environ["MOCK_SERVER_URL"] + "/fixtures/error_invalid_proxy"
        await scrape(engine, url)
    assert "connection" in str(exc_info.value) or "connection" in type(exc_info.value).__name__


@pytest.mark.asyncio
async def test_error_partial_response() -> None:
    """Handles incomplete or truncated HTTP response."""
    with pytest.raises(Exception) as exc_info:
        engine = create_engine(None)
        url = os.environ["MOCK_SERVER_URL"] + "/fixtures/error_partial_response"
        await scrape(engine, url)
    assert "data_loss" in str(exc_info.value) or "data_loss" in type(exc_info.value).__name__


@pytest.mark.asyncio
async def test_error_rate_limited() -> None:
    """Handles 429 rate limiting with Retry-After."""
    with pytest.raises(Exception) as exc_info:
        engine = create_engine(None)
        url = os.environ["MOCK_SERVER_URL"] + "/fixtures/error_rate_limited"
        await scrape(engine, url)
    assert "rate_limited" in str(exc_info.value) or "rate_limited" in type(exc_info.value).__name__


@pytest.mark.asyncio
async def test_error_retry_503() -> None:
    """Retries request on 503 Service Unavailable response."""
    with pytest.raises(Exception) as exc_info:
        engine = create_engine(None)
        url = os.environ["MOCK_SERVER_URL"] + "/fixtures/error_retry_503"
        await scrape(engine, url)
    assert "server_error" in str(exc_info.value) or "server_error" in type(exc_info.value).__name__


@pytest.mark.asyncio
async def test_error_retry_backoff() -> None:
    """Implements exponential backoff when retrying failed requests."""
    with pytest.raises(Exception) as exc_info:
        engine = create_engine(None)
        url = os.environ["MOCK_SERVER_URL"] + "/fixtures/error_retry_backoff"
        await scrape(engine, url)
    assert "rate_limited" in str(exc_info.value) or "rate_limited" in type(exc_info.value).__name__


@pytest.mark.asyncio
async def test_error_ssl_invalid_cert() -> None:
    """Handles SSL certificate validation error."""
    with pytest.raises(Exception) as exc_info:
        engine = create_engine(None)
        url = os.environ["MOCK_SERVER_URL"] + "/fixtures/error_ssl_invalid_cert"
        await scrape(engine, url)
    assert "ssl" in str(exc_info.value) or "ssl" in type(exc_info.value).__name__


@pytest.mark.asyncio
async def test_error_timeout() -> None:
    """Mock server delays response longer than request_timeout, surfacing a timeout error."""
    with pytest.raises(Exception) as exc_info:
        engine_config = CrawlConfig(request_timeout=0)
        engine = create_engine(engine_config)
        url = os.environ["MOCK_SERVER_URL"] + "/fixtures/error_timeout"
        await scrape(engine, url)
    assert "timeout" in str(exc_info.value) or "timeout" in type(exc_info.value).__name__


@pytest.mark.asyncio
async def test_error_unsupported_scheme() -> None:
    """Unsupported URL scheme (gopher) is rejected."""
    with pytest.raises(Exception) as exc_info:
        engine = create_engine(None)
        url = "gopher://invalid.example.com/"
        await scrape(engine, url)
    assert (
        "disallowed scheme: gopher" in str(exc_info.value)
        or "disallowed scheme: gopher" in type(exc_info.value).__name__
    )


@pytest.mark.asyncio
async def test_error_waf_akamai() -> None:
    """Akamai WAF detection returns WafBlocked error."""
    with pytest.raises(Exception):  # noqa: B017
        engine_config = CrawlConfig(browser=BrowserConfig(mode="never"))
        engine = create_engine(engine_config)
        url = os.environ["MOCK_SERVER_URL"] + "/fixtures/error_waf_akamai"
        await scrape(engine, url)


@pytest.mark.asyncio
async def test_error_waf_blocked() -> None:
    """WAF challenge/block detection returns WafBlocked error."""
    with pytest.raises(Exception) as exc_info:
        engine_config = CrawlConfig(browser=BrowserConfig(mode="never"))
        engine = create_engine(engine_config)
        url = os.environ["MOCK_SERVER_URL"] + "/fixtures/error_waf_blocked"
        await scrape(engine, url)
    assert "waf" in str(exc_info.value) or "waf" in type(exc_info.value).__name__


@pytest.mark.asyncio
async def test_error_waf_false_403() -> None:
    """Detects WAF/bot protection false 403 (Cloudflare challenge page)."""
    with pytest.raises(Exception) as exc_info:
        engine_config = CrawlConfig(browser=BrowserConfig(mode="never"))
        engine = create_engine(engine_config)
        url = os.environ["MOCK_SERVER_URL"] + "/fixtures/error_waf_false_403"
        await scrape(engine, url)
    assert "waf" in str(exc_info.value) or "waf" in type(exc_info.value).__name__


@pytest.mark.asyncio
async def test_error_waf_imperva() -> None:
    """Imperva/Incapsula WAF detection."""
    with pytest.raises(Exception):  # noqa: B017
        engine_config = CrawlConfig(browser=BrowserConfig(mode="never"))
        engine = create_engine(engine_config)
        url = os.environ["MOCK_SERVER_URL"] + "/fixtures/error_waf_imperva"
        await scrape(engine, url)
