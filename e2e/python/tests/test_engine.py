# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:2058a8a16fdd90db07dd3078568bd195b24675f6a2c4f7f45a950417f2531147
# To regenerate: alef generate
# To verify freshness: alef verify

"""E2e tests for category: engine."""

import os
import pytest  # noqa: F401
from crawlberg import batch_scrape, crawl, map_urls, scrape, crawl_stream, create_engine, CrawlConfig


@pytest.mark.asyncio
async def test_engine_batch_basic() -> None:
    """CrawlEngine with defaults batch scrapes like the free function."""
    engine = create_engine(None)
    urls_base = os.environ["MOCK_SERVER_URL"] + "/fixtures/engine_batch_basic"
    urls = [p if p.startswith("http") else f"{urls_base}{p}" for p in ["/page1", "/page2"]]

    result = await batch_scrape(engine, urls)
    assert result.completed_count == 2  # noqa: S101
    assert result.total_count == 2  # noqa: S101


@pytest.mark.asyncio
async def test_engine_crawl_basic() -> None:
    """CrawlEngine with defaults crawls multiple pages like the free function."""
    engine_config = CrawlConfig(max_depth=1)
    engine = create_engine(engine_config)
    url = (
        os.environ.get("MOCK_SERVER_ENGINE_CRAWL_BASIC")
        or os.environ["MOCK_SERVER_URL"] + "/fixtures/engine_crawl_basic"
    )

    result = await crawl(engine, url)
    assert len(result.pages) == 3  # noqa: S101
    assert len(result.pages) >= 3  # noqa: S101


@pytest.mark.asyncio
async def test_engine_map_basic() -> None:
    """CrawlEngine with defaults discovers URLs like the free function."""
    engine = create_engine(None)
    url = os.environ.get("MOCK_SERVER_ENGINE_MAP_BASIC") or os.environ["MOCK_SERVER_URL"] + "/fixtures/engine_map_basic"

    result = await map_urls(engine, url)
    assert len(result.urls) >= 2  # noqa: S101


@pytest.mark.asyncio
async def test_engine_scrape_basic() -> None:
    """CrawlEngine with defaults scrapes a page identically to the free function."""
    engine = create_engine(None)
    url = (
        os.environ.get("MOCK_SERVER_ENGINE_SCRAPE_BASIC")
        or os.environ["MOCK_SERVER_URL"] + "/fixtures/engine_scrape_basic"
    )

    result = await scrape(engine, url)
    assert result.status_code == 200  # noqa: S101
    assert result.content_type == "text/html"  # noqa: S101
    assert result.metadata.title == "Engine Test"  # noqa: S101
    assert result.metadata.description is not None  # noqa: S101
    assert "Testing the engine" in result.metadata.description  # noqa: S101
    assert len(result.links) >= 1  # noqa: S101
    assert len(result.metadata.headings) >= 1  # noqa: S101


@pytest.mark.asyncio
async def test_engine_stream_basic() -> None:
    """CrawlEngine with defaults streams events like the free function."""
    engine_config = CrawlConfig(max_depth=1)
    engine = create_engine(engine_config)
    url = (
        os.environ.get("MOCK_SERVER_ENGINE_STREAM_BASIC")
        or os.environ["MOCK_SERVER_URL"] + "/fixtures/engine_stream_basic"
    )

    result = crawl_stream(engine, url)
    chunks = []
    async for chunk in result:
        chunks.append(chunk)
    # skipped: streaming field 'stream.has_page_event': no python accessor
    # skipped: streaming field 'stream.has_complete_event': no python accessor
    assert len(chunks) >= 3  # noqa: S101
