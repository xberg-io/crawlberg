# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:8a4071b65feb2fd4b5fd8021cd0848c6e7a2528ed703571508edbf1f2c060009
# To regenerate: alef generate
# To verify freshness: alef verify

"""E2e tests for category: metadata."""

import os
import pytest  # noqa: F401
from crawlberg import scrape, create_engine


def _alef_e2e_text(value: object) -> str:
    return "" if value is None else str(value)


@pytest.mark.asyncio
async def test_metadata_article_times() -> None:
    """Extracts article:published_time, modified_time, author, section, and tags."""
    engine = create_engine(None)
    url = os.environ["MOCK_SERVER_URL"] + "/fixtures/metadata_article_times"

    result = await scrape(engine, url)
    assert result.status_code == 200  # noqa: S101
    assert result.metadata.article.published_time == "2024-01-15T10:00:00Z"  # noqa: S101
    assert result.metadata.article.modified_time == "2024-06-20T14:30:00Z"  # noqa: S101
    assert result.metadata.article.author == "Jane Developer"  # noqa: S101
    assert result.metadata.article.section == "Technology"  # noqa: S101
    assert len(result.metadata.article.tags) == 3  # noqa: S101


@pytest.mark.asyncio
async def test_metadata_favicons() -> None:
    """Extracts favicon link tags including apple-touch-icon."""
    engine = create_engine(None)
    url = os.environ["MOCK_SERVER_URL"] + "/fixtures/metadata_favicons"

    result = await scrape(engine, url)
    assert result.status_code == 200  # noqa: S101
    assert len(result.metadata.favicons) == 5  # noqa: S101
    assert any("apple-touch-icon" in str(_e.rel) for _e in (result.metadata.favicons or []))  # noqa: S101


@pytest.mark.asyncio
async def test_metadata_headings() -> None:
    """Extracts heading hierarchy (h1-h6) from HTML page."""
    engine = create_engine(None)
    url = os.environ["MOCK_SERVER_URL"] + "/fixtures/metadata_headings"

    result = await scrape(engine, url)
    assert result.status_code == 200  # noqa: S101
    assert result.metadata.headings[0].level == 1  # noqa: S101
    assert _alef_e2e_text(result.metadata.headings[0].text).lower() == "Primary Heading".lower()  # noqa: S101
    assert len(result.metadata.headings) == 8  # noqa: S101


@pytest.mark.asyncio
async def test_metadata_hreflang() -> None:
    """Extracts hreflang alternate link tags."""
    engine = create_engine(None)
    url = os.environ["MOCK_SERVER_URL"] + "/fixtures/metadata_hreflang"

    result = await scrape(engine, url)
    assert result.status_code == 200  # noqa: S101
    assert len(result.metadata.hreflangs) == 4  # noqa: S101
    assert any("en" in str(_e.lang) for _e in (result.metadata.hreflangs or []))  # noqa: S101


@pytest.mark.asyncio
async def test_metadata_keywords_author() -> None:
    """Extracts keywords, author, viewport, generator, theme-color, robots, lang, dir metadata."""
    engine = create_engine(None)
    url = os.environ["MOCK_SERVER_URL"] + "/fixtures/metadata_keywords_author"

    result = await scrape(engine, url)
    assert result.status_code == 200  # noqa: S101
    assert result.metadata.title == "Comprehensive Metadata Test Page"  # noqa: S101
    assert result.metadata.canonical_url is not None and (
        not hasattr(result.metadata.canonical_url, "__len__") or len(result.metadata.canonical_url) > 0
    )  # noqa: S101
    assert result.metadata.keywords is not None and (
        not hasattr(result.metadata.keywords, "__len__") or len(result.metadata.keywords) > 0
    )  # noqa: S101
    assert result.metadata.keywords is not None  # noqa: S101
    assert "rust" in result.metadata.keywords  # noqa: S101
    assert result.metadata.author == "Jane Developer"  # noqa: S101
    assert result.metadata.viewport is not None and (
        not hasattr(result.metadata.viewport, "__len__") or len(result.metadata.viewport) > 0
    )  # noqa: S101
    assert result.metadata.generator == "crawlberg/1.0"  # noqa: S101
    assert result.metadata.theme_color == "#ff6600"  # noqa: S101
    assert result.metadata.robots == "index, follow"  # noqa: S101
    assert result.metadata.html_lang == "en"  # noqa: S101
    assert result.metadata.html_dir == "ltr"  # noqa: S101


@pytest.mark.asyncio
async def test_metadata_og_video_audio() -> None:
    """Extracts og:video, og:audio, and og:locale:alternate metadata."""
    engine = create_engine(None)
    url = os.environ["MOCK_SERVER_URL"] + "/fixtures/metadata_og_video_audio"

    result = await scrape(engine, url)
    assert result.status_code == 200  # noqa: S101
    assert result.metadata.og_video == "https://example.com/video.mp4"  # noqa: S101
    assert result.metadata.og_audio == "https://example.com/audio.mp3"  # noqa: S101
    assert len(result.metadata.og_locale_alternates) == 2  # noqa: S101


@pytest.mark.asyncio
async def test_metadata_response_headers() -> None:
    """Extracts response metadata from HTTP headers (etag, server, content-language)."""
    engine = create_engine(None)
    url = os.environ["MOCK_SERVER_URL"] + "/fixtures/metadata_response_headers"

    result = await scrape(engine, url)
    assert result.status_code == 200  # noqa: S101
    assert result.response_meta.etag is not None  # noqa: S101
    assert "abc123" in result.response_meta.etag  # noqa: S101
    assert result.response_meta.last_modified is not None  # noqa: S101
    assert "01 Jan 2025" in result.response_meta.last_modified  # noqa: S101
    assert result.response_meta.server is not None  # noqa: S101
    assert "nginx" in result.response_meta.server  # noqa: S101
    assert result.response_meta.content_language == "en-US"  # noqa: S101


@pytest.mark.asyncio
async def test_metadata_word_count() -> None:
    """Computes word count from visible page text."""
    engine = create_engine(None)
    url = os.environ["MOCK_SERVER_URL"] + "/fixtures/metadata_word_count"

    result = await scrape(engine, url)
    assert result.status_code == 200  # noqa: S101
    assert result.metadata.word_count > 99  # noqa: S101
    assert result.metadata.word_count < 301  # noqa: S101
