#!/usr/bin/env bash
# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:6a98f5518d038bcb5298d9f9e017c24f6241346476bde0494e0335814908c158
# To regenerate: alef generate
# To verify freshness: alef verify
# E2e tests for category: error
set -euo pipefail

test_error_401_unauthorized() {
  # Handles 401 Unauthorized response correctly
  if output=$(crawlberg scrape "${MOCK_SERVER_ERROR_401_UNAUTHORIZED:-${MOCK_SERVER_URL}/fixtures/error_401_unauthorized}" --format json --browser-mode never 2>&1); then
    echo 'FAIL [error]: expected command to fail but it succeeded' >&2
    return 1
  fi
  if ! grep -qF 'unauthorized' <<<"$output"; then
    echo 'FAIL [error]: expected output to contain ''unauthorized' >&2
    return 1
  fi
}

test_error_403_forbidden() {
  # Handles 403 Forbidden response correctly
  if output=$(crawlberg scrape "${MOCK_SERVER_ERROR_403_FORBIDDEN:-${MOCK_SERVER_URL}/fixtures/error_403_forbidden}" --config '{"browser":{"mode":"never"}}' --format json --browser-mode never 2>&1); then
    echo 'FAIL [error]: expected command to fail but it succeeded' >&2
    return 1
  fi
  if ! grep -qF 'forbidden' <<<"$output"; then
    echo 'FAIL [error]: expected output to contain ''forbidden' >&2
    return 1
  fi
}

test_error_404_page() {
  # Handles 404 response correctly
  if output=$(crawlberg scrape "${MOCK_SERVER_ERROR_404_PAGE:-${MOCK_SERVER_URL}/fixtures/error_404_page}" --format json --browser-mode never 2>&1); then
    echo 'FAIL [error]: expected command to fail but it succeeded' >&2
    return 1
  fi
  if ! grep -qF 'not_found' <<<"$output"; then
    echo 'FAIL [error]: expected output to contain ''not_found' >&2
    return 1
  fi
}

test_error_408_request_timeout() {
  # Handles 408 Request Timeout response correctly
  if output=$(crawlberg scrape "${MOCK_SERVER_ERROR_408_REQUEST_TIMEOUT:-${MOCK_SERVER_URL}/fixtures/error_408_request_timeout}" --format json --browser-mode never 2>&1); then
    echo 'FAIL [error]: expected command to fail but it succeeded' >&2
    return 1
  fi
  if ! grep -qF 'timeout' <<<"$output"; then
    echo 'FAIL [error]: expected output to contain ''timeout' >&2
    return 1
  fi
}

test_error_410_gone() {
  # Handles 410 Gone response correctly
  if output=$(crawlberg scrape "${MOCK_SERVER_ERROR_410_GONE:-${MOCK_SERVER_URL}/fixtures/error_410_gone}" --format json --browser-mode never 2>&1); then
    echo 'FAIL [error]: expected command to fail but it succeeded' >&2
    return 1
  fi
  if ! grep -qF 'gone' <<<"$output"; then
    echo 'FAIL [error]: expected output to contain ''gone' >&2
    return 1
  fi
}

test_error_500_server() {
  # Handles 500 server error
  if output=$(crawlberg scrape "${MOCK_SERVER_ERROR_500_SERVER:-${MOCK_SERVER_URL}/fixtures/error_500_server}" --format json --browser-mode never 2>&1); then
    echo 'FAIL [error]: expected command to fail but it succeeded' >&2
    return 1
  fi
  if ! grep -qF 'server_error' <<<"$output"; then
    echo 'FAIL [error]: expected output to contain ''server_error' >&2
    return 1
  fi
}

test_error_502_bad_gateway() {
  # Handles 502 Bad Gateway response correctly
  if output=$(crawlberg scrape "${MOCK_SERVER_ERROR_502_BAD_GATEWAY:-${MOCK_SERVER_URL}/fixtures/error_502_bad_gateway}" --format json --browser-mode never 2>&1); then
    echo 'FAIL [error]: expected command to fail but it succeeded' >&2
    return 1
  fi
  if ! grep -qF 'bad_gateway' <<<"$output"; then
    echo 'FAIL [error]: expected output to contain ''bad_gateway' >&2
    return 1
  fi
}

test_error_browser_launch_failure() {
  # Browser launch fails when browser mode is always but browser is unavailable
  if output=$(crawlberg scrape "${MOCK_SERVER_ERROR_BROWSER_LAUNCH_FAILURE:-${MOCK_SERVER_URL}/fixtures/error_browser_launch_failure}" --config '{"browser":{"mode":"always","timeout":1}}' --format json --browser-mode never 2>&1); then
    echo 'FAIL [error]: expected command to fail but it succeeded' >&2
    return 1
  fi
  if ! grep -qF 'browser' <<<"$output"; then
    echo 'FAIL [error]: expected output to contain ''browser' >&2
    return 1
  fi
}

test_error_browser_page_timeout() {
  # Browser page load times out
  if output=$(crawlberg scrape "${MOCK_SERVER_ERROR_BROWSER_PAGE_TIMEOUT:-${MOCK_SERVER_URL}/fixtures/error_browser_page_timeout}" --config '{"browser":{"mode":"always","timeout":1}}' --format json --browser-mode never 2>&1); then
    echo 'FAIL [error]: expected command to fail but it succeeded' >&2
    return 1
  fi
  if ! grep -qF 'browser_timeout' <<<"$output"; then
    echo 'FAIL [error]: expected output to contain ''browser_timeout' >&2
    return 1
  fi
}

test_error_connection_refused() {
  # Handles connection refused error gracefully
  if output=$(crawlberg scrape "${MOCK_SERVER_ERROR_CONNECTION_REFUSED:-${MOCK_SERVER_URL}/fixtures/error_connection_refused}" --format json --browser-mode never 2>&1); then
    echo 'FAIL [error]: expected command to fail but it succeeded' >&2
    return 1
  fi
  if ! grep -qF 'connection' <<<"$output"; then
    echo 'FAIL [error]: expected output to contain ''connection' >&2
    return 1
  fi
}

test_error_data_loss_truncated() {
  # Content-Length mismatch causes data loss error
  if output=$(crawlberg scrape "${MOCK_SERVER_ERROR_DATA_LOSS_TRUNCATED:-${MOCK_SERVER_URL}/fixtures/error_data_loss_truncated}" --format json --browser-mode never 2>&1); then
    echo 'FAIL [error]: expected command to fail but it succeeded' >&2
    return 1
  fi
  if ! grep -qF 'data_loss:' <<<"$output"; then
    echo 'FAIL [error]: expected output to contain ''data_loss:' >&2
    return 1
  fi
}

test_error_dns_resolution() {
  # Handles DNS resolution failure gracefully
  if output=$(crawlberg scrape "${MOCK_SERVER_ERROR_DNS_RESOLUTION:-${MOCK_SERVER_URL}/fixtures/error_dns_resolution}" --format json --browser-mode never 2>&1); then
    echo 'FAIL [error]: expected command to fail but it succeeded' >&2
    return 1
  fi
  if ! grep -qF 'dns' <<<"$output"; then
    echo 'FAIL [error]: expected output to contain ''dns' >&2
    return 1
  fi
}

test_error_empty_batch_urls() {
  # Scraping a URL that cannot be found returns an error containing the URL path
  if output=$(crawlberg scrape "${MOCK_SERVER_ERROR_EMPTY_BATCH_URLS:-${MOCK_SERVER_URL}/fixtures/error_empty_batch_urls}" --config '{}' --format json --browser-mode never 2>&1); then
    echo 'FAIL [error]: expected command to fail but it succeeded' >&2
    return 1
  fi
  if ! grep -qF 'not_found' <<<"$output"; then
    echo 'FAIL [error]: expected output to contain ''not_found' >&2
    return 1
  fi
}

test_error_empty_response() {
  # Handles 200 with completely empty body gracefully
  local output
  output=$(crawlberg scrape "${MOCK_SERVER_ERROR_EMPTY_RESPONSE:-${MOCK_SERVER_URL}/fixtures/error_empty_response}" --format json --browser-mode never)

  local val_html
  val_html=$(echo "$output" | jq -r '.html // empty')
  assert_is_empty "$val_html" 'html'
}

test_error_invalid_proxy() {
  # Proxy pointing to unreachable address causes connection error during scrape
  if output=$(crawlberg scrape "${MOCK_SERVER_ERROR_INVALID_PROXY:-${MOCK_SERVER_URL}/fixtures/error_invalid_proxy}" --config '{"proxy":{"url":"http://127.0.0.1:1"}}' --format json --browser-mode never 2>&1); then
    echo 'FAIL [error]: expected command to fail but it succeeded' >&2
    return 1
  fi
  if ! grep -qF 'connection' <<<"$output"; then
    echo 'FAIL [error]: expected output to contain ''connection' >&2
    return 1
  fi
}

test_error_partial_response() {
  # Handles incomplete or truncated HTTP response
  if output=$(crawlberg scrape "${MOCK_SERVER_ERROR_PARTIAL_RESPONSE:-${MOCK_SERVER_URL}/fixtures/error_partial_response}" --format json --browser-mode never 2>&1); then
    echo 'FAIL [error]: expected command to fail but it succeeded' >&2
    return 1
  fi
  if ! grep -qF 'data_loss' <<<"$output"; then
    echo 'FAIL [error]: expected output to contain ''data_loss' >&2
    return 1
  fi
}

test_error_rate_limited() {
  # Handles 429 rate limiting with Retry-After
  if output=$(crawlberg scrape "${MOCK_SERVER_ERROR_RATE_LIMITED:-${MOCK_SERVER_URL}/fixtures/error_rate_limited}" --format json --browser-mode never 2>&1); then
    echo 'FAIL [error]: expected command to fail but it succeeded' >&2
    return 1
  fi
  if ! grep -qF 'rate_limited' <<<"$output"; then
    echo 'FAIL [error]: expected output to contain ''rate_limited' >&2
    return 1
  fi
}

test_error_retry_503() {
  # Retries request on 503 Service Unavailable response
  if output=$(crawlberg scrape "${MOCK_SERVER_ERROR_RETRY_503:-${MOCK_SERVER_URL}/fixtures/error_retry_503}" --format json --browser-mode never 2>&1); then
    echo 'FAIL [error]: expected command to fail but it succeeded' >&2
    return 1
  fi
  if ! grep -qF 'server_error' <<<"$output"; then
    echo 'FAIL [error]: expected output to contain ''server_error' >&2
    return 1
  fi
}

test_error_retry_backoff() {
  # Implements exponential backoff when retrying failed requests
  if output=$(crawlberg scrape "${MOCK_SERVER_ERROR_RETRY_BACKOFF:-${MOCK_SERVER_URL}/fixtures/error_retry_backoff}" --format json --browser-mode never 2>&1); then
    echo 'FAIL [error]: expected command to fail but it succeeded' >&2
    return 1
  fi
  if ! grep -qF 'rate_limited' <<<"$output"; then
    echo 'FAIL [error]: expected output to contain ''rate_limited' >&2
    return 1
  fi
}

test_error_ssl_invalid_cert() {
  # Handles SSL certificate validation error
  if output=$(crawlberg scrape "${MOCK_SERVER_ERROR_SSL_INVALID_CERT:-${MOCK_SERVER_URL}/fixtures/error_ssl_invalid_cert}" --format json --browser-mode never 2>&1); then
    echo 'FAIL [error]: expected command to fail but it succeeded' >&2
    return 1
  fi
  if ! grep -qF 'ssl' <<<"$output"; then
    echo 'FAIL [error]: expected output to contain ''ssl' >&2
    return 1
  fi
}

test_error_timeout() {
  # Mock server delays response longer than request_timeout, surfacing a timeout error
  if output=$(crawlberg scrape "${MOCK_SERVER_ERROR_TIMEOUT:-${MOCK_SERVER_URL}/fixtures/error_timeout}" --config '{"request_timeout":500}' --format json --browser-mode never 2>&1); then
    echo 'FAIL [error]: expected command to fail but it succeeded' >&2
    return 1
  fi
  if ! grep -qF 'timeout' <<<"$output"; then
    echo 'FAIL [error]: expected output to contain ''timeout' >&2
    return 1
  fi
}

test_error_unsupported_scheme() {
  # Unsupported URL scheme (gopher) is rejected
  if output=$(crawlberg scrape 'gopher://invalid.example.com/' --config '{}' --format json --browser-mode never 2>&1); then
    echo 'FAIL [error]: expected command to fail but it succeeded' >&2
    return 1
  fi
  if ! grep -qF 'disallowed scheme: gopher' <<<"$output"; then
    echo 'FAIL [error]: expected output to contain ''disallowed scheme: gopher' >&2
    return 1
  fi
}

test_error_waf_akamai() {
  # Akamai WAF detection returns WafBlocked error
  if crawlberg scrape "${MOCK_SERVER_ERROR_WAF_AKAMAI:-${MOCK_SERVER_URL}/fixtures/error_waf_akamai}" --config '{"browser":{"mode":"never"}}' --format json --browser-mode never >/dev/null 2>&1; then
    echo 'FAIL [error]: expected command to fail but it succeeded' >&2
    return 1
  fi
}

test_error_waf_blocked() {
  # WAF challenge/block detection returns WafBlocked error
  if output=$(crawlberg scrape "${MOCK_SERVER_ERROR_WAF_BLOCKED:-${MOCK_SERVER_URL}/fixtures/error_waf_blocked}" --config '{"browser":{"mode":"never"}}' --format json --browser-mode never 2>&1); then
    echo 'FAIL [error]: expected command to fail but it succeeded' >&2
    return 1
  fi
  if ! grep -qF 'waf' <<<"$output"; then
    echo 'FAIL [error]: expected output to contain ''waf' >&2
    return 1
  fi
}

test_error_waf_false_403() {
  # Detects WAF/bot protection false 403 (Cloudflare challenge page)
  if output=$(crawlberg scrape "${MOCK_SERVER_ERROR_WAF_FALSE_403:-${MOCK_SERVER_URL}/fixtures/error_waf_false_403}" --config '{"browser":{"mode":"never"}}' --format json --browser-mode never 2>&1); then
    echo 'FAIL [error]: expected command to fail but it succeeded' >&2
    return 1
  fi
  if ! grep -qF 'waf' <<<"$output"; then
    echo 'FAIL [error]: expected output to contain ''waf' >&2
    return 1
  fi
}

test_error_waf_imperva() {
  # Imperva/Incapsula WAF detection
  if crawlberg scrape "${MOCK_SERVER_ERROR_WAF_IMPERVA:-${MOCK_SERVER_URL}/fixtures/error_waf_imperva}" --config '{"browser":{"mode":"never"}}' --format json --browser-mode never >/dev/null 2>&1; then
    echo 'FAIL [error]: expected command to fail but it succeeded' >&2
    return 1
  fi
}

run_tests_error() {
  run_test test_error_401_unauthorized
  run_test test_error_403_forbidden
  run_test test_error_404_page
  run_test test_error_408_request_timeout
  run_test test_error_410_gone
  run_test test_error_500_server
  run_test test_error_502_bad_gateway
  run_test test_error_browser_launch_failure
  run_test test_error_browser_page_timeout
  run_test test_error_connection_refused
  run_test test_error_data_loss_truncated
  run_test test_error_dns_resolution
  run_test test_error_empty_batch_urls
  run_test test_error_empty_response
  run_test test_error_invalid_proxy
  run_test test_error_partial_response
  run_test test_error_rate_limited
  run_test test_error_retry_503
  run_test test_error_retry_backoff
  run_test test_error_ssl_invalid_cert
  run_test test_error_timeout
  run_test test_error_unsupported_scheme
  run_test test_error_waf_akamai
  run_test test_error_waf_blocked
  run_test test_error_waf_false_403
  run_test test_error_waf_imperva
}
