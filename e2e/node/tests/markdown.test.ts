// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:272d1e8d7090bd31fe60efca9bbadcb03c1179d625edc73758fd85b0452b7fb7
// To regenerate: alef e2e generate
// To verify freshness: alef verify

import { describe, expect, it } from "vitest";
import { scrape, crawl, createEngine } from "@xberg-io/crawlberg";

process.env.CRAWLBERG_ALLOW_PRIVATE_NETWORK ??= "true";


async function _alefE2eDecompressAndParseJson(response: Response): Promise<unknown> {
	const contentEncoding = response.headers.get("content-encoding");
	let bytes = new Uint8Array(await response.arrayBuffer());

	// The runtime's own fetch (undici on Node, and the browser equivalent) decodes
	// `content-encoding` transparently and leaves the header in place, so by the time the
	// body reaches here it is usually already plain text. Decoding unconditionally on the
	// header alone therefore fails ("incorrect header check") against a server that really
	// did compress. Decode only when the payload still carries the encoding: gzip announces
	// itself with the 0x1f8b magic pair; brotli has no magic, so it is attempted and the
	// already-decoded case falls back to the bytes as received.
	if (contentEncoding === "gzip" && bytes.length >= 2 && bytes[0] === 0x1f && bytes[1] === 0x8b) {
		const { gunzipSync } = await import("zlib");
		bytes = new Uint8Array(gunzipSync(bytes));
	} else if (contentEncoding === "br") {
		const { brotliDecompressSync } = await import("zlib");
		try {
			bytes = new Uint8Array(brotliDecompressSync(bytes));
		} catch {
			// Already decoded by the runtime.
		}
	}

	const text = new TextDecoder().decode(bytes);
	return JSON.parse(text);
}

function _alefE2eText(value: unknown): string {
	return value == null ? "" : String(value);
}

function _alefE2eItemTexts(item: unknown): string[] {
	if (item == null || typeof item !== "object") {
		return [_alefE2eText(item)];
	}
	const record = item as Record<string, unknown>;
	const itemsText = Array.isArray(record.items) ? record.items.map(_alefE2eText).join(" ") : "";
	return [_alefE2eText(item), _alefE2eText(record.kind), _alefE2eText(record.name), _alefE2eText(record.source), _alefE2eText(record.alias), _alefE2eText(record.text), _alefE2eText(record.signature), itemsText];
}

function _alefE2eFormatMetadataDisplay(fm: unknown): string {
	if (fm == null) return "";
	if (typeof fm !== "object") return String(fm);
	const record = fm as Record<string, unknown>;
	const formatType = record.format_type;

	// FormatMetadata is internally tagged, so serde merges the variant payload's own fields as
	// siblings of the tag rather than nesting them: { format_type: 'image', format: 'PNG', ... }.
	// Read `format` off the record directly. Deliberately NO nested (`record.image`) fallback --
	// a binding that regresses to the pre-flatten shape must fail this assertion loudly instead
	// of silently degrading to the variant name below.
	if (formatType === "image" && typeof record.format === "string") {
		return record.format;
	}

	// Fallback: return format_type variant name
	if (typeof record.format_type === "string") return record.format_type;
	return "";
}


describe("markdown", () => {

	it("citations_balanced_parens: Citations correctly handle links inside parentheses with balanced rendering", async () => {
		const engine = createEngine(null);
		const url = `${process.env.MOCK_SERVER_URL}/fixtures/citations_balanced_parens`;
		const result = await scrape(engine, url);
    expect(result.statusCode).toBe(200);
    {
        const _v = result.markdown?.content;
        if (typeof _v === "string" || Array.isArray(_v)) {
            expect(_v.length).toBeGreaterThan(0);
        } else {
            expect(_v).toBeDefined();
            expect(_v).not.toBeNull();
        }
    }
    expect(result.markdown?.content ?? "").toContain("(");

	}, 30000);

	it("citations_duplicate_urls: Citations deduplicates multiple links to the same URL", async () => {
		const engine = createEngine(null);
		const url = `${process.env.MOCK_SERVER_URL}/fixtures/citations_duplicate_urls`;
		const result = await scrape(engine, url);
    expect(result.statusCode).toBe(200);
    {
        const _v = result.markdown?.content;
        if (typeof _v === "string" || Array.isArray(_v)) {
            expect(_v.length).toBeGreaterThan(0);
        } else {
            expect(_v).toBeDefined();
            expect(_v).not.toBeNull();
        }
    }
    expect(result.markdown?.citations ?? null).not.toBeNull();

	}, 30000);

	it("markdown_basic_conversion: HTML is always converted to markdown alongside raw HTML", async () => {
		const engine = createEngine(null);
		const url = `${process.env.MOCK_SERVER_URL}/fixtures/markdown_basic_conversion`;
		const result = await scrape(engine, url);
    expect(result.statusCode).toBe(200);
    expect((result.metadata?.title ?? "")).toBe("Test");
    {
        const _v = result.html;
        if (typeof _v === "string" || Array.isArray(_v)) {
            expect(_v.length).toBeGreaterThan(0);
        } else {
            expect(_v).toBeDefined();
            expect(_v).not.toBeNull();
        }
    }
    {
        const _v = result.markdown?.content;
        if (typeof _v === "string" || Array.isArray(_v)) {
            expect(_v.length).toBeGreaterThan(0);
        } else {
            expect(_v).toBeDefined();
            expect(_v).not.toBeNull();
        }
    }
    expect(result.markdown?.content ?? "").toContain("Hello World");

	}, 30000);

	it("markdown_crawl_all_pages: All crawled pages have markdown field populated", async () => {
		const engineConfig = { maxDepth: 1 };
		const engine = createEngine(engineConfig);
		const url = process.env.MOCK_SERVER_MARKDOWN_CRAWL_ALL_PAGES ?? `${process.env.MOCK_SERVER_URL}/fixtures/markdown_crawl_all_pages`;
		const result = await crawl(engine, url);
    expect(result.pages?.length).toBe(2);

	}, 30000);

	it("markdown_fit_content: Fit markdown removes navigation and boilerplate content", async () => {
		const engine = createEngine(null);
		const url = process.env.MOCK_SERVER_MARKDOWN_FIT_CONTENT ?? `${process.env.MOCK_SERVER_URL}/fixtures/markdown_fit_content`;
		const result = await scrape(engine, url);
    expect(result.statusCode).toBe(200);
    {
        const _v = result.markdown?.content;
        if (typeof _v === "string" || Array.isArray(_v)) {
            expect(_v.length).toBeGreaterThan(0);
        } else {
            expect(_v).toBeDefined();
            expect(_v).not.toBeNull();
        }
    }

	}, 30000);

	it("markdown_headings_and_paragraphs: Markdown conversion preserves heading hierarchy and paragraph text", async () => {
		const engine = createEngine(null);
		const url = `${process.env.MOCK_SERVER_URL}/fixtures/markdown_headings_and_paragraphs`;
		const result = await scrape(engine, url);
    {
        const _v = result.markdown?.content;
        if (typeof _v === "string" || Array.isArray(_v)) {
            expect(_v.length).toBeGreaterThan(0);
        } else {
            expect(_v).toBeDefined();
            expect(_v).not.toBeNull();
        }
    }
    expect(result.markdown?.content ?? "").toContain("Main Title");

	}, 30000);

	it("markdown_links_converted: HTML links are converted to markdown link syntax", async () => {
		const engine = createEngine(null);
		const url = `${process.env.MOCK_SERVER_URL}/fixtures/markdown_links_converted`;
		const result = await scrape(engine, url);
    expect(result.statusCode).toBe(200);
    {
        const _v = result.html;
        if (typeof _v === "string" || Array.isArray(_v)) {
            expect(_v.length).toBeGreaterThan(0);
        } else {
            expect(_v).toBeDefined();
            expect(_v).not.toBeNull();
        }
    }
    {
        const _v = result.markdown?.content;
        if (typeof _v === "string" || Array.isArray(_v)) {
            expect(_v.length).toBeGreaterThan(0);
        } else {
            expect(_v).toBeDefined();
            expect(_v).not.toBeNull();
        }
    }
    expect(result.markdown?.content ?? "").toContain("Example");

	}, 30000);

	it("markdown_with_citations: Markdown includes citation conversion with numbered references", async () => {
		const engine = createEngine(null);
		const url = `${process.env.MOCK_SERVER_URL}/fixtures/markdown_with_citations`;
		const result = await scrape(engine, url);
    expect(result.statusCode).toBe(200);
    {
        const _v = result.markdown?.content;
        if (typeof _v === "string" || Array.isArray(_v)) {
            expect(_v.length).toBeGreaterThan(0);
        } else {
            expect(_v).toBeDefined();
            expect(_v).not.toBeNull();
        }
    }

	}, 30000);

});
