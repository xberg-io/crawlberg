// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:ac3389b8abf08d63c1e10366661f2b4659ea5333d1c3c4dea8f2674637f4f566
// To regenerate: alef generate
// To verify freshness: alef verify

import { describe, expect, it } from "vitest";
import { scrape, createEngine, BrowserMode } from "@xberg-io/crawlberg";

process.env.CRAWLBERG_ALLOW_PRIVATE_NETWORK ??= "true";


async function _alefE2eDecompressAndParseJson(response: Response): Promise<unknown> {
	const contentEncoding = response.headers.get("content-encoding");
	let bytes = new Uint8Array(await response.arrayBuffer());

	// The runtime's own fetch (undici on Node, and the browser equivalent) decodes
	// `content-encoding` transparently and leaves the header in place, so by the time the
	// body reaches here it is usually already plain text. Decoding unconditionally on the
	// header alone therefore fails ("incorrect header check") against a server that really
	// did compress. Decode only when the payload still carries the encoding: gzip announces
	// itself with the 0x1f8b magic pair; brotli has no magic, so it is attempted and the
	// already-decoded case falls back to the bytes as received.
	if (contentEncoding === "gzip" && bytes.length >= 2 && bytes[0] === 0x1f && bytes[1] === 0x8b) {
		const { gunzipSync } = await import("zlib");
		bytes = new Uint8Array(gunzipSync(bytes));
	} else if (contentEncoding === "br") {
		const { brotliDecompressSync } = await import("zlib");
		try {
			bytes = new Uint8Array(brotliDecompressSync(bytes));
		} catch {
			// Already decoded by the runtime.
		}
	}

	const text = new TextDecoder().decode(bytes);
	return JSON.parse(text);
}

function _alefE2eText(value: unknown): string {
	return value == null ? "" : String(value);
}

function _alefE2eItemTexts(item: unknown): string[] {
	if (item == null || typeof item !== "object") {
		return [_alefE2eText(item)];
	}
	const record = item as Record<string, unknown>;
	const itemsText = Array.isArray(record.items) ? record.items.map(_alefE2eText).join(" ") : "";
	return [_alefE2eText(item), _alefE2eText(record.kind), _alefE2eText(record.name), _alefE2eText(record.source), _alefE2eText(record.alias), _alefE2eText(record.text), _alefE2eText(record.signature), itemsText];
}

function _alefE2eFormatMetadataDisplay(fm: unknown): string {
	if (fm == null) return "";
	if (typeof fm !== "object") return String(fm);
	const record = fm as Record<string, unknown>;
	const formatType = record.format_type;

	// FormatMetadata is internally tagged, so serde merges the variant payload's own fields as
	// siblings of the tag rather than nesting them: { format_type: 'image', format: 'PNG', ... }.
	// Read `format` off the record directly. Deliberately NO nested (`record.image`) fallback --
	// a binding that regresses to the pre-flatten shape must fail this assertion loudly instead
	// of silently degrading to the variant name below.
	if (formatType === "image" && typeof record.format === "string") {
		return record.format;
	}

	// Fallback: return format_type variant name
	if (typeof record.format_type === "string") return record.format_type;
	return "";
}


describe("error", () => {

	it("error_401_unauthorized: Handles 401 Unauthorized response correctly", async () => {
		await expect(async () => {
			const engine = createEngine(null);
			const url = `${process.env.MOCK_SERVER_URL}/fixtures/error_401_unauthorized`;
			await scrape(engine, url);
		}).rejects.toSatisfy((error) => {
				const _message = error instanceof Error ? error.message : String(error);
				const _name = error instanceof Error ? error.name : "";
				return /unauthorized/.test(_message) || /unauthorized/.test(_name);
			});
	}, 30000);

	it("error_403_forbidden: Handles 403 Forbidden response correctly", async () => {
		await expect(async () => {
			const engineConfig = { browser: { mode: BrowserMode.Never } };
			const engine = createEngine(engineConfig);
			const url = `${process.env.MOCK_SERVER_URL}/fixtures/error_403_forbidden`;
			await scrape(engine, url);
		}).rejects.toSatisfy((error) => {
				const _message = error instanceof Error ? error.message : String(error);
				const _name = error instanceof Error ? error.name : "";
				return /forbidden/.test(_message) || /forbidden/.test(_name);
			});
	}, 30000);

	it("error_404_page: Handles 404 response correctly", async () => {
		await expect(async () => {
			const engine = createEngine(null);
			const url = `${process.env.MOCK_SERVER_URL}/fixtures/error_404_page`;
			await scrape(engine, url);
		}).rejects.toSatisfy((error) => {
				const _message = error instanceof Error ? error.message : String(error);
				const _name = error instanceof Error ? error.name : "";
				return /not_found/.test(_message) || /not_found/.test(_name);
			});
	}, 30000);

	it("error_408_request_timeout: Handles 408 Request Timeout response correctly", async () => {
		await expect(async () => {
			const engine = createEngine(null);
			const url = `${process.env.MOCK_SERVER_URL}/fixtures/error_408_request_timeout`;
			await scrape(engine, url);
		}).rejects.toSatisfy((error) => {
				const _message = error instanceof Error ? error.message : String(error);
				const _name = error instanceof Error ? error.name : "";
				return /timeout/.test(_message) || /timeout/.test(_name);
			});
	}, 30000);

	it("error_410_gone: Handles 410 Gone response correctly", async () => {
		await expect(async () => {
			const engine = createEngine(null);
			const url = `${process.env.MOCK_SERVER_URL}/fixtures/error_410_gone`;
			await scrape(engine, url);
		}).rejects.toSatisfy((error) => {
				const _message = error instanceof Error ? error.message : String(error);
				const _name = error instanceof Error ? error.name : "";
				return /gone/.test(_message) || /gone/.test(_name);
			});
	}, 30000);

	it("error_500_server: Handles 500 server error", async () => {
		await expect(async () => {
			const engine = createEngine(null);
			const url = `${process.env.MOCK_SERVER_URL}/fixtures/error_500_server`;
			await scrape(engine, url);
		}).rejects.toSatisfy((error) => {
				const _message = error instanceof Error ? error.message : String(error);
				const _name = error instanceof Error ? error.name : "";
				return /server_error/.test(_message) || /server_error/.test(_name);
			});
	}, 30000);

	it("error_502_bad_gateway: Handles 502 Bad Gateway response correctly", async () => {
		await expect(async () => {
			const engine = createEngine(null);
			const url = `${process.env.MOCK_SERVER_URL}/fixtures/error_502_bad_gateway`;
			await scrape(engine, url);
		}).rejects.toSatisfy((error) => {
				const _message = error instanceof Error ? error.message : String(error);
				const _name = error instanceof Error ? error.name : "";
				return /bad_gateway/.test(_message) || /bad_gateway/.test(_name);
			});
	}, 30000);

	it("error_browser_launch_failure: Browser launch fails when browser mode is always but browser is unavailable", async () => {
		await expect(async () => {
			const engineConfig = { browser: { mode: BrowserMode.Always, timeout: 1 } };
			const engine = createEngine(engineConfig);
			const url = `${process.env.MOCK_SERVER_URL}/fixtures/error_browser_launch_failure`;
			await scrape(engine, url);
		}).rejects.toSatisfy((error) => {
				const _message = error instanceof Error ? error.message : String(error);
				const _name = error instanceof Error ? error.name : "";
				return /browser/.test(_message) || /browser/.test(_name);
			});
	}, 30000);

	it("error_browser_page_timeout: Browser page load times out", async () => {
		await expect(async () => {
			const engineConfig = { browser: { mode: BrowserMode.Always, timeout: 1 } };
			const engine = createEngine(engineConfig);
			const url = `${process.env.MOCK_SERVER_URL}/fixtures/error_browser_page_timeout`;
			await scrape(engine, url);
		}).rejects.toSatisfy((error) => {
				const _message = error instanceof Error ? error.message : String(error);
				const _name = error instanceof Error ? error.name : "";
				return /browser_timeout/.test(_message) || /browser_timeout/.test(_name);
			});
	}, 30000);

	it("error_connection_refused: Handles connection refused error gracefully", async () => {
		await expect(async () => {
			const engine = createEngine(null);
			const url = `${process.env.MOCK_SERVER_URL}/fixtures/error_connection_refused`;
			await scrape(engine, url);
		}).rejects.toSatisfy((error) => {
				const _message = error instanceof Error ? error.message : String(error);
				const _name = error instanceof Error ? error.name : "";
				return /connection/.test(_message) || /connection/.test(_name);
			});
	}, 30000);

	it("error_data_loss_truncated: Content-Length mismatch causes data loss error", async () => {
		await expect(async () => {
			const engine = createEngine(null);
			const url = `${process.env.MOCK_SERVER_URL}/fixtures/error_data_loss_truncated`;
			await scrape(engine, url);
		}).rejects.toSatisfy((error) => {
				const _message = error instanceof Error ? error.message : String(error);
				const _name = error instanceof Error ? error.name : "";
				return /data_loss:/.test(_message) || /data_loss:/.test(_name);
			});
	}, 30000);

	it("error_dns_resolution: Handles DNS resolution failure gracefully", async () => {
		await expect(async () => {
			const engine = createEngine(null);
			const url = `${process.env.MOCK_SERVER_URL}/fixtures/error_dns_resolution`;
			await scrape(engine, url);
		}).rejects.toSatisfy((error) => {
				const _message = error instanceof Error ? error.message : String(error);
				const _name = error instanceof Error ? error.name : "";
				return /dns/.test(_message) || /dns/.test(_name);
			});
	}, 30000);

	it("error_empty_batch_urls: Scraping a URL that cannot be found returns an error containing the URL path", async () => {
		await expect(async () => {
			const engine = createEngine(null);
			const url = `${process.env.MOCK_SERVER_URL}/fixtures/error_empty_batch_urls`;
			await scrape(engine, url);
		}).rejects.toSatisfy((error) => {
				const _message = error instanceof Error ? error.message : String(error);
				const _name = error instanceof Error ? error.name : "";
				return /not_found/.test(_message) || /not_found/.test(_name);
			});
	}, 30000);

	it("error_empty_response: Handles 200 with completely empty body gracefully", async () => {
		const engine = createEngine(null);
		const url = `${process.env.MOCK_SERVER_URL}/fixtures/error_empty_response`;
		const result = await scrape(engine, url);
    expect((result.html ?? "").length).toBe(0);

	}, 30000);

	it("error_invalid_proxy: Proxy pointing to unreachable address causes connection error during scrape", async () => {
		await expect(async () => {
			const engineConfig = { proxy: { url: "http://127.0.0.1:1" } };
			const engine = createEngine(engineConfig);
			const url = `${process.env.MOCK_SERVER_URL}/fixtures/error_invalid_proxy`;
			await scrape(engine, url);
		}).rejects.toSatisfy((error) => {
				const _message = error instanceof Error ? error.message : String(error);
				const _name = error instanceof Error ? error.name : "";
				return /connection/.test(_message) || /connection/.test(_name);
			});
	}, 30000);

	it("error_partial_response: Handles incomplete or truncated HTTP response", async () => {
		await expect(async () => {
			const engine = createEngine(null);
			const url = `${process.env.MOCK_SERVER_URL}/fixtures/error_partial_response`;
			await scrape(engine, url);
		}).rejects.toSatisfy((error) => {
				const _message = error instanceof Error ? error.message : String(error);
				const _name = error instanceof Error ? error.name : "";
				return /data_loss/.test(_message) || /data_loss/.test(_name);
			});
	}, 30000);

	it("error_rate_limited: Handles 429 rate limiting with Retry-After", async () => {
		await expect(async () => {
			const engine = createEngine(null);
			const url = `${process.env.MOCK_SERVER_URL}/fixtures/error_rate_limited`;
			await scrape(engine, url);
		}).rejects.toSatisfy((error) => {
				const _message = error instanceof Error ? error.message : String(error);
				const _name = error instanceof Error ? error.name : "";
				return /rate_limited/.test(_message) || /rate_limited/.test(_name);
			});
	}, 30000);

	it("error_retry_503: Retries request on 503 Service Unavailable response", async () => {
		await expect(async () => {
			const engine = createEngine(null);
			const url = `${process.env.MOCK_SERVER_URL}/fixtures/error_retry_503`;
			await scrape(engine, url);
		}).rejects.toSatisfy((error) => {
				const _message = error instanceof Error ? error.message : String(error);
				const _name = error instanceof Error ? error.name : "";
				return /server_error/.test(_message) || /server_error/.test(_name);
			});
	}, 30000);

	it("error_retry_backoff: Implements exponential backoff when retrying failed requests", async () => {
		await expect(async () => {
			const engine = createEngine(null);
			const url = `${process.env.MOCK_SERVER_URL}/fixtures/error_retry_backoff`;
			await scrape(engine, url);
		}).rejects.toSatisfy((error) => {
				const _message = error instanceof Error ? error.message : String(error);
				const _name = error instanceof Error ? error.name : "";
				return /rate_limited/.test(_message) || /rate_limited/.test(_name);
			});
	}, 30000);

	it("error_ssl_invalid_cert: Handles SSL certificate validation error", async () => {
		await expect(async () => {
			const engine = createEngine(null);
			const url = `${process.env.MOCK_SERVER_URL}/fixtures/error_ssl_invalid_cert`;
			await scrape(engine, url);
		}).rejects.toSatisfy((error) => {
				const _message = error instanceof Error ? error.message : String(error);
				const _name = error instanceof Error ? error.name : "";
				return /ssl/.test(_message) || /ssl/.test(_name);
			});
	}, 30000);

	it("error_timeout: Mock server delays response longer than request_timeout, surfacing a timeout error", async () => {
		await expect(async () => {
			const engineConfig = { requestTimeout: 500 };
			const engine = createEngine(engineConfig);
			const url = `${process.env.MOCK_SERVER_URL}/fixtures/error_timeout`;
			await scrape(engine, url);
		}).rejects.toSatisfy((error) => {
				const _message = error instanceof Error ? error.message : String(error);
				const _name = error instanceof Error ? error.name : "";
				return /timeout/.test(_message) || /timeout/.test(_name);
			});
	}, 30000);

	it("error_unsupported_scheme: Unsupported URL scheme (gopher) is rejected", async () => {
		await expect(async () => {
			const engine = createEngine(null);
			const url = "gopher://invalid.example.com/";
			await scrape(engine, url);
		}).rejects.toSatisfy((error) => {
				const _message = error instanceof Error ? error.message : String(error);
				const _name = error instanceof Error ? error.name : "";
				return /disallowed scheme: gopher/.test(_message) || /disallowed scheme: gopher/.test(_name);
			});
	}, 30000);

	it("error_waf_akamai: Akamai WAF detection returns WafBlocked error", async () => {
		await expect(async () => {
			const engineConfig = { browser: { mode: BrowserMode.Never } };
			const engine = createEngine(engineConfig);
			const url = `${process.env.MOCK_SERVER_URL}/fixtures/error_waf_akamai`;
			await scrape(engine, url);
		}).rejects.toThrow();
	}, 30000);

	it("error_waf_blocked: WAF challenge/block detection returns WafBlocked error", async () => {
		await expect(async () => {
			const engineConfig = { browser: { mode: BrowserMode.Never } };
			const engine = createEngine(engineConfig);
			const url = `${process.env.MOCK_SERVER_URL}/fixtures/error_waf_blocked`;
			await scrape(engine, url);
		}).rejects.toSatisfy((error) => {
				const _message = error instanceof Error ? error.message : String(error);
				const _name = error instanceof Error ? error.name : "";
				return /waf/.test(_message) || /waf/.test(_name);
			});
	}, 30000);

	it("error_waf_false_403: Detects WAF/bot protection false 403 (Cloudflare challenge page)", async () => {
		await expect(async () => {
			const engineConfig = { browser: { mode: BrowserMode.Never } };
			const engine = createEngine(engineConfig);
			const url = `${process.env.MOCK_SERVER_URL}/fixtures/error_waf_false_403`;
			await scrape(engine, url);
		}).rejects.toSatisfy((error) => {
				const _message = error instanceof Error ? error.message : String(error);
				const _name = error instanceof Error ? error.name : "";
				return /waf/.test(_message) || /waf/.test(_name);
			});
	}, 30000);

	it("error_waf_imperva: Imperva/Incapsula WAF detection", async () => {
		await expect(async () => {
			const engineConfig = { browser: { mode: BrowserMode.Never } };
			const engine = createEngine(engineConfig);
			const url = `${process.env.MOCK_SERVER_URL}/fixtures/error_waf_imperva`;
			await scrape(engine, url);
		}).rejects.toThrow();
	}, 30000);

});
