---
id: fixture_wasm_interact_max_actions_exceeded
language: typescript
target: wasm
requires: []
side_effect: safe
---

<!-- This file is auto-generated by alef — DO NOT EDIT. -->
<!-- alef:hash:68998610e30b7f20e4045708fb5b945dd04caec03484c385d8f5d9facbc87827 -->
<!-- To regenerate: alef e2e generate -->
<!-- To verify freshness: alef verify -->

Submit 101 actions (exceeds MAX_ACTIONS=100) and verify validation error

```typescript title="WebAssembly"
import { WasmBrowserConfig, WasmBrowserMode, WasmCrawlConfig, createEngine, interact } from "@xberg-io/crawlberg-wasm";
async function main() {
  const engineConfig = WasmCrawlConfig.default();
  engineConfig.browser = (() => { const _u0 = WasmBrowserConfig.default(); _u0.mode = WasmBrowserMode.Always; return _u0; })();
  engineConfig.ssrf.denyPrivate = false;
  const engine = createEngine(engineConfig);
  const url = "https://example.com";
  try {
    await interact(engine, url, [{ selector: "#btn", type: "click" }, { selector: "#btn", type: "click" }, { selector: "#btn", type: "click" }, { selector: "#btn", type: "click" }, { selector: "#btn", type: "click" }, { selector: "#btn", type: "click" }, { selector: "#btn", type: "click" }, { selector: "#btn", type: "click" }, { selector: "#btn", type: "click" }, { selector: "#btn", type: "click" }, { selector: "#btn", type: "click" }, { selector: "#btn", type: "click" }, { selector: "#btn", type: "click" }, { selector: "#btn", type: "click" }, { selector: "#btn", type: "click" }, { selector: "#btn", type: "click" }, { selector: "#btn", type: "click" }, { selector: "#btn", type: "click" }, { selector: "#btn", type: "click" }, { selector: "#btn", type: "click" }, { selector: "#btn", type: "click" }, { selector: "#btn", type: "click" }, { selector: "#btn", type: "click" }, { selector: "#btn", type: "click" }, { selector: "#btn", type: "click" }, { selector: "#btn", type: "click" }, { selector: "#btn", type: "click" }, { selector: "#btn", type: "click" }, { selector: "#btn", type: "click" }, { selector: "#btn", type: "click" }, { selector: "#btn", type: "click" }, { selector: "#btn", type: "click" }, { selector: "#btn", type: "click" }, { selector: "#btn", type: "click" }, { selector: "#btn", type: "click" }, { selector: "#btn", type: "click" }, { selector: "#btn", type: "click" }, { selector: "#btn", type: "click" }, { selector: "#btn", type: "click" }, { selector: "#btn", type: "click" }, { selector: "#btn", type: "click" }, { selector: "#btn", type: "click" }, { selector: "#btn", type: "click" }, { selector: "#btn", type: "click" }, { selector: "#btn", type: "click" }, { selector: "#btn", type: "click" }, { selector: "#btn", type: "click" }, { selector: "#btn", type: "click" }, { selector: "#btn", type: "click" }, { selector: "#btn", type: "click" }, { selector: "#btn", type: "click" }, { selector: "#btn", type: "click" }, { selector: "#btn", type: "click" }, { selector: "#btn", type: "click" }, { selector: "#btn", type: "click" }, { selector: "#btn", type: "click" }, { selector: "#btn", type: "click" }, { selector: "#btn", type: "click" }, { selector: "#btn", type: "click" }, { selector: "#btn", type: "click" }, { selector: "#btn", type: "click" }, { selector: "#btn", type: "click" }, { selector: "#btn", type: "click" }, { selector: "#btn", type: "click" }, { selector: "#btn", type: "click" }, { selector: "#btn", type: "click" }, { selector: "#btn", type: "click" }, { selector: "#btn", type: "click" }, { selector: "#btn", type: "click" }, { selector: "#btn", type: "click" }, { selector: "#btn", type: "click" }, { selector: "#btn", type: "click" }, { selector: "#btn", type: "click" }, { selector: "#btn", type: "click" }, { selector: "#btn", type: "click" }, { selector: "#btn", type: "click" }, { selector: "#btn", type: "click" }, { selector: "#btn", type: "click" }, { selector: "#btn", type: "click" }, { selector: "#btn", type: "click" }, { selector: "#btn", type: "click" }, { selector: "#btn", type: "click" }, { selector: "#btn", type: "click" }, { selector: "#btn", type: "click" }, { selector: "#btn", type: "click" }, { selector: "#btn", type: "click" }, { selector: "#btn", type: "click" }, { selector: "#btn", type: "click" }, { selector: "#btn", type: "click" }, { selector: "#btn", type: "click" }, { selector: "#btn", type: "click" }, { selector: "#btn", type: "click" }, { selector: "#btn", type: "click" }, { selector: "#btn", type: "click" }, { selector: "#btn", type: "click" }, { selector: "#btn", type: "click" }, { selector: "#btn", type: "click" }, { selector: "#btn", type: "click" }, { selector: "#btn", type: "click" }, { selector: "#btn", type: "click" }, { selector: "#btn", type: "click" }, { selector: "#btn", type: "click" }, { selector: "#btn", type: "click" }]);
  } catch (error) {
    console.error(String(error));
  }
}

void main();
```
