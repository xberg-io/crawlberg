---
id: fixture_rust_stream_large_crawl
language: rust
target: rust
requires: ["crate:serde_json","crate:tokio","crate:tokio-stream"]
side_effect: safe
---

<!-- This file is auto-generated by alef — DO NOT EDIT. -->
<!-- alef:hash:f42bdec929beca9e64ea0926b70b3ef278dfb2ee4acd20a22419c6c7c80594b2 -->
<!-- To regenerate: alef e2e generate -->
<!-- To verify freshness: alef verify -->

Stream handles crawl of 5+ pages with multiple events

```rust title="Rust"
use crawlberg::crawl_stream;
use crawlberg::CrawlConfig;
use crawlberg::create_engine;

#[tokio::main]
async fn main() {
    let engine_config: CrawlConfig = serde_json::from_str("{\"max_depth\":1,\"respect_robots_txt\":false}").expect("config should parse");
    let engine = create_engine(Some(engine_config)).expect("handle creation should succeed");
    let url = r#"https://example.com"#.to_string();
    let stream = crawl_stream(&engine, &url).await.expect("call failed");
    let chunks: Vec<_> = tokio_stream::StreamExt::collect::<Vec<_>>(stream).await
        .into_iter()
        .map(|r| r.expect("stream item failed"))
        .collect();
    println!("{:?}", chunks);
}
```
